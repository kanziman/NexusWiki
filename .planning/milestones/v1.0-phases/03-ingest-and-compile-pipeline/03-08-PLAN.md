---
phase: 03-ingest-and-compile-pipeline
plan: 08
type: execute
wave: 7
depends_on: ["03-05", "03-06", "03-09"]
files_modified:
  - apps/worker/src/worker/queue.py
  - apps/worker/src/worker/errors.py
  - apps/worker/src/worker/db/service.py
  - apps/worker/src/worker/schema_guard.py
  - apps/worker/src/worker/settings.py
  - apps/worker/src/worker/__main__.py
  - apps/worker/tests/test_queue.py
  - apps/worker/tests/test_schema_guard.py
  - apps/worker/tests/test_service_client.py
  - apps/worker/tests/test_settings.py
  - packages/core/src/nexuswiki_core/logging.py
  - packages/core/tests/test_logging_redaction.py
  - scripts/verify_shrink_reprocess.sh
  - docs/ops/reap-timeout-baseline.md
  - .env.sample
autonomous: true
requirements: [COMP-02, COMP-07, COMP-08, OPS-01]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "provider 응답 본문이 `jobs.last_error`에 들어가지 않는다 — 실린 것은 오류 종류와 HTTP 상태뿐이고, 그 사실이 실제 응답 본문을 담은 예외로 검증된다"
    - "자격증명 형태의 문자열(키 접두사·Bearer 토큰·JWT)이 `last_error`에 남지 않는다 — 어느 예외 타입을 통해 들어와도 스크럽된다"
    - "우리 자신의 실패 사유(`needs_ocr` 등)는 마스킹되지 않고 사람이 읽을 수 있는 형태로 남는다 — provider 노출을 막는 것과 사유를 숨기는 것은 다른 일이다"
    - "`last_error` 절단이 코드 포인트 단위이고 잘린 결과가 항상 유효한 UTF-8이다"
    - "미등록 잡 type이 `max_attempts`만큼 왕복하지 않고 **한 번에** `dead`가 된다"
    - "재시도해도 결과가 같은 실패(`NON_RETRYABLE_ERRORS`)가 즉시 `dead`가 되고, provider 장애는 여전히 재시도된다"
    - "데드레터 호출이 `locked_by = worker_id and status = 'running'` 술어를 통과해야만 성립한다 — reap이 락을 뺏은 뒤 늦게 깨어난 워커가 남의 잡을 죽이지 못한다"
    - "`cancel_requested_at`이 찍힌 잡은 claim 직후 핸들러를 돌리지 않고 `canceled`로 마감된다 — 취소가 다음 체인 단계 경계에서 반영된다"
    - "워커 기동 시 Python enum과 `pg_constraint`의 CHECK 값을 대조해 불일치가 있으면 큐 루프를 시작하기 전에 프로세스가 죽는다"
    - "대조 실패 메시지가 어느 테이블·어느 컬럼·어느 값이 어긋났는지 밝힌다"
    - "`WorkerSettings`에 `OPENAI_API_KEY` 필드가 없고 `.env.sample`에도 없다 — 워커가 쓰지 않는 키를 Railway에 주입하지 않는다"
    - "`nexuswiki_core.logging.REDACTED_KEYS`는 여전히 그 키 이름을 담는다 — `supabase/config.toml`의 Studio AI가 같은 이름의 키를 별개 소비자로 갖기 때문이다"
    - "재처리가 더 적은 단위를 만들면 `source_chunks`와 `wiki_embeddings`의 잔여 행이 실제로 사라지고 `chunk_index`가 빈틈 없이 0부터 이어진다"
    - "reap 타임아웃이 실측 핸들러 지속시간에서 유도되고 그 유도 과정이 문서에 남는다 — 실측이 기존 15분을 지지하면 값을 바꾸지 않는 것도 유효한 결론이다 (02-CONTEXT.md > D-17의 이월이 여기서 닫힌다)"
    - "`compile` 핸들러 지속시간 통계가 `docs/ops/reap-timeout-baseline.md`에 **숫자 한 줄로** 기록되고, `REAP_TIMEOUT_SECONDS`가 그 숫자보다 크다 — '15분 유지'가 결론이더라도 그 부등식이 수치로 성립함이 확인된 뒤에만 유효하다. 통계는 성공 표본이 200 이상이면 p99, 미만이면 관측 최댓값(p99의 보수적 상계)이며 어느 쪽인지도 같은 줄에 기록된다"
    - "워커가 주기적으로 `reap_stale_jobs`를 부르고 여러 워커가 동시에 불러도 안전하다"
    - statement: "COMP-02·COMP-07·COMP-08은 결정적 분류기가 판정을 보류한 요구사항이다 — 아래 `## Flagged Planner Assumptions`가 이 플랜이 세운 가정을 드러낸다"
      verification: backstop
  artifacts:
    - path: "apps/worker/src/worker/schema_guard.py"
      provides: "기동 시 Python enum ↔ DB CHECK 대조"
      exports: ["SchemaContractError", "assert_enums_match_db"]
    - path: "apps/worker/src/worker/queue.py"
      provides: "provider 마스킹 · 원자적 데드레터 · 취소 관측 · 주기적 reap"
      exports: ["sanitize_error", "NON_RETRYABLE_ERRORS", "REAP_INTERVAL_CYCLES"]
    - path: "scripts/verify_shrink_reprocess.sh"
      provides: "축소 재처리 잔여 행 0 검증"
    - path: "docs/ops/reap-timeout-baseline.md"
      provides: "실측 핸들러 지속시간에서 유도한 reap 타임아웃과 그 한계"
  key_links:
    - from: "apps/worker/src/worker/queue.py"
      to: "public.dead_letter_job"
      via: "ServiceDb RPC — 미등록 type과 재시도 불가 실패를 한 번에 dead로"
      pattern: "dead_letter_job"
    - from: "apps/worker/src/worker/schema_guard.py"
      to: "public.enum_check_values"
      via: "ServiceDb 카탈로그 RPC로 CHECK 값을 읽어 DB_CHECK_ENUMS와 대조"
      pattern: "enum_check_values"
    - from: "apps/worker/src/worker/__main__.py"
      to: "apps/worker/src/worker/schema_guard.py"
      via: "큐 루프 시작 전에 부르고 실패를 삼키지 않는다"
      pattern: "assert_enums_match_db"
  prohibitions:
    - statement: "사용자 소스 본문·프롬프트·LLM 응답 본문이 구조화 로그나 `usage_events.metadata`에 원문 그대로 실려서는 안 된다 — `REDACTED_KEYS`는 필드 **이름**만 보므로 이름을 바꾸면 마스킹이 조용히 비껴간다"
      status: unverified
      verification: judgment
---

<objective>
파이프라인이 도는 것을 전제로, 그것이 **운영 가능한지**를 결정하는 계약 넷을 마감한다 —
provider 예외 마스킹(COMP-08) · 원자적 데드레터와 취소(D-03/OPS-01) · 기동 시 스키마 대조
(COMP-02) · 축소 재처리 잔여 행 0(COMP-07). 여기에 Phase 2가 이월한 reap 타임아웃 확정
(02-CONTEXT.md > D-17)과 D-04의 키 정리를 함께 닫는다.

Purpose: 이 넷의 공통점은 **실패해도 조용하다**는 것이다. provider 본문은 오류 없이 새어
나가고, `max_attempts` 왕복은 오류 없이 시간을 먹고, enum 불일치는 첫 INSERT에서야 드러나고,
잔여 청크는 검색 결과에 유령으로만 나타난다. 파이프라인이 도는 지금이 이것들을 관측 가능한
실패로 바꿀 수 있는 시점이다.

Output: 마스킹된 `sanitize_error` · 데드레터/취소 배선 · 기동 가드 · 축소 재처리 검증 ·
reap 타임아웃 근거 문서 · `OPENAI_API_KEY` 제거.
</objective>

## Flagged Planner Assumptions

결정적 edge 분류기가 판정을 보류한(`unclassified`) 요구사항 3건.

- **COMP-02 (unclassified).** 가정: "Python enum 정의"의 범위는
  `nexuswiki_core.domain.DB_CHECK_ENUMS` 의 8개 항목이며 `jobs.type` 은 **의도적으로 제외**된다
  (`0003:31-36` 이 그 컬럼에 CHECK를 걸지 않기로 했고 워커 레지스트리가 그 역할을 대신한다).
  "기동 시점"은 큐 루프 시작 **직전**이며 `service_client` 가 열린 뒤다 — 그보다 앞이면 DB에
  물어볼 수단이 없다. "즉시 실패"는 예외를 던져 프로세스가 죽는 것이다(로그만 남기고 계속
  도는 것이 아니다).
- **COMP-07 (unclassified).** 가정: `upsert_and_truncate` 의 "단위"는 `chunk_index` 로 세는
  행이며, 위키 **페이지** 수준의 축소는 페이지 삭제가 아니라 `sources` 역참조 제거다
  (`03-03-PLAN.md > D-P6`이 근거를 남겼다). 검증 범위는 COMP-07이 이름을 댄 세 테이블이다.
- **COMP-08 (unclassified).** 가정: "원본 provider 예외"는 OpenRouter·Supabase가 돌려준
  **응답 본문과 그 안의 구조**를 뜻하며, 우리가 만든 사유 토큰(`needs_ocr` 등)은 마스킹
  대상이 아니라 오히려 **보여야 하는 것**이다. 마스킹의 소유자는 `sanitize_error` 한 곳이고
  라우터는 저장된 값을 그대로 전달한다.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ingest-and-compile-pipeline/03-CONTEXT.md
@.planning/phases/03-ingest-and-compile-pipeline/03-PATTERNS.md
@.claude/CLAUDE.md
@supabase/migrations/0003_jobs.sql
@supabase/migrations/0009_pipeline_ops.sql
@docs/ops/reap-timeout-baseline.md
@.planning/phases/03-ingest-and-compile-pipeline/03-06-SUMMARY.md
@.planning/phases/03-ingest-and-compile-pipeline/03-09-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: provider 마스킹 · 원자적 데드레터 · 취소 관측</name>
  <files>apps/worker/src/worker/queue.py, apps/worker/src/worker/errors.py, apps/worker/src/worker/db/service.py, apps/worker/tests/test_queue.py, apps/worker/tests/test_service_client.py</files>
  <read_first>
    - `apps/worker/src/worker/queue.py` — 전문 253행. `sanitize_error`(88-100행: **Phase 3가 채울 자리라고 명시된 docstring**) · `_dead_letter`(112-136행: 인라인 한계 전문. ⚠️ 123행이 `0008`이 필요하다고 적었는데 D-01/D-03이 `0009+`로 옮겼다 — **이 주석도 함께 고친다**) · `QueueDb` Protocol(62-80행) · `process_next_job`(164-231행) · `run_queue_loop`(234-252행).
    - `apps/worker/src/worker/errors.py` — 03-04/03-06이 만든 상태. `ProviderError` 의 필드와 `NON_RETRYABLE_ERRORS` 의 원소.
    - `apps/worker/src/worker/db/service.py` — 03-04/03-06/03-09 이후 상태. `TABLE_HELPERS` / `QUEUE_RPC_FUNCTIONS` / `RPC_HELPERS` 세 집합과 `_rpc` 의 all-null 정규화.
    - `apps/worker/tests/test_service_client.py` 112-122행 — **분류 단언**. `RPC_HELPERS <= QUEUE_RPC_FUNCTIONS` 라는 현재 단언이 카탈로그 RPC를 추가하면 깨진다. 느슨하게 고치지 말고 세 번째 집합을 명시적으로 도입해 단언을 정확하게 다시 쓴다.
    - `apps/worker/tests/test_queue.py` — 큐 루프 테스트의 형태와 통합 픽스처의 skip 관례.
    - `supabase/migrations/0009_pipeline_ops.sql` — `dead_letter_job` · `cancel_job` · `enum_check_values` 의 시그니처와 반환 계약.
    - `supabase/migrations/0003_jobs.sql` 151-179행 · 182-210행 — `fail_job` 이 `dead` 에 도달하는 유일한 경로(`attempts >= max_attempts`)와 `reap_stale_jobs` 의 술어.
    - `.env.sample` — 자격증명 접두사의 실제 형태(스크럽 패턴의 근거). **이 파일에서 접두사를 읽어 쓰고 계획서에서 옮겨 적지 말 것.**
  </read_first>
  <behavior>
    `sanitize_error`
    - `ProviderError` 를 받으면 결과가 오류 종류와 HTTP 상태만 담고, 예외 생성에 쓴 응답 본문 문자열이 결과에 나타나지 않는다
    - `nexuswiki_core.extract.ExtractionQualityError` 를 받으면 사유 토큰과 수치(문자 수·페이지 수·임계값)가 결과에 그대로 남는다 — 우리 자신의 사유는 마스킹 대상이 아니다
    - `UnsafeFetchTarget` 을 받으면 사유 토큰이 남고 대상 URL 전문은 남지 않는다
    - `httpx.HTTPStatusError` 를 받으면 상태 코드는 남고 응답 본문은 남지 않는다
    - 메시지에 자격증명 형태의 문자열이 섞인 임의 예외를 받으면 그 문자열이 결과에 나타나지 않는다
    - 상한을 넘는 한국어 메시지가 코드 포인트 단위로 잘리고 결과를 UTF-8로 인코딩·디코딩해도 손실이 없다
    - 상한 이하 메시지는 잘리지 않는다

    `process_next_job`
    - 미등록 type을 만나면 `dead_letter_job` 이 정확히 한 번 불리고 `fail_job` 은 불리지 않는다
    - `dead_letter_job` 호출이 `worker_id` 를 keyword로 넘긴다
    - 핸들러가 `NON_RETRYABLE_ERRORS` 의 예외를 던지면 `dead_letter_job` 이 불린다
    - 핸들러가 `ProviderError` 를 던지면 `fail_job` 이 불린다 (재시도 여지를 남긴다)
    - claim한 잡의 `cancel_requested_at` 이 채워져 있으면 핸들러가 호출되지 않고 `cancel_job` 이 불린다
    - 취소로 마감한 경우에도 `True` 를 돌려준다 (루프가 다음 잡으로 넘어간다)

    `ServiceDb`
    - 모든 공개 헬퍼가 세 집합(`TABLE_HELPERS` · `QUEUE_RPC_FUNCTIONS` · `CATALOG_RPC_FUNCTIONS`) 중 하나에 정확히 한 번 등록되어 있다
    - `dead_letter_job` · `cancel_job` · `reap_stale_jobs` 가 `QUEUE_RPC_FUNCTIONS` 에 있다
    - `enum_check_values` 가 `CATALOG_RPC_FUNCTIONS` 에 있고 `list[str]` 을 돌려준다
  </behavior>
  <action>
    **(a) `apps/worker/src/worker/errors.py` (수정).** 자격증명 스크럽 헬퍼를 둔다 —
    `scrub_credentials(text: str) -> str`. 패턴은 `.env.sample` 에 실제로 쓰인 접두사 형태
    (OpenRouter 키 · Supabase secret 키 · Supabase publishable 키)와 `Bearer <토큰>` 형태,
    그리고 세 부분 base64로 이루어진 JWT 형태를 컴파일된 정규식 목록으로 두고 `[REDACTED]` 로
    치환한다. **접두사 문자열을 이 계획서가 아니라 `.env.sample` 에서 읽어 쓴다.**
    `⚠️` 로 남긴다 — 이것은 2차 방어선이다. 1차는 자격증명이 애초에 예외 메시지에 들어오지
    않는 것이고, 그 책임은 각 클라이언트가 진다.

    **(b) `apps/worker/src/worker/queue.py` — `sanitize_error` (수정).** 현재 docstring이 예약해
    둔 자리를 채운다. 분기 순서: `ProviderError` → 오류 종류 + `provider` + HTTP 상태만;
    `ExtractionQualityError` → 사유 토큰 + `chars`/`pages`/`threshold`;
    `UnsafeFetchTarget` · `StorageObjectMissing` → 사유 토큰만; `httpx.HTTPStatusError` →
    `upstream_error` + 상태 코드(본문 제외); 그 밖 → `type(error).__name__` + 메시지.
    **모든 분기의 결과를 마지막에 `scrub_credentials` 로 한 번 더 통과시킨 뒤** 기존
    `LAST_ERROR_MAX_CHARS` 절단을 적용한다. docstring을 "Phase 3이 채운다"에서 "채웠다"로
    고치고, 무엇이 마스킹 대상이고 무엇이 아닌지를 한 문장으로 남긴다 — provider가 준 본문은
    마스킹하고 **우리가 만든 사유 토큰은 마스킹하지 않는다**(사유를 숨기면 ING-04의
    `needs_ocr` 이 사용자에게 도달하지 못한다).

    **(c) `apps/worker/src/worker/db/service.py` (수정).** `CATALOG_RPC_FUNCTIONS:
    Final[frozenset[str]] = frozenset({"enum_check_values"})` 를 도입하고
    `QUEUE_RPC_FUNCTIONS` 에 `dead_letter_job` · `cancel_job` · `reap_stale_jobs` 를 더하며
    `RPC_HELPERS = QUEUE_RPC_FUNCTIONS | CATALOG_RPC_FUNCTIONS` 로 바꾼다. 헬퍼를 추가한다 —
    `dead_letter_job(job_id, *, worker_id, error)` (⚠️ `worker_id` 는 keyword-only·기본값 없음.
    `release_job`(182-191행)과 **동형**이며 이유가 같다: 락 소유자 술어를 우회할 길을 만들지
    않는다), `cancel_job(job_id, *, worker_id)` (같은 규약),
    `reap_stale_jobs(*, timeout: str)`, `enum_check_values(table, column) -> list[str]`.
    `enum_check_values` 는 `text[]` 를 돌려주므로 `_rpc` 의 dict 정규화 경로를 타지 않는다 —
    JSON 배열을 그대로 받는 별도 내부 경로를 쓰고 그 이유를 주석으로 남긴다.

    **(d) `apps/worker/src/worker/queue.py` — `QueueDb` Protocol (수정).** `dead_letter_job` ·
    `cancel_job` · `reap_stale_jobs` 시그니처를 더한다. **Protocol이 이 루프가 요구하는 계약의
    실물**이므로 구현만 더하고 여기 빠뜨리면 타입 검사가 조용히 통과한다.

    **(e) `_dead_letter` (교체).** 본문을 `await db.dead_letter_job(job_id, worker_id=worker_id,
    error=reason)` 한 줄로 바꾸고 `worker_id` 를 인자로 받게 시그니처를 넓힌다. docstring을
    다시 쓴다 — 인라인 한계 서술을 **`0009` 가 그 자리를 닫았다**는 사실로 바꾸고,
    123행의 "`0008`에 `dead_letter_job()`이 필요하다"는 문장을 `0009` 로 정정한다
    (D-01/D-03이 새 함수를 `0009+` 로 옮겼다). 0행이 돌아오는 경우(락을 이미 뺏겼다)를
    경고로 로깅하고 예외로 만들지 않는다 — at-least-once에서 정상 경로다.

    **(f) `process_next_job` (수정).** 두 지점을 더한다.
    1) claim 직후 `job.get("cancel_requested_at")` 이 참이면 핸들러를 만들지 않고
    `await db.cancel_job(job_id, worker_id=worker_id)` 후 `worker.job_canceled` 를 로깅하고
    `True` 를 돌려준다. `⚠️` 로 남긴다 — 취소가 **체인 단계 경계에서** 반영되는 것이
    COMP-04의 잡 분할이 하트비트 대신 사 준 것이다(02-CONTEXT.md > D-16).
    2) `await handler_task` 의 `except` 절에서 `isinstance(error, NON_RETRYABLE_ERRORS)` 이면
    `_dead_letter` 로, 아니면 기존대로 `fail_job` 으로 보낸다. 어느 쪽이든 `sanitize_error` 를
    통과한 문자열만 넘긴다. `⚠️` 로 남긴다 — provider 장애를 재시도 불가로 분류하면 일시적
    5xx가 잡을 죽이고, 반대로 재시도 불가를 재시도하면 `max_attempts` 만큼 같은 실패를 반복하며
    비용만 쓴다.

    **(g) 테스트.** `apps/worker/tests/test_queue.py` 를 확장해 `<behavior>` 의 `process_next_job`
    항목을 덮는다 — `QueueDb` Protocol을 만족하는 가짜 구현으로 호출을 기록하는 형태로 쓰고
    네트워크를 타지 않는다. `sanitize_error` 항목은 같은 파일 안에서 순수 함수 테스트로 쓴다.
    자격증명 스크럽 테스트는 **테스트 안에서 조립한 가짜 값**을 쓰고 실제 키를 커밋하지 않는다.
    `apps/worker/tests/test_service_client.py` 의 분류 단언을 세 집합 형태로 **정확하게** 다시
    쓴다 — `RPC_HELPERS == QUEUE_RPC_FUNCTIONS | CATALOG_RPC_FUNCTIONS` 이고 세 집합이 서로소다.
    단언을 지우거나 느슨하게 만들어 통과시키지 말 것.
  </action>
  <verify>
    <automated>uv run pytest apps/worker/tests/test_queue.py apps/worker/tests/test_service_client.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest apps/worker/tests/test_queue.py apps/worker/tests/test_service_client.py -q` 가 exit 0이다.
    - `test_queue.py` 에 실제 응답 본문 문자열을 담은 `ProviderError` 를 `sanitize_error` 에 넣고 그 본문 문자열이 결과에 없음을 단언하는 테스트가 있다.
    - `test_queue.py` 에 자격증명 형태 문자열을 담은 예외의 결과에 그 문자열이 없음을 단언하는 테스트가 있다.
    - `test_queue.py` 에 `ExtractionQualityError` 의 사유 토큰이 결과에 **남는지** 단언하는 테스트가 있다.
    - `uv run python -c "from worker.db.service import TABLE_HELPERS, QUEUE_RPC_FUNCTIONS, CATALOG_RPC_FUNCTIONS as C; print(len(TABLE_HELPERS & QUEUE_RPC_FUNCTIONS), len(TABLE_HELPERS & C), len(QUEUE_RPC_FUNCTIONS & C))"` 가 `0 0 0` 을 출력한다.
    - `uv run python -c "import inspect; from worker.db.service import ServiceDb; p=inspect.signature(ServiceDb.dead_letter_job).parameters['worker_id']; print(p.kind.name, p.default is inspect.Parameter.empty)"` 가 `KEYWORD_ONLY True` 를 출력한다.
    - `apps/worker/src/worker/queue.py` 의 `_dead_letter` 본문에 `fail_job` 이 나타나지 않는다.
    - `apps/worker/src/worker/queue.py` 에 `0008` 이 `dead_letter_job` 과 같은 문장에 나타나지 않는다 (낡은 주석 정정 확인).
    - `uv run ruff check apps packages` 가 exit 0이다.
    - `pre-commit run --all-files` 가 통과한다.
  </acceptance_criteria>
  <done>provider 본문과 자격증명이 `last_error`에 도달하지 못하고, 미등록 type과 재시도 불가 실패가 한 번에 `dead`가 되며, 취소가 체인 경계에서 반영된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 기동 시 enum ↔ CHECK 대조와 `OPENAI_API_KEY` 정리</name>
  <files>apps/worker/src/worker/schema_guard.py, apps/worker/src/worker/__main__.py, apps/worker/src/worker/settings.py, apps/worker/tests/test_schema_guard.py, apps/worker/tests/test_settings.py, packages/core/src/nexuswiki_core/logging.py, packages/core/tests/test_logging_redaction.py, .env.sample</files>
  <read_first>
    - `packages/core/src/nexuswiki_core/domain.py` — 03-03의 `DB_CHECK_ENUMS` 8개 항목과 `jobs.type` 이 제외된 이유.
    - `apps/worker/src/worker/__main__.py` — 전문 138행. 기동 순서(설정 → 로깅 → 시그널 → RTT 프로브 → `service_client` → 큐 기준선 → 큐 루프)와 66-70행의 "부팅 시점 실패가 일어나는 지점" 규약.
    - `apps/worker/src/worker/settings.py` — 현재 필드 전체. **19-23행의 `⚠️`**: 네 secret 필드 이름의 casefold가 `REDACTED_KEYS` 원소와 정확히 일치해야 하고 `packages/core/tests/test_logging_redaction.py` 가 그 커플링을 단언한다.
    - `packages/core/tests/test_logging_redaction.py` 31-44행 — `secret_fields` 집합이 `OPENAI_API_KEY` 를 하드코딩한다. **필드를 지우기 전에 이 테스트를 먼저 읽고 함께 고친다.**
    - `packages/core/src/nexuswiki_core/logging.py` 16-33행 — `REDACTED_KEYS` 목록.
    - `apps/worker/tests/test_service_client.py` 13-22행 — `COMPLETE_WORKER_ENV` 가 `OPENAI_API_KEY` 를 담는다.
    - `supabase/config.toml` 86행 부근 — Studio AI가 같은 이름의 환경변수를 참조한다. **이것은 다른 소비자다. 제거하지 말 것.**
    - `.env.sample` §3 — `OPENAI_API_KEY` 줄과 그 주석("text-embedding-3-small 1536차원"). 그 주석은 `0008` 이후 거짓이다.
    - `supabase/migrations/0009_pipeline_ops.sql` — `enum_check_values` 의 반환 계약(일치하는 제약이 없으면 null이 아니라 빈 배열).
    - `.planning/phases/03-ingest-and-compile-pipeline/03-CONTEXT.md` D-04 — 키 제거의 근거와 planner 체크리스트 5항목.
  </read_first>
  <behavior>
    `assert_enums_match_db`
    - `DB_CHECK_ENUMS` 의 8개 항목을 전부 조회한다
    - 어느 항목이든 값 집합이 다르면 `SchemaContractError` 를 던지고 메시지가 테이블명·컬럼명·양쪽에만 있는 값을 담는다
    - DB가 빈 배열을 돌려주면(CHECK 부재) 그것도 불일치로 판정한다 — CHECK가 사라진 것을 통과시키면 가드의 의미가 없다
    - 전부 일치하면 아무것도 돌려주지 않고 예외도 던지지 않는다
    - `jobs.type` 은 조회하지 않는다

    `WorkerSettings`
    - `OPENAI_API_KEY` 필드가 존재하지 않는다
    - `OPENAI_API_KEY` 없이 나머지 필수 env만으로 인스턴스가 만들어진다
    - 나머지 secret 3종 이름의 casefold가 여전히 `REDACTED_KEYS` 의 원소다

    `REDACTED_KEYS`
    - `openai_api_key` 를 여전히 담는다 (Studio AI라는 별개 소비자가 있다)
  </behavior>
  <action>
    **(a) `apps/worker/src/worker/schema_guard.py` (신규).** 헤더에 `관련 태스크: P2-LLM-01` ·
    `설계 근거: 03-08-PLAN.md` 와 `supabase/migrations/0001_core_schema.sql` 을 인용한다.
    `SchemaContractError(RuntimeError)` 와
    `async def assert_enums_match_db(db, *, mapping = DB_CHECK_ENUMS) -> None` 을 노출한다.
    각 `(table, column)` 에 대해 `db.enum_check_values(table, column)` 을 부르고 결과 집합을
    enum의 값 집합과 비교한다. 다르면 **양쪽에만 있는 값을 각각 나열한** 메시지로
    `SchemaContractError` 를 던진다 — "불일치"만 알리는 메시지는 원인을 되짚는 데 쓸모가 없다.
    빈 배열도 불일치다(CHECK가 사라졌다는 뜻이며, 통과시키면 가드가 방어하는 대상 자체가
    사라진 상태를 승인하게 된다). 전부 통과하면 `worker.schema_contract_ok` 를 항목 수와 함께
    로깅한다.
    `⚠️` 로 남긴다 — 이 가드가 없으면 enum 불일치가 **첫 INSERT의 23514**에서야 드러나고,
    그것은 이미 LLM 비용을 쓴 뒤다.

    **(b) `apps/worker/src/worker/__main__.py` (수정).** `service_client` 컨텍스트 안에서
    `ServiceDb` 를 만든 **직후**, 큐 기준선 프로브와 큐 루프보다 **앞에서**
    `await assert_enums_match_db(db)` 를 부른다. 예외를 삼키지 않는다 — RTT 프로브와 큐 기준선
    프로브는 `try/except` 로 감싸 실패해도 계속 도는데, 이것은 그 부류가 아니다. 그 차이를
    주석으로 남긴다: 프로브는 관측이고 이 가드는 **계약**이다.

    **(c) `apps/worker/src/worker/settings.py` (수정).** `OPENAI_API_KEY` 필드를 지운다.
    19-23행의 `⚠️` 주석에서 필드 개수를 셋으로 고치고, `REDACTED_KEYS` 에는 그 이름이 **남는다**는
    사실과 이유(`supabase/config.toml` 의 Studio AI가 별개 소비자다)를 한 줄로 덧붙인다.

    **(d) `packages/core/tests/test_logging_redaction.py` (수정).** `secret_fields` 집합에서
    `OPENAI_API_KEY` 를 뺀다. 단언의 형태는 그대로 둔다 — 이 테스트가 지키는 것은
    "`WorkerSettings` 의 secret 필드 전부가 `REDACTED_KEYS` 안에 있다"이고 그 불변식은 유지된다.
    별도로 `openai_api_key` 가 `REDACTED_KEYS` 에 **여전히 있음**을 단언하는 테스트를 하나 더해
    나중에 누가 정리라는 이름으로 지우지 못하게 한다. 그 테스트의 주석에 `supabase/config.toml`
    의 소비자를 가리킨다.

    **(e) `apps/worker/tests/test_service_client.py` · `apps/worker/tests/test_settings.py` (수정).**
    `COMPLETE_WORKER_ENV` 에서 `OPENAI_API_KEY` 를 빼고, `test_settings.py` 에 그 필드가 더 이상
    존재하지 않음을 단언하는 테스트를 더한다.

    **(f) `.env.sample` (수정).** §3에서 `OPENAI_API_KEY` 줄과 그 위의 설명 주석을 지운다 —
    그 주석의 모델·차원 서술은 `0008` 이후 거짓이다. §3 머리말에 `OPENROUTER_API_KEY` 하나가
    LLM과 임베딩을 모두 덮는다는 사실과 근거 키(`03-CONTEXT.md > D-04`)를 한 줄로 남긴다.
    `supabase/config.toml` 은 건드리지 않는다.

    **(g) 테스트.** `apps/worker/tests/test_schema_guard.py` 를 `<behavior>` 항목마다 하나씩 쓴다.
    DB를 타지 않는다 — `enum_check_values` 만 흉내 내는 가짜 객체로 일치/불일치/빈 배열 세
    경우를 만든다. 불일치 테스트는 메시지에 테이블명·컬럼명·차집합 값이 들어 있는지 단언한다.
    별도로 **로컬 스택이 있을 때만 도는** 통합 테스트를 하나 더해 실제 `enum_check_values` 가
    `DB_CHECK_ENUMS` 와 전부 일치하는지 확인한다(스택 없으면 skip — 기존 관례).
  </action>
  <verify>
    <automated>uv run pytest apps/worker/tests/test_schema_guard.py apps/worker/tests/test_settings.py packages/core/tests/test_logging_redaction.py -q -rs</automated>
  </verify>
  <acceptance_criteria>
    - 위 `pytest` 명령이 exit 0이다.
    - `uv run python -c "from worker.settings import WorkerSettings; print('OPENAI_API_KEY' in WorkerSettings.model_fields)"` 가 `False` 를 출력한다.
    - `uv run python -c "from nexuswiki_core.logging import REDACTED_KEYS; print('openai_api_key' in REDACTED_KEYS)"` 가 `True` 를 출력한다.
    - `supabase/config.toml` 이 이 플랜에서 변경되지 않았다 (`git diff --stat` 로 확인).
    - `.env.sample` 에 `OPENAI_API_KEY` 가 나타나지 않는다.
    - 로컬 스택이 떠 있는 상태에서 통합 테스트가 skip되지 않고 통과한다 (`DB_CHECK_ENUMS` 8개 항목 전부 일치).
    - `wiki_pages.category` 의 CHECK에서 값 하나를 임시로 뺀 로컬 DB에 대해 워커를 기동하면 큐 루프 이전에 프로세스가 실패하고 메시지가 `wiki_pages` 와 `category` 를 담는다.
    - `uv run pytest -rs` 가 exit 0이다.
    - `pre-commit run --all-files` 가 통과한다.
  </acceptance_criteria>
  <done>워커가 어긋난 스키마 위에서 도는 일이 구조적으로 없어지고, 쓰지 않는 secret 키가 워커 설정과 배포 env 목록에서 사라졌다.</done>
</task>

<task type="auto">
  <name>Task 3: 축소 재처리 검증과 reap 타임아웃 확정</name>
  <files>scripts/verify_shrink_reprocess.sh, apps/worker/src/worker/queue.py, apps/worker/src/worker/settings.py, docs/ops/reap-timeout-baseline.md</files>
  <read_first>
    - `docs/ops/reap-timeout-baseline.md` — 전문. noop 큐 오버헤드 실측(p50 84.49 / p95 107.92 / p99 127.05ms, N=219)과 **§한계** 절이 명시한 것: 이 데이터는 15분을 반박도 지지도 하지 못하고, reap 타임아웃을 정하는 것은 전송이 아니라 **핸들러 지속시간**이다.
    - `.planning/phases/02-security-spine-and-shared-domain/02-CONTEXT.md` D-16 · D-17 — 하트비트를 넣지 않고 잡 분할로 간 결정과, reap 타임아웃을 Phase 3에서 확정하기로 한 이월.
    - `supabase/migrations/0003_jobs.sql` 182-210행 — `reap_stale_jobs(p_timeout interval default '15 minutes')` 의 술어와 `⚠️` 경고("정상 잡의 최장 실행 시간보다 넉넉해야 한다").
    - `apps/worker/src/worker/queue.py` — Task 1 이후 상태. `run_queue_loop`(234-252행)과 `QUEUE_POLL_INTERVAL_SECONDS`.
    - `apps/worker/src/worker/__main__.py` — 워커가 현재 `reap_stale_jobs` 를 **한 번도 부르지 않는다**는 사실. DOM-08이 reaper를 요구했으나 배선이 없다.
    - `scripts/verify_queue_functions.sh` · `scripts/smoke_pipeline.sh` — 셸 검증 스크립트의 형태와 처분 가능한 워크스페이스 규약.
    - `supabase/migrations/0002_search_schema.sql` 58-153행 — `source_chunks` 와 `wiki_embeddings` 의 업서트 키.
  </read_first>
  <action>
    **(a) `scripts/verify_shrink_reprocess.sh` (신규).** COMP-07의 축소 재처리를 실제 스택에서
    증명한다. `set -euo pipefail`. `smoke_pipeline.sh` 와 같이 **처분 가능한 워크스페이스**를
    만들고 `trap` 으로 정리한다 — `jobs` 에 DELETE 권한이 없어 cascade가 유일한 정리 경로라는
    사실을 헤더에 남긴다.

    절차: (1) 긴 한국어 텍스트 소스를 넣고 체인이 끝날 때까지 기다린다. (2) `source_chunks` ·
    `wiki_embeddings` · `wiki_links` 각각의 행 수와 최대 `chunk_index` 를 기록한다.
    (3) service key로 `raw_sources.content` 를 **훨씬 짧은 텍스트**로 갱신한다(`raw_sources` 는
    `service_role` 에 UPDATE가 있다 — `0007:365`). (4) `enqueue_source_job` 으로 `parse` 를 다시
    걸고 체인이 끝날 때까지 기다린다. (5) 재처리 후를 확인한다 — `source_chunks` 행 수가
    **줄었고**, `chunk_index` 가 0부터 빈틈 없이 이어지며 `max(chunk_index) + 1 = count`,
    그리고 위키 본문이 짧아졌다면 `wiki_embeddings` 도 같은 성질을 만족한다. 어느 조건이든
    어긋나면 관측값을 출력하고 실패한다. 마지막 줄에 `shrink_reprocess: ok` 를 출력한다.
    ⚠️ 3)에서 `content_hash` 를 바꾸지 않는다 — 바꾸면 중복 판정 경로가 섞여 이 스크립트가
    무엇을 증명하는지 흐려진다. 이 사실을 주석으로 남긴다.

    **(b) reap 타임아웃 확정.** `smoke_pipeline.sh` 와 이 스크립트가 도는 동안 각 잡의
    `updated_at - created_at` 이 아니라 **claim → 종료** 구간을 관측한다. 관측이 어려우면
    워커가 핸들러마다 남기는 구조화 로그의 시각 차를 쓴다. 잡 종류별 최댓값을 기록하고
    `REAP_TIMEOUT_SECONDS` 를 `max(관측 최댓값 × 4, 900)` 으로 유도한다 — 4배는 LLM 지연의
    꼬리를 덮는 여유이고, 하한 900초(=15분)는 `0003` 의 기본값이다. **실측이 15분을 지지하면
    값을 바꾸지 않는 것도 유효한 결론이다**(`reap-timeout-baseline.md` §한계가 이미 그렇게
    적었다). 표본 수가 적어 최댓값을 신뢰할 수 없으면 그 사실을 기록하고 하한을 그대로 쓴다 —
    02-08이 "성공 표본 200 미만이면 p99를 계산하지 않는다"고 한 것과 같은 규율이다.

    ⚠️ **"값을 바꾸지 않는다"는 결론도 수치로 검사받는다.** 이 페이즈가 경고하는 실패 양상은
    타임아웃이 정상 `compile` 잡의 지속시간보다 짧아 LLM 대기 중인 잡이 탈취되고 이중 과금되는
    것이다. 그러므로 유도의 산출물은 서술이 아니라 **부등식**이어야 한다 —
    `REAP_TIMEOUT_SECONDS > (compile 핸들러 지속시간 통계)`. 통계의 정의는 성공 표본이 200
    이상이면 p99, 미만이면 **관측 최댓값**(표본이 적을 때 p99의 보수적 상계다)이다.
    이 네 수를 `docs/ops/reap-timeout-baseline.md` 에 기계가 읽을 수 있는 **한 줄**로 남긴다.
    줄의 형식은 `reap_timeout_derivation` 로 시작하는 콜론 라벨 뒤에 공백으로 구분된 네 개의
    `키=값` 쌍이며, 키는 순서대로 관측된 compile 지속시간(초, 정수), 그 값이 어느 통계인지
    (`p99` 또는 `max`), 성공 표본 수, 그리고 유도된 타임아웃(초, 정수)이다. 정확한 키 이름과
    정규식은 아래 `<acceptance_criteria>` 가 글자 그대로 고정하므로 거기에 적힌 형태를 그대로
    따른다. 부등식이 성립하지 않으면 타임아웃을 **올려서** 성립시킨다 — 관측값을 내려 적어
    맞추지 않는다.

    **(c) 주기적 reaper 배선.** `WorkerSettings` 에 `REAP_TIMEOUT_SECONDS: int` 를 (b)에서
    유도한 값을 기본값으로 두어 추가하고, `REAP_ENABLED: bool = True` 를 둔다.
    `run_queue_loop` 에 `REAP_INTERVAL_CYCLES: Final[int]` 를 두어 빈 큐 폴링이 그 횟수만큼
    반복될 때마다 `await db.reap_stale_jobs(timeout=f"{settings.REAP_TIMEOUT_SECONDS} seconds")`
    를 부르고 회수된 잡 수를 로깅한다. ⚠️ 여러 워커가 동시에 불러도 안전하다는 사실
    (`reap_stale_jobs` 가 `for update skip locked` 를 쓴다, `0003:207`)과, **폴링이 바쁠 때가
    아니라 한가할 때** 부르는 이유(잡을 처리 중인 워커가 reap 쿼리로 자기 왕복을 늦출 이유가
    없다)를 주석으로 남긴다. `run_queue_loop` 의 시그니처 변경은 기존 호출부
    (`__main__.py`, `test_queue.py`)와 함께 고친다.

    **(d) `docs/ops/reap-timeout-baseline.md` (갱신).** 기존 문서를 지우지 말고 **Phase 3 절을
    덧붙인다** — 02-08의 전송 오버헤드 실측은 그대로 유효한 기록이다. 새 절에 담을 것:
    잡 종류별 관측 지속시간(표본 수 포함), 유도식과 그 결과값, 값을 바꿨는지 아닌지와 그 이유,
    주기적 reaper 배선의 위치와 호출 주기, **(b)가 규정한 `reap_timeout_derivation` 한 줄**,
    그리고 `§한계` 에 (a) 표본이 스모크 한 번 분량이라
    p99가 아니라 최댓값 기반이라는 사실, (b) 컴파일 잡의 지속시간이 소스 길이와 모델에 선형으로
    의존하므로 `LLM_MODEL` 을 바꾸면 이 값을 다시 봐야 한다는 사실, (c) 타임아웃이 짧으면
    LLM 대기 중인 잡이 reap되어 **이중 처리와 이중 과금**이 된다는 사실.
    02-CONTEXT.md > D-17의 이월이 여기서 닫혔음을 명시한다.

    관측을 지어내지 말 것 — 측정이 불가능하면 그 사실과 시도한 방법을 기록하고 하한값을 쓴다.
  </action>
  <verify>
    <automated>bash scripts/verify_shrink_reprocess.sh &amp;&amp; bash scripts/smoke_pipeline.sh &amp;&amp; uv run pytest -rs</automated>
    <human-check>`docs/ops/reap-timeout-baseline.md`의 Phase 3 절에 적힌 잡 종류별 지속시간이 실제 워커 로그에서 관측된 값과 일치한다 — 즉 `reap_timeout_derivation` 줄의 숫자가 로그에서 실제로 나온 값이지 채워 넣은 값이 아니다. (부등식 자체는 아래 자동 단언이 판정한다.)</human-check>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify_shrink_reprocess.sh` 가 exit 0으로 끝나고 `shrink_reprocess: ok` 를 출력한다.
    - 스크립트가 재처리 후 `source_chunks` 행 수가 재처리 전보다 **작음**을 단언한다.
    - 스크립트가 재처리 후 `max(chunk_index) + 1 = count(*)` 를 단언한다 (빈틈 없음 = 잔여 행 없음).
    - 스크립트 종료 후 스모크 워크스페이스의 `jobs` 잔여가 0행이다.
    - `uv run python -c "from worker.settings import WorkerSettings; print('REAP_TIMEOUT_SECONDS' in WorkerSettings.model_fields)"` 가 `True` 를 출력한다.
    - `uv run python -c "from worker.settings import WorkerSettings; import os; print(WorkerSettings.model_fields['REAP_TIMEOUT_SECONDS'].default >= 900)"` 가 `True` 를 출력한다.
    - `docs/ops/reap-timeout-baseline.md` 에 Phase 3 절이 있고 `한계` 를 제목에 포함한 하위 절을 갖는다.
    - `docs/ops/reap-timeout-baseline.md` 의 02-08 실측 절이 그대로 남아 있다.
    - `grep -cE '^reap_timeout_derivation: compile_observed_seconds=[0-9]+ statistic=(p99|max) sample_count=[0-9]+ reap_timeout_seconds=[0-9]+$' docs/ops/reap-timeout-baseline.md` 가 `1` 을 출력한다 — 유도 결과가 산문이 아니라 기계가 읽는 네 개의 수로 남았다.
    - `uv run python -c "import re,pathlib;from worker.settings import WorkerSettings;m=re.search(r'^reap_timeout_derivation: compile_observed_seconds=(\d+) statistic=(p99|max) sample_count=(\d+) reap_timeout_seconds=(\d+)\$', pathlib.Path('docs/ops/reap-timeout-baseline.md').read_text(), re.M);t=int(m.group(4));print(t > int(m.group(1)) and t == WorkerSettings.model_fields['REAP_TIMEOUT_SECONDS'].default)"` 가 `True` 를 출력한다 — reap 타임아웃이 관측된 compile 지속시간 통계보다 **크고**, 문서의 수와 코드의 기본값이 같다.
    - 위 줄의 `statistic` 이 `p99` 이면 `sample_count` 가 200 이상이고, `max` 이면 200 미만이다 (02-08의 표본 규율과 같은 형태).
    - `bash scripts/smoke_pipeline.sh` 가 여전히 exit 0이다.
    - `uv run pytest -rs` 가 exit 0이다.
    - `pre-commit run --all-files` 가 통과한다.
  </acceptance_criteria>
  <done>축소 재처리가 잔여 행을 남기지 않음이 실제 스택에서 관측되고, `REAP_TIMEOUT_SECONDS`가 문서에 숫자로 기록된 compile 지속시간 통계보다 크다는 부등식이 자동으로 확인된 채 주기적 reaper가 실제로 돈다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider 예외 → `jobs.last_error` → 멤버 SELECT → 프론트 | `jobs_select_member` 가 `select *` 를 열어 이 문자열이 사용자에게 도달한다 |
| 워커 프로세스 → `jobs` 상태 전이 | `service_role` 은 BYPASSRLS이고 상태 전이는 함수를 통해서만 일어나야 한다 |
| Python enum ↔ DB CHECK | 어긋나면 검증은 통과하고 INSERT에서 터진다 — LLM 비용을 쓴 뒤에 |
| 구조화 로그 → 배포 로그 수집 | `REDACTED_KEYS` 는 필드 **이름**만 본다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-54 | Information Disclosure | `jobs.last_error` 의 provider 본문 | critical | mitigate | `sanitize_error` 가 `ProviderError`/`HTTPStatusError` 에서 상태 코드만 남기고 본문을 버린다. 마지막에 자격증명 스크럽을 한 번 더 통과시킨다. 실제 본문을 담은 예외로 테스트가 이를 단언한다 |
| T-03-55 | Information Disclosure | 자격증명이 섞인 임의 예외 메시지 | high | mitigate | `scrub_credentials` 가 키 접두사·Bearer·JWT 형태를 치환한다. 2차 방어선임을 주석에 명시 — 1차는 클라이언트가 애초에 넣지 않는 것 |
| T-03-56 | Tampering | 늦게 깨어난 워커의 데드레터 | high | mitigate | `dead_letter_job` 의 `worker_id` 가 keyword-only·무기본값이고 SQL 술어가 락 소유자를 검사한다. `release_job` 과 동형 |
| T-03-57 | Denial of Service | 재시도 불가 실패의 `max_attempts` 왕복 | medium | mitigate | `NON_RETRYABLE_ERRORS` 분류로 즉시 `dead`. provider 장애는 이 목록에 넣지 않아 재시도 여지를 남긴다 |
| T-03-58 | Tampering | enum ↔ CHECK 불일치 | high | mitigate | 기동 시 8개 항목 대조. 실패는 예외로 프로세스를 죽인다 — 프로브와 달리 `try/except` 로 감싸지 않는다 |
| T-03-59 | Denial of Service | reap 타임아웃이 짧아 정상 잡 탈취 | high | mitigate | 실측 최댓값의 4배, 하한 900초. 유도 과정과 표본 수를 문서에 남긴다. `LLM_MODEL` 변경 시 재측정 필요를 한계로 명시 |
| T-03-60 | Information Disclosure | 로그 필드에 실린 소스·프롬프트 본문 | high | mitigate | 핸들러·클라이언트가 본문을 로그 필드로 싣지 않는다. ⚠️ `REDACTED_KEYS` 는 이름만 보므로 필드명을 바꾸면 비껴간다 — 이 한계를 주석과 금지항목으로 남긴다 |
| T-03-61 | Repudiation | 취소된 잡의 이미 발생한 비용 | medium | accept | 협조적 취소의 구조적 결과다. `usage_events` 행은 남고 03-07의 취소 응답이 그 요약을 돌려준다 |
| T-03-62 | Denial of Service | 축소 재처리의 잔여 행 | medium | mitigate | `verify_shrink_reprocess.sh` 가 `max(chunk_index) + 1 = count(*)` 를 단언한다 — 잔여 행이 있으면 이 등식이 깨진다 |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 플랜은 패키지를 설치하지 않는다 |
</threat_model>

## Artifacts this phase produces (이 플랜 몫)

**Python 심볼 (신규)**
- `worker.schema_guard` — `SchemaContractError` · `assert_enums_match_db`
- `worker.errors` — `scrub_credentials`
- `worker.db.service` — `CATALOG_RPC_FUNCTIONS` · `ServiceDb.dead_letter_job` · `ServiceDb.cancel_job` · `ServiceDb.reap_stale_jobs` · `ServiceDb.enum_check_values`
- `worker.queue` — `REAP_INTERVAL_CYCLES` (`QueueDb` Protocol에 `dead_letter_job` · `cancel_job` · `reap_stale_jobs` 추가)
- `worker.settings.WorkerSettings` — `REAP_TIMEOUT_SECONDS` · `REAP_ENABLED` (그리고 `OPENAI_API_KEY` **제거**)

**파일**
- `apps/worker/src/worker/schema_guard.py`
- `apps/worker/tests/test_schema_guard.py`
- `scripts/verify_shrink_reprocess.sh` (출력 토큰 `shrink_reprocess: ok`)
- `docs/ops/reap-timeout-baseline.md` Phase 3 절 (갱신)

**계약 변경**
- `.env.sample` §3에서 `OPENAI_API_KEY` 제거 (`supabase/config.toml` 은 유지)
- `worker.queue._dead_letter` 가 `fail_job` 왕복이 아니라 `dead_letter_job` 단일 호출

<verification>
- `bash scripts/verify_shrink_reprocess.sh` exit 0
- `bash scripts/smoke_pipeline.sh` exit 0
- `uv run pytest -rs` exit 0
- `uv run ruff check apps packages` exit 0
- `bash scripts/verify_queue_functions.sh` · `verify_search_contract.sh` · `verify_pipeline_ops.sh` 전부 exit 0
- `pre-commit run --all-files` exit 0
</verification>

<success_criteria>
- provider 응답 본문과 자격증명이 `jobs.last_error`에 도달하지 못한다
- 우리 자신의 실패 사유(`needs_ocr` 등)는 사용자에게 그대로 보인다
- 미등록 type과 재시도 불가 실패가 한 번에 `dead`가 된다
- `cancel_requested_at`이 찍힌 잡이 다음 체인 경계에서 `canceled`로 마감된다
- enum ↔ CHECK 불일치가 워커 기동 시점에 프로세스를 죽인다
- 축소 재처리 후 잔여 행이 없고 `chunk_index`에 빈틈이 없다
- reap 타임아웃이 실측에서 유도되고, 문서의 `reap_timeout_derivation` 한 줄이 `REAP_TIMEOUT_SECONDS > 관측 compile 지속시간 통계`를 수치로 증명하며, 주기적 reaper가 실제로 돈다
- 워커가 쓰지 않는 secret 키가 설정·샘플·배포 env에서 사라졌다
</success_criteria>

<output>
Create `.planning/phases/03-ingest-and-compile-pipeline/03-08-SUMMARY.md` when done
</output>
