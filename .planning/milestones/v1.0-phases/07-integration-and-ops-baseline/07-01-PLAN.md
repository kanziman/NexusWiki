---
phase: 07-integration-and-ops-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/api/tests/fixtures/pipeline.py, apps/api/tests/test_pipeline_e2e.py, apps/api/tests/test_reingestion_idempotency.py]
autonomous: true
requirements: [OPS-02, OPS-03]
user_setup: []
estimate: {tokens: 65000, raw_tokens: 65000, tasks: 3, confidence: medium}
must_haves:
  truths:
    - "OPS-02 crosses POST /sources, real queue RPCs, worker stages, Storage, and retrieval from an empty local workspace; only LLM/embedding and URL transports are doubles (D-01, D-02, D-04)."
    - "OPS-03 proves duplicate normalized content has no row-count growth and a multi-chunk shrinking reprocess removes stale chunks and embeddings."
    - "The focused suites remain in the normal pytest PR gate with visible local-stack skips (D-03)."
  artifacts:
    - {path: apps/api/tests/fixtures/pipeline.py, provides: shared local-stack sources, provider doubles, and queue drain}
    - {path: apps/api/tests/test_pipeline_e2e.py, provides: OPS-02 tracer proof}
    - {path: apps/api/tests/test_reingestion_idempotency.py, provides: OPS-03 idempotency/shrink proof}
  key_links:
    - "source API -> enqueue_source_job -> claim/complete_job_and_chain -> run_parse/run_compile/run_link_sync/run_embed -> retrieval API"
    - "parse reprocess -> upsert_and_truncate/delete_source_chunks_from -> source_chunks and wiki_embeddings"
assumptions:
  - "OPS-02 and OPS-03 were unclassified/unresolved in the deterministic edge probe; the task behavior tests below are their concrete pass/fail definitions."
---

<objective>
Deliver reusable real-local-Supabase integration evidence for OPS-02 and OPS-03. Preserve all
persistence behavior and replace only paid, nondeterministic provider boundaries.
</objective>

<context>
@.planning/phases/07-integration-and-ops-baseline/07-CONTEXT.md
@.planning/phases/07-integration-and-ops-baseline/07-RESEARCH.md
@.planning/phases/07-integration-and-ops-baseline/07-PATTERNS.md
@.planning/codebase/TESTING.md
@apps/api/tests/conftest.py
@apps/api/tests/test_jobs_router.py
@apps/api/src/api/routers/sources.py
@apps/api/src/api/routers/retrieval.py
@apps/worker/tests/test_handlers.py
@apps/worker/src/worker/db/service.py
@apps/worker/src/worker/handlers/parse.py
</context>

<tasks>
<task type="auto" tdd="true">
<name>Task 1: Build the shared local-stack tracer fixture and OPS-02 test</name>
<precondition>apps/api/tests/conftest.py local_stack remains fixed to its loopback-only Supabase address.</precondition>
<files>apps/api/tests/fixtures/pipeline.py, apps/api/tests/test_pipeline_e2e.py</files>
<behavior>
 - Create exactly one text, file, and URL source through existing API paths, returning ids/job ids and workspace-prefixed file Storage paths (D-04).
 - Use deterministic compile and 1024-dimension embed/query doubles, while Postgres/RLS/queue/Storage/HNSW remain real (D-01).
 - Drain jobs through claim and complete/fail queue RPCs and established run_parse/run_compile/run_link_sync/embed seams; no direct jobs mutation.
 - Assert 202 API responses, terminal jobs, persisted chunks/pages/links/embeddings, and exact source/wiki evidence returned by retrieval (D-02).
</behavior>
<read_first>apps/api/tests/conftest.py (LOCAL_STACK/authed_client/user_db), apps/api/tests/test_jobs_router.py (_enqueue), apps/worker/tests/test_handlers.py (run handler seams), apps/worker/src/worker/db/service.py (queue RPC methods), apps/api/src/api/routers/retrieval.py (retrieval_service injection), 07-PATTERNS.md (pipeline fixture and OPS-02).</read_first>
<action>Create the Phase-7-only `fixtures/pipeline.py` helper and the API-level tracer test. Per D-01/D-04, intercept only external URL, LLM, and embedding clients with deterministic transports. Invoke real source routes and real queue RPC state transitions, then established worker run seams against local Supabase. Per D-02, start empty, create all three sources, drain the complete chain, call retrieval through the API, and assert state/evidence identity rather than generated prose.</action>
<verify><automated>UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run pytest -q apps/api/tests/test_pipeline_e2e.py</automated></verify>
<done>OPS-02 has a repeatable real-persistence tracer from source creation to retrieval with no paid provider calls (D-01, D-02, D-04).</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Prove duplicate ingest and genuinely shrinking reprocessing</name>
<precondition>Task 1 produces the shared local-stack fixture and queue drain helper.</precondition>
<files>apps/api/tests/test_reingestion_idempotency.py</files>
<behavior>
 - Duplicate normalized text uses the existing API duplicate contract and leaves raw_sources, source_chunks, pages, and embeddings counts unchanged.
 - A long source creates more than one chunk, then a supported shorter reprocess produces fewer chunks.
 - Remaining chunk indexes equal range(new_count), and no embedding references a removed chunk.
</behavior>
<read_first>apps/api/src/api/routers/sources.py (duplicate handling), apps/worker/src/worker/handlers/parse.py (reprocess), apps/worker/src/worker/db/service.py (upsert_and_truncate/delete_source_chunks_from), apps/api/tests/test_sources_router.py, 07-PATTERNS.md (OPS-03), .planning/WINDOWS.md (item 6).</read_first>
<action>Add the OPS-03 integration module using the D-04 fixture. Post equivalent normalized text twice through the API and compare scoped row counts around the established duplicate response. Independently run a multi-chunk source then a materially shorter body through the supported reprocess path; query real rows to prove contiguous retained indexes and removal of stale source chunks and embeddings. A same-size rerun is not sufficient.</action>
<verify><automated>UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run pytest -q apps/api/tests/test_reingestion_idempotency.py</automated></verify>
<done>OPS-03 proves both duplicate no-growth and actual shrink deletion, including stale embedding cleanup.</done>
</task>
<task type="auto">
<name>Task 3: Keep both suites in the existing pytest PR gate</name>
<files>apps/api/tests/test_pipeline_e2e.py, apps/api/tests/test_reingestion_idempotency.py</files>
<read_first>.github/workflows/ci.yml (pytest job and -rs), 02-09-PLAN.md (four-job CI contract), 07-RESEARCH.md (CI pitfall).</read_first>
<action>Verify both conventionally named modules are collected by the existing pytest job and retain `-rs` skip output. Per D-03, do not create another workflow, credentials, Cloud target, browser runner, or paid-provider path; local-stack skips remain explicit and authoritative execution follows `supabase start` locally.</action>
<verify><automated>UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run pytest -rs apps/api/tests/test_pipeline_e2e.py apps/api/tests/test_reingestion_idempotency.py</automated></verify>
<done>The OPS-02/03 tests are in the normal pytest gate with transparent local-stack skip reporting (D-03).</done>
</task>
</tasks>

## Artifacts this phase produces

- `apps/api/tests/fixtures/pipeline.py` — shared deterministic-provider, real-local-stack test utilities
- `apps/api/tests/test_pipeline_e2e.py` — OPS-02 tracer evidence
- `apps/api/tests/test_reingestion_idempotency.py` — OPS-03 idempotency and shrink evidence

<threat_model>
## ASVS L1 threat model
| Threat | Severity | Mitigation and blocker |
|---|---:|---|
| Test calls cloud or a paid provider | high | Loopback fixture and deterministic provider seams; block if a test reads cloud settings or calls OpenRouter. |
| Test bypasses actual queue/RLS/Storage | high | Requester JWT plus queue RPC-only drain; block direct job writes or mocked persistence. |
| Stale shrink rows pass unnoticed | high | Contiguous index and absent-embedding assertions; block a same-size-only test. |
</threat_model>
