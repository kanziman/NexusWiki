---
phase: 07-integration-and-ops-baseline
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [apps/api/tests/conftest.py, apps/api/tests/test_tenant_isolation_full_path.py]
autonomous: true
requirements: [OPS-04]
user_setup: []
estimate: {tokens: 70000, raw_tokens: 70000, tasks: 3, confidence: medium}
must_haves:
  truths:
    - "All nine tables—workspaces, workspace_members, raw_sources, wiki_pages, source_chunks, wiki_embeddings, wiki_links, prompt_templates, jobs—have requester-visible SELECT/INSERT/UPDATE/DELETE isolation proof (D-09)."
    - "A owner/editor/viewer, B owner, non-member, and anon principals run against real local Supabase; service role is setup-only, and the matrix consumes the one file/URL/text source set created by the shared Phase-7 pipeline fixture (D-04, D-09)."
    - "Each foreign denial has own-workspace allowed controls and both directions; reads are empty, mapped writes are 403, and WITH CHECK/definer cases assert 42501."
    - "Queue RPCs and API-generated Storage paths are included in the normal pytest gate (D-03, D-09)."
  artifacts:
    - {path: apps/api/tests/conftest.py, provides: role-aware local principal helpers}
    - {path: apps/api/tests/test_tenant_isolation_full_path.py, provides: 9-table/RPC/Storage matrix}
  key_links:
    - "matrix -> requester JWT -> API/UserDb -> local Supabase RLS policies"
    - "pipeline.py API-created file/URL/text sources -> matrix setup -> requester JWT Storage requests"
assumptions:
  - "OPS-04 was unclassified/unresolved in the deterministic edge probe; the explicit 9-table CRUD, queue/Storage, direction/control, and anonymous tests below define its requirement proof."
---
<objective>Deliver OPS-04 as a full API-level local-Supabase isolation matrix that consumes the D-04 shared file/URL/text fixture from Plan 07-01, extending rather than replacing Phase 2's single-table security proof.</objective>
<context>
@.planning/phases/07-integration-and-ops-baseline/07-CONTEXT.md
@.planning/phases/07-integration-and-ops-baseline/07-RESEARCH.md
@.planning/phases/07-integration-and-ops-baseline/07-PATTERNS.md
@.planning/codebase/TESTING.md
@apps/api/tests/conftest.py
@apps/api/tests/fixtures/pipeline.py
@apps/api/tests/test_workspaces_isolation.py
@apps/api/tests/test_jobs_router.py
@supabase/migrations/0004_rls.sql
@supabase/migrations/0007_search_and_queue_extensions.sql
</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Add role-complete local principal and setup helpers</name>
<precondition>Plan 07-01 has created `apps/api/tests/fixtures/pipeline.py`, and apps/api/tests/conftest.py local-stack guard is fixed to loopback addresses.</precondition>
<files>apps/api/tests/conftest.py, apps/api/tests/test_tenant_isolation_full_path.py</files>
<behavior>
 - Supply A owner/editor/viewer, B owner, authenticated non-member, and anonymous request clients with fresh function-scoped identities.
 - Reuse the D-04 API-created text/file/URL source records from `pipeline.py`; permit service-role setup only for derived rows without requester-visible creation paths, while assertion actions always use a subject JWT or none.
 - Prove fixture ids are distinct and cleanup remains successful.
</behavior>
<read_first>apps/api/tests/conftest.py (TenantActor/workspace_member_with_role), apps/api/tests/fixtures/pipeline.py (shared API-created text/file/URL sources and fixture contract), apps/api/tests/test_workspaces_isolation.py (principal assertions), .planning/codebase/TESTING.md (RLS fixture pattern), 07-PATTERNS.md (isolation section).</read_first>
<action>Extend `conftest.py` without weakening its loopback guard to create only the D-09 principal helpers. Do not create another source fixture there. Import and consume Plan 07-01's `pipeline.py` fixture in the new suite so its API-created text, file, and URL records are the common D-04 setup for source-backed rows and Storage paths. Keep all actors function scoped and cleanup-backed. Add deliberately narrow derived-row setup helpers and document service-role use as setup-only. Begin the new suite with identity freshness and own-requester controls so an anonymous response or reused state cannot satisfy later tenant assertions.</action>
<verify><automated>UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run pytest -q apps/api/tests/test_tenant_isolation_full_path.py -k 'fixture or control'</automated></verify>
<done>The suite has safe reusable fixtures for every D-09 principal and cannot confuse authentication failure with isolation.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Implement the declarative nine-table, queue RPC, and Storage matrix</name>
<precondition>Plan 07-01's shared `pipeline.py` fixture and Task 1's principal helpers are available, and migrations through 0009 are applied locally.</precondition>
<files>apps/api/tests/test_tenant_isolation_full_path.py</files>
<behavior>
 - Each table has SELECT/INSERT/UPDATE/DELETE coverage where its contract permits the request, with setup, own control, foreign action, and exact expected shape.
 - Cross-workspace cases run A-to-B and B-to-A; non-member and anon cases are separate.
 - Queue transitions use public RPC contracts, and foreign Storage get/list/delete uses user JWTs on the shared pipeline fixture's API-created file path.
</behavior>
<read_first>apps/api/tests/fixtures/pipeline.py (D-04 shared source ids and API-created Storage path), apps/api/tests/test_workspaces_isolation.py (bidirectional/0-row/403 discipline), apps/api/tests/test_jobs_router.py (queue contracts), supabase/migrations/0007_search_and_queue_extensions.sql (grants/RPCs), apps/worker/src/worker/handlers/parse.py (Storage prefix boundary), 07-PATTERNS.md.</read_first>
<action>Implement a table-driven D-09 matrix covering `workspaces`, `workspace_members`, `raw_sources`, `wiki_pages`, `source_chunks`, `wiki_embeddings`, `wiki_links`, `prompt_templates`, and `jobs`. Seed source-backed matrix rows by consuming the single D-04 text/file/URL set from `pipeline.py`, then derive only rows that have no requester-visible source creation path. Every row declares setup, own-workspace control, foreign operation, and expected response; run both directions. Add queue RPC and Storage cases against the fixture's API-created file path. Keep RLS semantics distinct: blocked reads assert empty results, mapped API writes assert canonical 403, and direct WITH CHECK/definer denials assert SQLSTATE 42501. Never directly update jobs or use a service-role client for an assertion.</action>
<verify><automated>UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run pytest -q apps/api/tests/test_tenant_isolation_full_path.py</automated></verify>
<done>All nine tables, queue RPCs, and Storage paths have bidirectional deny proof and verified own-workspace access (D-09).</done>
</task>
<task type="auto">
<name>Task 3: Retain full-path isolation in the existing pytest PR gate</name>
<files>apps/api/tests/test_tenant_isolation_full_path.py</files>
<read_first>.github/workflows/ci.yml, 02-09-PLAN.md, 07-RESEARCH.md (local-stack CI behavior).</read_first>
<action>Verify the conventionally named new module is collected by the existing pytest job with `-rs`. Per D-03/D-09, add no Cloud credentials, new job, browser test, or on-demand suite; developer-local `supabase start` remains the authoritative matrix environment.</action>
<verify><automated>UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run pytest -rs apps/api/tests/test_tenant_isolation_full_path.py</automated></verify>
<done>OPS-04 runs through the established pytest gate with local-stack availability reported transparently (D-03).</done>
</task>
</tasks>

## Artifacts this phase produces

- `apps/api/tests/test_tenant_isolation_full_path.py` — complete table/RPC/Storage isolation proof
- `apps/api/tests/conftest.py` — role-aware loopback-only fixtures

<threat_model>
## ASVS L1 threat model
| Threat | Severity | Mitigation and blocker |
|---|---:|---|
| Cross-tenant read/write exposure | high | Own controls and bidirectional foreign cases for every matrix row; block missing table/action coverage. |
| Privileged test path masks RLS defects | high | Service role is setup-only; block if it makes an assertion request. |
| Queue/Storage tenant bypass | high | Use public requester-JWT RPC/Storage paths; block direct SQL/job-state mutation. |
</threat_model>
