---
phase: 02-security-spine-and-shared-domain
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/core/src/nexuswiki_core/settings.py
  - packages/core/pyproject.toml
  - packages/core/tests/test_settings.py
  - packages/core/tests/test_logging_redaction.py
  - apps/api/src/api/settings.py
  - apps/api/src/api/main.py
  - apps/api/src/api/routers/health.py
  - apps/api/pyproject.toml
  - apps/api/tests/test_health.py
  - apps/worker/src/worker/settings.py
  - apps/worker/src/worker/__main__.py
  - apps/worker/pyproject.toml
  - apps/worker/tests/test_settings.py
  - pyproject.toml
autonomous: false
requirements: [SEC-01]
user_setup: []

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "ApiSettings.model_fields에 SUPABASE_SECRET_KEY · DATABASE_URL · OPENROUTER_API_KEY · OPENAI_API_KEY가 하나도 없다"
    - "필수 env가 누락되거나 빈 문자열일 때 해당 키 이름을 포함한 메시지로 기동이 실패한다"
    - "create_app()이 ApiSettings를 주입받고, api와 worker 진입 경로가 환경변수를 직접 읽지 않는다"
    - "WorkerSettings의 secret 필드 이름들이 REDACTED_KEYS의 문자열과 casefold 일치해 로그 마스킹이 계속 덮는다"
    - "pytest --collect-only가 저장소 루트에서 apps/worker/tests를 수집한다"
    - "ruff check가 TID 규칙을 적용한다"
  artifacts:
    - packages/core/src/nexuswiki_core/settings.py
    - apps/api/src/api/settings.py
    - apps/worker/src/worker/settings.py
    - packages/core/tests/test_settings.py
  key_links:
    - "BaseAppSettings → ApiSettings / WorkerSettings 상속 경계가 SEC-01의 역량 부재 지점"
    - "WorkerSettings → 02-03-PLAN의 service_client(settings: WorkerSettings) 팩토리 인자 타입"
    - "create_app(settings) → 02-03-PLAN의 단일 예외 핸들러 등록 지점"
    - "REDACTED_KEYS ↔ WorkerSettings 필드명 casefold 일치"
---

<objective>
api 프로세스가 service key를 담을 **필드 자체를 갖지 않도록** Settings 계층을 분리하고, 그 규칙이 실제로 집행될 수 있도록 루트 툴링(ruff `TID` · pytest `testpaths`)을 정합시킨다.

Purpose: SEC-01의 주장("api는 키를 담을 그릇이 없다")이 현재 코드에 존재하지 않는다 — `apps/api/src/api/main.py:24`가 환경을 직접 읽는다. 단일 Dockerfile(01-CONTEXT.md > D-01) 때문에 import는 막히지 않으므로, 격리의 1차 방어선은 **키의 부재**다 (02-CONTEXT.md > D-06). 이 인과를 뒤집어 서술하면 사실과 어긋난다.
Output: `BaseAppSettings` / `ApiSettings` / `WorkerSettings` 3계층 · `create_app(settings)` 주입 · 루트 `pyproject.toml`의 `TID`와 `testpaths` 수정
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-security-spine-and-shared-domain/02-SPEC.md
@.planning/phases/02-security-spine-and-shared-domain/02-CONTEXT.md
@.planning/phases/02-security-spine-and-shared-domain/02-PATTERNS.md
@packages/core/src/nexuswiki_core/logging.py
@apps/api/src/api/main.py
@apps/api/src/api/routers/health.py
@apps/worker/src/worker/__main__.py
</context>

## Artifacts this phase produces

| Artifact | Kind | Notes |
|---|---|---|
| `packages/core/src/nexuswiki_core/settings.py` | file | 공통 조상 모듈 |
| `BaseAppSettings` | Python class | `SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`, `ENVIRONMENT`, `LOG_LEVEL` |
| `MissingSettingError` | Python exception | 누락/빈 문자열 env를 키 이름과 함께 보고 |
| `apps/api/src/api/settings.py` / `ApiSettings` | file / class | `BaseAppSettings`를 필드 추가 없이 상속 |
| `apps/worker/src/worker/settings.py` / `WorkerSettings` | file / class | `SUPABASE_SECRET_KEY`, `DATABASE_URL`, `OPENROUTER_API_KEY`, `OPENAI_API_KEY`, `LLM_MODEL` 추가 |
| `create_app(settings: ApiSettings)` | changed signature | `apps/api/src/api/main.py` |
| `app.state.settings` | app attribute | 라우터가 환경 대신 읽는 지점 |
| `packages/core/tests/test_settings.py` | file | `model_fields` 고정 테스트 |
| `apps/worker/tests/test_settings.py` | file | `WorkerSettings` 필드·마스킹 정합 테스트 |
| root `pyproject.toml` `TID` in `select`, `apps/worker/tests` in `testpaths` | config change | R12 |

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Gate: pydantic-settings 패키지 정당성 확인</name>
  <what-built>아직 아무것도 만들지 않았다. 이 플랜은 `pydantic-settings`를 `packages/core`의 신규 고정 의존성으로 추가한다.</what-built>
  <how-to-verify>
RESEARCH.md가 없어 `## Package Legitimacy Audit` 표를 참조할 수 없다. 폴백 정책상 `pydantic-settings`는 `[ASSUMED]`이며 설치 전 사람이 확인해야 한다.

1. `https://pypi.org/project/pydantic-settings/` 를 연다
2. 확인할 것: 배포 조직이 `pydantic` 팀인지 · 최근 릴리스 날짜 · 프로젝트 홈페이지가 `github.com/pydantic/pydantic-settings`를 가리키는지 · 월 다운로드 규모
3. 고정할 정확한 버전을 정한다 (`packages/core/pyproject.toml`은 `orjson==3.11.9` 처럼 정확한 버전만 쓴다)
  </how-to-verify>
  <resume-signal>확인 결과와 고정할 버전 문자열을 함께 적을 것. 예: "approved — pydantic-settings==2.13.0". 문제가 있으면 무엇이 이상한지 적을 것.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: BaseAppSettings / ApiSettings / WorkerSettings 3계층</name>
  <files>packages/core/src/nexuswiki_core/settings.py, packages/core/pyproject.toml, apps/api/src/api/settings.py, apps/api/pyproject.toml, apps/worker/src/worker/settings.py, apps/worker/pyproject.toml, packages/core/tests/test_settings.py, apps/worker/tests/test_settings.py, packages/core/tests/test_logging_redaction.py</files>
  <read_first>
    - packages/core/src/nexuswiki_core/logging.py — 1-14행 헤더·import 패턴, 16-33행 `REDACTED_KEYS` 상수 배치, 67행 keyword-only 공개 시그니처
    - packages/core/pyproject.toml — 9-12행 정확 버전 고정 스타일
    - apps/api/src/api/routers/health.py — 28-34행 현재의 "빈 문자열도 누락으로 취급하는" 검사 루프. 이 의미론을 pydantic 검증기로 옮긴다
    - apps/api/pyproject.toml, apps/worker/pyproject.toml — 의존성 선언 위치
    - .planning/phases/02-security-spine-and-shared-domain/02-CONTEXT.md — D-06, D-07, D-10, D-22
    - .env.sample — 실제 키 이름 (`SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY`, `DATABASE_URL`, `OPENROUTER_API_KEY`, `OPENAI_API_KEY`, `LLM_MODEL`, `ENVIRONMENT`, `LOG_LEVEL`)
  </read_first>
  <behavior>
    - Test 1: `ApiSettings.model_fields`에 `SUPABASE_SECRET_KEY`, `DATABASE_URL`, `OPENROUTER_API_KEY`, `OPENAI_API_KEY` 네 이름이 하나도 없다
    - Test 2: `WorkerSettings.model_fields`에 그 네 이름이 전부 있다
    - Test 3: `SUPABASE_URL`이 환경에 없을 때 생성이 실패하고 예외 메시지에 `SUPABASE_URL` 문자열이 들어 있다
    - Test 4: `SUPABASE_URL`이 빈 문자열일 때도 같은 방식으로 실패한다 (SPEC Edge Coverage R1)
    - Test 5: `WorkerSettings`의 secret 필드 이름을 casefold한 집합이 `REDACTED_KEYS`의 부분집합이다
    - Test 6: `LLM_MODEL`에 코드상의 기본값이 없다 — 환경에서만 읽는다 (D-22)
  </behavior>
  <action>
`packages/core/src/nexuswiki_core/settings.py`를 만든다. 헤더는 `logging.py:1-5`와 같은 형태 — 한국어 한 줄 요약, `관련 태스크: P2-BE-01`, `설계 근거: 02-CONTEXT.md > D-06, D-07, D-10`. 근거를 재서술하지 않는다.

`BaseAppSettings`는 `pydantic_settings.BaseSettings`를 상속하고 `SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`, `ENVIRONMENT`, `LOG_LEVEL` **네 개만** 선언한다. `ENVIRONMENT`와 `LOG_LEVEL`은 기본값을 갖고(현재 코드가 각각 `development` / `INFO`를 쓰고 있다), `SUPABASE_URL`과 `SUPABASE_PUBLISHABLE_KEY`는 필수다. `model_config`에서 `env_file`을 읽지 않도록 하고 필드 이름을 그대로 환경변수 이름으로 쓴다(대문자 필드명 = 대문자 env 키).

빈 문자열을 누락과 동일하게 취급한다. 필수 필드에 `str` 대신 공백 제거 후 길이 검사를 하는 검증기를 붙이고, 실패 시 `MissingSettingError`를 던지되 메시지에 **해당 키 이름을 그대로** 포함시킨다. 현재 `health.py:33`이 `missing_env:{key}` 형태로 키를 밝히고 있으므로 그 의미론을 보존한다. <!-- planner-discipline-allow: missing_env --> 이 클래스에 대해 `⚠️` 주석을 남긴다: worker가 secret 없이 조용히 뜨면 BYPASSRLS 경로가 인증 없이 도는 것과 같아진다. 근거는 `02-CONTEXT.md > D-10`.

`apps/api/src/api/settings.py`에 `ApiSettings(BaseAppSettings)`를 두되 **필드를 하나도 추가하지 않는다**. 클래스 본문에 `⚠️` 주석을 남긴다 — 이 클래스에 secret 필드를 추가하는 순간 SEC-01이 무너지고, 단일 이미지(01-CONTEXT.md > D-01) 때문에 import 차단으로는 이를 되찾을 수 없다. 근거는 `02-CONTEXT.md > D-06`.

`apps/worker/src/worker/settings.py`에 `WorkerSettings(BaseAppSettings)`를 두고 `SUPABASE_SECRET_KEY`, `DATABASE_URL`, `OPENROUTER_API_KEY`, `OPENAI_API_KEY`, `LLM_MODEL` 다섯 개를 **추가로** 선언한다. `LLM_MODEL`에 코드 기본값을 박지 않는다 — `.env.sample`과 PROJECT.md의 기본값이 서로 다르고 Phase 2에는 검증 수단이 없다 (02-CONTEXT.md > D-22). 필드 이름들이 `logging.py:28-31`의 `REDACTED_KEYS` 원소(`supabase_secret_key`, `openrouter_api_key`, `openai_api_key`, `database_url`)와 casefold 일치해야 하며, 어긋나면 로그 마스킹이 **조용히** 이 값들을 덮지 않게 된다. `⚠️` 주석으로 이 커플링을 필드 블록 바로 위에 남긴다.

체크포인트에서 승인된 버전으로 `packages/core/pyproject.toml`의 `dependencies`에 `pydantic-settings`를 정확한 버전으로 고정 추가한다(기존 `orjson==3.11.9` 스타일). `apps/api`와 `apps/worker`는 각자 `settings.py`에서 `pydantic-settings`를 직접 import하므로 두 `pyproject.toml`의 `dependencies`에도 같은 고정 버전을 추가한다. `uv lock`으로 잠금 파일을 갱신한다.

`packages/core/tests/test_settings.py`와 `apps/worker/tests/test_settings.py`를 `packages/core/tests/test_logging_redaction.py` 형식으로 쓴다 — 한국어 한 줄 모듈 docstring, 긴 서술형 `test_<behavior>` 이름, `-> None` 반환 주석, 클래스 없이 평범한 `assert`. 위 `<behavior>`의 6개 케이스를 전부 덮는다. `packages/core/tests/test_logging_redaction.py`에는 `WorkerSettings` 필드명 집합이 `REDACTED_KEYS`에 덮인다는 단언을 추가한다 (PATTERNS.md `### Secret handling`).
  </action>
  <verify>
    <automated>uv sync && uv run pytest packages/core/tests/test_settings.py apps/worker/tests/test_settings.py packages/core/tests/test_logging_redaction.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "from api.settings import ApiSettings; f={'SUPABASE_SECRET_KEY','DATABASE_URL','OPENROUTER_API_KEY','OPENAI_API_KEY'}; assert f.isdisjoint(ApiSettings.model_fields), sorted(f & set(ApiSettings.model_fields)); print('ok')"` 가 `ok`를 출력한다
    - `uv run python -c "from worker.settings import WorkerSettings; f={'SUPABASE_SECRET_KEY','DATABASE_URL','OPENROUTER_API_KEY','OPENAI_API_KEY','LLM_MODEL'}; assert f <= set(WorkerSettings.model_fields); print('ok')"` 가 `ok`를 출력한다
    - `SUPABASE_URL`을 지운 환경에서 `ApiSettings()` 생성이 예외를 던지고 그 문자열 표현에 `SUPABASE_URL`이 포함된다
    - `SUPABASE_URL=""`(빈 문자열) 환경에서도 같은 예외가 나온다
    - `uv run python -c "from worker.settings import WorkerSettings; from nexuswiki_core.logging import REDACTED_KEYS; s={'SUPABASE_SECRET_KEY','DATABASE_URL','OPENROUTER_API_KEY','OPENAI_API_KEY'}; assert {k.casefold() for k in s} <= REDACTED_KEYS; print('ok')"` 가 `ok`를 출력한다
    - `uv run python -c "from worker.settings import WorkerSettings; d=WorkerSettings.model_fields['LLM_MODEL'].default; assert d is None or repr(d)=='PydanticUndefined', d; print('ok')"` 가 `ok`를 출력한다
    - `grep -c 'pydantic-settings==' packages/core/pyproject.toml apps/api/pyproject.toml apps/worker/pyproject.toml` 가 세 파일 모두 1을 보고한다
    - `packages/core/src/nexuswiki_core/settings.py`와 `apps/api/src/api/settings.py`가 각각 `02-CONTEXT.md > D-` 문자열을 헤더에 포함한다
  </acceptance_criteria>
  <done>`ApiSettings`에 secret 필드가 존재하지 않고, `WorkerSettings`만 그것을 갖고, 누락·빈 문자열이 키 이름을 밝히며 실패하는 것이 테스트로 고정되었다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: create_app(settings) 주입과 진입 경로의 환경 직접 읽기 제거</name>
  <files>apps/api/src/api/main.py, apps/api/src/api/routers/health.py, apps/worker/src/worker/__main__.py, apps/worker/src/worker/settings.py, apps/api/tests/test_health.py</files>
  <read_first>
    - apps/api/src/api/main.py — 전체 (9행 import, 20-36행 lifespan과 `create_app`, 39행 모듈 레벨 `app`)
    - apps/api/src/api/routers/health.py — 전체 (7행 import, 22행, 28-39행)
    - apps/api/tests/test_health.py — 10행이 `api.main.app`을 직접 import하고 22-24행이 환경을 지운다. 시그니처를 바꾸면 여기가 함께 움직인다
    - apps/worker/src/worker/__main__.py — 22-46행 설정 읽기 구간
    - apps/api/src/api/health_check.py — `check_db_roundtrip`의 keyword-only 인자 형태
    - .planning/phases/02-security-spine-and-shared-domain/02-PATTERNS.md — `apps/api/src/api/routers/workspaces.py` 절의 "Settings injection point"
  </read_first>
  <behavior>
    - Test 1: `create_app(settings=ApiSettings(...))`가 만든 앱의 `app.state.settings`가 그 인스턴스와 동일하다
    - Test 2: `/health`가 여전히 200과 `status: ok`를 반환하고 Supabase 설정 없이도 동작한다
    - Test 3: 도달 불가한 Supabase 주소로 구성된 설정에서 `/health/ready`가 503과 `db_unreachable` 또는 `db_roundtrip_timeout` 사유를 반환한다
    - Test 4: 필수 설정이 비어 있으면 앱 생성 이전에 실패한다 — `/health/ready`가 런타임에 사유를 만들어내지 않는다
  </behavior>
  <action>
`create_app`이 `ApiSettings`를 인자로 받도록 바꾸고 `app.state.settings`에 저장한다. `lifespan`은 `app.state.settings`에서 값을 읽어 `configure_logging(environment=..., log_level=...)`를 호출한다. 모듈 최하단의 즉시 생성 `app` 객체는 `apps/api/tests/test_health.py:10`과 uvicorn 진입점이 함께 쓰므로 유지하되, 설정 인스턴스를 그 자리에서 만들어 넘긴다. ⚠️ 부팅 시점 실패가 여기서 일어난다 — 설정이 불완전하면 프로세스가 라우터를 세우기 전에 키 이름을 밝히며 죽는다 (02-CONTEXT.md > D-10).

`apps/api/src/api/routers/health.py`에서 환경변수 직접 읽기를 전부 제거한다 <!-- planner-discipline-allow: os.environ -->. `/health`의 git SHA는 설정 계층이 아니라 배포 메타데이터이므로 `BaseAppSettings`에 `GIT_SHA` 필드를 추가하지 말고, `create_app`이 만들 때 한 번 읽어 `app.state.git_sha`로 넘긴다 — 라우터는 `request.app.state`만 본다. `/health/ready`의 필수 env 루프는 삭제한다: 부팅 시점 검증이 그 역할을 이미 했으므로 런타임에 같은 검사를 반복하면 죽은 코드가 된다. `check_db_roundtrip` 호출은 `request.app.state.settings`의 값으로 바꾼다.

`apps/worker/src/worker/__main__.py`도 같은 원칙으로 바꾼다: `main()`이 진입 즉시 `WorkerSettings`를 만들고, 이후 모든 값(로깅 설정, RTT 프로브의 Supabase URL/키, git SHA)을 그 인스턴스에서 읽는다. `RTT_PROBE_ENABLED`는 운영 토글이지 자격증명이 아니므로 `WorkerSettings`에 기본값 `True`인 불리언 필드로 올린다. 40-46행의 "설정이 없으면 건너뛴다" 분기 중 **필수 키에 대한 것**은 제거한다 — 필수 키는 이제 기동 시점에 실패한다. 프로브 비활성 토글 분기는 남긴다.

`apps/api/tests/test_health.py`를 새 시그니처에 맞춰 갱신한다. `app_client()` 헬퍼의 형태는 유지하되(02-04-PLAN의 격리 테스트가 이 헬퍼를 그대로 쓴다) 테스트별로 필요한 설정을 만들어 `create_app`에 넘기는 팩토리 형태로 바꾼다. 위 `<behavior>` 4개 케이스를 덮는다.
  </action>
  <verify>
    <automated>uv run pytest apps/api/tests/test_health.py -x -q && uv run ruff check apps packages</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'os\.environ' apps/api/src/api/ apps/worker/src/worker/__main__.py` 가 아무것도 출력하지 않는다 (exit 1)
    - `uv run python -c "import inspect; from api.main import create_app; p=inspect.signature(create_app).parameters; assert 'settings' in p, list(p); print('ok')"` 가 `ok`를 출력한다
    - `uv run pytest apps/api/tests/test_health.py -q` 가 exit 0
    - `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8000/health` 를 대신해, `apps/api/tests/test_health.py`의 `/health` 테스트가 200과 `status: ok`를 단언한다
    - `grep -c 'missing_env' apps/api/src/api/routers/health.py` 가 0 — 런타임 필수 env 루프가 제거되었다
    - `uv run ruff check apps packages` 가 exit 0
  </acceptance_criteria>
  <done>api와 worker의 진입 경로가 환경을 직접 읽지 않고 주입된 Settings 인스턴스만 보며, 기존 health 테스트가 전부 통과한다.</done>
</task>

<task type="auto">
  <name>Task 3: 루트 pyproject.toml 툴링 정합 — TID 활성화와 worker 테스트 수집</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml — 20-23행 `[tool.pytest.ini_options]`의 `testpaths`, 30-35행 `[tool.ruff.lint]`의 `select`와 `per-file-ignores`
    - apps/worker/tests/test_rtt.py — 현재 수집되지 않는 테스트. `testpaths` 수정 후 이것이 실제로 통과해야 한다
    - .planning/phases/02-security-spine-and-shared-domain/02-CONTEXT.md — D-09, D-14
    - .planning/phases/02-security-spine-and-shared-domain/02-PATTERNS.md — `### Ruff / tooling` 절
  </read_first>
  <action>
루트 `pyproject.toml` 한 파일만 고친다. 두 가지 결함이 각각 다른 요구사항의 집행 수단을 무력화하고 있다.

첫째, `[tool.ruff.lint]`의 `select` 배열에 `"TID"`를 추가한다. 현재 `["E", "F", "I", "UP", "B", "ASYNC", "S"]`이고 `TID`가 없어 `flake8-tidy-imports`의 banned-api 규칙이 **선택되지 않은 상태**다 — 02-03-PLAN이 규칙을 써도 동작하지 않는다 (02-CONTEXT.md > D-09). 실제 banned-api 설정 블록과 worker 예외 처리는 02-03-PLAN이 같은 파일에 이어 붙인다. 이 태스크는 규칙군을 켜는 것까지다.

둘째, `[tool.pytest.ini_options]`의 `testpaths`에 `"apps/worker/tests"`를 추가한다. 현재 `["packages/core/tests", "apps/api/tests"]`라서 `apps/worker/tests/test_rtt.py`가 **조용히 수집되지 않는다**. Phase 2가 worker 테스트를 여러 개 추가하므로(02-07-PLAN, 02-08-PLAN) 이걸 먼저 고치지 않으면 그 테스트들이 통과하는지 아무도 모르는 상태가 된다 (02-CONTEXT.md > D-14).

수정 후 `apps/worker/tests/test_rtt.py`가 실제로 수집되고 통과하는지 확인한다. 통과하지 않으면 Phase 1 이후 누적된 실패가 있다는 뜻이므로 그것부터 해결한다 — 새로 켠 수집 경로를 실패한 채로 넘기지 않는다.

이 파일은 02-03-PLAN(Wave 3)이 이어서 수정한다. 같은 웨이브 안에서 다른 플랜이 이 파일을 건드리지 않으므로 충돌은 없다.
  </action>
  <verify>
    <automated>uv run pytest --collect-only -q 2>&1 | grep -q 'apps/worker/tests/test_rtt.py' && uv run pytest -q && uv run ruff check --select TID apps packages</automated>
  </verify>
  <acceptance_criteria>
    - `grep -vE '^\s*#' pyproject.toml | grep -c '"TID"'` 가 1 이상
    - `grep -vE '^\s*#' pyproject.toml | grep -c 'apps/worker/tests'` 가 1 이상
    - `uv run pytest --collect-only -q` 출력에 `apps/worker/tests/test_rtt.py` 가 등장한다
    - `uv run pytest -q` 가 exit 0 (worker 테스트 포함 전체 통과)
    - `uv run ruff check --select TID apps packages` 가 exit 0
    - `uv run python -c "import tomllib,pathlib; c=tomllib.loads(pathlib.Path('pyproject.toml').read_text()); assert 'TID' in c['tool']['ruff']['lint']['select']; assert 'apps/worker/tests' in c['tool']['pytest']['ini_options']['testpaths']; print('ok')"` 가 `ok`를 출력한다
  </acceptance_criteria>
  <done>`ruff check`가 TID 규칙군을 적용하고 `pytest --collect-only`가 저장소 루트에서 worker 테스트를 수집하며, 전체 테스트가 통과한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Railway env → api 프로세스 | api 서비스 환경에는 secret 값이 주입되지 않는다 (01-CONTEXT.md > D-12). 코드가 그것을 담을 필드도 갖지 않는다 |
| Railway env → worker 프로세스 | secret이 실제로 들어오는 유일한 경계 |
| 단일 이미지 내부 api ↔ worker 모듈 | 물리적 import는 가능하다. 막히는 것은 키다 (02-CONTEXT.md > D-06) |
| Settings 값 → structlog 렌더러 | 필드명이 `REDACTED_KEYS`와 어긋나면 값이 로그로 샌다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Elevation of Privilege | `ApiSettings` | critical | mitigate | `model_fields` 부재를 단언하는 테스트가 이 클래스에 secret 필드가 생기는 순간 red가 된다. 클래스 본문의 `⚠️` 주석이 이유를 D-06으로 가리킨다 |
| T-02-08 | Information Disclosure | `WorkerSettings` 필드명 | high | mitigate | 필드명 casefold 집합이 `REDACTED_KEYS`의 부분집합임을 `test_logging_redaction.py`가 단언한다. 어긋나면 로그 마스킹이 조용히 멈춘다 |
| T-02-09 | Denial of Service | 부팅 시점 검증 | medium | mitigate | 누락·빈 문자열을 키 이름과 함께 즉시 실패로 처리한다. 반쯤 설정된 프로세스가 뜨는 것보다 안 뜨는 편이 안전하다 (D-10) |
| T-02-10 | Spoofing | `SUPABASE_PUBLISHABLE_KEY`를 secret으로 오인 | medium | accept | publishable 키는 설계상 클라이언트에 노출되는 값이며 RLS가 방어선이다. `BaseAppSettings`에 두는 것이 맞다 |
| T-02-11 | Tampering | `LLM_MODEL` 코드 기본값 | low | mitigate | 기본값을 박지 않는다 (D-22). `.env.sample`과 PROJECT.md의 불일치를 Phase 2가 임의 확정하지 않는다 |
| T-02-SC | Tampering | `pydantic-settings` 설치 | high | mitigate | 위 blocking-human 체크포인트가 PyPI 페이지 확인을 요구한다. `workflow.auto_advance`로 자동 승인되지 않는다 |
</threat_model>

<verification>
1. `uv run pytest -q` 가 exit 0이고 worker 테스트가 수집된다
2. `grep -rn 'os\.environ' apps/api/src/api/ apps/worker/src/worker/__main__.py` 가 아무것도 출력하지 않는다
3. `uv run ruff check apps packages` 가 exit 0
4. `ApiSettings.model_fields`에 secret 키 4종이 없음이 테스트로 고정되어 있다
5. `uv lock --check` 가 exit 0
</verification>

<success_criteria>
- SEC-01이 충족된다: api 프로세스가 secret 키를 담을 필드를 갖지 않는다
- R12가 충족된다: `ruff check`가 TID를 적용하고 `pytest`가 worker 테스트를 수집한다
- 02-03-PLAN이 `WorkerSettings` 타입을 `service_client` 팩토리 인자로 쓸 수 있다
- 02-04-PLAN이 `create_app(settings)`가 만든 앱 위에 라우터와 예외 핸들러를 올릴 수 있다
</success_criteria>

<output>
Create `.planning/phases/02-security-spine-and-shared-domain/02-02-SUMMARY.md` when done
</output>
