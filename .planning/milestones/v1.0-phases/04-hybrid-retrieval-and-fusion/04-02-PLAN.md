---
phase: 04-hybrid-retrieval-and-fusion
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - supabase/migrations/0011_retrieval.sql
  - supabase/tests/0011_retrieval_contract.sql
  - scripts/verify_retrieval_contract.sh
  - scripts/ci_check_retrieval_contract.sh
  - apps/worker/src/worker/db/service.py
  - apps/worker/src/worker/handlers/parse.py
  - apps/worker/src/worker/handlers/compile.py
  - apps/worker/tests/test_handlers.py
  - apps/api/tests/test_hybrid_search_integration.py
  - docs/ops/migration-0011-record.md
autonomous: false
requirements: [RTV-03, RTV-07, RTV-08]
must_haves:
  truths:
    - Both lexical columns are populated/backfilled using versioned Python bigrams before any lexical benchmark.
    - User retrieval RPCs are SECURITY INVOKER/STABLE, RLS-safe, search-path-pinned, authenticated-only, and vector RPCs retain three HNSW GUCs; lexical writer RPCs are separate VOLATILE service-role-only operations.
    - Graph recursion is SQL-bounded, cycle-safe, workspace-constrained, and accepts fused wiki seeds only.
  artifacts:
    - path: supabase/migrations/0011_retrieval.sql
      provides: service-role lexical materialization plus authenticated retrieval/graph RPCs
    - path: supabase/tests/0011_retrieval_contract.sql
      provides: transactional ACL/RLS/HNSW/graph contract
  key_links:
    - from: apps/worker/src/worker/handlers/parse.py
      to: public.index_source_chunk_lexical
      via: normalized bigram plus TSV_TOKENIZER_VERSION
      pattern: "index_source"
    - from: supabase/migrations/0011_retrieval.sql
      to: apps/api/src/api/db/user.py
      via: authenticated-only PostgREST RPC signatures
      pattern: "grant execute.*authenticated"
  prohibitions:
    - requirement_id: RTV-07
      category: safety
      status: flagged-unverified
      statement: Graph SQL must not permit unbounded recursion, unresolved-link traversal, or cross-workspace expansion.
    - requirement_id: SEC-01
      category: safety
      status: flagged-unverified
      statement: Retrieval integration must not execute user searches through service role.
---

<objective>Complete the database retrieval boundary and lexical maintenance contract before quality measurement.</objective>

## Flagged assumptions requiring manual confirmation

- RTV-03: `strict_order`, `ef_search`, and `max_scan_tuples` values are provisional policy/DB contract values pending same-corpus comparison in Plan 04.
- RTV-07: graph is default-off; its initial policy and SQL contract are depth `<=2`, at most 10 seeds, fan-out `<=5` per frontier node, and `<=50` total graph rows. SQL rejects boundary-plus-one requests.
- RTV-08: the selective corpus/EXPLAIN condition must be representative enough that index use is meaningful, not forced by a tiny fixture alone.

<tasks>
<task type="auto">
  <name>Task 1: create migration 0011 with lexical indexing, retrieval RPCs, and bounded graph RPC</name>
  <reversibility rating="one-way">A new applied migration and RPC contract cannot be edited in place; corrective changes require a later migration.</reversibility>
  <read_first>
    - `supabase/migrations/0002_search_schema.sql`, `0007_search_and_queue_extensions.sql`, `0008_embedding_dimension.sql`, `0009_pipeline_ops.sql` — schema, HNSW, and latest ACL conventions.
    - `packages/core/src/nexuswiki_core/tokenizer.py` and worker parse/compile handlers — lexical write paths.
    - `04-RESEARCH.md` “Database Work Needed”.
  </read_first>
  <action>Create only `0011_retrieval.sql` (never edit applied migrations). Split lexical writers from user retrieval: create `index_source_chunk_lexical(p_workspace_id, p_source_chunk_id, p_bigrams, p_tokenizer_version)` and `index_wiki_page_lexical(p_workspace_id, p_wiki_id, p_bigrams, p_tokenizer_version)` as `SECURITY INVOKER VOLATILE` RPCs that atomically update only the row whose workspace matches the explicit argument, calling `to_tsvector('simple', p_bigrams)`. Revoke these writers from `public`, `anon`, and `authenticated`; grant execute only to `service_role`. Invoke them through explicit worker `ServiceDb` methods in new/reprocessed parse and wiki compile paths and provide a bounded idempotent service-role backfill path. Add `search_wiki_embeddings`, `search_wiki_lexical`, `search_source_lexical`, and `expand_wiki_graph` while retaining/replacing source vector safely. Every user-search/graph RPC must remain `SECURITY INVOKER STABLE`, `set search_path=public`, revoked from `public`, `anon`, and `service_role`, and granted only to `authenticated`; both vector RPCs must set all three HNSW GUCs and `operator(extensions.<=>)`. Lexical search RPCs use `phraseto_tsquery('simple', p_bigrams)` and tokenizer-version filtering. `expand_wiki_graph` accepts only a UUID seed array of at most 10 plus requested fan-out at most 5 and total rows at most 50; it rejects any boundary-plus-one input, fixes depth at `<=2`, and constrains every recursive term by workspace, resolved links, per-frontier fan-out, total rows, and UUID-path cycle guard. Notify PostgREST reload. SQL contains no RRF weights/final k/policy tuning.</action>
  <acceptance_criteria>
    - `rg -n 'security invoker|set search_path = public|hnsw\.iterative_scan|hnsw\.ef_search|hnsw\.max_scan_tuples|reload schema' supabase/migrations/0011_retrieval.sql` finds all required contract tokens.
    - Catalog/ACL contract distinguishes both directions: every user-search/graph RPC is `STABLE`, unavailable to `service_role`, and executable only by `authenticated`; both lexical writer RPCs are `VOLATILE`, unavailable to `public`/`anon`/`authenticated`, and executable only by `service_role`.
    - Handler tests exercise the worker writer RPC path and prove new and reprocessed source/wiki rows store normalized bigram TSV plus the current tokenizer version.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: add executable SQL, source-level, and real-RLS retrieval contracts</name>
  <files>supabase/tests/0011_retrieval_contract.sql, scripts/verify_retrieval_contract.sh, scripts/ci_check_retrieval_contract.sh, apps/api/tests/test_hybrid_search_integration.py</files>
  <read_first>
    - `supabase/tests/0008_search_contract.sql`, `scripts/verify_search_contract.sh`, `scripts/ci_check_search_contract.sh` — transaction and CI conventions.
    - `apps/api/tests/conftest.py` — local two-tenant fixture/loopback guard.
  </read_first>
  <action>Write transactional fixed-UUID SQL contract tests and runners. Assert writer and search function catalog volatility/security/ACL properties in both directions, including service-role execution of the workspace-scoped writers and rejection of authenticated writer calls; assert tokenizer filtering and actual lexical hits, graph depth/fan-out/total/cycle/resolved-link behavior, and foreign-workspace invisibility under authenticated requester role. Use exact graph boundaries of 10 seeds, fan-out 5, and total 50, and assert 11 seeds, fan-out 6, and total 51 are rejected by SQL before traversal. Seed a suitably selective local corpus (or documented controlled planner condition) and assert JSON EXPLAIN contains named `source_chunks_embedding_idx` and `wiki_embeddings_embedding_idx`, not just a tiny-fixture forced plan. Add a source-level CI contract gate and local API integration test using two real authenticated tenants.</action>
  <acceptance_criteria>
    - `scripts/verify_retrieval_contract.sh` exits 0 against the local Supabase stack and prints `retrieval_contract: ok`.
    - `uv run pytest -q apps/api/tests/test_hybrid_search_integration.py` exits 0 when local stack is available; it skips only through the existing loopback guard otherwise.
    - SQL test fails for a foreign workspace ID, cycle, unresolved link, depth 3, 11 seeds, fan-out 6, total 51, missing HNSW GUC, incorrect writer volatility/ACL, or non-authenticated search ACL.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] apply migration and record local/remote proof</name>
  <precondition>Confirm `supabase migration list` remote ledger ends at `0010_budget_error_sqlstate.sql`; required local Docker and Supabase authorization are present.</precondition>
  <reversibility rating="one-way">Cloud `supabase db push` applies the immutable 0011 migration to the shared database.</reversibility>
  <read_first>
    - `docs/ops/migration-0008-record.md` — applied-migration record format.
    - `supabase/migrations/0011_retrieval.sql` and `scripts/verify_retrieval_contract.sh`.
  </read_first>
  <action>Run `supabase db reset --local`, then `scripts/verify_retrieval_contract.sh`; inspect remote ledger and run noninteractive `supabase db push --include-all`. Record commands, migration ledger, contract output, environment/date, HNSW plan caveat and rollback path in `docs/ops/migration-0011-record.md`. If credentials/project authorization are unavailable, leave this task manually blocked and do not claim it applied.</action>
  <acceptance_criteria>
    - Record contains local reset, SQL contract, remote ledger before/after, push result, and no secret output.
    - Remote ledger lists 0011 exactly once after success.
  </acceptance_criteria>
</task>
</tasks>

<verification>Run `scripts/verify_retrieval_contract.sh`, `bash scripts/ci_check_retrieval_contract.sh`, and `uv run pytest -q apps/worker/tests/test_handlers.py apps/api/tests/test_hybrid_search_integration.py`.</verification>
<success_criteria>Lexical recall has a maintained corpus, all physical retrieval channels are safe callable primitives, and the graph database work is bounded before the orchestrator enables it.</success_criteria>
<threat_model>
- T-04-02 RLS/ACL bypass: authenticated search stays SECURITY INVOKER/STABLE with requester JWT; VOLATILE lexical writers are service-role-only and verify their explicit workspace argument; catalog ACL/volatility and two-tenant tests prove the split.
- T-04-05 graph DoS/cycles: SQL enforces depth 2, 10 seeds, fan-out 5, and total 50 with UUID path cycle rejection; SQL boundary and boundary-plus-one tests prove each.
- T-04-06 search-path/operator ambiguity: pinned search_path and schema-qualified vector operator; catalog contract verifies both.
- T-04-07 stale lexical index: atomic service-role writer RPC/backfill plus worker write-path tests; tokenizer version prevents silent mixing.
</threat_model>

## Artifacts this phase produces

- `supabase/migrations/0011_retrieval.sql`: service-role-only VOLATILE `index_source_chunk_lexical` / `index_wiki_page_lexical` and authenticated-only STABLE `search_wiki_embeddings`, `search_wiki_lexical`, `search_source_lexical`, `expand_wiki_graph`.
- `supabase/tests/0011_retrieval_contract.sql`: `retrieval_contract: ok` database contract.
- `scripts/verify_retrieval_contract.sh` and `scripts/ci_check_retrieval_contract.sh`: local and CI verification commands.
- `apps/worker/src/worker/db/service.py`: explicit service-role lexical-writer RPC methods with required workspace scope.
- `docs/ops/migration-0011-record.md`: immutable migration apply evidence.
