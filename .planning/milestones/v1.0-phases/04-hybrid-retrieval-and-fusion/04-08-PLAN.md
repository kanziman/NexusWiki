---
phase: 04-hybrid-retrieval-and-fusion
plan: 08
type: gap_closure
wave: 6
depends_on: [04-01, 04-02, 04-03, 04-06, 04-07]
files_modified:
  - scripts/benchmark_retrieval.py
  - scripts/generate_retrieval_benchmark_corpus.py
  - packages/core/tests/test_retrieval_golden.py
  - packages/core/tests/fixtures/retrieval/representative_corpus.v1.json
  - packages/core/tests/fixtures/retrieval/golden_queries.v1.json
  - apps/api/src/api/services/retrieval.py
  - apps/api/tests/test_retrieval_service.py
  - docs/ops/benchmark-records/phase-04-rerun-strict-order.json
  - docs/ops/benchmark-records/phase-04-rerun-relaxed-order.json
  - docs/ops/benchmark-records/phase-04-rerun-graph-off.json
  - docs/ops/benchmark-records/phase-04-rerun-graph-on.json
  - docs/ops/hnsw-order-benchmark.md
  - docs/ops/retrieval-policy-change-log.md
autonomous: true
requirements: [RTV-04, RTV-07]
must_haves:
  truths:
    - The strict and relaxed records are produced by executing every pinned golden-query text through the real four-channel RetrievalService path, with lexical and dense results actually fused by Python RRF; they do not construct rankings from golden labels.
    - The graph-on arm invokes the real expand_wiki_graph second wave from the first-wave fused wiki seeds, then re-fuses its returned hits; graph-off is the same path with that wave disabled.
    - Each retained raw record contains a canonical recursively key-sorted complete policy-content object plus its SHA-256, real per-query evidence UUIDs plus their deterministic logical-fixture-ID mapping, channel requested/returned/underfill/contribution/latency values, and derived aggregate quality and paired graph deltas.
    - Existing successful Phase 04 retrieval, HNSW safety, policy, and default-off work is preserved. strict_order and graph-off remain safe operating defaults unless regenerated evidence and the existing policy-change process explicitly authorize a change.
  artifacts:
    - path: scripts/benchmark_retrieval.py
      provides: fail-closed operational full-path golden-query benchmark runner and paired-record validator
    - path: scripts/generate_retrieval_benchmark_corpus.py
      provides: deterministic labelled corpus/lexical/graph fixture loader plus scoped benchmark cleanup
    - path: docs/ops/benchmark-records/phase-04-rerun-graph-on.json
      provides: actual graph-enabled, second-wave measurement record
  key_links:
    - from: scripts/benchmark_retrieval.py
      to: apps/api/src/api/services/retrieval.py
      via: every golden query is passed to RetrievalService.retrieve with an injected deterministic benchmark query embedder and an actual local-database RPC adapter
      pattern: "RetrievalService\\(|retrieve\\("
    - from: apps/api/src/api/services/retrieval.py
      to: public.expand_wiki_graph
      via: graph_enabled selects fused wiki seeds, invokes the bounded RPC, then calls fuse_ranked_hits again
      pattern: "expand_wiki_graph"
  prohibitions:
    - requirement_id: RTV-04
      category: correctness
      status: enforced
      statement: No operational metric, evaluation, or raw record may be synthesized from required_evidence labels or a vector count query.
    - requirement_id: RTV-07
      category: safety
      status: enforced
      statement: Benchmark graph traversal must use the deployed bounded RPC and must never enable graph by changing the production default or relaxing SQL depth/fan-out/cycle limits.
---

<objective>
Close only the two verification-evidence gaps left by Plan 07: make the retained strict/relaxed records execute the real golden-query five-channel retrieval path and make graph off/on records measure the real bounded second wave. Preserve all successful Phase 04 implementation and the current strict-order, graph-off defaults; this plan does not add a migration, deploy, answer API, or graph store.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: make the controlled corpus capable of exercising real lexical, dense, and graph retrieval</name>
  <files>scripts/generate_retrieval_benchmark_corpus.py, packages/core/tests/fixtures/retrieval/representative_corpus.v1.json, packages/core/tests/fixtures/retrieval/golden_queries.v1.json, packages/core/tests/test_retrieval_golden.py</files>
  <read_first>
    - `scripts/generate_retrieval_benchmark_corpus.py` and `scripts/benchmark_retrieval.py` — current generated-decoy loader and the synthetic operational defect.
    - `supabase/migrations/0011_retrieval.sql` — exact search RPC parameter/result contracts, lexical tokenizer filters, and bounded `expand_wiki_graph` behavior.
    - `packages/core/src/nexuswiki_core/tokenizer.py` and `apps/api/src/api/services/retrieval.py` — canonical query normalization, bigrams, and evidence identity.
    - `.planning/phases/04-hybrid-retrieval-and-fusion/04-VERIFICATION.md` — the two failed truths this task must make testable.
  </read_first>
  <action>Extend the controlled local benchmark loader with a checked-in deterministic mapping from every logical fixture ID (for example `source:src-onboarding` and `wiki:wiki-sso`) to a UUID derived from the pinned manifest namespace/seed. Load those mapped UUIDs into Postgres before decoys, retain the mapping in each run record, and use the same mapped wiki UUIDs for `wiki_links` and graph assertions. Returned `RetrievalResult` UUID evidence must be mapped back to logical IDs only after retrieval for evaluation; labels remain expected evidence and must never construct rankings. Load deterministic query-compatible 1024-d embeddings, normalized-bigram lexical materialization at the current `TSV_TOKENIZER_VERSION`, and resolved `wiki_links` covering positive graph, distractor, and cycle cases. Keep the existing manifest-pinned target/foreign decoy counts and named-index preflight conditions, but make generated decoys non-matching for labelled lexical queries. Add a deterministic, test-only benchmark query embedder derived from the same checked-in corpus/generator contract; inject it into `RetrievalService`, never API settings or production worker capability. Update golden labels only where needed to include reviewable graph-positive and graph-negative/cycle scenarios, recompute pins deliberately, and add tests proving every golden query has an executable input path rather than a label-derived expected ranking. Scope loader cleanup to fixed benchmark identities and verify cleanup after failed loads as well as successful runs.</action>
  <acceptance_criteria>
    - Fixture tests fail if a golden query lacks an executable deterministic embedding, logical-ID-to-UUID mapping, labelled source/wiki fixture, normalized lexical representation, or required graph topology; tests prove returned UUID evidence maps back to the expected logical ID only after it is ranked.
    - Tests prove generated graph fixtures include a bounded depth-two reachable case, a distractor, and a cycle that does not produce an unbounded/repeated candidate.
    - Tests retain manifest/hash/count validation and prove the benchmark loader cannot delete non-benchmark workspace data.
  </acceptance_criteria>
  <verification>Run `UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run pytest -q packages/core/tests/test_retrieval_golden.py`.</verification>
</task>

<task type="auto">
  <name>Task 2: execute actual golden-query RetrievalService runs and serialize measured evidence</name>
  <files>scripts/benchmark_retrieval.py, apps/api/src/api/services/retrieval.py, apps/api/tests/test_retrieval_service.py, packages/core/tests/test_retrieval_golden.py</files>
  <read_first>
    - `apps/api/src/api/services/retrieval.py` and `packages/core/src/nexuswiki_core/rrf.py` — the production four-way gather, error envelopes, RRF, graph seed selection, re-fusion, and metadata semantics to exercise rather than duplicate.
    - `supabase/migrations/0011_retrieval.sql` and `supabase/tests/0011_retrieval_contract.sql` — deployed user RPC contracts, RLS constraints, HNSW GUC equivalence, and exact-dataset preflight.
    - `scripts/benchmark_retrieval.py` — remove only its synthetic count/evaluation behavior while retaining fail-closed pins, no-overwrite, and cleanup.
  </read_first>
  <action>Replace the operational runner's synthetic count query and `required_evidence`-constructed rankings with a controlled local-database adapter that invokes `RetrievalService.retrieve()` once for each actual golden `query` text. The adapter must execute all four first-wave channel contracts against the loaded benchmark workspace: use exact deployed lexical RPCs and a security-equivalent controlled direct vector query mirroring the deployed RPC body, workspace/non-null filter, `<=>` ordering, clamped candidate limit, and all three HNSW GUCs while selecting only the requested benchmark order mode. Do not claim the relaxed direct-vector arm changes the deployed strict RPC. Use the same `fuse_ranked_hits` implementation through `RetrievalService`, retain actual channel envelopes, serialize both raw returned UUID evidence IDs and their manifest mapping to logical fixture IDs, and derive each evaluation solely from mapped returned `evidence`. For `--graph on`, inject an otherwise identical policy with `graph_enabled=True` so the service calls real bounded `expand_wiki_graph` from first-wave wiki seeds and re-fuses; for `--graph off`, retain the disabled channel state. If a channel fails, record its actual safe failed envelope and keep the query result; never replace it with labels. Every operational record must retain `policy_content`: the complete recursively key-sorted JSON-normalized policy-field object whose canonical serialization yields `policy_content_sha256`, not only its digest. Persist per-query query ID/language, final ranked UUID and logical evidence IDs, evaluation checks, total latency, and each channel's status/requested/returned/underfill/raw IDs/contribution/latency. Derive aggregate recall, strict pass rate, MRR/required-rank, per-channel hit and contribution rates, underfill counts/rates, and p50/p95 from those observations. Implement two explicit comparators: `compare-order-records` requires byte-equivalent normalized `policy_content`, matching SHA, and all other pins equal; `compare-graph-records` retains each full policy object/hash but normalizes and diffs the persisted objects, requires all non-graph policy fields/pins equal, and fails unless its sole diff is `graph_enabled: false -> true`. The graph comparator then computes per-query and aggregate graph-on-minus-off quality, contribution, underfill, and latency deltas. Both reject `not_measured_fixture_adapter`, missing five-channel envelopes, fabricated/label-only evaluations, or an object whose recomputed canonical SHA differs from its stored hash. Preserve cancellation propagation and existing route/service contracts; modify `retrieval.py` only if an injectable benchmark seam is genuinely needed and keep it private/testable, not a new production configuration surface.</action>
  <acceptance_criteria>
    - Focused tests prove a deliberately altered golden label cannot alter an operational ranking, while altered actual RPC rows do alter the serialized ranking/evaluation.
    - A graph-enabled controlled run observes a real `expand_wiki_graph` call using only fused wiki seeds and its final output includes the re-fused graph contribution; graph-off records `graph.status=disabled` and makes no graph RPC call.
    - Unit tests cover actual lexical+dense four-channel execution, RRF result use, malformed/failed channel envelopes, per-channel underfill, graph delta pairing, canonical policy-content object/SHA validation, order-pair full-policy equality, graph-pair rejection of any persisted policy-object diff beyond the sole allowed `graph_enabled` diff, canonical pin mismatch rejection, and no-overwrite without requiring a live database.
    - `--verify` remains explicitly fixture-only and cannot write a record accepted as RTV-04/RTV-07 evidence.
  </acceptance_criteria>
  <verification>Run `UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run pytest -q packages/core/tests/test_retrieval_golden.py apps/api/tests/test_retrieval_service.py` and `UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run python scripts/benchmark_retrieval.py --verify`.</verification>
</task>

<task type="auto">
  <name>Task 3: retain append-only rerun records and update the decision evidence</name>
  <files>docs/ops/benchmark-records/phase-04-rerun-strict-order.json, docs/ops/benchmark-records/phase-04-rerun-relaxed-order.json, docs/ops/benchmark-records/phase-04-rerun-graph-off.json, docs/ops/benchmark-records/phase-04-rerun-graph-on.json, docs/ops/hnsw-order-benchmark.md, docs/ops/retrieval-policy-change-log.md</files>
  <read_first>
    - The completed operational CLI help/output and its record-schema/comparison validator.
    - `docs/ops/hnsw-order-benchmark.md`, `docs/ops/retrieval-policy-change-log.md`, and all four existing record files — replace invalid synthetic evidence transparently; do not present it as valid historical measurement.
    - `.planning/phases/04-hybrid-retrieval-and-fusion/04-VERIFICATION.md` — valid closure definition and current safe-default boundary.
  </read_first>
  <action>After focused tests pass, use a reset controlled local Supabase environment and run loader/preflight/full-path retrieval for strict+graph-off, relaxed+graph-off, strict+graph-off, and strict+graph-on, with scoped cleanup after every arm. Preserve the four Plan-07 JSON records byte-for-byte as immutable invalid historical evidence and mark their invalid/superseded status only in new documentation. Write four newly named append-only rerun records; never overwrite either historical or valid operational output and never fabricate values if a database/preflight/run fails. Require the strict/relaxed pair to match all corpus/golden/manifest/generator/workspace/model/database/repeat pins plus byte-equivalent persisted normalized `policy_content` and its matching SHA. Require the graph pair to retain and validate each complete persisted policy object/hash, match all pins and normalized non-graph policy fields, and accept only `graph_enabled: false -> true` as their object-level diff. Run the named comparator commands before documentation. Update the decision document with links to both retired and new records, per-query/aggregate quality/underfill/channel-contribution/latency tables, controlled-direct-query caveat for relaxed order, and actual graph off/on deltas. Preserve `strict_order` and `graph_enabled=False` unless valid records and the existing reviewer/version-bump process explicitly approve a different change; do not create a migration, run `supabase db push`, or claim a default change merely from the benchmark.</action>
  <acceptance_criteria>
    - The four Plan-07 records remain unchanged and are linked as invalid/superseded; all four newly named rerun records contain real query text IDs, returned UUID plus mapped logical evidence rankings, all four first-wave channel envelopes, graph envelope, actual per-query evaluation, and non-constant derived aggregate metrics; none carries `not_measured_fixture_adapter` or label-derived evidence.
    - The graph-on record proves a real second-wave call/result on graph scenarios, and the comparison output includes per-query plus aggregate quality, contribution, underfill, and latency deltas rather than fixed `0` placeholders.
    - Documentation distinguishes the superseded invalid records from the regenerated evidence, links exact records, explains strict/relaxed controlled-measurement scope, and records why the safe defaults remain or the separately approved change process used.
  </acceptance_criteria>
  <verification>Run `scripts/verify_retrieval_contract.sh`, then `UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run python scripts/benchmark_retrieval.py --order-mode strict_order --graph off --repeat-count 3 --db-container supabase_db_NexusWiki --run-id phase-04-rerun-strict --output docs/ops/benchmark-records/phase-04-rerun-strict-order.json`; `UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run python scripts/benchmark_retrieval.py --order-mode relaxed_order --graph off --repeat-count 3 --db-container supabase_db_NexusWiki --run-id phase-04-rerun-relaxed --output docs/ops/benchmark-records/phase-04-rerun-relaxed-order.json`; `UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run python scripts/benchmark_retrieval.py --order-mode strict_order --graph off --repeat-count 3 --db-container supabase_db_NexusWiki --run-id phase-04-rerun-graph-off --output docs/ops/benchmark-records/phase-04-rerun-graph-off.json`; and `UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run python scripts/benchmark_retrieval.py --order-mode strict_order --graph on --repeat-count 3 --db-container supabase_db_NexusWiki --run-id phase-04-rerun-graph-on --output docs/ops/benchmark-records/phase-04-rerun-graph-on.json`. Then run `UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run python scripts/benchmark_retrieval.py compare-order-records --left docs/ops/benchmark-records/phase-04-rerun-strict-order.json --right docs/ops/benchmark-records/phase-04-rerun-relaxed-order.json` and `UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run python scripts/benchmark_retrieval.py compare-graph-records --off docs/ops/benchmark-records/phase-04-rerun-graph-off.json --on docs/ops/benchmark-records/phase-04-rerun-graph-on.json`. Finally run `UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run pytest -q packages/core/tests/test_retrieval_golden.py apps/api/tests/test_retrieval_service.py`.</verification>
</task>
</tasks>

<verification>
Run the focused offline suite and the local retrieval contract, then require every record to pass the runner's immutable-pins/completeness validator. Inspect the four records to confirm rankings originate from each returned `RetrievalResult`, all five channel envelopes are present, graph-on has an executed second wave for graph fixtures, and pairwise deltas are derived from those records. `git diff --check` must pass.
</verification>

<success_criteria>RTV-04 and RTV-07 have genuine, reproducible evidence: strict/relaxed quality uses actual golden-query four-channel retrieval and Python RRF, graph-on invokes the bounded second wave and re-fuses it, and paired records/documentation retain real per-query and aggregate deltas while preserving existing safe defaults absent a separately approved change.</success_criteria>

<threat_model>
- Security enforcement: ASVS L1; block high. The benchmark uses only a controlled local workspace and deterministic test embedding seam; it must not add provider keys, service-role capability, or public routes to API/browser code.
- T-04-29 benchmark-result fabrication: operational rankings/evaluations are derived exclusively from returned RetrievalService evidence; tests reject label-derived output and fixture-adapter records.
- T-04-30 traversal amplification or tenant leakage: graph-on invokes existing requester-safe bounded RPC limits (depth, fan-out, total, cycle/RLS guards) and controlled cleanup targets fixed benchmark identities only.
- T-04-31 unsafe default/migration drift: evidence collection cannot alter the deployed strict GUC or graph default, create migrations, or deploy; the established policy-change approval gate remains mandatory.
- T-04-32 incomparable evidence: fail closed on mismatched pins, absent exact-dataset HNSW preflight, failed channels hidden as success, missing envelopes, or malformed per-query results.
</threat_model>

## Artifacts this phase produces

- `scripts/benchmark_retrieval.py`: full-path operational golden-query runner, record schema validation, and paired graph-delta comparison.
- `scripts/generate_retrieval_benchmark_corpus.py`: deterministic labelled lexical/dense/graph corpus loader with safe scoped cleanup.
- `packages/core/tests/test_retrieval_golden.py`: regression tests proving records are retrieved, not synthesized.
- `docs/ops/benchmark-records/phase-04-rerun-*.json`: append-only honest strict/relaxed and graph off/on raw evidence; the Plan-07 records remain immutable invalid history.
- `docs/ops/hnsw-order-benchmark.md`: decision record based on regenerated evidence while preserving the current defaults.
