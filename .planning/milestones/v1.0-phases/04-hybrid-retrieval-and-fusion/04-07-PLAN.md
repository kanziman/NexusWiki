---
phase: 04-hybrid-retrieval-and-fusion
plan: 07
type: execute
wave: 5
depends_on: [04-03, 04-04, 04-06]
gap_closure: true
files_modified:
  - scripts/benchmark_retrieval.py
  - scripts/generate_retrieval_benchmark_corpus.py
  - packages/core/tests/test_retrieval_golden.py
  - packages/core/tests/fixtures/retrieval/golden_queries.v1.json
  - packages/core/tests/fixtures/retrieval/benchmark_hnsw_corpus.v1.json
  - docs/ops/hnsw-order-benchmark.md
  - docs/ops/benchmark-records/phase-04-strict-order.json
  - docs/ops/benchmark-records/phase-04-relaxed-order.json
  - docs/ops/benchmark-records/phase-04-graph-off.json
  - docs/ops/benchmark-records/phase-04-graph-on.json
  - supabase/migrations/0012_retrieval_order_mode.sql
  - supabase/tests/0011_retrieval_contract.sql
  - scripts/ci_check_retrieval_contract.sh
  - docs/ops/migration-0012-record.md
  - docs/ops/retrieval-policy-change-log.md
autonomous: false
requirements: [RTV-04, RTV-07]
must_haves:
  truths:
    - Strict-order and relaxed-order runs execute against identical pinned labelled corpus plus repository-owned HNSW decoy corpus, golden set, canonical policy content hash, POLICY_VERSION, embedding model, commit, and database context, with raw immutable records retained.
    - Graph-off and graph-on runs execute over the same inputs and expose per-query plus aggregate quality, underfill, channel contribution, and latency deltas.
    - The order-mode selection has a checkpoint-gated deployment/rollback path: strict selection leaves 0011 unchanged, while relaxed selection creates and verifies a new migration before deployment. The graph default is based on retained measured evidence and remains default-off unless the comparison and explicit approval justify promotion.
  artifacts:
    - path: scripts/benchmark_retrieval.py
      provides: operational paired-run CLI with order and graph controls
    - path: scripts/generate_retrieval_benchmark_corpus.py
      provides: deterministic, hash-pinned 1024-d labelled-plus-decoy corpus loader and cleanup for local HNSW measurements
    - path: packages/core/tests/fixtures/retrieval/benchmark_hnsw_corpus.v1.json
      provides: repository-owned immutable v1 generator manifest, cardinality, seed, raw identity fields, and expected content hash
    - path: docs/ops/benchmark-records/phase-04-strict-order.json
      provides: immutable strict-order raw run
    - path: docs/ops/benchmark-records/phase-04-relaxed-order.json
      provides: immutable relaxed-order raw run
    - path: docs/ops/benchmark-records/phase-04-graph-off.json
      provides: immutable graph-off raw run
    - path: docs/ops/benchmark-records/phase-04-graph-on.json
      provides: immutable graph-on raw run
    - path: docs/ops/hnsw-order-benchmark.md
      provides: evidence-backed Phase 4 decision record
  key_links:
    - from: scripts/benchmark_retrieval.py
      to: supabase/migrations/0011_retrieval.sql
      via: benchmark vector SQL mirrors deployed RPC filters, `<=>` operator, candidate limits, and applies only the selected session-local order mode; its direct-plan design is linked to the RPC body/GUC equivalence contract from Plan 06
      pattern: "strict_order|relaxed_order|hnsw\.iterative_scan"
    - from: docs/ops/hnsw-order-benchmark.md
      to: docs/ops/benchmark-records/phase-04-strict-order.json
      via: raw-record path and matching pinned metadata
      pattern: "phase-04-strict-order"
---

<objective>
Turn the pinned golden set into actual paired HNSW order-mode and graph-value evidence, retaining raw runs and a reproducible decision record rather than a fixture-only placeholder.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: extend the benchmark harness for real paired order and graph runs</name>
  <files>scripts/benchmark_retrieval.py, scripts/generate_retrieval_benchmark_corpus.py, packages/core/tests/test_retrieval_golden.py, packages/core/tests/fixtures/retrieval/golden_queries.v1.json, packages/core/tests/fixtures/retrieval/benchmark_hnsw_corpus.v1.json, supabase/tests/0011_retrieval_contract.sql</files>
  <read_first>
    - `scripts/benchmark_retrieval.py` — current fixture-only adapter and record schema.
    - `apps/api/src/api/services/retrieval.py` and `packages/core/src/nexuswiki_core/retrieval_policy.py` — live retrieval orchestration and graph default.
    - `supabase/migrations/0011_retrieval.sql` and `04-06-PLAN.md` — vector order settings and HNSW plan precondition.
    - `docs/ops/hnsw-order-benchmark.md` — the precise unmeasured claims this work replaces.
  </read_first>
  <action>Preserve `--verify` as the offline fixture contract, then add a separately explicit operational mode that loads the labelled `representative-corpus-v1` and a repository-owned generated HNSW corpus into a controlled local-Supabase benchmark workspace before every run and writes a new immutable JSON record. Add the checked-in `benchmark_hnsw_corpus.v1.json` manifest with `version`, canonical manifest SHA-256, a fixed generator algorithm/version, seed, fixed UUID namespaces, model/dimension (`1024`), and exact cardinality: for **each** vector relation seed 25,000 benchmark-workspace rows (the 12 labelled rows plus 24,988 generated target rows) and 25,000 foreign-workspace decoy rows, for 50,000 rows per relation / 100,000 vectors total. `scripts/generate_retrieval_benchmark_corpus.py` must deterministically derive every raw-record identity (`workspace_id`, source/wiki/chunk IDs, relation, ordinal, text marker, embedding) from that manifest seed using a documented SHA-256 counter-stream and L2-normalized 1024-d floats; it must verify the manifest/content hash before loading, insert labelled data first and generated rows second, `ANALYZE` both relations, and provide an idempotent cleanup command deleting only the fixed benchmark UUID namespaces. The benchmark runner must call load → exact-dataset preflight → run → cleanup in `finally` (cleanup also on failed preflight), never depend on Plan 06's rollback-only fixture, and reject a run if the loader reports a different identity/count/hash.

Define a canonical, recursively key-sorted JSON serialization of every retrieval-policy field (including nested mappings and graph limits) and persist its SHA-256 as `policy_content_sha256` alongside `POLICY_VERSION`; do not use a source-file hash or version string as a substitute. Before **each** strict, relaxed, graph-off, and graph-on invocation, run a reusable Plan-06-equivalent `EXPLAIN (FORMAT JSON)` preflight against the rows just loaded in this exact benchmark workspace, under the same security-equivalent direct source/wiki query, predicates, clamped limit, and three `SET LOCAL hnsw.*` settings. Require recursive proof of the exact `source_chunks_embedding_idx` and `wiki_embeddings_embedding_idx` names; fail closed and emit the observed plan if either index is absent. The preflight implementation may be factored into `0011_retrieval_contract.sql`, but it must accept and assert the benchmark manifest's workspace/identity/count rather than seeding its own rollback-only corpus.

For vector channels, execute benchmark SQL that exactly mirrors the deployed RPC workspace filter, non-null predicate, `<=>` operator, clamped candidate limit, and retrieval shape while setting the requested order mode with `SET LOCAL` in that benchmark transaction. Link this direct query to Plan 06's explicit RPC body/GUC equivalence contract. Function-level `strict_order` settings cannot be overridden by a caller session, so label these as controlled direct-query order measurements and never claim the unchanged production RPC directly measured relaxed order. Reuse the deployed lexical and graph RPCs, and the same Python policy/RRF representation, for the remaining channels. Require explicit `--order-mode strict_order|relaxed_order`, `--graph off|on`, repeat count, output path, and environment metadata; reject overwrite, unsupported combinations, policy-content hash/version mismatches, an HNSW-plan preflight failure, or missing database/model identity. Persist the benchmark manifest version/SHA-256, generator algorithm/version, seed, raw workspace UUID, per-relation target/decoy counts, inserted row counts, and exact preflight-plan hashes in every raw record. Extend graph-focused labels if needed so the pinned set contains enough positive and distractor/cycle cases to evaluate per-query graph impact; update fixture hashes and schema tests deliberately.</action>
  <acceptance_criteria>
    - The operational mode emits one record per invocation with labelled corpus/golden hashes, benchmark-manifest version/SHA-256, generator algorithm/version/seed, raw workspace UUID, target/decoy cardinalities, inserted counts, exact preflight-plan hashes, `POLICY_VERSION`, deterministic `policy_content_sha256`, policy/model/git/database identity, repeat count, order mode, graph state, per-query ranked evidence, channel requested/returned/underfill/contribution, and p50/p95 latency.
    - It refuses to label fixture-adapter output as an order or graph measurement and refuses a run unless the exact loaded dataset's named HNSW-index preflight succeeds; a Plan 06 test that seeded and rolled back a separate corpus is not sufficient.
    - Focused fixture tests cover manifest/hash/schema validation, deterministic vector/identity generation from the fixed seed, exact target/decoy counts, scoped cleanup, argument validation, no-overwrite behavior, pinned-input mismatch rejection, canonical policy-content hashing (including a failure on a policy-field change without a version change), record schema, and graph-delta computation without requiring a live database.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: execute and retain comparable strict/relaxed and graph off/on raw records</name>
  <files>docs/ops/benchmark-records/phase-04-strict-order.json, docs/ops/benchmark-records/phase-04-relaxed-order.json, docs/ops/benchmark-records/phase-04-graph-off.json, docs/ops/benchmark-records/phase-04-graph-on.json</files>
  <read_first>
    - `docs/ops/retrieval-policy-change-log.md` — comparable-run and approval protocol.
    - The final CLI `--help` and its HNSW preflight output.
  </read_first>
  <action>With a reset controlled local Supabase benchmark workspace, run the repository loader before **each** invocation so it seeds the same labelled corpus alongside the manifest-pinned 100,000-vector decoy corpus, then run the exact-dataset HNSW preflight before issuing queries; each invocation must execute scoped cleanup afterward. Execute strict and relaxed with graph off on the identical corpus/golden/manifest/`POLICY_VERSION`/`policy_content_sha256`/model/git/database configuration and repeat count. Then execute graph off and graph on under the selected order mode with those same pins. Before calculating a pairwise result, validate exact equality of both version and canonical policy-content SHA-256, the labelled corpus/golden hashes, and every generator/raw-record identity field; reject and report a mismatch rather than comparing it. Commit four immutable raw JSON records; do not fabricate values, reuse fixture-adapter records, rely on Plan 06's rollback fixture, or overwrite prior records. If the controlled environment cannot load the manifest corpus or meet its exact-dataset HNSW preflight, stop this task and report the exact loader/count/plan condition instead of producing incomparable evidence.</action>
  <acceptance_criteria>
    - Strict and relaxed raw records have byte-for-byte matching labelled corpus/golden/benchmark-manifest version/SHA-256/generator seed/raw workspace UUID/per-relation target-and-decoy counts/`POLICY_VERSION`/`policy_content_sha256`/model/git/database identifiers, graph state, and repeat count; only order mode and measured outcomes differ.
    - Graph-off and graph-on raw records have matching labelled corpus/golden/benchmark-manifest and generator identity fields, `POLICY_VERSION` and `policy_content_sha256`, selected order mode, and repeat count; only graph state and measured outcomes differ.
    - Each record contains per-query results plus aggregate recall/pass/rank, underfill, channel contribution, and latency metrics; no `not_measured_fixture_adapter` status remains in these four records.
  </acceptance_criteria>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: record the evidence-backed order and graph decisions</name>
  <reversibility rating="costly">Changing an HNSW order mode or enabling graph alters retrieval quality, latency, and policy behavior for all callers; rollback is code/configuration work and requires a new paired benchmark record.</reversibility>
  <decision>Which measured HNSW order result, and whether any graph-default promotion, should enter the deployment path?</decision>
  <context>Read the four immutable Task 2 records and their exact corpus/golden/POLICY_VERSION/policy_content_sha256 pins. Quality (recall, strict pass rate, required-evidence rank, and underfill) outranks latency. The relaxed arm is a controlled direct-query measurement only: the unchanged 0011 RPC has a function-level strict_order GUC, so it cannot be called deployed until a successor migration passes the local Plan 06 contract and the explicit deployment gate. `graph_enabled` remains false unless the measured off/on evidence and reviewer both justify promotion.</context>
  <options>
    <option id="strict_keep_graph_off">
      <name>Keep strict_order and graph off</name>
      <pros>Retains the deployed 0011 RPC contract; requires no order-mode migration and preserves the safe graph default.</pros>
      <cons>Forgoes a measured relaxed-order latency benefit if quality is genuinely equivalent.</cons>
    </option>
    <option id="relaxed_keep_graph_off">
      <name>Adopt relaxed_order, keep graph off</name>
      <pros>Allows the measured relaxed-order result to proceed through the explicit 0012 migration, local contract, deployment evidence, and rollback path.</pros>
      <cons>Does not change production immediately; a successor migration and approval are mandatory before deployment.</cons>
    </option>
    <option id="promote_graph_with_selected_order">
      <name>Promote graph with selected order</name>
      <pros>Can adopt a demonstrated graph quality benefit alongside the chosen order mode.</pros>
      <cons>Requires reviewer approval, a policy version/content update, and the same benchmark evidence gate; reject this option if graph evidence is not clearly positive and safe.</cons>
    </option>
  </options>
  <resume-signal>Select one option and identify the reviewed record comparison. Any selection differing from the quality-first result must state why for the decision record.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: apply the approved decision record and conditional deployment path</name>
  <files>docs/ops/hnsw-order-benchmark.md, docs/ops/retrieval-policy-change-log.md, supabase/migrations/0012_retrieval_order_mode.sql, supabase/tests/0011_retrieval_contract.sql, scripts/ci_check_retrieval_contract.sh, docs/ops/migration-0012-record.md</files>
  <reversibility rating="costly">A relaxed-order default changes two deployed RPCs through an immutable migration; rollback requires a later migration restoring strict_order and rerunning the same plan/benchmark evidence checks.</reversibility>
  <read_first>
    - The four raw records from Task 2.
    - `docs/ops/retrieval-policy-change-log.md` — evidence gate and reviewer-approval requirements.
  </read_first>
  <action>Replace the current unmeasured-decision text in `docs/ops/hnsw-order-benchmark.md` with commands, environment details, raw-record links, matching hashes (including canonical `policy_content_sha256`), aggregate tables, per-query regressions, and rationale. Select strict or relaxed only after comparing quality (recall, strict pass rate, required-evidence ranks, and underfill) before latency. Record graph-off/on deltas separately and leave `graph_enabled = False` unless the record explicitly approves promotion after quality and safety review; if it remains off, say so based on the measured delta rather than an absence of evidence. If strict is selected, record that 0011 remains the deployed default and no migration is created. If relaxed is selected, do not claim the default changed at this checkpoint: after reviewer approval, create a new immutable `0012_retrieval_order_mode.sql` (never edit 0011) that replaces both vector RPCs with the identical signature, ACL, predicates, order expression, clamped limit, `ef_search`, and `max_scan_tuples`, changing only `hnsw.iterative_scan` to `relaxed_order`; update the SQL/CI contract to expect the selected mode; run local reset, Plan 06's direct-query named-index/equivalence contract, and the paired benchmark again against 0012. Then record migration ledger/push evidence, exact deployment command/result, and rollback as a subsequent migration restoring strict_order with the same verification sequence in `docs/ops/migration-0012-record.md`. Do not run `supabase db push` or present relaxed as deployed without the checkpoint's explicit approval. Record any approved policy/default change through the existing version-bump/reviewer protocol; otherwise state the verified default remains unchanged.</action>
  <acceptance_criteria>
    - The decision document links all four raw records and demonstrates identical-input comparability for each pair, including exact `POLICY_VERSION` and `policy_content_sha256` matches.
    - It names the selected order mode with a quality-first rationale, labels relaxed evidence as controlled direct-query evidence until 0012 is deployed, and records the graph default/promotion decision with aggregate and per-query evidence.
    - A reviewer approves any mode/default/policy change. A relaxed selection has a new-migration, local-contract, deployment-evidence, and rollback record; no unapproved graph promotion or undocumented policy version/content change occurs.
  </acceptance_criteria>
</task>
</tasks>

<verification>
Run `UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run pytest -q packages/core/tests/test_retrieval_golden.py`, the generator's deterministic/cleanup tests, `scripts/verify_retrieval_contract.sh`, and the four recorded operational benchmark commands. Each operational command must show the manifest hash/counts plus a successful Plan-06-equivalent named-index preflight on that invocation's loaded rows before measuring, then a successful scoped cleanup. Run a record-comparison validation that rejects mismatched pins, generator identity fields, `POLICY_VERSION`, canonical `policy_content_sha256`, or `not_measured_fixture_adapter`. If relaxed is approved, also run `supabase db reset`, the updated Plan 06 contract, the post-0012 paired command, and record migration deployment/rollback evidence before claiming the default changed. Confirm every raw-record path cited by `docs/ops/hnsw-order-benchmark.md` exists and is immutable.
</verification>

<success_criteria>Phase 4 retains four actual, comparable benchmark records: strict vs relaxed and graph off vs on, each guarded by a deterministic policy-content SHA-256 as well as POLICY_VERSION. The documented defaults are traceable to those measurements; relaxed cannot be called deployed until its checkpoint-gated successor migration, contract, deployment evidence, and rollback path exist, and graph cannot be promoted without explicit evidence and approval.</success_criteria>

<threat_model>
- T-04-24 incomparable tuning evidence: fail closed on mismatched corpus/golden/POLICY_VERSION/canonical-policy-content-SHA-256/model/git/database/repeat metadata and retain immutable raw records for both arms.
- T-04-25 false HNSW measurement: before every arm, load the repository-owned labelled-plus-100,000-vector manifest corpus and require a Plan-06-equivalent named-index preflight on those exact rows; reject tiny, rollback-only-fixture, or fixture-adapter results.
- T-04-26 unjustified graph promotion: compute per-query and aggregate graph deltas, maintain default-off absent explicit approved evidence, and preserve graph distractor/cycle cases.
- T-04-27 misleading or destructive benchmark data: use the controlled repository-owned corpus/workspace only, avoid production tenant data, and make output paths append-only/no-overwrite.
- T-04-28 undeployable relaxed-order result: distinguish direct-query measurement from RPC behavior; a relaxed selection must pass the checkpoint-gated 0012 migration, local plan/equivalence contract, explicit deployment evidence, and documented successor-migration rollback before it is called the production default.
</threat_model>
