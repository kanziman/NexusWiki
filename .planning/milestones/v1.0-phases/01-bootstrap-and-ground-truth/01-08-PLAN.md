---
phase: 01-bootstrap-and-ground-truth
plan: 08
type: execute
wave: 4
depends_on: ["01-03", "01-06"]
files_modified:
  - apps/worker/src/worker/rtt.py
  - apps/worker/src/worker/__main__.py
  - apps/worker/tests/test_rtt.py
  - apps/worker/pyproject.toml
  - docs/ops/rtt-baseline.md
  - checklists.json
autonomous: true
requirements: [BOOT-09]

estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "RTT가 워밍업 후 N≥50회 실행으로 p50/p95가 산출되고, 콜드 첫 요청 값이 별도로 기록된다"
    - "RTT 문서에 p50 · p95 · 콜드 첫 요청 · N값 · 측정 방법 · ×5 환산이 모두 존재한다"
    - "로컬이 아니라 **배포된 Railway 서비스에서** 측정했음이 문서에 명시된다"
    - "`checklists.json` open question #2가 해소로 표시된다"
    - "배포된 `api`의 `/health/ready`가 클라우드 마이그레이션 적용 후 200을 반환한다"
  artifacts:
    - apps/worker/src/worker/rtt.py
    - apps/worker/tests/test_rtt.py
    - docs/ops/rtt-baseline.md
  key_links:
    - "RTT 프로브는 `/health/ready`와 **같은 PostgREST 왕복 형태**를 쓴다 — 두 곳이 갈라지면 문서의 숫자가 실제 readiness 경로의 지연을 대표하지 못한다"
    - "측정이 worker 엔트리포인트에서 일어나는 이유는 SPEC 경계가 `/health`·`/health/ready` 외의 라우터를 금지하기 때문이다 (D-14) — `railway run`은 로컬에서 환경변수만 주입하므로 '배포 환경에서 측정'을 만족하지 못한다"
    - "`docs/ops/rtt-baseline.md`가 금지사항 P4의 grep 대상 경로다"
    - "Task 1은 `worker` 소스를 수정하므로 배포 로그 관측 이전에 커밋·push·`worker` 재배포 완료가 반드시 일어나야 한다 (D-12: GitHub 연동 자동 배포) — 계획 01-06이 배포한 컨테이너에는 `worker.rtt_measured` 이벤트가 존재하지 않는다"
    - "배포된 api URL은 `docs/ops/railway-deploy-record.md`의 `api_public_domain:` 줄에서 유도한다 (계획 01-06 Task 2가 남긴 기계 판독 키)"
  prohibitions:
    - statement: "RTT 측정 문서가 Supabase project ref · 접속 문자열 · 어떤 API 키도 포함해서는 안 된다"
      status: resolved
      verification: test
---

<objective>
Railway `asia-southeast1` ↔ Supabase `ap-southeast-1` 왕복 지연을 **배포 환경에서** 실측해 숫자로
남기고, 2년 가까이 열려 있던 `checklists.json` open question #2를 닫는다.

Purpose: BOOT-09. 리전 페어링의 지연 추정치(연구 문서의 ~1–2 ms는 지리적 추정이지 실측이 아니다)를
숫자로 바꾸지 않으면, Phase 4의 5채널 검색이 느릴 때 원인이 리전인지 쿼리인지 판정할 기준선이 없다.
5채널은 요청마다 왕복을 곱하므로 ×5 환산까지 함께 기록한다.
Output: 재실행 가능한 RTT 프로브, 숫자가 담긴 `docs/ops/rtt-baseline.md`, 그리고 갱신된 태스크 원장.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bootstrap-and-ground-truth/01-SPEC.md
@.planning/phases/01-bootstrap-and-ground-truth/01-CONTEXT.md
@.claude/CLAUDE.md
@.planning/phases/01-bootstrap-and-ground-truth/01-03-SUMMARY.md
@.planning/phases/01-bootstrap-and-ground-truth/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: worker 기동 시 1회 RTT 프로브 (워밍업 후 N≥50, p50/p95 + 콜드 첫 요청)</name>
  <precondition>배포된 Railway `worker` 서비스가 running 이고 `SUPABASE_URL`·`SUPABASE_PUBLISHABLE_KEY`가 그 서비스 스코프에 주입되어 있으며, 클라우드에 `0001`~`0006`이 적용되어 `workspaces` 테이블이 존재한다 (계획 01-03)</precondition>
  <read_first>
    - `apps/worker/src/worker/__main__.py` — 현재 기동 흐름(`configure_logging` → 시그널 핸들러 → `worker.started` 로그 → `await stop.wait()`)과 `git_sha` 필드
    - `apps/api/src/api/health_check.py` — `check_db_roundtrip`이 때리는 정확한 PostgREST 엔드포인트·헤더·타임아웃 (RTT 프로브가 같은 형태를 써야 한다)
    - `packages/core/src/nexuswiki_core/logging.py` — `bind_job_context` · `get_logger` 시그니처와 `REDACTED_KEYS` 목록
    - `apps/worker/pyproject.toml` — 현재 의존성 (`httpx`가 이미 있는지)
    - `.planning/phases/01-bootstrap-and-ground-truth/01-CONTEXT.md` D-14 — 측정을 worker에 붙인 이유와 `railway run` 함정
    - `.planning/phases/01-bootstrap-and-ground-truth/01-SPEC.md` §Requirements R9
  </read_first>
  <files>apps/worker/src/worker/rtt.py, apps/worker/src/worker/__main__.py, apps/worker/tests/test_rtt.py, apps/worker/pyproject.toml</files>
  <behavior>
    - Test 1: 응답 지연을 고정한 스텁 트랜스포트로 `measure_rtt`를 돌리면 `RttResult`가 `cold_first_ms`·`p50_ms`·`p95_ms`·`sample_count`·`warmup_count`·`failures` 여섯 필드를 모두 채운다
    - Test 2: 알려진 지연 수열(예: 1..100 ms)을 주입하면 `p50_ms`와 `p95_ms`가 그 수열의 50/95 백분위와 일치한다 (백분위 계산이 자체 검증됨)
    - Test 3: `sample_count=50`으로 호출하면 워밍업 요청은 표본에서 제외되고 정확히 50개 표본만 집계된다
    - Test 4: 일부 요청이 실패하면 `failures`가 증가하고, 성공 표본이 하나라도 있으면 예외를 던지지 않는다
    - Test 5: 모든 요청이 실패하면 `RttResult.sample_count`가 0이고 `p50_ms`/`p95_ms`가 `None`이며 예외를 던지지 않는다
  </behavior>
  <action>
    **`apps/worker/src/worker/rtt.py`** — 파일 헤더에 `관련 태스크: P0-INIT-04`,
    `설계 근거: 01-CONTEXT.md > D-14 (SPEC 경계가 새 라우터를 금지하므로 측정을 worker 기동에 붙인다)`.
    공개 API:
    - `RTT_WARMUP_COUNT: int = 5`
    - `RTT_SAMPLE_COUNT: int = 50` — SPEC R9의 N≥50 하한을 상수로 노출한다
    - `RTT_REQUEST_TIMEOUT_SECONDS: float = 5.0`
    - `RttResult` — `cold_first_ms: float | None`, `p50_ms: float | None`, `p95_ms: float | None`,
      `sample_count: int`, `warmup_count: int`, `failures: int` 여섯 필드의 `dataclass`
    - `async def measure_rtt(client: httpx.AsyncClient, *, supabase_url: str, publishable_key: str,
      sample_count: int = RTT_SAMPLE_COUNT, warmup_count: int = RTT_WARMUP_COUNT) -> RttResult`
    동작 순서: (1) **콜드 첫 요청 1회**를 따로 재서 `cold_first_ms`에 담는다 — 워밍업 이전이라 커넥션
    수립·TLS 핸드셰이크가 포함된 값이며, 이 값을 표본에 섞으면 p95가 오염된다. (2) `warmup_count`회를
    실행해 버린다. (3) `sample_count`회를 각각 `time.perf_counter()`로 재서 밀리초 목록을 만든다.
    (4) 정렬한 목록에서 p50과 p95를 구한다 — 인덱스 계산을 직접 하고(예: 최근접 순위법) 그 방식을
    주석에 명시한다. 표본이 0개면 두 값 모두 `None`.
    요청 형태는 `apps/api/src/api/health_check.py`가 쓰는 것과 **동일하게** 맞춘다 —
    `GET {supabase_url}/rest/v1/workspaces?select=id&limit=1`, 헤더 `apikey: {publishable_key}`,
    호출마다 명시 타임아웃. 두 곳이 갈라지면 문서의 숫자가 실제 readiness 경로를 대표하지 못한다.
    개별 요청 실패는 `failures`만 올리고 계속 진행한다 — 측정 실패가 worker 기동을 막으면 안 된다.
    함수는 어떤 경우에도 URL·키·응답 본문을 로그나 예외 메시지에 넣지 않는다.

    **`apps/worker/src/worker/__main__.py`** — `worker.started` 로그 직후, `await stop.wait()` 이전에
    프로브를 1회 실행한다. `RTT_PROBE_ENABLED` 환경변수가 `false`가 아니면 실행하고(기본 활성),
    `SUPABASE_URL`이나 `SUPABASE_PUBLISHABLE_KEY`가 없으면 건너뛰며 `worker.rtt_skipped`를 사유
    코드와 함께 남긴다. 성공하면 `worker.rtt_measured` 이벤트 한 줄에 `cold_first_ms`·`p50_ms`·
    `p95_ms`·`sample_count`·`warmup_count`·`failures`·`git_sha`를 싣는다. 예외는 잡아서
    `worker.rtt_failed`를 남기고 기동을 계속한다.
    ⚠️ 이 측정은 **배포된 컨테이너 안에서** 일어나야 의미가 있다. `railway run`은 로컬 프로세스에
    Railway 환경변수만 주입하므로 네트워크 경로가 개발자 머신 → Supabase가 되어 SPEC R9를 만족하지
    못한다. 이 함정을 주석으로 남긴다.

    **재배포 — 배포 로그 관측의 하드 전제.** `rtt.py`와 `__main__.py` 편집은 소스 변경이므로, 계획
    01-06이 배포한 컨테이너에는 아직 이 프로브가 없다. 단위 테스트가 통과한 뒤 다음 순서를 밟는다:
    1. 변경을 커밋하고 연동된 GitHub 브랜치에 push한다 (D-12: GitHub 연동 자동 배포).
    2. Railway에서 `worker` 서비스에 **새 배포**가 생성되는지 확인한다. `api`도 같은 저장소를 보므로
       함께 재배포되지만, 이 태스크의 관측 대상은 `worker` 배포다.
    3. 그 배포가 완료(`SUCCESS`/running)될 때까지 기다리고 **새 deployment id**를 적어 둔다.
    4. `worker.rtt_measured` 는 **그 deployment id의 로그에서만** 읽는다. 이전 배포의 로그에는 이
       이벤트가 아예 없으므로, 배포를 기다리지 않고 로그를 뒤지면 "관측되지 않음"이 "실패"로 잘못
       읽히거나, 반대로 옛 로그를 훑다가 판정을 건너뛰게 된다.
    이 프로브는 기동 시 1회만 실행되므로, 값을 다시 얻으려면 그 서비스를 재시작하거나 재배포해야 한다는
    사실도 함께 기록한다 (Task 2가 숫자를 옮겨 적을 때 필요하다).

    **`apps/worker/tests/test_rtt.py`** — 위 Behavior 1~5. 네트워크를 쓰지 않는다 —
    `httpx.MockTransport`로 응답과 지연을 제어하고, 시간은 주입 가능한 클록이나
    `monkeypatch`로 `time.perf_counter`를 고정해 결정적으로 만든다.
    Test 2가 이 태스크의 핵심이다: 백분위 계산이 스스로 검증되지 않으면 문서의 숫자를 믿을 근거가 없다.

    **`apps/worker/pyproject.toml`** — `httpx`가 없으면 `httpx==0.28.1`을 추가한다. 새 패키지는
    도입하지 않는다(계획 01-01의 정당성 게이트를 이미 통과한 목록 안에서만 움직인다).
    루트 `[tool.pytest.ini_options] testpaths` 에 `apps/worker/tests` 가 포함되어 있는지 확인하고,
    없으면 이 태스크에서 추가한다.
  </action>
  <acceptance_criteria>
    - `uv run pytest apps/worker/tests/test_rtt.py -q` 종료 코드 0이고 5개 이상의 테스트가 통과한다
    - `apps/worker/src/worker/rtt.py` 에 `RTT_SAMPLE_COUNT` 가 정의되어 있고 값이 50 이상
    - `measure_rtt` 가 네트워크 없이 `httpx.MockTransport` 로 완전히 테스트된다 (테스트가 실제 호스트에 접속하지 않음)
    - `apps/worker/src/worker/__main__.py` 에 `worker.rtt_measured` 문자열이 등장한다
    - `apps/worker/src/worker/__main__.py` 가 `RTT_PROBE_ENABLED` 를 참조한다
    - 프로브 추가 커밋이 push되어 Railway `worker` 서비스에 **새 배포**가 생성·완료되었고, 그 deployment id가 SUMMARY에 기록되었다
    - **그 새 배포의** `worker` 로그에 `worker.rtt_measured` 이벤트가 한 줄 나타나고 `sample_count` 가 50 이상이다 (계획 01-06이 배포한 이전 컨테이너에는 이 이벤트가 존재하지 않는다)
    - 그 로그 줄에 URL·키 값이 포함되지 않는다 (`nexuswiki_core.logging`의 마스킹 프로세서 통과)
    - 프로브 실패 시에도 `worker` 서비스가 running 을 유지한다
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest apps/worker/tests/test_rtt.py -q</automated>
    <human-check>(1) 프로브 커밋을 push한 뒤 Railway `worker` 서비스에 새 배포가 생겨 완료되었는지 확인하고 그 deployment id를 적는다. (2) **그 배포의** 로그에서 `worker.rtt_measured` 한 줄을 찾아 `sample_count`가 50 이상이고 `p50_ms`·`p95_ms`·`cold_first_ms`가 숫자로 채워져 있는지, 그리고 그 줄에 URL이나 키가 섞이지 않았는지 확인</human-check>
  </verify>
  <done>배포된 worker가 기동 시 워밍업 후 50회 이상을 측정해 p50/p95와 콜드 첫 요청을 구조화 로그 한 줄로 남기고, 백분위 계산은 결정적 단위 테스트로 검증된다.</done>
</task>

<task type="auto">
  <name>Task 2: `docs/ops/rtt-baseline.md` 작성 — 숫자 · 방법 · ×5 환산, 시크릿 제로</name>
  <precondition>배포된 `worker` 로그에 `worker.rtt_measured` 이벤트가 나타났고 그 값을 읽을 수 있다</precondition>
  <read_first>
    - Railway `worker` 서비스의 배포 로그 중 `worker.rtt_measured` 줄 — 실제 숫자
    - `HANDOFF.md` §3~3d — 이 저장소에서 "측정을 검증 기록으로 남기는" 유일한 선례(방법 → 수치 → pass 문장, 한국어)
    - `docs/ops/cloud-bootstrap-record.md`, `docs/ops/railway-deploy-record.md` — 계획 01-03·01-06이 확립한 `측정 일시` · `방법` · `결과` 표제 구조와 시크릿 미기재 규약
    - `.planning/phases/01-bootstrap-and-ground-truth/01-SPEC.md` §Requirements R9, §Prohibitions 4번째 행
    - `.planning/research/STACK.md` §"Three findings" 2번 — 지리적 추정치(~1–2 ms)와 그것이 추정임을 밝힌 문장 (실측과 대조해 적는다)
  </read_first>
  <files>docs/ops/rtt-baseline.md</files>
  <action>
    한국어로 작성한다. `측정 일시` · `방법` · `결과` 세 표제를 쓰고 아래를 모두 담는다.

    **측정 일시** — 측정한 날짜와 시각, 그리고 그 시점의 커밋 `git_sha`.

    **방법** — 다섯 가지를 명시한다:
    (1) 측정 주체가 **배포된 Railway `worker` 서비스(리전 `asia-southeast1`)** 이고 개발자 머신이
    아니라는 것. `railway run`은 로컬 프로세스에 환경변수만 주입해 네트워크 경로가 달라지므로 쓰지
    않았다는 한 줄을 반드시 넣는다.
    (2) SPEC R9는 "배포된 `api`에서"를 요구했으나 같은 SPEC의 경계가 `/health`·`/health/ready` 외의
    라우터를 금지하므로, 같은 이미지의 `worker` 엔트리포인트 기동 경로에서 측정했다는 것 —
    근거는 `01-CONTEXT.md > D-14`. 이 편차를 숨기지 말고 한 문단으로 적는다.
    (3) 대상이 Supabase `ap-southeast-1` 프로젝트이고, 요청 형태가 `/health/ready`가 쓰는 것과 같은
    PostgREST 단순 조회(단일 행 select, publishable 키의 `apikey` 헤더)라는 것.
    (4) 콜드 첫 요청 1회를 따로 잰 뒤 워밍업을 버리고 본 표본을 잰 순서와, 워밍업 횟수·표본 수.
    (5) p50/p95의 산출 방식(어떤 백분위 정의를 썼는지).

    **결과** — 표 하나로 다음을 적는다: `콜드 첫 요청(ms)` · `p50(ms)` · `p95(ms)` · `N(표본 수)` ·
    `워밍업 횟수` · `실패 수`. 그 아래에 **×5 환산** 절을 따로 두고 `p50 × 5`와 `p95 × 5`를 계산해
    적으며, 5채널 하이브리드 검색이 요청당 왕복을 곱한다는 이유를 한 줄로 붙인다.
    마지막에 연구 문서의 지리적 추정치와 실측을 대조하는 두 줄을 적는다 — 추정이 맞았는지 틀렸는지,
    그리고 Phase 4 검색 지연 기준선(OPS-05)이 이 숫자를 출발점으로 쓴다는 포인터.

    **이 문서는 금지사항 P4의 grep 대상이다.** Supabase project ref, 접속 문자열, 어떤 API 키도 적지
    않는다. 대상은 "Supabase `ap-southeast-1` 프로젝트"로만 지칭하고, Railway 서비스는 이름
    (`api`/`worker`)으로만 지칭한다. 공개 도메인도 적지 않는다.
    <!-- planner-discipline-allow: DATABASE_URL -->
    <!-- planner-discipline-allow: supabase.co -->
    구체적으로 다음 세 종류가 문서에 나타나서는 안 된다 — `DATABASE_URL` 값 또는 그 이름,
    `supabase.co` 호스트가 포함된 URL, `sb_publishable_`/`sb_secret_` 로 시작하는 문자열.
  </action>
  <acceptance_criteria>
    - `docs/ops/rtt-baseline.md` 가 존재하고 `측정 일시` · `방법` · `결과` 세 표제를 모두 포함한다
    - 문서에 `콜드` · `p50` · `p95` 세 문자열이 모두 등장하고 각각 숫자가 붙어 있다
    - 문서에 표본 수가 숫자로 적혀 있고 그 값이 50 이상이다
    - 문서에 `×5` 환산 절이 있고 `p50 × 5`와 `p95 × 5`의 계산 결과가 숫자로 적혀 있다
    - 문서에 `배포된` 과 `asia-southeast1` 두 문자열이 등장하고, 로컬 측정이 아님이 명시되어 있다
    - 문서에 `railway run` 을 쓰지 않았다는 문장이 있다
    - 문서에 `D-14` 가 등장한다 (api 대신 worker에서 측정한 편차의 근거)
    - `grep -c 'DATABASE_URL' docs/ops/rtt-baseline.md` 이 0
    - `grep -c 'supabase.co' docs/ops/rtt-baseline.md` 이 0
    - `grep -cE 'sb_(publishable|secret)_' docs/ops/rtt-baseline.md` 이 0
    - `grep -c 'postgresql://' docs/ops/rtt-baseline.md` 이 0
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/ops/rtt-baseline.md &amp;&amp; grep -q '측정 일시' docs/ops/rtt-baseline.md &amp;&amp; grep -q 'asia-southeast1' docs/ops/rtt-baseline.md &amp;&amp; ! grep -qE '(DATABASE_URL|supabase\.co|sb_(publishable|secret)_|postgresql://)' docs/ops/rtt-baseline.md</automated>
  </verify>
  <done>p50 · p95 · 콜드 첫 요청 · N · 방법 · ×5 환산이 모두 담긴 문서가 존재하고, 배포 환경에서 측정했음이 명시되며, 어떤 시크릿도 들어 있지 않다.</done>
</task>

<task type="auto">
  <name>Task 3: `checklists.json` 원장 갱신 — open question #2 해소 및 Phase 1 태스크 상태</name>
  <read_first>
    - `checklists.json` — `_notes` 세 줄(파생/요약 필드 금지, `$schema` 금지, verification은 기계 판정 기준), `open_questions` 배열, `phases[0].tasks`(`P0-INIT-00`~`P0-INIT-04`), `phases[1]`의 `P1-STO-01`
    - `.claude/CLAUDE.md` §Task Ledger Convention — `status` 전이, `verification_result`의 `date`/`method`/`results[]`, `deviations_from_plan`, "rationale를 두 곳에 적지 않고 `decisions.<key>`를 가리킨다"
    - `docs/ops/rtt-baseline.md` (Task 2), `docs/ops/cloud-bootstrap-record.md`, `docs/ops/railway-deploy-record.md`, `docs/ops/auth-hardening-record.md` — `verification_result.results[]`가 가리킬 근거 문서들
    - 이 페이즈의 SUMMARY들 (`01-01`~`01-07`) — 각 태스크의 실제 검증 결과
    - `.planning/phases/01-bootstrap-and-ground-truth/01-SPEC.md` §Acceptance Criteria — "`checklists.json` open question #2가 해소로 표시된다"
  </read_first>
  <files>checklists.json</files>
  <action>
    원장은 데이터 파일이다. `_notes`가 금지한 것을 새로 만들지 않는다 — 파생/요약 필드도, `$schema`도,
    같은 rationale의 두 번째 사본도 두지 않는다. 계획 01-07이 이미 경로 표기를 고쳤으므로 그 변경을
    되돌리지 않도록 현재 파일을 먼저 읽고 시작한다.

    **1. open question #2 해소.** `open_questions` 배열의 두 번째 항목(리전 조합과 왕복 지연 측정에 관한
    항목)을 해소된 형태로 갱신한다. 항목을 배열에서 제거하지 말고 문자열을 다시 쓴다 — 제거하면 이 질문이
    존재했다는 기록이 사라진다. 새 문자열은 세 가지를 담는다: 해소 표시와 날짜, 확정된 리전 조합
    (Supabase `ap-southeast-1` + Railway `asia-southeast1`), 그리고 실측 근거 경로
    `docs/ops/rtt-baseline.md`. 원래 문자열에 있던 "서울 `ap-northeast-2` 권장"은 실측·제약으로 뒤집혔으므로
    남기지 않는다. 나머지 세 open question은 손대지 않는다.

    **2. 태스크 상태 전이.** 아래 여섯 태스크의 `status`를 `done`으로 바꾸고 각각 `verification_result`를
    `date` · `method` · `results[]` 세 필드로 기록한다. `results[]`의 각 원소는 기계적으로 판정 가능한
    pass 문장이어야 한다(`_notes` 세 번째 줄).
    - `P0-INIT-00` (Supabase 프로젝트 생성 및 로컬 개발 스택 구성) — 근거: CLI 2.111.0+ 업그레이드,
      `supabase db reset` 성공, 클라우드 리전 `ap-southeast-1` 확인, `0001`~`0006` 클라우드 적용.
      문서 포인터는 `docs/ops/cloud-bootstrap-record.md`
    - `P0-INIT-01` (monorepo 구조 및 공통 툴링) — 근거: 클린 클론 `uv sync --frozen` 성공,
      `uv.lock` 추적, `pre-commit run --all-files` 통과 및 기존 파일 무변경, 두 언어 프로브 커밋 거부
    - `P0-INIT-02` (FastAPI 스캐폴딩 및 설정 관리) — 근거: `/health` 200(DB 없이), `/health/ready`
      3초 내 503, `job_id`/`workspace_id` 동시 포함 JSON 로그 한 줄, 마스킹 단위 테스트 통과
    - `P0-INIT-03` (Next.js 15 앱 스캐폴딩 및 테스트 러너) — 근거: next 15.5.22 이상,
      `tsc --noEmit` 통과, `vitest run` 2건 통과
    - `P0-INIT-04` (Railway 배포 파이프라인) — 근거: 두 서비스 running(`asia-southeast1`),
      배포 `/health` 200, 동일 빌드 판정(1차 또는 2차), `api` 서비스에 service key 부재.
      문서 포인터는 `docs/ops/railway-deploy-record.md`
    - `P1-STO-01` (Supabase Storage 버킷 및 접근 정책) — 근거: 비멤버 경로·2세그먼트 경로·UUID 아님
      경로가 모두 42501로 거부, 정상 3세그먼트 성공, 클라우드에 버킷 1행 + 정책 3행

    **3. 계획 대비 편차 기록.** 아래 두 건을 해당 태스크의 `deviations_from_plan`에 적는다. 이유는
    다시 서술하지 말고 결정 키를 가리킨다.
    - `P0-INIT-02` 또는 `P0-INIT-04` — RTT 측정을 배포된 `api` 라우터가 아니라 같은 이미지의 `worker`
      기동 경로에서 수행했다. 근거: `01-CONTEXT.md > D-14`
    - `P0-INIT-01` — Python 런타임을 3.12로 고정했다(연구 문서는 3.13 권장). 근거:
      `01-CONTEXT.md > D-04`

    **4. 인증 하드닝 기록.** BOOT-10에 대응하는 기존 태스크가 원장에 없으면 새 태스크를 만들지 말고,
    `P0-INIT-00`의 `verification_result.results[]`에 pass 문장 두 줄(6자 비밀번호 가입 거부, 미확인
    이메일 로그인 불가)과 문서 포인터 `docs/ops/auth-hardening-record.md`를 추가한다 — 원장 구조를
    새로 늘리지 않는다.

    `updated_at`과 `revision` 필드가 있으면 갱신한다. 편집 후 파일이 여전히 유효한 JSON인지 확인한다.
  </action>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('checklists.json','utf8'))"` 종료 코드 0
    - `node -e "const j=require('./checklists.json');process.exit(j.open_questions.length===4?0:1)"` 종료 코드 0 (항목이 제거되지 않고 갱신됨)
    - `node -e "const j=require('./checklists.json');process.exit(j.open_questions[1].includes('해소')&&j.open_questions[1].includes('rtt-baseline.md')?0:1)"` 종료 코드 0
    - `node -e "const j=require('./checklists.json');process.exit(/ap-northeast-2/.test(JSON.stringify(j.open_questions))?1:0)"` 종료 코드 0 (뒤집힌 권장이 남아 있지 않음)
    - `node -e "const j=require('./checklists.json');const ids=['P0-INIT-00','P0-INIT-01','P0-INIT-02','P0-INIT-03','P0-INIT-04'];const t=j.phases[0].tasks;process.exit(ids.every(i=>{const x=t.find(y=>y.id===i);return x&&x.status==='done'&&x.verification_result&&x.verification_result.date&&x.verification_result.method&&Array.isArray(x.verification_result.results)&&x.verification_result.results.length>0})?0:1)"` 종료 코드 0
    - `node -e "const j=require('./checklists.json');let f=null;for(const p of j.phases)for(const t of (p.tasks||[]))if(t.id==='P1-STO-01')f=t;process.exit(f&&f.status==='done'&&f.verification_result?0:1)"` 종료 코드 0
    - `node -e "const j=require('./checklists.json');process.exit(j.\$schema===undefined?0:1)"` 종료 코드 0 (`_notes`의 금지 준수)
    - `node -e "const j=require('./checklists.json');process.exit(JSON.stringify(j).includes('D-14')&&JSON.stringify(j).includes('D-04')?0:1)"` 종료 코드 0 (두 편차가 결정 키로 기록됨)
    - `grep -c 'fastapi-backend' checklists.json` 이 0 (계획 01-07의 경로 정합이 보존됨)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const j=require('./checklists.json');const ids=['P0-INIT-00','P0-INIT-01','P0-INIT-02','P0-INIT-03','P0-INIT-04'];const t=j.phases[0].tasks;const ok=ids.every(i=>{const x=t.find(y=>y.id===i);return x&amp;&amp;x.status==='done'&amp;&amp;x.verification_result});if(!ok)process.exit(1);if(j.open_questions.length!==4)process.exit(1);if(!j.open_questions[1].includes('해소'))process.exit(1);console.log('ledger: ok')"</automated>
  </verify>
  <done>open question #2가 배열에서 사라지지 않고 해소 표시와 근거 문서 경로를 담은 형태로 갱신되었고, 여섯 태스크가 `done` + `verification_result`(date/method/results)를 갖는다. 두 편차가 결정 키 참조로 기록되었다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 배포된 `worker` 컨테이너 → Supabase PostgREST | 측정 자체가 55회 이상의 아웃바운드 요청을 만든다. 요청·응답 어느 쪽도 로그에 원문으로 남아서는 안 된다 |
| 배포 로그 → 저장소 문서 (`docs/ops/rtt-baseline.md`) | 사람이 로그에서 숫자를 옮겨 적는 경로. 여기서 ref·키·URL이 함께 딸려 오기 쉽다 |
| 태스크 원장 (`checklists.json`) → 공개 저장소 | 검증 기록이 공개 열람 대상이 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information Disclosure | `docs/ops/rtt-baseline.md` | high | mitigate | 금지사항 P4의 grep 대상. project ref · 접속 문자열 · 키 · 공개 도메인을 적지 않고 negative grep 4건을 acceptance criteria로 고정. 대상은 "Supabase `ap-southeast-1` 프로젝트"로만 지칭 |
| T-08-02 | Information Disclosure | `worker.rtt_measured` 로그 이벤트 | high | mitigate | 이벤트 필드를 숫자 6종 + `git_sha`로 제한하고 URL·키·응답 본문을 싣지 않는다. `nexuswiki_core.logging`의 마스킹 프로세서가 두 번째 방어선 |
| T-08-03 | Denial of Service | 기동 시 55회 이상의 동기 프로브 | medium | mitigate | 요청마다 5초 타임아웃, 개별 실패는 `failures` 카운트만 올리고 계속 진행, 전체 실패해도 예외를 던지지 않아 worker 기동을 막지 않는다. `RTT_PROBE_ENABLED`로 끌 수 있다 |
| T-08-04 | Tampering | 백분위 계산 오류로 인한 잘못된 기준선 | medium | mitigate | 알려진 지연 수열을 주입해 p50/p95를 단언하는 결정적 단위 테스트(Behavior 2). 계산 방식(백분위 정의)을 코드 주석과 문서 §방법에 함께 명시 |
| T-08-05 | Repudiation | 편차를 숨긴 원장 | medium | mitigate | api 대신 worker에서 측정한 편차와 Python 3.12 고정 편차를 `deviations_from_plan`에 결정 키(`D-14`·`D-04`) 참조로 기록. 문서 §방법에도 같은 편차를 한 문단으로 적는다 |
| T-08-06 | Tampering | 원장 구조 훼손 (계획 01-07 변경 되돌리기) | medium | mitigate | 편집 전 현재 파일을 읽고 시작하며, `open_questions` 길이 4 유지 · `$schema` 부재 · 경로 표기 보존을 acceptance criteria로 단언 |
| T-08-07 | Repudiation | 재배포 전 로그를 근거로 한 위양성 판정 | high | mitigate | Task 1이 `worker` 소스를 수정하므로 커밋·push·`worker` 재배포 완료를 명시적 단계로 요구하고, deployment id를 SUMMARY에 기록한다. `worker.rtt_measured` 관측이 그 배포에서 왔음을 acceptance criterion으로 단언 |
| T-08-SC | Tampering | 패키지 매니저 설치 | low | accept | 새 패키지를 도입하지 않는다. `httpx`는 계획 01-01의 blocking-human 정당성 게이트를 이미 통과했고 `uv.lock`에 고정되어 있다 |
</threat_model>

<verification>
1. `uv run pytest apps/worker/tests/test_rtt.py -q` 종료 코드 0 (백분위 계산 자체 검증 포함)
2. 프로브 커밋 push 후 `worker` 서비스에 새 배포가 생성·완료되고 그 deployment id가 기록됨
3. **그 배포의** `worker` 로그에 `worker.rtt_measured` 한 줄, `sample_count` ≥ 50
4. `docs/ops/rtt-baseline.md`에 p50 · p95 · 콜드 첫 요청 · N · 방법 · ×5 환산이 모두 존재
5. 같은 문서에 negative grep 4건 통과 (ref · 호스트 · 키 · 접속 문자열 부재)
6. `checklists.json`이 유효 JSON이고 open question #2가 해소 표시, 여섯 태스크가 `done` + `verification_result`
7. 배포된 `api`의 `/health/ready`가 200 (클라우드 마이그레이션 적용 후) — URL은 `docs/ops/railway-deploy-record.md`의 `api_public_domain:` 줄에서 유도한다:
   `API_PUBLIC_URL="https://$(awk -F': *' '/^api_public_domain: /{print $2; exit}' docs/ops/railway-deploy-record.md)"` 후 `curl -fsS -o /dev/null -w '%{http_code}' "$API_PUBLIC_URL/health/ready"`
</verification>

<success_criteria>
- 배포 환경에서 측정한 p50/p95와 콜드 첫 요청이 문서에 숫자로 남았다
- ×5 환산이 함께 기록되어 Phase 4 지연 기준선의 출발점이 생겼다
- 문서에 어떤 시크릿도 없다
- `checklists.json` open question #2가 해소로 표시되고 Phase 1 태스크 상태가 원장에 반영되었다
</success_criteria>

## Artifacts this phase produces

**파일:** `apps/worker/src/worker/rtt.py`, `apps/worker/tests/test_rtt.py`, `docs/ops/rtt-baseline.md`

**Python 심볼:** `worker.rtt.RTT_WARMUP_COUNT`, `worker.rtt.RTT_SAMPLE_COUNT`,
`worker.rtt.RTT_REQUEST_TIMEOUT_SECONDS`, `worker.rtt.RttResult`, `worker.rtt.measure_rtt`

**env 키(신규 참조):** `RTT_PROBE_ENABLED`

**로그 이벤트(신규):** `worker.rtt_measured`, `worker.rtt_skipped`, `worker.rtt_failed`

**원장 변경:** `checklists.json` `open_questions[1]` 해소 표시,
`P0-INIT-00`~`P0-INIT-04` 및 `P1-STO-01`의 `status` → `done` + `verification_result`,
`P0-INIT-01`·`P0-INIT-04`의 `deviations_from_plan`

<output>
Create `.planning/phases/01-bootstrap-and-ground-truth/01-08-SUMMARY.md` when done
</output>
