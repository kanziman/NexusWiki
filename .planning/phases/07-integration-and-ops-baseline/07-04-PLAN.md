---
phase: 07-integration-and-ops-baseline
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/api/routers/jobs.py
  - apps/api/tests/test_jobs_router.py
  - apps/dashboard/app/w/[workspaceId]/settings/page.tsx
  - apps/dashboard/components/SettingsMembersPanel.tsx
  - apps/dashboard/components/OperationsPanel.tsx
  - apps/dashboard/tests/OperationsPanel.test.tsx
autonomous: true
requirements: [OPS-06]
user_setup: []
estimate: {tokens: 70000, raw_tokens: 70000, tasks: 3, confidence: medium}
must_haves:
  truths:
    - "A server-authorized owner/editor Operations snapshot combines display-only monthly usage budget and five server-ordered queued/running/dead stage rows; viewer receives neither API data nor panel/request (D-05, D-06, D-08)."
    - "The Settings route remains the only surface; it has accessible member/operations tabs, retains the 640px shell, and never creates top-level navigation (D-05)."
    - "Data loads on tab entry/page load and explicit refresh only; refresh disables the 44px text-plus-icon control with aria-busy and retains the prior snapshot on failure (D-07)."
    - "The snapshot never includes payload, last_error, usage metadata, provider/model, or raw API error details; server authorization is authoritative (D-08)."
    - {statement: "Long localized workspace/settings labels wrap or truncate accessibly with a full-name tooltip.", verification: backstop}
    - {statement: "Unusually large locale-formatted costs and timestamps do not overlap the refresh control.", verification: backstop}
  artifacts:
    - {path: apps/api/src/api/routers/jobs.py, provides: editor/owner operations snapshot DTO}
    - {path: apps/dashboard/components/OperationsPanel.tsx, provides: manual-refresh operations tab}
    - {path: apps/dashboard/tests/OperationsPanel.test.tsx, provides: UI authorization/state/backstop coverage}
  key_links:
    - "SettingsMembersPanel -> owner/editor OperationsPanel -> apiFetch GET /workspaces/{id}/operations -> UserDb RLS aggregate"
    - "jobs.py CHAIN_ORDER/STEP_LABELS and budget aggregate -> fixed DTO -> server-provided dashboard labels"
assumptions:
  - "OPS-06 was unclassified/unresolved in the deterministic edge probe; server role checks, fixed safe DTO, no-viewer-request UI test, and manual-refresh state tests are its concrete requirement proof."
---
<objective>Deliver OPS-06 as a safe owner/editor Operations tab within Settings, with server-owned budget and pipeline health snapshot semantics.</objective>
<context>
@.planning/phases/07-integration-and-ops-baseline/07-CONTEXT.md
@.planning/phases/07-integration-and-ops-baseline/07-RESEARCH.md
@.planning/phases/07-integration-and-ops-baseline/07-PATTERNS.md
@.planning/phases/07-integration-and-ops-baseline/07-UI-SPEC.md
@apps/api/src/api/routers/jobs.py
@apps/api/tests/test_jobs_router.py
@apps/dashboard/components/SettingsMembersPanel.tsx
@apps/dashboard/components/JobStepper.tsx
@apps/dashboard/lib/api-client.ts
@apps/dashboard/tests/JobStepper.test.tsx
</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Define the server-owned Operations snapshot and authorization</name>
<files>apps/api/src/api/routers/jobs.py, apps/api/tests/test_jobs_router.py</files>
<behavior>
 - GET `/workspaces/{workspace_id}/operations` returns budget `{cap_micros,spent_micros,remaining_micros,month_start,truncated,authoritative:false}`, five ordered stage rows, and observed_at.
 - Owner/editor receive 200; unauthenticated receives 401; viewer and non-member/foreign caller are rejected by the server.
 - DTO excludes payload, last_error, usage metadata, provider, and model, and uses existing server labels/order.
</behavior>
<read_first>apps/api/src/api/routers/jobs.py (CHAIN_ORDER/STEP_LABELS/workspace_budget), apps/api/tests/test_jobs_router.py (credential matrix), 05-CONTEXT.md (D-06 editor+ gate), 07-PATTERNS.md (Operations API).</read_first>
<action>Add the D-06 single snapshot endpoint to `jobs.py`, using requester-JWT `UserDb` direct RLS-scoped reads/aggregates and a server membership helper that permits owner/editor only per D-08. Use the fixed server-owned CHAIN_ORDER/STEP_LABELS list to assemble queued/running/dead rows and reuse the current UTC month, 1,000-row display aggregate, and `authoritative: false`, without returning internal job or usage fields. First prove the direct PostgREST/UserDb aggregation produces the exact DTO in router tests. Only if those tests demonstrate it technically cannot do so, add one narrow `SECURITY INVOKER` RPC with fixed workspace-scoped return schema and explicit membership enforcement; do not add a migration proactively. Add local-stack tests for every authorization branch, zero/aggregate/truncation behavior, exact stage order/labels, direct-read path, and sensitive-key absence.</action>
<verify><automated>UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run pytest -q apps/api/tests/test_jobs_router.py</automated></verify>
<done>The API provides an owner/editor-only, safe, fixed Operations DTO with both cost and pipeline health (D-06, D-08).</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Add accessible Settings tabs and the Operations panel</name>
<files>apps/dashboard/app/w/[workspaceId]/settings/page.tsx, apps/dashboard/components/SettingsMembersPanel.tsx, apps/dashboard/components/OperationsPanel.tsx, apps/dashboard/tests/OperationsPanel.test.tsx</files>
<behavior>
 - Owner/editor see keyboard-operable 멤버/운영 현황 tabs; viewer sees 멤버 alone and issues no operations request.
 - Initial Operations load renders neutral skeletons; manual refresh has aria-busy/disabled 44px control and failure retains last snapshot with contracted Korean error copy.
 - Render Korean-only budget, cap-zero/no-usage/truncated states, five server labels/counts, all-zero pipeline note, local timestamp, and horizontal narrow-width stage scroll.
</behavior>
<read_first>07-UI-SPEC.md (entire layout/copy/UI Considerations), apps/dashboard/components/SettingsMembersPanel.tsx (640px workaround), apps/dashboard/components/JobStepper.tsx (refresh state), apps/dashboard/lib/api-client.ts, apps/dashboard/tests/JobStepper.test.tsx, 07-PATTERNS.md (Operations UI).</read_first>
<action>Keep D-05 on the existing Settings route and evolve its wrapper into semantic, arrow-key-operable tabs. Resolve membership server-side or pass current role from the Settings data path; use role visibility only as defense in depth because D-08 remains server-authorized. Implement `OperationsPanel` with a one-time entry fetch and explicit `apiFetch` refresh—no polling or background refetch (D-07). Use the UI-SPEC copy/tokens, `style={{ maxWidth: "640px" }}`, server labels, safe formatting, internal horizontal stage scroll, and no retry/cancel/payload/raw-error surface. Add Testing Library tests for roles/no viewer request, all loading/error/empty/partial/populated branches, refresh retention, labels, large values/timestamp backstop, and accessible label tooltip/truncation backstop.</action>
<verify><automated>pnpm --dir apps/dashboard test -- OperationsPanel.test.tsx && pnpm --dir apps/dashboard typecheck</automated></verify>
<done>The Settings-only Operations tab safely renders all specified states and cannot request or render operations data for a viewer (D-05, D-07, D-08).</done>
</task>
<task type="auto">
<name>Task 3: Run the complete API and dashboard operations evidence set</name>
<files>apps/api/tests/test_jobs_router.py, apps/dashboard/tests/OperationsPanel.test.tsx</files>
<read_first>07-VALIDATION.md (OPS-06 command map), 07-UI-SPEC.md (UI Considerations), 07-RESEARCH.md (Operations DTO boundary).</read_first>
<action>Run and stabilize the focused router and component tests as one OPS-06 contract. Confirm tests assert server authorization before UI defense, never assert raw backend error strings, and explicitly prove no polling mechanism. Preserve existing Settings members behavior while exercising both tab states.</action>
<verify><automated>UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run pytest -q apps/api/tests/test_jobs_router.py && pnpm --dir apps/dashboard test -- OperationsPanel.test.tsx && pnpm --dir apps/dashboard typecheck</automated></verify>
<done>OPS-06 has focused automated API and dashboard evidence for authorization, safe DTO, manual refresh, and all UI-SPEC states.</done>
</task>
</tasks>

## Artifacts this phase produces

- `apps/api/src/api/routers/jobs.py` — server-authorized Operations snapshot endpoint
- `apps/dashboard/components/OperationsPanel.tsx` — Settings operations rendering and manual refresh UX
- `apps/dashboard/tests/OperationsPanel.test.tsx` — UI contract and UI-SPEC backstop evidence

<threat_model>
## ASVS L1 threat model
| Threat | Severity | Mitigation and blocker |
|---|---:|---|
| Viewer/foreign user obtains operational financial data | high | Server editor/owner authorization plus UI no-request test; block any 200 viewer response. |
| Sensitive job/provider fields leak to dashboard | high | Fixed allowlist DTO and absent-key tests; block payload/error/provider/model fields. |
| Polling amplifies load or exposes stale operations state | high | One entry fetch plus explicit refresh only; block timers/background refetch. |
</threat_model>
