---
phase: 03-ingest-and-compile-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0008_embedding_dimension.sql
  - supabase/tests/0008_search_contract.sql
  - scripts/verify_search_contract.sh
  - scripts/ci_check_search_contract.sh
  - .github/workflows/ci.yml
  - docs/ops/migration-0008-record.md
autonomous: true
requirements: [COMP-06]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`source_chunks.embedding`과 `wiki_embeddings.embedding`이 로컬과 클라우드 양쪽에서 `extensions.vector(1024)`다"
    - "`public.search_chunks`의 두 번째 인자 타입이 `extensions.vector(1024)`이고, 재생성 후에도 `prosecdef=false` · `provolatile='s'` · `proconfig`에 `search_path` + hnsw GUC 3종이 그대로 있다"
    - "재생성된 `search_chunks`에 `authenticated`만 EXECUTE를 갖고 `anon`과 `service_role`은 갖지 않는다 — drop이 지운 ACL이 같은 마이그레이션에서 복원되었다"
    - "`source_chunks`를 벡터 최근접으로 조회하면 `EXPLAIN`에 HNSW 인덱스 스캔이 나타난다"
    - "`0008`은 단일 트랜잭션이며 부분 적용된 상태(컬럼은 1024인데 인덱스나 함수가 1536)가 존재할 수 없다"
    - "`0008` 헤더가 `0002:76`의 `text-embedding-3-small(1536차원)` 주석이 무효임과 대체 근거 키를 명시한다"
    - statement: "`0008` 적용 시점에 두 임베딩 컬럼의 행이 0개라 `alter column ... type`이 데이터 재생성 없이 끝났다 — 이 창은 이 페이즈가 첫 임베딩을 만드는 순간 닫힌다"
      verification: backstop
  artifacts:
    - path: "supabase/migrations/0008_embedding_dimension.sql"
      provides: "임베딩 차원 1024 보정 · HNSW 인덱스 2종 재생성 · search_chunks 재생성 + ACL 복원"
      contains: "extensions.vector(1024)"
    - path: "supabase/tests/0008_search_contract.sql"
      provides: "pg_proc 기반 검색 함수 계약 단언 + EXPLAIN HNSW 단언"
      contains: "search_contract: ok"
    - path: "scripts/verify_search_contract.sh"
      provides: "ON_ERROR_STOP=1 + grep 기반 계약 러너"
    - path: "scripts/ci_check_search_contract.sh"
      provides: "DB 없이 도는 소스 수준 검색 계약 회귀 게이트"
    - path: "docs/ops/migration-0008-record.md"
      provides: "로컬 reset · 클라우드 push 실측 기록과 한계"
  key_links:
    - from: "supabase/migrations/0008_embedding_dimension.sql"
      to: "supabase/migrations/0007_search_and_queue_extensions.sql"
      via: "0007:386-387의 revoke/grant 쌍을 drop 이후 재실행"
      pattern: "grant execute on function public\\.search_chunks"
    - from: "scripts/ci_check_search_contract.sh"
      to: ".github/workflows/ci.yml"
      via: "5번째 잡 search-contract가 이 스크립트를 실행"
      pattern: "ci_check_search_contract\\.sh"
  prohibitions: []
---

<objective>
`0008_embedding_dimension.sql`을 작성해 두 임베딩 컬럼과 그에 딸린 HNSW 인덱스 2종,
그리고 시그니처에 차원이 박힌 `public.search_chunks`를 1024차로 옮기고, 그 상태를
로컬 `supabase db reset`과 클라우드 `supabase db push`까지 이 플랜 안에서 끝낸다.

Purpose: 재임베딩 창은 **Phase 3가 첫 임베딩을 만드는 순간 닫힌다**. 지금은 두 컬럼의
행이 0개라 차원 변경이 `using` 절 없는 한 줄이지만, 뒤로 미루면 되돌릴 비용이 0에서
전체 재임베딩으로 점프한다. 그리고 `0002`/`0007`이 이미 1536차로 클라우드에 올라가
있으므로, push 이전에는 클라우드에서 **어떤 임베딩도 만들 수 없다** —
이 플랜이 닫히기 전에는 Phase 3의 어떤 임베딩 코드도 검증될 수 없다.
근거: 03-CONTEXT.md > D-01, D-02.

Output: `0008` 마이그레이션 · SQL 계약 러너 한 쌍 · CI 5번째 잡 · 적용 기록 문서.
</objective>

## 되돌릴 수 없는 문 — 체크포인트를 두지 않는 이유

Task 1과 Task 3은 `<reversibility rating="one-way">` 로 표시되어 있으나 앞에
`checkpoint:decision` 을 두지 **않는다**. 이 문은 이미 통과되었기 때문이다 —
`03-CONTEXT.md > D-01` 과 `D-02` 가 **사용자 결정**으로 기록되어 있고, D-01은 그 자리에서
`Reversibility: one-way` 라고 스스로 표시하며 대가(모든 임베딩 재생성)까지 적었다.
이미 내린 결정을 실행 직전에 다시 묻는 것은 체크포인트 피로만 만들고 아무것도 게이트하지
않는다. 등급은 기록으로 남기고 게이트는 두지 않는다.

대신 Task 3의 `<precondition>` 이 원격 원장의 마지막 항목이 `0007` 인지 확인하게 하고,
`<verify>` 의 `human-check` 가 push 결과를 관측으로 대조한다.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ingest-and-compile-pipeline/03-CONTEXT.md
@.planning/phases/03-ingest-and-compile-pipeline/03-PATTERNS.md
@.claude/CLAUDE.md
@supabase/migrations/0007_search_and_queue_extensions.sql
@supabase/migrations/0002_search_schema.sql
@supabase/tests/0007_queue_functions.sql
@scripts/verify_queue_functions.sh
@docs/ops/migration-0007-record.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: `0008_embedding_dimension.sql` 작성 — 단일 트랜잭션, 7개 계약 복원</name>
  <reversibility rating="one-way">`0008`이 클라우드에 push되면 `0008` 이하 번호는 영구히 추가 불가이고, 차원을 되돌리려면 그때 존재하는 모든 임베딩을 재생성해야 한다 (03-CONTEXT.md > D-01이 사용자 결정으로 이 문을 이미 통과했다 — 재질의 체크포인트를 두지 않는다).</reversibility>
  <files>supabase/migrations/0008_embedding_dimension.sql</files>
  <read_first>
    - `supabase/migrations/0007_search_and_queue_extensions.sql` — 전문. 헤더 형식(1-32행) · `begin;`(34행) · warmup 한 줄(68행) · `search_chunks` 정의(70-104행) · 권한 방향(373-387행) · `notify pgrst`(390행) · `commit;`(392행). **이 파일이 형식의 유일한 실물이다.**
    - `supabase/migrations/0002_search_schema.sql` 58-153행 — 바꿀 컬럼 2개(`:77`, `:138`)와 인덱스 2개(`:115`, `:152`)의 원본 정의. `:76`의 무효 주석도 여기 있다.
    - `.claude/CLAUDE.md` — SQL 스타일(소문자 키워드 · 2칸 들여쓰기 · `-- ---` 구분선) · 파일 헤더에 태스크 ID와 결정 키를 인용하고 근거를 재서술하지 않는 규약 · 모든 주석은 한국어.
    - `.planning/phases/03-ingest-and-compile-pipeline/03-CONTEXT.md` — D-01(작업 목록 4항) · D-06(무효 주석 처리) · D-07(단일 트랜잭션) · D-08(옮겨야 할 계약) · D-09(파일명·커밋 형태).
  </read_first>
  <action>
    `supabase/migrations/0008_embedding_dimension.sql`을 새로 만든다. 파일 전체를 `begin;` ... `commit;`으로 감싼다 (D-07 · `0007`이 세운 관례).

    헤더 블록은 `0007:1-32`와 같은 형식으로 쓰고 다음을 담는다 — 제목 `NexusWiki 0008: 임베딩 차원 1024 보정`; `관련 태스크: P2-EMB-01`; `설계 근거: checklists.json > decisions.embedding_model` 과 `03-CONTEXT.md > D-01, D-06, D-07, D-08`; 그리고 `0002_search_schema.sql:76`의 모델·차원 주석이 이 마이그레이션 이후 사실이 아니며 대체 근거는 `checklists.json > decisions.embedding_model`이라는 문장 (D-06 — 근거를 재서술하지 말고 키만 가리킬 것). 헤더에 `⚠️` 항목으로 두 가지를 적는다: (1) 지금 두 컬럼의 행이 0개라 타입 변경에 `using` 절이 필요 없다는 사실과 그 창이 이 페이즈의 첫 임베딩에서 닫힌다는 사실, (2) 부분 적용이 만드는 "컬럼은 1024인데 인덱스나 함수가 1536"이라는 상태를 트랜잭션이 구조적으로 막는다는 사실.

    본문 실행 순서를 이대로 고정한다. 1) `0007:68`과 같은 pgvector warmup 한 줄 (`select '[1,2,3]'::extensions.vector as pgvector_warmup;`) — 지우면 뒤의 `set hnsw.*`가 `permission denied to set parameter`로 거부된다. 2) `drop function public.search_chunks(uuid, extensions.vector, int);` — 인자 타입이 바뀌므로 `create or replace`는 새 오버로드를 만들 뿐이다. 3) `drop index public.source_chunks_embedding_idx;` 와 `drop index public.wiki_embeddings_embedding_idx;` — 컬럼 타입 변경 전에 내려야 한다. 4) `alter table public.source_chunks alter column embedding type extensions.vector(1024) using embedding::extensions.vector(1024);` 와 `wiki_embeddings`에 대한 같은 문장. 5) 두 HNSW 인덱스를 `0002:115`·`0002:152`와 **같은 이름·같은 연산자 클래스**(`using hnsw (embedding extensions.vector_cosine_ops)`)로 재생성. 6) `create or replace function public.search_chunks(...)`를 `0007:70-101`과 글자 그대로 같게 쓰되 `p_query`의 타입만 `extensions.vector(1024)`로 바꾼다 — `returns table` 5컬럼 · `language sql` · `security invoker` · `stable` · `set search_path = public` · `set hnsw.iterative_scan = 'strict_order'` · `set hnsw.ef_search = '200'` · `set hnsw.max_scan_tuples = '40000'` · 본문의 `operator(extensions.<=>)` 수식 2곳 · `where c.workspace_id = p_workspace_id and c.embedding is not null` · `limit p_k`. 7) `comment on function public.search_chunks(uuid, extensions.vector, int)` — `0007:103-104`의 문장을 그대로 옮긴다. 8) **ACL 복원** — `revoke all on function public.search_chunks(uuid, extensions.vector, int) from public, anon;` 와 `grant execute on function public.search_chunks(uuid, extensions.vector, int) to authenticated;`. 이 두 줄이 없으면 drop이 함께 지운 `0007:386-387`의 권한이 돌아오지 않고, Supabase가 새 함수에 주는 기본 실행 권한만 남는다. 9) `notify pgrst, 'reload schema';` — 없으면 재생성된 함수 호출이 PGRST202로 떨어진다.

    새 테이블을 만들지 않으므로 `0007` 섹션 8의 테이블 revoke/grant 매트릭스는 **재적용하지 않는다** — 그 매트릭스는 기존 9개 테이블에 이미 걸려 있고 컬럼 타입 변경은 ACL을 건드리지 않는다. `dead_letter_job()`과 `usage_events`는 이 파일에 넣지 않는다 (D-01).
  </action>
  <verify>
    <automated>test -f supabase/migrations/0008_embedding_dimension.sql &amp;&amp; grep -v '^\s*--' supabase/migrations/0008_embedding_dimension.sql | grep -c 'extensions.vector(1024)' | awk '$1 &gt;= 3 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0008_embedding_dimension.sql`이 존재하고 첫 실행문이 `begin;`, 마지막 실행문이 `commit;`이다.
    - 주석을 제외한 본문에 `extensions.vector(1024)`가 3회 이상 나타난다 (컬럼 2개 + 함수 인자 1개).
    - 주석을 제외한 본문에 `drop function public.search_chunks`가 정확히 1회, `create or replace function public.search_chunks`가 정확히 1회 나타난다.
    - 주석을 제외한 본문에 `grant execute on function public.search_chunks`가 1회 나타난다 (drop이 지운 `0007:387`의 복원).
    - 주석을 제외한 본문에 `set hnsw.iterative_scan`, `set hnsw.ef_search`, `set hnsw.max_scan_tuples`가 각각 1회 나타나고 `security invoker`와 `stable`이 함수 정의에 있다.
    - 주석을 제외한 본문에 `operator(extensions.<=>)`가 2회 나타난다.
    - 마지막 `commit;` 직전 줄이 `notify pgrst, 'reload schema';`이다.
    - 헤더 주석이 `0002_search_schema.sql:76`을 문자열로 언급하고 `checklists.json > decisions.embedding_model`을 인용한다.
    - `pre-commit run --all-files`가 통과한다.
  </acceptance_criteria>
  <done>`0008`이 단일 트랜잭션으로 존재하고, 7개 계약(security invoker · stable · search_path · hnsw GUC 3종 · 연산자 수식 · warmup · ACL 복원)이 전부 파일 안에 있다.</done>
</task>

<task type="auto">
  <name>Task 2: SQL 계약 러너와 DB 없는 CI 게이트</name>
  <files>supabase/tests/0008_search_contract.sql, scripts/verify_search_contract.sh, scripts/ci_check_search_contract.sh, .github/workflows/ci.yml</files>
  <read_first>
    - `supabase/tests/0007_queue_functions.sql` — 전문. 헤더 규약(1-17행: "마이그레이션이 아님" · 트랜잭션 + rollback · 실행 명령) · `do $tN$ ... raise exception` 단언 형태(45-59행) · 고정 UUID 픽스처 규약(21-40행) · 마지막의 `select '...: ok' as result;`(205행).
    - `scripts/verify_queue_functions.sh` — 13행 전문이 그대로 템플릿이다. `ON_ERROR_STOP=1`이 계약의 일부다.
    - `.github/workflows/ci.yml` — 4잡 구조와 헤더의 "실패를 삼키는 구문을 두지 않는다" 규약, 그리고 155-169행의 "러너에 Supabase 스택을 세우지 않는다"는 명시적 결정.
    - `scripts/ci_check_service_usage.sh` — 탐색 대상 0개를 pass가 아니라 fail로 처리하는 형태.
    - `supabase/migrations/0008_embedding_dimension.sql` — Task 1이 만든 파일.
  </read_first>
  <action>
    (a) `supabase/tests/0008_search_contract.sql`을 만든다. `0007_queue_functions.sql`의 헤더 규약을 그대로 따르고(마이그레이션이 아님 · `supabase/migrations/` 밖 · 전체가 하나의 트랜잭션이고 마지막이 `rollback` · 실행 명령 주석), 픽스처 UUID는 같은 대역(`10000000-…` 사용자 / `20000000-…` 워크스페이스)을 쓴다.

    단언은 `do $tN$ ... raise exception` 형태로 `pg_catalog`를 읽어 다음 여덟 가지를 확인한다. 1) `pg_proc`에서 `proname = 'search_chunks'` 인 행이 정확히 1개다 — 2개면 drop을 빠뜨리고 오버로드가 생긴 것이다. 2) `prosecdef` 가 false다 (definer면 교차 테넌트 검색이 열린다). 3) `provolatile` 가 `s` 다. 4) `proconfig` 가 `search_path=public` · `hnsw.iterative_scan=strict_order` · `hnsw.ef_search=200` · `hnsw.max_scan_tuples=40000` 네 원소를 모두 포함한다. 5) `pg_get_function_arguments(oid)` 문자열이 `vector(1024)` 를 포함하고 `vector(1536)` 은 포함하지 않는다. 6) `prosrc` 가 `operator(extensions.<=>)` 를 포함한다. 7) **ACL** — `has_function_privilege('authenticated', oid, 'EXECUTE')` 가 true이고 `has_function_privilege('anon', oid, 'EXECUTE')` 와 `has_function_privilege('service_role', oid, 'EXECUTE')` 가 둘 다 false다. 8) `format_type(atttypid, atttypmod)` 로 읽은 `source_chunks.embedding` 과 `wiki_embeddings.embedding` 이 둘 다 1024차 벡터다.

    그 다음 EXPLAIN 단언을 둔다. 픽스처 워크스페이스에 `raw_sources` 1행과 `source_chunks` 여러 행(임의의 1024차 벡터)을 넣고, `set local enable_seqscan = off` 상태에서 `explain (format json) select ... from public.source_chunks where workspace_id = ... order by embedding operator(extensions.<=>) '<1024차 벡터>' limit 20` 의 계획 JSON을 텍스트로 받아 `Index Scan` 과 `source_chunks_embedding_idx` 가 함께 나타나는지 검사하고, 아니면 `raise exception`. 마지막 줄은 `select 'search_contract: ok' as result;` 이고 그 뒤가 `rollback;` 이다.

    (b) `scripts/verify_search_contract.sh` — `scripts/verify_queue_functions.sh`를 복사해 입력 파일 경로와 grep 문자열 두 곳만 `supabase/tests/0008_search_contract.sql` 과 `search_contract: ok` 로 바꾼다. `set -euo pipefail` 과 `-v ON_ERROR_STOP=1` 은 그대로 둔다. 실행 비트를 준다.

    (c) `scripts/ci_check_search_contract.sh` — GitHub Actions 러너에는 Supabase 스택이 없으므로(ci.yml 155-169행의 명시적 결정) DB 없이 도는 **소스 수준** 게이트를 만든다. `03-CONTEXT.md > D-08`은 "PR 게이트에 추가"를 요구하고 `Claude's Discretion` 블록 안에 있으므로, 러너에 스택을 세우는 대신 형태를 바꾸고 이유를 스크립트 헤더에 남긴다: psql 러너는 로컬·수동 게이트로 남고, CI는 "Phase 4가 검색 함수를 다시 건드릴 때의 회귀"라는 D-08의 실제 목표를 소스에서 잡는다. 동작은 — `supabase/migrations/` 에서 `create or replace function public.search_chunks` 를 담은 파일을 파일명 정렬 기준 **가장 나중 것** 하나로 고르고, 그 파일이 0개면 즉시 실패(탐색 대상 0개를 pass로 처리하지 않는다), 그 파일에서 `grep -v '^\s*--'` 로 주석을 걷어낸 뒤 `security invoker` · `stable` · `set search_path = public` · `set hnsw.iterative_scan` · `set hnsw.ef_search` · `set hnsw.max_scan_tuples` · `operator(extensions.<=>)` · `pgvector_warmup` · `grant execute on function public.search_chunks` 아홉 토큰이 모두 있는지 확인하고 하나라도 없으면 그 토큰 이름을 출력하며 실패한다. `set -euo pipefail` 을 쓰고 실패를 삼키는 구문(항상 참을 내는 파이프 꼬리, 오류 중단 해제)을 두지 않는다.

    (d) `.github/workflows/ci.yml` 에 다섯 번째 잡 `search-contract` 를 추가한다. 이름은 `검색 함수 계약 (COMP-06)`, `runs-on: ubuntu-latest`, 스텝은 `actions/checkout@v5` 하나와 `bash scripts/ci_check_search_contract.sh` 하나뿐이다 — Python도 Node도 자격증명도 필요하지 않다. 기존 4잡과 서로 의존시키지 않는다 (ci.yml 45-46행의 규약). 잡 위에 이 잡이 DB를 세우지 않는 이유와 psql 러너의 위치를 한국어 주석으로 남긴다.
  </action>
  <verify>
    <automated>bash scripts/ci_check_search_contract.sh &amp;&amp; test -x scripts/verify_search_contract.sh &amp;&amp; grep -q 'search-contract:' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/ci_check_search_contract.sh` 가 exit 0으로 끝난다.
    - `supabase/migrations/` 에서 `search_chunks` 정의 파일을 하나도 못 찾는 상황을 인위적으로 만들면(예: 임시 디렉터리에서 실행) 스크립트가 0이 아닌 코드로 끝난다.
    - `scripts/verify_search_contract.sh` 가 실행 가능하고 본문에 `ON_ERROR_STOP=1` 과 `search_contract: ok` 를 모두 포함한다.
    - `supabase/tests/0008_search_contract.sql` 의 마지막 실행문이 `rollback;` 이고 그 직전 줄이 `search_contract: ok` 를 select한다.
    - `.github/workflows/ci.yml` 의 `jobs:` 아래 키가 5개다 (`pre-commit`, `service-usage`, `bundle-secrets`, `pytest`, `search-contract`).
    - `.github/workflows/ci.yml` 의 `search-contract` 잡이 어떤 `secrets.` 참조도 하지 않는다.
    - `pre-commit run --all-files` 가 통과한다.
  </acceptance_criteria>
  <done>계약 러너 한 쌍과 DB 없이 도는 CI 게이트가 존재하고, 게이트가 clean tree에서 green이며 대상 부재에서 red다.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] 로컬 reset · 계약 러너 실행 · 클라우드 `db push` · 적용 기록</name>
  <precondition>`SUPABASE_ACCESS_TOKEN`이 셸에 있고, `supabase link` 된 클라우드 프로젝트(`ap-southeast-1`)의 마이그레이션 원장 마지막 항목이 `0007`이다 — `supabase migration list`로 확인한다.</precondition>
  <reversibility rating="one-way">클라우드 push는 편도다 — `0008`이 원격에 오르면 `0008` 이하 번호의 마이그레이션은 영구히 추가할 수 없고, 차원을 되돌리려면 그 시점에 존재하는 모든 임베딩을 재생성해야 한다.</reversibility>
  <files>docs/ops/migration-0008-record.md</files>
  <read_first>
    - `docs/ops/migration-0007-record.md` — 전문. 이 문서가 대칭으로 따라갈 형식이며 특히 `§한계와 되돌리기` 절의 서술 방식을 그대로 쓴다.
    - `.claude/CLAUDE.md` — 로컬 스택 포트 544xx · 로컬 `psql` 없음(`docker exec -it supabase_db_NexusWiki psql -U postgres -d postgres`) · 커밋 형태 `type(scope): <migration number> — <구체적으로 무엇이 바뀌었나>`.
    - `supabase/migrations/0008_embedding_dimension.sql` · `supabase/tests/0008_search_contract.sql` · `scripts/verify_search_contract.sh` — Task 1·2의 산출물.
    - `.planning/phases/03-ingest-and-compile-pipeline/03-CONTEXT.md` D-02 — 이 작업이 이 플랜 안에서 끝나야 하는 이유.
  </read_first>
  <action>
    순서를 지킨다. 1) `supabase db reset` 으로 로컬 스택에 `0001`~`0008`을 처음부터 적용한다. 2) `bash scripts/verify_queue_functions.sh` 와 `bash scripts/verify_search_contract.sh` 를 둘 다 돌려 통과를 확인한다 — 새 계약과 기존 큐 계약이 함께 서는지 보는 것이 목적이다. 3) `uv run pytest -rs` 로 루트 스위트를 돌린다. 4) `supabase db push` 로 클라우드에 적용한다. 비대화형이면 `SUPABASE_ACCESS_TOKEN` 환경변수를 세워 실행한다. 5) 클라우드에 대해 적용 결과를 직접 관측한다 — `supabase migration list` 의 원격 열에 `0008`이 있는지, 그리고 클라우드 SQL 경로로 `source_chunks.embedding` · `wiki_embeddings.embedding` 의 `format_type` 이 1024차인지, `pg_get_function_arguments` 에 `vector(1024)` 가 있는지, `has_function_privilege('authenticated', ..., 'EXECUTE')` 가 true이고 `anon`·`service_role` 이 false인지.

    `docs/ops/migration-0008-record.md` 를 `migration-0007-record.md` 와 대칭 형식으로 쓴다. 담을 것 — 적용 일시와 대상(로컬/클라우드 각각); 5)에서 실제로 읽은 값들을 **관측값 그대로**(지어내지 말 것); `0002:76` 주석이 왜 여전히 거짓인 채 남는지(D-06); `§한계와 되돌리기` 절에 (a) `0008` 이하 번호가 이제 영구히 추가 불가라는 사실, (b) 되돌리려면 그 시점의 모든 임베딩을 재생성해야 하며 지금은 0건이라 비용이 0이었다는 사실, (c) CI 게이트가 소스 수준이고 psql 계약 러너는 로컬·수동이라는 D-08 이탈과 그 이유.

    관측을 지어내지 말 것 — 어느 단계라도 실패하면 기록에 실패를 남기고 멈춘다.
  </action>
  <verify>
    <automated>bash scripts/verify_search_contract.sh &amp;&amp; bash scripts/verify_queue_functions.sh &amp;&amp; uv run pytest -rs &amp;&amp; test -s docs/ops/migration-0008-record.md</automated>
    <human-check>클라우드 `supabase migration list` 출력에 `0008`이 원격 열까지 채워져 나타나고, 클라우드 쪽 `source_chunks.embedding` 타입이 1024차로 관측된다 (기록 문서의 값과 대조).</human-check>
  </verify>
  <acceptance_criteria>
    - `supabase db reset` 이 오류 없이 끝나고 `0001`~`0008` 이 전부 적용된다.
    - `bash scripts/verify_search_contract.sh` 가 exit 0으로 끝나고 출력에 `search_contract: ok` 가 있다.
    - `bash scripts/verify_queue_functions.sh` 가 exit 0으로 끝난다 (기존 계약 회귀 없음).
    - `uv run pytest -rs` 가 exit 0으로 끝난다.
    - `supabase migration list` 의 원격 열에 `0008` 이 나타난다.
    - `docs/ops/migration-0008-record.md` 가 존재하고 로컬·클라우드 양쪽의 관측값(컬럼 타입 · 함수 인자 타입 · 3개 롤의 EXECUTE 권한)을 담는다.
    - `docs/ops/migration-0008-record.md` 에 `한계` 를 제목에 포함한 절이 있다.
    - 커밋 메시지가 `feat(db): 0008 —` 로 시작한다.
  </acceptance_criteria>
  <done>로컬과 클라우드 양쪽이 1024차이고, 계약 러너 2종과 루트 스위트가 통과하며, 적용 기록이 남았다. Phase 3의 나머지 플랜이 이 위에 쌓일 수 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 개발 머신 → Supabase Cloud (`db push`) | 편도 스키마 변경이 운영 프로젝트를 직접 바꾼다 |
| PostgREST `/rpc/` → `public.search_chunks` | 로그인 사용자가 직접 호출하는 표면. 격리 수단은 `security invoker` + 요청자 JWT 하나뿐 |
| GitHub Actions 러너 → 저장소 | fork PR이 워크플로우를 실행한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `public.search_chunks` 재생성 | critical | mitigate | `security invoker` 를 `pg_proc.prosecdef = false` 로 단언한다 (`0008_search_contract.sql`). definer로 바뀌면 RLS가 우회되어 교차 테넌트 검색이 열린다 |
| T-03-02 | Elevation of Privilege | `drop function` 이 지운 ACL | high | mitigate | `0008` 안에서 `revoke ... from public, anon` + `grant execute ... to authenticated` 를 재실행하고, 계약 테스트가 `has_function_privilege` 로 세 롤을 전부 단언한다. 복원하지 않으면 Supabase 기본 실행 권한만 남아 `anon` 이 호출 가능해질 수 있다 |
| T-03-03 | Tampering | 부분 적용된 마이그레이션 | high | mitigate | `begin;`/`commit;` 단일 트랜잭션 (D-07). 컬럼만 1024이고 함수가 1536인 중간 상태를 구조적으로 만들 수 없다 |
| T-03-04 | Denial of Service | hnsw GUC 3종 유실 | medium | mitigate | `proconfig` 단언 + CI 소스 게이트. GUC가 빠지면 오류 없이 검색 재현율만 조용히 떨어진다 |
| T-03-05 | Repudiation | 클라우드 push 결과 미관측 | medium | mitigate | `docs/ops/migration-0008-record.md` 에 원격 관측값을 기록하고 `human-check` 로 대조한다 |
| T-03-06 | Information Disclosure | CI 워크플로우의 자격증명 참조 | low | accept | 새 잡은 어떤 `secrets.` 도 참조하지 않는다 — 수용 근거는 ci.yml 헤더의 기존 결정과 같다 |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 플랜은 패키지를 하나도 설치하지 않는다 — 설치 표면은 `03-03-PLAN` 이 소유하며 거기서 slopcheck + 차단형 사람 체크포인트를 거친다 |
</threat_model>

## Artifacts this phase produces (이 플랜 몫)

**SQL 객체**
- `public.search_chunks(uuid, extensions.vector, int)` — 재생성 (시그니처의 `p_query` 가 `extensions.vector(1024)`)
- `source_chunks_embedding_idx` · `wiki_embeddings_embedding_idx` — HNSW 인덱스 재생성 (이름·연산자 클래스 동일)
- `public.source_chunks.embedding` · `public.wiki_embeddings.embedding` — 타입 `extensions.vector(1024)`

**파일**
- `supabase/migrations/0008_embedding_dimension.sql`
- `supabase/tests/0008_search_contract.sql` (출력 토큰 `search_contract: ok`)
- `scripts/verify_search_contract.sh`
- `scripts/ci_check_search_contract.sh`
- `docs/ops/migration-0008-record.md`

**CI**
- `.github/workflows/ci.yml` 잡 `search-contract`

<verification>
- `supabase db reset` 후 `0001`~`0008` 전부 적용
- `scripts/verify_search_contract.sh` · `scripts/verify_queue_functions.sh` 둘 다 exit 0
- `scripts/ci_check_search_contract.sh` exit 0
- `uv run pytest -rs` exit 0
- `pre-commit run --all-files` exit 0
- `supabase migration list` 원격 열에 `0008`
</verification>

<success_criteria>
- 로컬과 클라우드 양쪽에서 두 임베딩 컬럼이 `extensions.vector(1024)`다
- `search_chunks` 가 1024차 시그니처로 존재하며 7개 계약(security invoker · stable · search_path · hnsw GUC 3종 · 연산자 수식 · warmup · ACL)이 전부 살아 있다
- `authenticated` 만 `search_chunks` 에 EXECUTE를 갖는다
- `docs/ops/migration-0008-record.md` 가 관측값과 한계를 기록한다
- Phase 3의 나머지 플랜이 1024차 전제 위에서 시작할 수 있다
</success_criteria>

<output>
Create `.planning/phases/03-ingest-and-compile-pipeline/03-01-SUMMARY.md` when done
</output>
