---
phase: 6
slug: dashboard
status: approved
shadcn_initialized: false
preset: none
created: 2026-08-12
reviewed_at: 2026-08-12
---

# Phase 6 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **No RESEARCH.md exists for this phase** (`/gsd-plan-phase 6 --skip-research`). Library/version
> choices below rely on the existing `apps/dashboard` scaffold (Next.js 15.5.22 · React 19.2.8 ·
> Tailwind 4.3.3 · TS strict · Vitest) and this researcher's own knowledge, not a research pass.
> Planner should sanity-check exact package versions at execution time.
>
> **Revision (2026-08-12):** gsd-ui-checker flagged two BLOCKER findings — Typography exceeding
> the 2-weight cap (400/500/600/700 in use) and two Spacing-scale values off the multiples-of-4
> grid (`xxs`=2px, `md`=12px). Both are fixed in place below by overriding the *usage* of the
> inherited token file down to a compliant contract (token file itself untouched — see the
> revision notes inline in **Typography** and **Spacing Scale**). Two non-blocking recommendations
> (primary visual anchor per screen, aria-label on icon-only touch targets) were also applied.

---

## Auto-Mode Notice

This UI-SPEC was produced under an explicit user instruction: *"recommend best practice for each,
and apply this"* — no interactive questions were asked. Every open design decision below was
auto-selected using the locked Airbnb-style token file (`docs/design-systems/design-tokens.css`)
and `06-CONTEXT.md` D-01~D-11 as the ground truth, with rationale noted inline at the point of
each decision so it stays auditable. Two decisions could not simply be "recommended" — they required
a project fact-check first — and are called out explicitly:

1. **shadcn was not initialized this phase.** The `shadcn init --preset {paste}` step requires the
   developer to visit `ui.shadcn.com/create` in a browser and paste back a preset string — a
   real interactive round-trip, not an open design question I can resolve by picking a "best
   practice" answer. Since this run cannot pause for that exchange, `Tool: none` is recorded below.
   Recommendation for the executor/team: run `npx shadcn init` manually before or during
   Phase 6 execution if headless Radix primitives (chosen below) turn out to be insufficient for a
   given component; this is a reversible, additive choice and does not block Phase 6 planning.
2. **The existing 18-token color palette has no positive/neutral-warning semantic color** — only
   the brand red spectrum (`--color-primary*`), one link blue, and two luxe/plus accents. QC-01
   (disputed) and QC-02 (verified/expires_at) require a "verified = good" signal the palette
   cannot express today. Rather than reinvent the palette, two minimally-scoped semantic text
   colors are proposed as an addition (see **Color** section) — not a redesign, a gap-fill for a
   state the existing system was never asked to express.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none this phase — see Auto-Mode Notice #1 |
| Preset | not applicable |
| Component library | Radix UI primitives (headless, unstyled) — `@radix-ui/react-dropdown-menu` (workspace switcher), `@radix-ui/react-tabs` (dropzone file/URL/text tabs, D-06), `@radix-ui/react-dialog` (destructive confirmations), `@radix-ui/react-tooltip` (truncated long-text), `@radix-ui/react-select` (invite role picker). Rationale: same accessible primitives shadcn itself wraps, without the interactive CLI preset step; styled directly against the existing token file per CONTEXT.md "reuse tokens, do not reinvent" |
| Icon library | `lucide-react` — tree-shakeable, React 19 compatible, the de facto pairing with Radix-based UIs |
| Font | `next/font/google` **Inter** (variable), assigned into the existing `--font-family-base` slot. Rationale: `docs/design-systems/design-tokens.css:75` specifies `'Airbnb Cereal VF', Circular, ...` — both are proprietary/licensed fonts (Airbnb-owned Cereal, Lineto-licensed Circular) not legally usable here. Inter is free, self-hostable via `next/font`, and matches the geometric-grotesque weight class Cereal occupies. Swapping to a licensed font later is a single `next/font` config change, not a token rewrite |
| Auth/session | `@supabase/ssr` (locked, D-02) |
| Graph rendering | `cytoscape` directly — **not** `react-cytoscapejs`, per `REQUIREMENTS.md` Out of Scope table (unmaintained since 2022, pre-React-18-StrictMode) |
| Elevation (inherited) | `--shadow-dropdown` (workspace switcher, role select), `--shadow-modal` (destructive confirm dialogs), `--shadow-card-hover` (source rows, wiki cards) — reused as-is from `design-tokens.css`, no new shadow tokens needed |
| Radius (inherited) | `--rounded-sm` (8px) buttons/inputs/tabs, `--rounded-md` (14px) cards (source rows, citation side-panel cards, wiki page container), `--rounded-full` badges (role pills, step-stepper dots, citation superscript markers) — reused as-is |
| State management | **Out of scope for this document** — `06-CONTEXT.md` explicitly reserves "정확한 컴포넌트 분해 ... 상태관리 라이브러리(또는 미사용) 선택은 planner 재량" (component decomposition and state-management library choice are the planner's call, not this contract's) |

---

## Spacing Scale

**Revision note:** the underlying token file (`docs/design-systems/design-tokens.css:60-69`) also
defines `--spacing-xxs: 2px` and `--spacing-md: 12px` — neither is a multiple of 4, so neither is
usable under this UI-SPEC's base-scale rule (standard set: 4, 8, 16, 24, 32, 48, 64). This contract
governs what Phase 6's components actually consume; the two off-scale CSS custom properties remain
defined in the file for other, non-Phase-6 consumers but are declared **out of use** here. The base
scale below is the multiples-of-4 subset only:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding, badge internal padding, citation superscript micro-kerning (was `xxs`/2px — moved up to the nearest on-scale rung, see below) |
| sm | 8px | Compact element spacing (stepper dot gaps, tab underline offset) |
| base | 16px | Default element spacing (form field gaps, card padding); list-row internal padding — source rows, member rows (was `md`/12px — moved up to the nearest on-scale rung, see below) |
| lg | 24px | Section padding (dropzone panel, wiki page margin) |
| xl | 32px | Layout gaps (sidebar-to-content gutter) |
| xxl | 48px | Major section breaks (auth page vertical rhythm) |
| section | 64px | Page-level spacing (top-level page padding on wide viewports) |

Resolved deviations from the inherited token file:
- **Citation superscript kerning:** was `--spacing-xxs` (2px), now **`xs` (4px)**. A 2px value is
  sub-perceptible as a spacing rung and was really acting as typographic kerning, not layout
  spacing — 4px reads cleanly at the marker's small type size without visually merging the marker
  into the preceding word.
- **List-row internal padding** (source rows, member rows): was `--spacing-md` (12px), now **`base`
  (16px)**. 16px is closer to Airbnb's original row-density intent than dropping to `sm` (8px)
  would be, and keeps every row on the project's touch-target-friendly grid.

Exceptions:
- **Icon-only touch targets: 44×44px minimum hit area** (WCAG 2.5.5), applied to the job-retry
  icon button (D-08), workspace-switcher chevron, and red-link create icon. Achieved via `base`
  (16px) padding around a 20–24px `lucide-react` glyph, not a new token. Each of these three
  targets **must** also carry an explicit `aria-label` (not icon-only visual affordance alone):
  job-retry → `aria-label="재시도"`; workspace-switcher chevron → `aria-label="워크스페이스 전환"`;
  red-link create icon → `aria-label="지금 생성"`.
- No other exceptions.

---

## Typography

Project has a much larger existing type scale (`design-tokens.css:73-100`, 16 named composite
tokens, spanning 4 distinct weights: 400/500/600/700). The 4 roles below are the subset Phase 6's
surfaces actually use.

**Revision note — explicit weight-cap deviation from the token file:** the UI-SPEC rule caps a
phase's typography contract at 2 declared weights. Lifting `--font-caption` (500) and
`--font-display-xl` (700) as-is would put 4 weights on screen at once, exceeding the cap. Rather
than silently keep 4 weights under an "inherited exception" label, this contract **overrides** the
weight component of those two composite tokens down to the nearest of the 2 weights this phase
commits to, while keeping their size/line-height unchanged. This is a deliberate, labeled deviation
from the token file's literal weight values — not a redesign of the file, and not a silent breach
of the 2-weight cap:

| Role | Size | Weight (used) | Line Height | Source token | Deviation |
|------|------|----------------|-------------|---------------|-----------|
| Body | 16px | **400** | 1.5 | `--font-body-md` — wiki article body, ask chat bubbles, form inputs | none — token used as-is |
| Label | 14px | **600** | 1.29 | size/line-height from `--font-caption`, weight overridden | 500 → 600 — role badges, source-row meta, stepper step labels, table headers need enough visual weight to read as labels, not body text |
| Heading | 16px | **600** | 1.25 | `--font-title-md` — card titles, section headers, member-row name | none — token used as-is |
| Display | 28px | **600** | 1.43 | size/line-height from `--font-display-xl`, weight overridden | 700 → 600 — page-level titles only (wiki page H1, "NexusWiki" wordmark); 600 already reads as a clear display weight against 400 body text at this size, so 700 buys no additional hierarchy this phase needs |

Weights actually rendered by Phase 6: exactly **2** — **400 (regular)** for Body, **600
(semibold)** for Label/Heading/Display. The token file itself is untouched and keeps its full
4-weight palette for other phases/consumers; this contract only constrains what Phase 6's
components are permitted to request.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `--color-canvas` `#ffffff` | Page background, all surfaces default to white |
| Secondary (30%) | `--color-surface-soft` `#f7f7f7` + `--color-hairline` `#dddddd` | Dropzone panel background, sidebar/nav background, member-row/source-row dividers, wiki-viewer read-only banner background |
| Accent (10%) | `--color-primary` `#ff385c` | **Reserved for:** primary CTA buttons ("소스 등록", "질문하기", "초대 보내기"), the active workspace indicator in the switcher dropdown, citation superscript markers on hover/active, the current step in the job stepper (D-05), red-link "지금 생성" CTA text/icon |
| Destructive | `--color-primary-error-text` `#c13515` | **Reserved for:** job-cancel confirmation, member-removal confirmation, destructive dialog primary action. Deliberately **not** `--color-primary` — Airbnb's brand red is already the primary-CTA accent, so reusing it for destructive actions would make "add source" and "cancel job / remove member" visually indistinguishable. `--color-primary-error-text` is the existing muted-but-legible error red already reserved for inline form errors, giving destructive actions a distinct, lower-saturation red |

**Semantic addition — verification/dispute states (QC-01, QC-02, gap-fill, see Auto-Mode Notice #2):**

| Role | Value | Usage |
|------|-------|-------|
| Success text (new) | `#0a7d34` | `verification_status = 'verified'` callout icon/text (not yet expired) |
| Warning text (new) | `#8a5300` | `verification_status = 'partial'`, or `verified` **and** `expires_at` has passed ("만료됨") |
| Disputed (reuse) | `--color-primary-error-text` `#c13515` | `verification_status = 'disputed'` **or** `disputed = true` — same value as Destructive, semantically consistent ("conflicting information" is error-adjacent) |
| Unverified (reuse) | `--color-muted` `#6a6a6a` | `verification_status = 'unverified'` (default state) — neutral, not alarming |

Both new hex values are chosen at the same darkness/saturation level as the already-approved
`--color-primary-error-text` (`#c13515`) — dark, desaturated enough to clear AA text contrast on
white, consistent with the existing token file's approach to status text rather than status fills.
Executor should add these two as `--color-success-text` / `--color-warning-text` custom properties
in `design-tokens.css` during the `globals.css` `@theme` integration task already marked at
`apps/dashboard/app/globals.css:8`.

**`confidence` field (`high`/`medium`/`low`) is explicitly NOT rendered as a badge anywhere in
Phase 6** — per `REQUIREMENTS.md` Out of Scope: "수치 confidence 배지 노출" (LLM self-confidence is
uncalibrated and competes visually with the verification badge). Do not add a confidence chip.

---

## Primary Visual Anchor (per screen)

Each screen has exactly one element that should draw the eye first — the accent color (10%),
largest/heaviest type, and layout position all reinforce this element rather than competing:

| Screen | Anchor | Why |
|--------|--------|-----|
| Auth (UI-01) | Login submit button | Single-purpose screen; button is the only accent-colored element on the page |
| Workspace/Invite (UI-02) | "초대 보내기" primary CTA | Everything else (switcher, member rows) is neutral-colored chrome; invite is the accent-colored action |
| Dropzone (UI-03) | "소스 등록" submit button (pre-submit) → job stepper (post-submit) | The anchor changes with state: before submit, the CTA is the only accent element; after submit, ING-06's no-spinner rule makes the stepper the thing actively communicating progress |
| Ask UI (UI-04) | Ask input field | Empty-state heading draws the eye first, but the input is what the user must act on — kept full-width, high-contrast border, with the accent-colored send affordance attached to it |
| Wiki viewer (UI-05) | Wiki page H1 (Display, 28px/600) | The largest, heaviest type on the page; citation markers and verification callouts are secondary to the content itself |
| Graph canvas (UI-06) | The graph canvas viewport | Full-bleed Cytoscape render is the entire point of the screen; the lens filter is chrome around it, not the anchor |

---

## Copywriting Contract

Primary table (dominant flow — source ingestion, ING-06's explicit non-spinner requirement):

| Element | Copy |
|---------|------|
| Primary CTA | "소스 등록" (register source — dropzone submit) |
| Empty state heading | "아직 등록된 소스가 없습니다" |
| Empty state body | "파일을 드래그하거나 URL/텍스트를 붙여넣어 첫 소스를 추가하세요." |
| Error state | "{단계} 단계에서 실패했습니다 — {last_error 요약}. 재시도를 눌러 다시 시도하세요." |
| Destructive confirmation | 잡 취소: "진행 중인 작업을 취소하시겠습니까? 이미 사용된 비용은 환불되지 않습니다." |

**Per-screen copy reference** (full traceability across UI-01…UI-06; all copy in Korean per
project-wide convention):

| Screen | Element | Copy |
|--------|---------|------|
| Auth (UI-01) | Login error | "이메일 또는 비밀번호가 올바르지 않습니다." |
| Auth (UI-01) | Password rule hint | "비밀번호는 12자 이상이어야 합니다." (mirrors `01-CONTEXT.md` D-hardening: min 12자) |
| Workspace switcher (UI-02) | Nav CTA | Dropdown item = workspace name; selecting navigates to `/w/[workspaceId]` (D-03) |
| Invite form (UI-02) | Primary CTA | "초대 보내기" |
| Invite form (UI-02) | Duplicate-member error | "이미 워크스페이스 멤버입니다." |
| Invite form (UI-02) | Role field default | Defaults to **viewer** (least privilege) when unselected — submit stays disabled only on invalid email format, not on role choice |
| Dropzone (UI-03) | Job stepper labels (verbatim, D-05) | "업로드 → 파싱 → 컴파일 → 링크 동기화 → 임베딩" |
| Dropzone (UI-03) | Dedup banner (verbatim, D-07) | "이미 수집됨 — 건너뜀" |
| Dropzone (UI-03) | Retry action label (D-08) | "재시도" |
| Dropzone (UI-03) | `needs_ocr` failure | "텍스트를 추출하지 못했습니다 (스캔본으로 보임) — OCR이 적용된 파일로 다시 업로드해주세요." |
| Dropzone (UI-03) | Cost-cap 402 rejection | "이번 달 워크스페이스 사용량 한도를 초과했습니다. 관리자에게 문의하거나 다음 달까지 기다려주세요." |
| Ask UI (UI-04) | Primary CTA | "질문하기" |
| Ask UI (UI-04) | Empty/initial heading | "무엇이든 물어보세요" |
| Ask UI (UI-04) | Empty/initial body | "워크스페이스에 등록된 소스와 위키에서 답을 찾아드립니다." |
| Ask UI (UI-04) | No-evidence card (verbatim, CITE-04/D-11) | "근거를 찾지 못했습니다." — rendered as a visually distinct warning card, not a chat bubble |
| Ask UI (UI-04) | Missing-channel notice | "일부 검색 채널을 사용할 수 없어 답변이 불완전할 수 있습니다." (small inline meta notice when `meta`'s missing-channel list is non-empty, RTV-09) |
| Ask UI (UI-04) | Stream-drop error | "연결이 끊어졌습니다. 다시 시도해주세요." with a retry affordance on the incomplete message |
| Wiki viewer (UI-05) | Read-only banner (core phrase verbatim) | "이 페이지는 컴파일됩니다 — 직접 편집할 수 없으며, 소스가 갱신되면 다시 컴파일됩니다." |
| Wiki viewer (UI-05) | Red-link CTA (verbatim) | "아직 작성되지 않음 · 지금 생성" |
| Wiki viewer (UI-05) | Verified callout | "검증됨 · {verifier} · {verified_at 날짜}" |
| Wiki viewer (UI-05) | Expired-verification callout | "검증 만료됨 · {expires_at 날짜} 이후 재검증 필요" |
| Wiki viewer (UI-05) | Disputed callout | "충돌 감지됨 — 상충하는 정보가 있습니다. 원문을 확인하세요." |
| Wiki viewer (UI-05) | Empty wiki (no pages yet) | "아직 컴파일된 위키 페이지가 없습니다" / "소스를 추가하면 자동으로 위키 페이지가 생성됩니다." |
| Graph canvas (UI-06) | Empty (filtered to nothing) | "표시할 노드가 없습니다" / "카테고리 필터를 초기화하거나 다른 워크스페이스를 선택하세요." |
| Graph canvas (UI-06) | 1000-row cap notice (visible, not silent — ROADMAP success criterion 5) | "이 워크스페이스는 그래프 표시 한도(1,000개 노드)를 초과했습니다 — 카테고리 필터로 범위를 좁혀주세요." |
| Graph canvas (UI-06) | Load failure | "그래프를 불러오지 못했습니다. 새로고침해주세요." |
| Members (UI-02) | Removal confirmation | "제거: {name}님을 이 워크스페이스에서 제거하시겠습니까? 소유한 콘텐츠는 유지되지만 접근 권한이 즉시 사라집니다." |

---

## UI Considerations

Applicable state considerations resolved: 26 covered, 7 backstop, 0 unresolved.

Elements classified: `login-form`(form) · `workspace-switcher`(nav) · `invite-form`(form) ·
`members-list`(list-collection) · `dropzone`(form) · `sources-job-list`(list-collection) ·
`job-stepper`(interactive-control) · `ask-conversation`(list-collection) ·
`citation-marker`(interactive-control) · `citation-side-panel`(static-content) ·
`wiki-page-content`(static-content) · `red-link-cta`(interactive-control) ·
`graph-canvas`(media) · `graph-lens-filter`(interactive-control).

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| empty | dropzone | ✅ covered | D-06 tabs render their own instructions by default; no separate empty state needed |
| empty | members-list | ✅ covered | `workspaces_add_owner_member` trigger guarantees ≥1 member always — zero-member state is structurally unreachable |
| empty | sources-job-list | ✅ covered | Copy row "Empty state heading/body" above |
| empty | ask-conversation | ✅ covered | Copy row "Empty/initial heading/body" above |
| empty | graph-canvas | ✅ covered | Copy row "Empty (filtered to nothing)" above |
| loading | dropzone | ✅ covered | Submit is replaced immediately by the D-05 stepper — ING-06 forbids indefinite spinners by design |
| loading | sources-job-list | 🧪 backstop | Recommend skeleton rows on initial fetch; needs a wired loading-state test |
| loading | ask-conversation | ✅ covered | D-09 defines the gray placeholder-marker streaming behavior explicitly |
| loading | graph-canvas | 🧪 backstop | Recommend a centered spinner/skeleton before Cytoscape layout settles; not decided in CONTEXT |
| loading | workspace-switcher | 🧪 backstop | Recommend disabling the dropdown item + inline spinner during `/w/[id]` route revalidation |
| error | login-form | ✅ covered | Copy row "Login error" above |
| error | invite-form | ✅ covered | Copy row "Duplicate-member error" above |
| error | dropzone / job-stepper | ✅ covered | Copy rows "needs_ocr failure" / "Cost-cap 402" / generic step-error template above |
| error | ask-conversation | ✅ covered | Copy row "Stream-drop error" above |
| error | graph-canvas | ✅ covered | Copy rows "Load failure" / "1000-row cap notice" above |
| populated | members-list | ✅ covered | Standard row list, role badge per member |
| populated | sources-job-list | ✅ covered | Standard row list, one stepper per source |
| populated | ask-conversation | ✅ covered | D-09/D-10 fully specify marker + side-panel behavior |
| populated | graph-canvas | ✅ covered | Cytoscape render with lens filter (UI-06 discretion resolution) |
| partial | invite-form | ✅ covered | Role field default = viewer resolves the partial-fill ambiguity (copy table above) |
| partial | sources-job-list | ✅ covered | Mixed per-row stepper states are the expected normal condition, not a special case |
| overflow | wiki-page-content | ✅ covered | Full-length compiled article, natural scroll — no truncation (this is the "read the whole page" surface) |
| overflow | sources-job-list | 🧪 backstop | Recommend pagination/virtualization past ~50 rows; threshold not decided, needs a wired test |
| overflow | graph-canvas | ✅ covered | This **is** UI-06's core requirement — PostgREST 1000-row cap handled via visible notice + category-filter narrowing, not silent truncation |
| overflow | workspace-switcher | 🧪 backstop | Recommend a scrollable dropdown (+ search input past ~8 workspaces); not decided in CONTEXT |
| zero-one-many | members-list / sources-job-list / ask-conversation / graph-canvas | ✅ covered | Korean has no plural inflection, so no singular/plural copy branching is needed; zero handled by empty-state rows above, one/many share the same row layout |
| long-text | wiki-page-content | ✅ covered | Natural reflow, no truncation (full article context) |
| long-text | citation-side-panel | ✅ covered | Source/wiki excerpt cards get a fixed max-height (~400px) with internal scroll; the highlighted `char_start`/`char_end` span auto-scrolls into view on open |
| long-text | workspace-switcher | 🧪 backstop | Recommend CSS `truncate` + native `title` tooltip for long workspace names; not decided in CONTEXT |
| long-text | red-link-cta | 🧪 backstop | Recommend 2-line clamp with ellipsis for long red-link page titles; not decided in CONTEXT |

<!-- Status vocabulary (locked by probe-core projectTruths):
     ✅ covered   → a plain truth string lifted into must_haves.truths
     🧪 backstop  → a flat scalar { statement, verification: backstop }; at verify time, no explicit
                    evidence → insufficient_spec → human_needed (never a silent pass, #1154)
     ⚠ unresolved → an explicit planner assumption (surfaced, never silently dropped)
     Rows are REPLACED (not appended) on a probe re-run — idempotent. -->

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none this phase — Tool: none (see Auto-Mode Notice #1) | not required |
| third-party | none | not applicable |

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS (revision: anchor table + aria-labels added)
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS (revision: 400/500/600/700 → 400/600)
- [x] Dimension 5 Spacing: PASS (revision: xxs/md off-grid values removed)
- [x] Dimension 6 Registry Safety: PASS

**Approval:** approved — 2026-08-12 (gsd-ui-checker, after one revision pass — see Revision note in header)
