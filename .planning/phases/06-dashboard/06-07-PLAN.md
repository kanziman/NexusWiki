---
phase: 06-dashboard
plan: 07
type: execute
wave: 3
depends_on: ["06-01", "06-04"]
files_modified:
  - apps/dashboard/app/w/[workspaceId]/wiki/[slug]/page.tsx
  - apps/dashboard/app/w/[workspaceId]/wiki/page.tsx
  - apps/dashboard/components/WikiPageContent.tsx
  - apps/dashboard/components/RedLinkCta.tsx
  - apps/dashboard/lib/wiki-links.ts
  - apps/dashboard/tests/wiki-links.test.ts
  - apps/dashboard/tests/RedLinkCta.test.tsx
autonomous: true
requirements: [UI-05]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The wiki page renders the read-only banner verbatim: '이 페이지는 컴파일됩니다 — 직접 편집할 수 없으며, 소스가 갱신되면 다시 컴파일됩니다.'"
    - "A resolved [[WikiLink]] navigates to /w/[workspaceId]/wiki/[slug]; an unresolved one renders RedLinkCta with the exact copy '아직 작성되지 않음 · 지금 생성' and aria-label='지금 생성' on its icon"
    - "Verification status renders one of four distinct callouts (verified/expired/disputed/unverified) using the copy templates from 06-UI-SPEC.md, colored via --color-success-text/--color-warning-text/--color-primary-error-text/--color-muted"
    - "The empty-wiki state ('아직 컴파일된 위키 페이지가 없습니다' / '소스를 추가하면 자동으로 위키 페이지가 생성됩니다.') renders when the workspace has zero wiki_pages rows"
    - { statement: "wiki page content reflows naturally with no truncation regardless of length (this IS the read-the-whole-page surface)", verification: backstop }
    - { statement: "long red-link titles clamp to 2 lines with ellipsis (RedLinkCta.tsx, UI-SPEC UI Considerations: red-link-cta/long-text)", verification: backstop }
  artifacts:
    - apps/dashboard/lib/wiki-links.ts
    - apps/dashboard/components/WikiPageContent.tsx
    - apps/dashboard/components/RedLinkCta.tsx
  key_links:
    - "WikiPageContent.tsx -> lib/wiki-links.ts resolveWikiLinks(content, wiki_links rows) -> RedLinkCta.tsx (unresolved) or next/link (resolved)"
    - "WikiPageContent.tsx verify action -> apiFetch(PATCH /workspaces/{id}/wiki/{wiki_id}/verify) -> wiki.py (QC-02)"
    - "RedLinkCta.tsx onClick -> router.push(/w/[id]/sources?prefillTitle=<title>&tab=text) — creates a NEW SOURCE, never edits wiki_pages.content directly (v1 has no free-text page editing)"
  prohibitions:
    - requirement_id: UI-05
      category: values
      status: resolved
      verification: test
      resolution: "WikiPageContent.tsx renders wiki_pages.content as static text/React nodes only — no contentEditable region, no save-to-wiki_pages control anywhere in this component tree. Enforced by this plan's task 2 acceptance criteria (grep for contentEditable and for any PATCH/PUT targeting wiki_pages.content)."
      reason: null
      statement: "MUST NOT render any control that implies direct inline editing of wiki_pages.content (no contentEditable, no 'save changes' affordance) — v1's read-only promise is structural (compile-time upsert on (workspace_id, slug)), not a UI convention; a free-text edit path would silently reintroduce the exact conflict REQUIREMENTS.md's Out-of-Scope table already rejected"
    - requirement_id: UI-05
      category: safety
      status: resolved
      verification: judgment
      resolution: "The disputed/conflict callout renders above WikiPageContent's body content in DOM order (not below or in a collapsed/secondary region) so a user cannot scroll past it unseen — enforced by this plan's task 2 layout requirement."
      reason: null
      statement: "MUST NOT visually subordinate the disputed/conflict callout below or beneath the page body — a user must see the 'conflicting information' warning before reading potentially-contested content, not after"

assumptions:
  - "UI-05's requirement-level edge coverage came back 'unclassified' from the deterministic probe — the ROADMAP success-criterion text and 06-CONTEXT.md's Claude's Discretion constraints (read-only banner, WikiLink nav, red-link CTA, verification callouts) are the acceptance bar."
  - "wiki-links.ts's baseSlug() ports only nexuswiki_core.slug's _base_slug (the collision-free base transform), not the full slugify() with its taken-set collision-suffix resolution — a rare title that collided server-side into a `-2` suffixed slug will not match via client-side baseSlug() recomputation and will render as an unresolved/red link even though a page with that content exists. This is a documented, safe-failure edge case (worst case: a working link shows a redundant create-CTA), not a silent data-loss bug — see wiki-links.ts's own header comment."
  - "'검증됨으로 표시' (mark-as-verified) is included as an editor-only action even though UI-05's literal requirement text only asks for read-only status DISPLAY, not an edit control — QC-02's transition API exists specifically for this and leaving it completely unreachable from the UI would be a dangling backend capability; this is a Claude's-Discretion addition, not a locked decision, kept minimal (one PATCH call, no new state machine UI)."
---

<objective>
Deliver UI-05: a read-only wiki page viewer that renders `[[WikiLink]]` navigation (resolved and
red), the four verification-status callouts, and the required read-only banner — never an editing
surface, since v1's `(workspace_id, slug)` upsert model structurally conflicts with free-text
editing (REQUIREMENTS.md Out of Scope).

Purpose: make the compiled wiki actually navigable and trustworthy to read.
Output: `lib/wiki-links.ts`, `WikiPageContent.tsx`, `RedLinkCta.tsx`, the wiki route(s).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-dashboard/06-CONTEXT.md
@.planning/phases/06-dashboard/06-UI-SPEC.md
@apps/api/src/api/routers/wiki.py
@apps/worker/src/worker/handlers/link_sync.py
@packages/core/src/nexuswiki_core/slug.py
@packages/core/src/nexuswiki_core/tokenizer.py
@apps/dashboard/lib/api-client.ts
@apps/dashboard/lib/supabase/client.ts
@supabase/migrations/0002_search_schema.sql (wiki_links table)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/wiki-links.ts — slug port + link resolution (pure)</name>
  <files>apps/dashboard/lib/wiki-links.ts, apps/dashboard/tests/wiki-links.test.ts</files>
  <behavior>
    - `baseSlug("한국어 제목")` produces a hyphenated, lowercase, `[0-9a-z가-힣-]`-only slug
      matching the same transform `nexuswiki_core.slug._base_slug` applies (NFKC normalize,
      space->hyphen, filter disallowed chars, collapse hyphen runs, trim, truncate to 80 chars)
      — verified against 2-3 known Python outputs recorded as fixtures in this task's test file
      (a Korean title, an English title with punctuation, a mixed title).
    - `baseSlug("")` (or all-disallowed-chars input) returns a non-empty deterministic
      fallback string (does not attempt to replicate the Python SHA-256 fallback exactly —
      documented as an accepted approximation, since this path is only reachable for
      essentially-empty titles which should not occur in real compiled content).
    - `resolveWikiLinks("본문 [[주제 A]] 계속 [[주제 B]] 끝", links)` where `links` is the
      `wiki_links` rows for this page (`{target_slug, to_wiki_id, resolved}[]`) returns an
      ordered array of text/link parts; a `[[Title]]` whose `baseSlug(Title)` matches a
      `resolved: true` row's `target_slug` becomes a resolved-link part carrying that row's
      slug; a `[[Title]]` matching a `resolved: false` row (or matching nothing) becomes an
      unresolved/red-link part.
  </behavior>
  <read_first>
    packages/core/src/nexuswiki_core/slug.py (full file, already read this session — port
    `_base_slug` exactly: NFKC -> replace spaces with hyphens -> filter
    `[^0-9a-z가-힣-]` -> collapse `-{2,}` -> trim hyphens -> truncate 80),
    packages/core/src/nexuswiki_core/tokenizer.py `normalize()` (NFKC -> casefold -> NFKC ->
    whitespace collapse — JS approximation: `.normalize("NFKC").toLowerCase().normalize("NFKC")`
    + whitespace collapse; document that JS `toLowerCase()` is not byte-identical to Python
    `casefold()` for all Unicode edge cases, an accepted approximation for this display-only
    purpose), apps/worker/src/worker/handlers/link_sync.py (the exact regex `\[\[([^\]]+)\]\]`
    this task's content-scanning regex must match — note it is NOT the same pattern as the
    citation-anchor regex in `06-06-PLAN.md`'s `lib/citation-anchors.ts`; these are two
    unrelated `[[...]]` grammars in this codebase and must not be unified).
  </behavior>
  <action>
    Create `apps/dashboard/lib/wiki-links.ts`: export `function baseSlug(title: string):
    string` porting `_base_slug` per the read_first notes above (header comment documenting the
    known collision-suffix and casefold-approximation limitations, per this plan's
    `assumptions`). Export `type WikiLinkPart = { type: "text"; value: string } | { type: "link";
    title: string; resolved: boolean; slug: string }`. Export `function resolveWikiLinks(content:
    string, links: { target_slug: string; resolved: boolean }[]): WikiLinkPart[]` — scan
    `content` with `/\[\[([^\]]+)\]\]/g`, for each match compute `const slug =
    baseSlug(matchText)`, look up `links.find(l => l.target_slug === slug)`, emit a `link` part
    (`resolved` from the found row, or `resolved: false` with `slug` set to the computed
    `baseSlug` value if no row matched at all — the red-link CTA still needs SOME slug-shaped
    identifier for its create-source prefill even when `link_sync` hasn't run yet).
  </action>
  <verify>
    <automated>pnpm --filter @nexuswiki/dashboard exec vitest run tests/wiki-links.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 3 behavior-spec cases have a corresponding test, including the 2-3 known-Python-output
      fixtures for `baseSlug`.
    - `resolveWikiLinks` on content with zero `[[...]]` occurrences returns a single text part
      equal to the input.
  </acceptance_criteria>
  <done>baseSlug() and resolveWikiLinks() correctly mirror the server's slug/link-resolution semantics for the common case, with documented, safe-failure limitations for the rare collision-suffix case.</done>
</task>

<task type="auto">
  <name>Task 2: WikiPageContent.tsx (read-only banner + verification callouts + verify action)</name>
  <files>apps/dashboard/components/WikiPageContent.tsx</files>
  <read_first>
    apps/api/src/api/routers/wiki.py (full file, already read this session — the PATCH
    verify endpoint's exact request/response shape), 06-UI-SPEC.md Color section (the 4
    verification-state color mapping: success-text/warning-text/primary-error-text/muted) and
    Copywriting Contract rows under "Wiki viewer (UI-05)".
  </read_first>
  <action>
    Create `apps/dashboard/components/WikiPageContent.tsx` ("use client", named export
    `WikiPageContent`, `WikiPageContentProps = { page: { id: string; title: string; content:
    string; verification_status: string; verified_by: string | null; verified_at: string |
    null; expires_at: string | null; disputed: boolean }; links: { target_slug: string;
    resolved: boolean }[]; workspaceId: string; canVerify: boolean }`).

    Render order (top to bottom, per this plan's disputed-prominence prohibition): (1) the
    read-only banner "이 페이지는 컴파일됩니다 — 직접 편집할 수 없으며, 소스가 갱신되면
    다시 컴파일됩니다." (`--color-surface-soft` background, per UI-SPEC Color/Secondary row);
    (2) IF `disputed` is true, the disputed callout "충돌 감지됨 — 상충하는 정보가
    있습니다. 원문을 확인하세요." in `--color-primary-error-text`, positioned here — above the
    body — regardless of `verification_status`; (3) ELSE the verification callout matching
    `verification_status`: `"verified"` and `expires_at` in the future ->
    "검증됨 · {verified_by} · {verified_at 날짜}" in `--color-success-text`; `"verified"` and
    `expires_at` in the past -> "검증 만료됨 · {expires_at 날짜} 이후 재검증 필요" in
    `--color-warning-text`; `"partial"` -> render with `--color-warning-text` (no dedicated
    copy row exists for `partial` in UI-SPEC — use the same expired-style treatment since both
    signal "needs attention", documented here as this component's own reasonable extension);
    `"unverified"` -> no callout at all (UI-SPEC calls this the neutral default, not something
    requiring an explicit line); (4) the Display-weight title (28px/600, per UI-SPEC
    Typography); (5) IF `canVerify` (editor+ role, passed from the page's server-side role
    check), a small "검증됨으로 표시" button calling `apiFetch(PATCH
    /workspaces/${workspaceId}/wiki/${page.id}/verify, {method:"PATCH", body:
    {verification_status:"verified"}})`; (6) the body: `resolveWikiLinks(page.content,
    links).map(...)` — text parts render as plain text/paragraphs, resolved link parts render
    as `next/link` to `/w/${workspaceId}/wiki/${part.slug}`, unresolved link parts render
    `<RedLinkCta title={part.title} slug={part.slug} workspaceId={workspaceId} />` (Task 3).
    No `contentEditable` anywhere in this file, no control that writes to `page.content` — the
    ONLY write path in this component is the verify-status PATCH above.
  </action>
  <verify>
    <automated>pnpm --filter @nexuswiki/dashboard exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "contentEditable" apps/dashboard/components/WikiPageContent.tsx` returns 0.
    - `grep -c "wiki_pages.*content\|content.*wiki_pages" apps/dashboard/components/WikiPageContent.tsx | grep -v "^0$"` — no write call targets `wiki_pages.content` (the only mutation in this file is the verify PATCH, which never touches `content`).
    - Source order: the disputed-callout JSX block appears before the body-rendering block in
      the file (structural check — DOM order follows source order for this component).
  </acceptance_criteria>
  <done>WikiPageContent renders the read-only banner, correctly-prioritized verification/disputed callouts, and navigable WikiLinks, with zero inline-edit surface — proven by the grep-based structural checks above.</done>
</task>

<task type="auto">
  <name>Task 3: RedLinkCta.tsx + wiki route pages</name>
  <files>apps/dashboard/components/RedLinkCta.tsx, apps/dashboard/app/w/[workspaceId]/wiki/[slug]/page.tsx, apps/dashboard/app/w/[workspaceId]/wiki/page.tsx, apps/dashboard/tests/RedLinkCta.test.tsx</files>
  <read_first>
    06-UI-SPEC.md "UI Considerations" row "long-text | red-link-cta" (2-line clamp + ellipsis
    for long titles — backstop).
  </read_first>
  <action>
    Create `apps/dashboard/components/RedLinkCta.tsx` ("use client", named export
    `RedLinkCta`, `RedLinkCtaProps = { title: string; slug: string; workspaceId: string }`):
    renders the title text (2-line `-webkit-line-clamp` for long titles, per the backstop) plus
    the exact copy "아직 작성되지 않음 · 지금 생성" and a create icon
    (`aria-label="지금 생성"`, 44x44px hit target, `--color-primary` per UI-SPEC). `onClick`
    navigates via `useRouter().push` to
    `${workspacePath(workspaceId)}/sources?prefillTitle=${encodeURIComponent(title)}&tab=text`
    — this creates a NEW SOURCE (via the text tab, `06-05-PLAN.md`'s `Dropzone.tsx`) that will
    compile into a page with this title, it never attempts to create/edit a `wiki_pages` row
    directly (v1 has no such write path, per the Out-of-Scope constraint).

    Create `apps/dashboard/app/w/[workspaceId]/wiki/[slug]/page.tsx`: Server Component, direct
    reads `supabase.from("wiki_pages").select(
    "id,title,content,verification_status,verified_by,verified_at,expires_at,disputed").eq(
    "workspace_id", workspaceId).eq("slug", slug).single()` and
    `supabase.from("wiki_links").select("target_slug,resolved").eq("from_wiki_id", page.id)`
    (only when the page lookup succeeded); if the page lookup returns null/error, render a
    same-tenant "페이지를 찾을 수 없습니다" state (this is NOT the cross-tenant D-12
    no-enumeration case — the slug is already scoped to the caller's own workspace by the
    `.eq("workspace_id", workspaceId)` filter, so distinguishing "wrong slug" from anything else
    here leaks nothing). Determine `canVerify` server-side via
    `supabase.rpc("has_workspace_role", {ws_id: workspaceId, min_role: "editor"})` if such an
    RPC is exposed by existing migrations (check `0004_rls_policies.sql`'s helper-function grants
    before relying on this — if `has_workspace_role` is not directly callable via PostgREST RPC
    for this purpose, fall back to reading the caller's own `workspace_members.role` row
    directly via `.eq("workspace_id", workspaceId).eq("user_id", user.id).single()` and compare
    client-side against `["owner","editor"]`, which is always available since
    `workspace_members_select_member` already permits a member to read their own row). Render
    `<WikiPageContent page={...} links={...} workspaceId={workspaceId} canVerify={...} />`.

    Create `apps/dashboard/app/w/[workspaceId]/wiki/page.tsx` (the wiki index `06-02-PLAN.md`'s
    nav links to): direct-reads `supabase.from("wiki_pages").select(
    "id,slug,title,category").eq("workspace_id", workspaceId).order("title")`; empty state
    "아직 컴파일된 위키 페이지가 없습니다" / "소스를 추가하면 자동으로 위키 페이지가
    생성됩니다." when zero rows; otherwise a simple linked list to each page's
    `/w/[workspaceId]/wiki/[slug]`.
  </action>
  <verify>
    <automated>pnpm --filter @nexuswiki/dashboard exec vitest run tests/RedLinkCta.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - RedLinkCta renders the exact copy and calls `router.push` with a path containing
      `/sources?prefillTitle=` and the URL-encoded title.
    - `next build` succeeds for both wiki route files.
  </acceptance_criteria>
  <done>The wiki index and per-page routes are wired end to end: browsing lists pages, opening one shows content with correct WikiLink navigation and verification state, and red links route to source creation, never to a nonexistent page-edit form.</done>
</task>

</tasks>

## Artifacts this phase produces

- `apps/dashboard/lib/wiki-links.ts` — `baseSlug(title)`, `resolveWikiLinks(content, links)`, `WikiLinkPart` type
- `apps/dashboard/components/WikiPageContent.tsx` — `WikiPageContent` component, `WikiPageContentProps` type
- `apps/dashboard/components/RedLinkCta.tsx` — `RedLinkCta` component, `RedLinkCtaProps` type
- `apps/dashboard/app/w/[workspaceId]/wiki/[slug]/page.tsx` — route `/w/[workspaceId]/wiki/[slug]`
- `apps/dashboard/app/w/[workspaceId]/wiki/page.tsx` — route `/w/[workspaceId]/wiki` (index)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC -> PostgREST (wiki_pages/wiki_links by workspace) | Direct RLS-scoped reads |
| Browser -> apps/api PATCH verify | JWT-per-request via apiFetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-21 | Elevation of Privilege | verify action visibility | medium | mitigate | `canVerify` is computed server-side from the caller's actual `workspace_members.role` row (or an RLS-backed RPC), not a client-trusted prop passed from an untrusted source; even if a viewer forced the button to render client-side, the PATCH itself is still governed by `wiki.py`'s own RLS-backed write path (editor+ only) server-side — this component's gate is UX convenience, not the security boundary |
| T-06-22 | Tampering | WikiPageContent | high | mitigate | No inline-edit surface exists at all (must_haves.prohibitions) — removes the class of bug entirely rather than relying on a permission check to prevent misuse of an edit control that shouldn't exist |
| T-06-23 | Spoofing | RedLinkCta navigation target | low | accept | The prefilled title is URL-encoded but user-controlled (it originated from LLM-compiled wiki content); it only ever pre-fills a form field on the sources page, never executes or is trusted as anything beyond a text default a human still reviews before submitting |
</threat_model>

<verification>
Vitest suites pass for `wiki-links.ts` and `RedLinkCta.tsx`; `tsc --noEmit` and `next build`
succeed; grep-based structural checks confirm zero inline-edit surface; manual click-through
confirms WikiLink navigation (resolved and red) and all 4 verification-callout states render
correctly against seeded Phase 3-5 wiki data.
</verification>

<success_criteria>
UI-05 fully satisfied: read-only wiki viewer with working WikiLink navigation, red-link CTA
routed to source creation (not page editing), and correctly-prioritized verification/disputed
callouts.
</success_criteria>

<output>
Create `.planning/phases/06-dashboard/06-07-SUMMARY.md` when done
</output>
