---
phase: 06-dashboard
plan: 05
type: execute
wave: 3
depends_on: ["06-01", "06-04"]
files_modified:
  - apps/dashboard/app/w/[workspaceId]/sources/page.tsx
  - apps/dashboard/components/Dropzone.tsx
  - apps/dashboard/components/JobStepper.tsx
  - apps/dashboard/tests/Dropzone.test.tsx
  - apps/dashboard/tests/JobStepper.test.tsx
autonomous: true
requirements: [UI-03]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Job progress renders as the real stage stepper 업로드 → 파싱 → 컴파일 → 링크 동기화 → 임베딩 (D-05, verbatim) — never an indeterminate spinner"
    - "Submitting a source whose content_hash already exists (409 already_ingested) shows the exact banner '이미 수집됨 — 건너뜀', not a generic error toast (D-07)"
    - "A dead job exposes a retry button labeled 재시도 with aria-label='재시도' and a 44x44px hit target (D-08)"
    - "The dropzone's three tabs (file/URL/text) are Radix Tabs in one component (D-06), not three separate pages"
    - "Cancelling a running job requires confirming the exact copy '진행 중인 작업을 취소하시겠습니까? 이미 사용된 비용은 환불되지 않습니다.'"
    - { statement: "the source list shows skeleton rows on initial fetch before data resolves", verification: backstop }
    - { statement: "the source list paginates or virtualizes past roughly 50 rows", verification: backstop }
  artifacts:
    - apps/dashboard/components/Dropzone.tsx
    - apps/dashboard/components/JobStepper.tsx
    - apps/dashboard/app/w/[workspaceId]/sources/page.tsx
  key_links:
    - "Dropzone.tsx file tab -> apiFetch('/workspaces/{id}/sources/file?filename=&title=', {method:'POST', body: rawBytes}) — raw bytes, never FormData/multipart (D-P11)"
    - "JobStepper.tsx -> apiFetch('/workspaces/{id}/sources/{raw_source_id}/jobs') -> step_label/chain_position/chain_total/status fields (jobs.py, already computed server-side)"
    - "JobStepper.tsx retry -> apiFetch('/workspaces/{id}/jobs/{job_id}/retry', {method:'POST'})"
  prohibitions:
    - requirement_id: UI-03
      category: transparency
      status: resolved
      verification: judgment
      resolution: "The exact 'not refunded' disclosure from UI-SPEC's Destructive Confirmation copy row is required verbatim on every job-cancel affordance in this plan (JobStepper's cancel button), not only documented once in the spec — enforced by this plan's task 2 acceptance criteria."
      reason: null
      statement: "MUST NOT let any job-cancellation control imply already-incurred LLM cost is refunded — the disclosure that it is not must accompany every cancel affordance, not just exist somewhere in a copy document"

assumptions:
  - "UI-03's requirement-level edge coverage came back 'unclassified' from the deterministic probe — no additional backstop beyond this plan's own must_haves.truths and the UI-SPEC-lifted rows exists; D-05/D-06/D-07/D-08 and ING-06's literal 'no spinner' text are the acceptance bar."
---

<objective>
Deliver UI-03: a dropzone that ingests via all three `apps/api` source paths and shows real
job-chain progress per source — never an indeterminate spinner (ING-06's explicit prohibition
against exactly that, since a 4-minute compile that looks stalled causes users to re-submit and
double the LLM cost).

Purpose: make source ingestion + progress visibility real, using `06-04`'s `apiFetch`.
Output: `Dropzone.tsx`, `JobStepper.tsx`, the sources page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-dashboard/06-CONTEXT.md
@.planning/phases/06-dashboard/06-UI-SPEC.md
@apps/api/src/api/routers/sources.py
@apps/api/src/api/routers/jobs.py
@apps/dashboard/lib/api-client.ts
@apps/dashboard/lib/supabase/client.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dropzone.tsx — 3-tab ingest (D-06)</name>
  <files>apps/dashboard/components/Dropzone.tsx, apps/dashboard/tests/Dropzone.test.tsx</files>
  <behavior>
    - File tab: selecting a file and submitting sends the raw bytes as the request body with
      the file's own `type` as `Content-Type`, `filename`/`title` as query params — never
      `FormData`.
    - URL tab: submitting an invalid-scheme URL (client-side pre-check, `http(s)` only) shows an
      inline error before any request; a valid URL submits to the URL endpoint.
    - Text tab: submitting empty text keeps submit disabled (matches the backend's `min_length=1`).
    - A `409 already_ingested` response (from `apiFetch`'s `ApiError`) renders the exact banner
      "이미 수집됨 — 건너뜀".
    - A `402 budget_exceeded` response renders "이번 달 워크스페이스 사용량 한도를
      초과했습니다. 관리자에게 문의하거나 다음 달까지 기다려주세요."
    - A `422 invalid_source` response with `extra.reason === "unsupported_mime"` renders a
      generic invalid-file message (do not echo the raw MIME string back — matches the
      backend's own reflected-exposure avoidance for this field).
    - A successful 202 response calls `onIngested?.(jobId, rawSourceId)` so the parent page can
      add the new source to its list without a full refetch.
  </behavior>
  <read_first>
    apps/api/src/api/routers/sources.py (full file, already read this session — the exact 3
    request shapes and the file endpoint's raw-bytes contract at lines 334-346),
    apps/dashboard/lib/api-client.ts (06-04's `apiFetch`/`ApiError` — this component's only
    network path), 06-UI-SPEC.md Copywriting Contract rows under "Dropzone (UI-03)".
  </read_first>
  <action>
    Create `apps/dashboard/components/Dropzone.tsx` ("use client", named export `Dropzone`,
    `DropzoneProps = { workspaceId: string; onIngested?: (jobId: string, rawSourceId: string)
    => void }`). Use `@radix-ui/react-tabs` for the file/URL/text tabs (D-06). File tab: a
    drag-and-drop zone + `<input type="file">` fallback; on submit, `apiFetch(
    `/workspaces/${workspaceId}/sources/file?filename=${encodeURIComponent(file.name)}&title=${encodeURIComponent(title)}`,
    { method: "POST", body: file, contentType: file.type })` (using `06-04`'s `contentType`
    escape hatch for non-JSON bodies). URL tab: text input + optional title, `apiFetch(
    `/workspaces/${workspaceId}/sources/url`, { method: "POST", body: { url, title } })`. Text
    tab: title + textarea, `apiFetch(`/workspaces/${workspaceId}/sources/text`, { method:
    "POST", body: { title, text } })`. Catch `ApiError` from any of the three and branch on
    `.status`/`.detail` per the behavior spec — render the matching UI-SPEC copy row for each
    documented case; anything else (network failure, 5xx) renders a generic retry-later message.
    Submit button "소스 등록" is the screen's sole accent-colored element pre-submit (UI-SPEC
    Primary Visual Anchor).
  </action>
  <verify>
    <automated>pnpm --filter @nexuswiki/dashboard exec vitest run tests/Dropzone.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The file-tab test asserts the mocked `fetch`/`apiFetch` call body is the raw `File`
      object, not a `FormData` instance, and that no `multipart` content-type is ever set.
    - Each documented `ApiError` case (409/402/422) has a corresponding test asserting the exact
      copy string renders.
  </acceptance_criteria>
  <done>All three ingest tabs work end to end against apiFetch with correct error-copy mapping — proven by the behavior-spec test suite.</done>
</task>

<task type="auto">
  <name>Task 2: JobStepper.tsx — real stage stepper (D-05) + retry/cancel</name>
  <files>apps/dashboard/components/JobStepper.tsx, apps/dashboard/tests/JobStepper.test.tsx</files>
  <read_first>
    apps/api/src/api/routers/jobs.py (full file, already read this session — `_job_response`'s
    exact shape: `step_label`, `chain_position`, `chain_total`, `status`, `attempts`,
    `max_attempts`, `last_error`, `cancel_requested_at`; `CHAIN_ORDER` = parse -> compile ->
    link_sync -> embed -> conflict_check; STEP_LABELS are server-owned Korean strings — do not
    hardcode a second copy of this map client-side, consume `step_label` from the response).
  </read_first>
  <action>
    Create `apps/dashboard/components/JobStepper.tsx` ("use client", named export
    `JobStepper`, `JobStepperProps = { workspaceId: string; rawSourceId: string }`). On mount
    and on a polling interval (~3s, cleared on unmount), `apiFetch(
    `/workspaces/${workspaceId}/sources/${rawSourceId}/jobs`)` returning `{jobs: [...]}`. Render
    one dot/label per entry in the fixed stage sequence "업로드 → 파싱 → 컴파일 → 링크 동기화
    → 임베딩" (D-05 verbatim) — map `parse`/`compile`/`link_sync`/`embed` job rows onto stages 2-5
    by `chain_position`; stage 1 "업로드" is implicit (the raw_source row already exists once
    this component renders at all, so it is always shown as complete). The current step
    (matching `--color-primary` per D-05/UI-SPEC) is the first row with `status` in
    `("queued","running")`; completed stages render checked, a `dead` row renders an error state
    with the copy template "{단계} 단계에서 실패했습니다 — {last_error 요약}. 재시도를 눌러
    다시 시도하세요." (substitute `step_label` for `{단계}`, and `last_error` truncated to a
    reasonable length for `{last_error 요약}` — do not render the raw provider exception text
    if `last_error` is unexpectedly long, truncate at ~200 chars with an ellipsis) and a retry
    button (`aria-label="재시도"`, 44x44px hit target) calling `apiFetch(
    `/workspaces/${workspaceId}/jobs/${jobId}/retry`, { method: "POST" })`. Any non-dead,
    non-terminal row shows a cancel affordance opening a `@radix-ui/react-dialog` confirmation
    with the exact copy "진행 중인 작업을 취소하시겠습니까? 이미 사용된 비용은 환불되지
    않습니다." — on confirm, `apiFetch(`/workspaces/${workspaceId}/jobs/${jobId}/cancel`,
    { method: "POST" })`.
  </action>
  <verify>
    <automated>pnpm --filter @nexuswiki/dashboard exec vitest run tests/JobStepper.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Test mocks `apiFetch` returning 4 job rows (parse done, compile running, link_sync
      queued, embed queued) and asserts the stepper renders all 5 stage labels with "컴파일" as
      the current/highlighted stage.
    - A `dead` row's retry button has `aria-label="재시도"`.
    - The cancel dialog's confirm text matches the required copy exactly (string equality, not
      substring).
    - `grep -c "spinner\|Spinner" apps/dashboard/components/JobStepper.tsx` returns 0 — no
      indeterminate-spinner component is ever rendered here (ING-06).
  </acceptance_criteria>
  <done>JobStepper renders the real 5-stage progress from live job data, with working retry and confirmed cancel — proven by the mocked-response test suite.</done>
</task>

<task type="auto">
  <name>Task 3: sources/page.tsx — source list + wiring</name>
  <files>apps/dashboard/app/w/[workspaceId]/sources/page.tsx</files>
  <read_first>
    supabase/migrations/0004_rls_policies.sql section 5 "raw_sources" (raw_sources_select_member
    — the direct-read policy this page relies on for the list, distinct from the apps/api
    ingest/job endpoints Task 1/2 use).
  </read_first>
  <action>
    Create `apps/dashboard/app/w/[workspaceId]/sources/page.tsx`: Server Component fetches the
    initial list — `supabase.from("raw_sources").select("id,title,source_type,created_at,
    content_hash").eq("workspace_id", workspaceId).order("created_at", { ascending: false
    }).limit(50)` (the pagination/virtualization backstop threshold from UI-SPEC — 50 rows is
    the documented recommendation) — and passes it as initial props into a client wrapper
    component (`"use client"`, inline in this file or a small `SourcesList` sub-component) that
    renders: the empty state ("아직 등록된 소스가 없습니다" / "파일을 드래그하거나
    URL/텍스트를 붙여넣어 첫 소스를 추가하세요.") when the list is empty, otherwise one row per
    source (title, source_type icon, created_at) each rendering a `<JobStepper
    rawSourceId={source.id} workspaceId={workspaceId} />` beneath it, with `<Dropzone
    workspaceId={workspaceId} onIngested={...}>` above the list prepending newly-ingested
    sources to local state without a full page reload.
  </action>
  <verify>
    <automated>pnpm --filter @nexuswiki/dashboard exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `next build` succeeds; the page renders the empty-state copy verbatim when
      `raw_sources` returns zero rows.
    - The direct read's `.select(...)` does not include `content` (the potentially-large
      extracted text column) — the list view only needs metadata, not full content, per
      normal payload-size hygiene.
  </acceptance_criteria>
  <done>Sources page wires Dropzone + per-source JobStepper over a real, workspace-scoped list, with the documented empty state — UI-03 is fully satisfied.</done>
</task>

</tasks>

## Artifacts this phase produces

- `apps/dashboard/components/Dropzone.tsx` — `Dropzone` component, `DropzoneProps` type
- `apps/dashboard/components/JobStepper.tsx` — `JobStepper` component, `JobStepperProps` type
- `apps/dashboard/app/w/[workspaceId]/sources/page.tsx` — route `/w/[workspaceId]/sources`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> apps/api sources/jobs endpoints | JWT-per-request via apiFetch; no new boundary type vs 06-04 |
| RSC -> PostgREST raw_sources | Direct read, RLS-scoped, list-only (no content column) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-15 | Information Disclosure | sources list direct read | low | mitigate | `.select()` excludes `content` — the list view never fetches full extracted text into the browser for rows the user may not even open |
| T-06-16 | Denial of Service | JobStepper polling | low | accept | ~3s polling per visible source is bounded by how many sources are on-screen (max 50, page-limited); no unbounded fan-out |
| T-06-17 | Tampering | File-tab raw-bytes upload | medium | mitigate | Handled entirely server-side (MIME allow-list, size cap) per `03-05-PLAN.md`'s existing design — this plan only forwards the file, does not weaken or duplicate that validation client-side |
</threat_model>

<verification>
Vitest suites for Dropzone and JobStepper pass all behavior-spec cases; `tsc --noEmit` and
`next build` succeed; manual click-through against a running `apps/api` + `supabase start`
confirms file/URL/text ingest each produce a visible job chain progressing through real stages.
</verification>

<success_criteria>
UI-03 fully satisfied: drop a source, watch real stage-by-stage progress, see dedup/budget/error
states with the exact required copy, retry dead jobs, cancel running jobs with cost disclosure.
</success_criteria>

<output>
Create `.planning/phases/06-dashboard/06-05-SUMMARY.md` when done
</output>
