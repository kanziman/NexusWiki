---
phase: 06-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - apps/dashboard/components/WorkspaceSwitcher.tsx
  - apps/dashboard/components/NavShell.tsx
  - apps/dashboard/app/w/[workspaceId]/layout.tsx
  - apps/dashboard/tests/WorkspaceSwitcher.test.tsx
autonomous: true
requirements: [UI-01]
user_setup: []

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Selecting a different workspace in the switcher navigates to /w/[newId] (D-03) — the URL owns tenancy, not component state"
    - "The active workspace is visually indicated with --color-primary in the switcher dropdown (UI-SPEC Color/Accent row)"
    - "The switcher chevron and any other icon-only control in the nav shell carries an explicit aria-label, not icon-only visual affordance (UI-SPEC WCAG 2.5.5 exception, 44x44px minimum hit area)"
    - { statement: "workspace names longer than the switcher's fixed width truncate with CSS `truncate` and expose the full name via a Radix Tooltip", verification: backstop }
    - { statement: "the switcher dropdown scrolls (does not grow unbounded) past roughly 8 workspaces", verification: backstop }
    - { statement: "the workspace-switcher dropdown item is disabled and shows an inline spinner while /w/[id] route revalidation is in flight (UI-SPEC UI Considerations: workspace-switcher/loading)", verification: backstop }
  artifacts:
    - apps/dashboard/components/WorkspaceSwitcher.tsx
    - apps/dashboard/components/NavShell.tsx
  key_links:
    - "WorkspaceSwitcher.tsx onSelect -> router.push(workspacePath(id)) (lib/workspace-path.ts, reused not reimplemented)"
    - "app/w/[workspaceId]/layout.tsx -> direct PostgREST read of `workspaces` (all rows the caller can see) -> WorkspaceSwitcher props"
---

<objective>
Complete UI-01: give the workspace shell a real navigation surface — a workspace switcher
dropdown (D-03) and the nav links to the other four Phase 6 surfaces (sources / ask / wiki /
graph / settings) — replacing the bare header `06-01-PLAN.md`'s tracer left in place.

Purpose: make `/w/[workspaceId]` navigable, not just landable-on.
Output: `WorkspaceSwitcher.tsx`, a `NavShell.tsx` wrapping the workspace layout's chrome.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-dashboard/06-CONTEXT.md
@.planning/phases/06-dashboard/06-UI-SPEC.md
@apps/dashboard/lib/workspace-path.ts
@apps/dashboard/components/HealthBadge.tsx
@apps/dashboard/app/w/[workspaceId]/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: WorkspaceSwitcher.tsx — Radix dropdown over a direct-read workspace list</name>
  <files>apps/dashboard/components/WorkspaceSwitcher.tsx, apps/dashboard/tests/WorkspaceSwitcher.test.tsx</files>
  <read_first>
    apps/dashboard/lib/workspace-path.ts (the helper `onSelect` MUST call, not reimplement),
    apps/dashboard/components/HealthBadge.tsx (component/export/Props-type/data-* convention),
    06-UI-SPEC.md Design System table (Radix `@radix-ui/react-dropdown-menu` + `lucide-react`
    already installed in 06-01), Copywriting Contract row "Workspace switcher (UI-02) | Nav CTA".
  </read_first>
  <action>
    Create `apps/dashboard/components/WorkspaceSwitcher.tsx` ("use client", named export
    `WorkspaceSwitcher`, `WorkspaceSwitcherProps = { workspaces: { id: string; name: string
    }[]; currentWorkspaceId: string }`). Use `@radix-ui/react-dropdown-menu`'s
    `Root`/`Trigger`/`Portal`/`Content`/`Item` primitives: trigger shows the current workspace's
    `name` (CSS `truncate` on a fixed max-width) + a `lucide-react` `ChevronDown` icon with
    `aria-label="워크스페이스 전환"` on the icon button itself (not just the trigger), padded
    to a 44x44px hit area per UI-SPEC's exception list. `Content` renders one `Item` per
    workspace; the item matching `currentWorkspaceId` gets `color: var(--color-primary)` (or
    the Tailwind-mapped equivalent from `06-01`'s `@theme` block) and a `data-active="true"`
    attribute (Testing Library asserts on this). `onSelect` for a non-current item calls
    `useRouter().push(workspacePath(workspace.id))` — import `workspacePath` from
    `lib/workspace-path.ts`, do not inline `/w/${id}`. Past 8 items, wrap `Content` in a
    `max-height` + `overflow-y: auto` container (the scrollable-dropdown backstop). Wrap each
    `Item`'s name in `@radix-ui/react-tooltip` `Root`/`Trigger`/`Content` so a truncated long
    name is recoverable via hover/focus (the long-text backstop) — `Content` shows the
    untruncated `name`.
  </action>
  <verify>
    <automated>pnpm --filter @nexuswiki/dashboard exec vitest run tests/WorkspaceSwitcher.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Rendering with 2 workspaces and clicking the non-active one's `Item` calls
      `router.push` with `workspacePath(otherId)`'s exact return value (mock `next/navigation`'s
      `useRouter` in the test, assert on the `push` mock's call args).
    - The active workspace's `Item` has `data-active="true"`; the other does not.
    - The chevron icon button has `aria-label="워크스페이스 전환"`.
  </acceptance_criteria>
  <done>WorkspaceSwitcher renders, navigates via workspacePath() on select, and marks the active workspace — covered by a passing Vitest suite.</done>
</task>

<task type="auto">
  <name>Task 2: NavShell.tsx — wire switcher + route links into the workspace layout</name>
  <files>apps/dashboard/components/NavShell.tsx, apps/dashboard/app/w/[workspaceId]/layout.tsx</files>
  <read_first>
    apps/dashboard/app/w/[workspaceId]/layout.tsx (06-01's tracer version — a bare header +
    `{children}`; this task replaces the bare header, not the RLS-scoped data read logic).
  </read_first>
  <action>
    Create `apps/dashboard/components/NavShell.tsx` ("use client", named export `NavShell`,
    `NavShellProps = { workspaces: {id:string;name:string}[]; currentWorkspaceId: string }`):
    renders `<WorkspaceSwitcher>` plus five nav links using `next/link` to
    `workspacePath(currentWorkspaceId) + "/sources"`, `"/ask"`, `"/wiki"` (a bare wiki index —
    `06-07-PLAN.md` builds the actual index/empty-state page under this route),
    `"/graph"`, `"/settings"` — active-route styling via `usePathname()` comparison, `<Label>`
    weight/size per UI-SPEC Typography (14px/600). Route labels: "소스", "질문하기", "위키",
    "그래프", "설정".

    In `apps/dashboard/app/w/[workspaceId]/layout.tsx`: extend the existing direct-read of the
    current workspace (Task 1's `06-01` code, unchanged) with a second direct read — `const {
    data: workspaces } = await supabase.from("workspaces").select("id,name").order("name")` (no
    `.eq` filter: RLS `workspaces_select_member` already scopes this to every workspace the
    caller belongs to, which is exactly the switcher's full list). Replace the bare header with
    `<NavShell workspaces={workspaces ?? []} currentWorkspaceId={workspaceId} />` above
    `{children}`.
  </action>
  <verify>
    <automated>pnpm --filter @nexuswiki/dashboard exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `app/w/[workspaceId]/layout.tsx` issues exactly one additional `supabase.from("workspaces")`
      call beyond the single-row lookup already present from `06-01` (grep: two
      `.from("workspaces")` call sites in the file, one `.single()` and one `.order("name")`).
    - `NavShell` renders 5 links plus the switcher; `next build` succeeds.
  </acceptance_criteria>
  <done>The workspace layout shows a real switcher (all of the caller's workspaces via RLS) and links to sources/ask/wiki/graph/settings — UI-01 is fully satisfied.</done>
</task>

</tasks>

## Artifacts this phase produces

- `apps/dashboard/components/WorkspaceSwitcher.tsx` — `WorkspaceSwitcher` component, `WorkspaceSwitcherProps` type
- `apps/dashboard/components/NavShell.tsx` — `NavShell` component, `NavShellProps` type

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC -> PostgREST (workspace list) | Second direct read added to the layout; still requester-JWT scoped, no new boundary type vs 06-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Tampering | WorkspaceSwitcher onSelect | low | accept | Navigating to an arbitrary `/w/{id}` the user pastes/guesses already redirects at the layout's RLS-scoped read (06-01) if they're not a member — the switcher itself only ever lists ids RLS already proved they can see, so it cannot be used to navigate anywhere unauthorized |
| T-06-07 | Information Disclosure | NavShell route links | low | accept | Links are relative to the current (already-authorized) workspace id; no cross-tenant id is ever embedded in a link |
</threat_model>

<verification>
`tsc --noEmit` clean, `vitest run tests/WorkspaceSwitcher.test.tsx` passes, `next build`
succeeds, manual click-through confirms switching workspaces navigates and reloads real data.
</verification>

<success_criteria>
UI-01 fully satisfied: login, tenancy-gated `/w/[workspaceId]`, workspace switching by URL, and
navigation to every other Phase 6 surface all work end to end.
</success_criteria>

<output>
Create `.planning/phases/06-dashboard/06-02-SUMMARY.md` when done
</output>
