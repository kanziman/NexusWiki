---
phase: 05-citation-integrity-and-answer-apis
plan: 07
type: execute
wave: 4
depends_on: ["05-04", "05-05", "05-06"]
files_modified:
  - supabase/migrations/0013_ask_budget_and_verification_audit.sql
  - apps/worker/src/worker/db/service.py
  - apps/worker/tests/test_service_client.py
  - apps/worker/tests/test_queue.py
autonomous: true
gap_closure: true
requirements: [QC-02, OPS-01]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 4
  confidence: medium

must_haves:
  truths:
    - "Ask budget preflight includes every usage_events row in the caller-supplied UTC month window, including when the workspace has more than 1,000 events, and blocks a stream at the inclusive spent >= cap boundary (OPS-01 / 05-04 gap closure)."
    - "A service-role conflict transition to disputed preserves an already-recorded human verified_by and verified_at audit pair; it cannot replace that pair with the service identity or NULL (QC-02)."
    - "The aggregate and audit behavior are proven against the local Supabase stack as well as by HTTP-boundary unit tests."
  artifacts:
    - supabase/migrations/0013_ask_budget_and_verification_audit.sql
    - apps/worker/src/worker/db/service.py::sum_usage_events_since
    - apps/worker/tests/test_queue.py
  key_links:
    - "worker.__main__::_check_ask_budget -> ServiceDb.sum_usage_events_since() -> public.sum_usage_events_since(uuid, timestamptz) -> complete usage_events aggregate"
    - "worker.handlers.conflict::run_conflict_check -> ServiceDb.set_wiki_page_disputed() -> public.stamp_wiki_verification() -> OLD.verified_by / OLD.verified_at preserved for service-role transition"
---

<objective>
Close the two correctness gaps recorded in `05-VERIFICATION.md` and `05-REVIEW.md` without broadening Phase 05's public API surface: replace the first-1,000-row Ask spend read with a database aggregate that sees the whole UTC window, and correct the verification trigger so automated service-role disputes retain the most recent human verifier identity and timestamp.

Purpose: a cost cap that silently overlooks high-volume usage is not a cap, and an automated contradiction marker must not erase the human audit record QC-02 is intended to retain. This plan is deliberately limited to those two verified gaps and their regressions.
Output: migration `0013`, a service-role-only aggregate RPC consumed by the existing Ask preflight, unit plus local-stack regressions for the two failure modes, and a blocking linked-Cloud deployment confirmation.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-citation-integrity-and-answer-apis/05-CONTEXT.md
@.planning/phases/05-citation-integrity-and-answer-apis/05-04-PLAN.md
@.planning/phases/05-citation-integrity-and-answer-apis/05-05-PLAN.md
@.planning/phases/05-citation-integrity-and-answer-apis/05-06-PLAN.md
@.planning/phases/05-citation-integrity-and-answer-apis/05-VERIFICATION.md
@.planning/phases/05-citation-integrity-and-answer-apis/05-REVIEW.md
@apps/worker/src/worker/db/service.py
@apps/worker/src/worker/__main__.py
@apps/worker/src/worker/handlers/conflict.py
@apps/worker/tests/test_service_client.py
@apps/worker/tests/test_queue.py
@supabase/migrations/0009_pipeline_ops.sql
@supabase/migrations/0010_budget_error_sqlstate.sql
@supabase/migrations/0012_ask_citation_and_graph.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a service-role-only complete usage aggregate and preserve human verification stamps</name>
  <files>supabase/migrations/0013_ask_budget_and_verification_audit.sql</files>
  <read_first>
    supabase/migrations/0009_pipeline_ops.sql lines 96-145 (usage_events columns, `usage_events_workspace_occurred_idx`, and service_role-only INSERT convention),
    supabase/migrations/0010_budget_error_sqlstate.sql lines 20-55 (the authoritative UTC month-window aggregate and inclusive `v_spent >= v_cap` boundary),
    supabase/migrations/0012_ask_citation_and_graph.sql lines 181-215 (current `stamp_wiki_verification()` trigger and the exact defect: it unconditionally assigns `auth.uid()`/`now()` for every status transition),
    supabase/migrations/0004_rls_policies.sql lines 230-245 (editor-only wiki update policy; this migration must not weaken it).
  </read_first>
  <behavior>
    - `public.sum_usage_events_since(p_workspace_id uuid, p_since timestamptz)` returns one bigint equal to `coalesce(sum(cost_micros), 0)` for every matching row, with no row cap and no client-controlled SQL fragment.
    - Only `service_role` can execute the aggregate; anon/authenticated/public cannot use it to enumerate another workspace's cost total.
    - An authenticated human verification-status transition still stamps `verified_by = auth.uid()` and a fresh `verified_at` server timestamp.
    - A service-role status transition (the conflict path to `disputed`) leaves `OLD.verified_by` and `OLD.verified_at` intact, including when both were previously NULL.
  </behavior>
  <action>
    Create `supabase/migrations/0013_ask_budget_and_verification_audit.sql`, wrapped in `begin;`/`commit;` like migrations 0007+.

    First define `public.sum_usage_events_since(p_workspace_id uuid, p_since timestamptz) returns bigint` as a `stable`, `security invoker`, `set search_path = public` SQL function. Its sole query must be `select coalesce(sum(u.cost_micros), 0) from public.usage_events u where u.workspace_id = p_workspace_id and u.occurred_at >= p_since;`. Do not accept `limit`, `offset`, raw predicates, or a client-provided workspace collection. Revoke all on this exact signature from `public, anon, authenticated, service_role`, then grant execute only to `service_role`; add a comment naming the Ask preflight and explaining why a row fetch is unsafe at scale.

    Then `create or replace` `public.stamp_wiki_verification()` (keep its existing trigger name and `security invoker`/`search_path` conventions). On a real status change, stamp `NEW.verified_by := auth.uid()` and `NEW.verified_at := now()` only when `auth.uid() is not null`. When there is no authenticated human identity (the worker/service-role automated conflict transition), preserve the old pair explicitly: `NEW.verified_by := OLD.verified_by; NEW.verified_at := OLD.verified_at;`. Preserve all unrelated columns and do not add a bypass to the editor RLS policy. Update the function comment so it states the distinction between human verification and automated dispute state, including QC-02's durable human audit guarantee. Finish with `notify pgrst, 'reload schema';`.
  </action>
  <verify>
    <automated>supabase db reset && supabase db lint</automated>
  </verify>
  <done>The database owns unbounded cost aggregation and distinguishes human audit stamping from an automated service-role status update, with least-privilege execution grants preserved.</done>
  <acceptance_criteria>
    - The aggregate function contains `coalesce(sum(u.cost_micros), 0)` and no `limit`/pagination clause.
    - The only post-migration execute grant for `public.sum_usage_events_since(uuid, timestamptz)` is `service_role`.
    - `stamp_wiki_verification()` cannot overwrite a non-NULL old human audit pair when `auth.uid()` is NULL.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route Ask preflight through the aggregate RPC and lock its HTTP contract</name>
  <files>apps/worker/src/worker/db/service.py, apps/worker/tests/test_service_client.py</files>
  <read_first>
    apps/worker/src/worker/db/service.py (full file — especially helper classification sets, `_rpc`/`_rpc_rows`, and the current first-page `sum_usage_events_since()` implementation),
    apps/worker/src/worker/__main__.py lines 48-65 (`_check_ask_budget()` must remain the sole Ask preflight caller and retain `spent < cap`),
    apps/worker/tests/test_service_client.py lines 108-145 (public-helper classification/disjointness hard gates),
    apps/worker/tests/test_service_client.py lines 350-405 (existing cap/usage helper regression to replace rather than duplicate).
  </read_first>
  <behavior>
    - Test: `sum_usage_events_since(workspace_id, since)` POSTs exactly `{p_workspace_id, p_since}` to `/rpc/sum_usage_events_since`, normalizes the one-row composite/scalar response to an integer, and never requests `/usage_events` directly.
    - Test: a returned aggregate representing 1,001 individual events is returned in full (for example `1001`), proving the Python path cannot reintroduce a first-page truncation.
    - Test: empty/all-null aggregate output normalizes to zero, retaining the existing no-spend behavior and never treating it as an absent cap.
  </behavior>
  <action>
    In `apps/worker/src/worker/db/service.py`, replace the direct `usage_events` `_select(... limit=1000)` implementation of `sum_usage_events_since()` with the aggregate RPC. Put `sum_usage_events_since` in a new clearly named `BUDGET_RPC_FUNCTIONS` allow-list (export it and union it into `RPC_HELPERS`), and remove it from `TABLE_HELPERS`; it remains workspace-scoped with required keyword-only `workspace_id` and accepts the caller-calculated ISO-8601 `since` string. Reuse the established `_rpc` single-result normalization seam rather than hand-parsing `httpx` responses. Normalize the RPC result robustly whether PostgREST returns a scalar or its one-column object representation, but fail loudly for an unexpected nonnumeric populated shape; only a null/no-row result becomes `0`.

    Update `apps/worker/tests/test_service_client.py`'s helper-union and disjointness assertions for `BUDGET_RPC_FUNCTIONS`. Replace the prior GET-plus-`limit=1000` expectations with the exact RPC request/response assertions above. Keep `get_workspace_budget_cap()` as a table helper and retain its test separately, so the cap's missing-row `None` semantics remain distinct from aggregate zero spend.
  </action>
  <verify>
    <automated>uv run pytest apps/worker/tests/test_service_client.py apps/worker/tests/test_worker_main.py -x</automated>
  </verify>
  <done>The existing `_check_ask_budget()` compares the full database aggregate to the cap without changing its inclusive reject boundary, and no worker path reads a capped usage_events page for this decision.</done>
  <acceptance_criteria>
    - Searching `sum_usage_events_since` finds an RPC call, not a `usage_events` select with `limit=1000`.
    - The service-client regression proves a 1,001-event-equivalent aggregate is returned as 1,001.
    - `test_every_public_helper_is_classified_as_table_or_queue_rpc` (rename only if its message is now misleading) still proves every public helper is in exactly one approved allow-list.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the two fixes on a real local stack</name>
  <files>apps/worker/tests/test_queue.py</files>
  <read_first>
    apps/worker/tests/test_queue.py lines 480-640 (`local_queue`'s loopback-only service-role fixture, creation/teardown order, and its disposable workspace),
    apps/worker/src/worker/handlers/conflict.py (the `run_conflict_check()` call path and its narrow injected-db seam),
    apps/worker/src/worker/db/service.py::set_wiki_page_disputed (the actual service-role PATCH that must fire the trigger),
    apps/api/src/api/routers/wiki.py and apps/api/tests/test_workspaces_isolation.py lines 295-320 (the established requester-JWT verification transition proving how a real human audit stamp is created).
  </read_first>
  <behavior>
    - Local-stack regression: insert 1,001 same-window `usage_events` rows in the disposable workspace with a known total, call the new ServiceDb aggregate, and assert the exact total (not merely nonzero). Set a cap at that total and assert the existing `_check_ask_budget()` rejects at equality, without an OpenRouter call.
    - Local-stack integration: create a page, make its status `verified` through the fixture owner's requester JWT so the trigger produces the owner id/timestamp, then invoke a confirmed conflict through `run_conflict_check()` using the real ServiceDb dispute write. Assert both rows are `disputed`, while the initially human-verified page retains exactly its original `verified_by` and `verified_at` values.
    - Local-stack least-privilege regression: POSTing `{p_workspace_id, p_since}` to `/rest/v1/rpc/sum_usage_events_since` with a requester JWT is denied with 401 or 403; if the fixture exposes an anon key, the same request is also denied with 401 or 403. The fixture's real service-role client calls the same RPC successfully and receives the expected aggregate.
    - The conflict integration must use a narrowly delegating test adapter only to supply the one candidate and deterministic LLM result; its `set_wiki_page_disputed()` delegates to the real `ServiceDb`, so the database trigger—not a fake—proves the regression.
  </behavior>
  <action>
    Extend `LocalQueueStack` only as needed to expose the disposable owner's `user_id` and requester token to its own test module; preserve the loopback guard and guaranteed workspace-before-user teardown. Add two `pytest.mark.asyncio` integration tests to the existing real-DB section.

    For the budget test, bulk-insert 1,001 minimal valid `usage_events` rows through the fixture's real service client (reuse its workspace; no production/env credentials), all at or after a deterministic UTC-month boundary. Assert `await local_queue.db.sum_usage_events_since(...)` equals the full expected total. Update that workspace's `monthly_budget_micros` through the authorized setup path or a narrowly scoped fixture helper, and call `_check_ask_budget()` with local worker settings/client wiring so equality produces `False`. Do not use a mocked aggregate for this test.

    In that same budget integration, make direct loopback HTTP POSTs to `/rest/v1/rpc/sum_usage_events_since` using the fixture owner's requester JWT and assert 401 or 403. If `LocalQueueStack` already exposes a usable anon key without widening fixture scope, perform the equivalent anon request and assert 401 or 403; otherwise document the fixture limitation in the test's skip/branch message rather than manufacturing a key. Make the service-role happy-path request through the real ServiceDb client and assert its returned aggregate is the known full total. This regression must exercise PostgREST permissions, not only Python allow-list classification.

    For the audit test, create two wiki pages in the fixture workspace. Use an HTTP client bearing the fixture owner's access token to transition the first page to `verified`, record the returned/stored audit pair, and assert it belongs to the owner. Call `run_conflict_check()` with a deterministic `ConflictJudgement(is_contradiction=True, ...)`, a candidate adapter that returns the second page, and delegates page reads/dispute writes to the fixture's real ServiceDb. Re-read both pages after the service-role dispute. Assert the first page has `verification_status == 'disputed'` and `disputed is True` while its `verified_by` and `verified_at` exactly equal the pre-conflict pair; also assert the second page is disputed. Do not assert a synthetic service-role identity.
  </action>
  <verify>
    <automated>uv run pytest apps/worker/tests/test_queue.py apps/worker/tests/test_service_client.py apps/worker/tests/test_handlers.py apps/worker/tests/test_worker_main.py -x -rs && uv run pytest -q</automated>
  </verify>
  <done>The two defects are reproducible and closed against an actual Postgres/PostgREST trigger and aggregate, with loopback-only fixtures that leave no users, workspaces, or usage events behind.</done>
  <acceptance_criteria>
    - The high-volume test fails if the implementation again reads only the first 1,000 rows.
    - The audit test fails if `stamp_wiki_verification()` again stamps a service-role dispute with `auth.uid()`/`now()`.
    - A requester JWT receives 401 or 403 from the aggregate RPC, while the real service-role client succeeds with the known aggregate; anon receives the same denial when the existing fixture provides an anon credential.
    - With no local stack, the integration tests are reported as skipped using the existing fixture convention; with the stack, `-rs` reports no unexpected skips.
  </acceptance_criteria>
</task>

<task type="auto" gate="blocking">
  <name>Task 4: Deploy the validated migration to the linked Supabase Cloud project and confirm its live definitions</name>
  <files>supabase/migrations/0013_ask_budget_and_verification_audit.sql</files>
  <read_first>
    supabase/migrations/0013_ask_budget_and_verification_audit.sql (final migration to deploy),
    `.planning/phases/05-citation-integrity-and-answer-apis/05-VERIFICATION.md` (the two gaps this Cloud deployment closes).
  </read_first>
  <behavior>
    - The linked Supabase Cloud project receives migration 0013 only after local reset, lint, and regression verification are green.
    - The live Cloud schema contains `public.sum_usage_events_since(uuid, timestamptz)` and the replacement `public.stamp_wiki_verification()` definition that preserves `OLD.verified_by` and `OLD.verified_at` when there is no authenticated human identity.
  </behavior>
  <action>
    After Tasks 1–3's local commands pass, deploy to the already-linked Cloud project with exactly `supabase db push --yes`. This is a blocking deployment checkpoint: halt immediately and report the full prompt/error context if the CLI presents an unexpected confirmation, targets an unexpected project, or returns a non-zero status. Do not attempt a manual SQL workaround, retry against another project, or mark this plan complete until the linked deployment outcome is clear.

    On a successful push, dump only the linked Cloud public schema to a disposable location (for example `supabase db dump --linked --schema public --file /tmp/nexuswiki-phase05-0013-linked.sql`) and inspect it with `rg`. Confirm the dump contains `sum_usage_events_since(p_workspace_id uuid, p_since timestamp with time zone)` (or the equivalent canonical PostgreSQL rendering), `GRANT EXECUTE ... TO service_role`, and the replacement `stamp_wiki_verification` body that assigns `NEW.verified_by := OLD.verified_by` and `NEW.verified_at := OLD.verified_at` for the unauthenticated/service-role branch. Record the exact linked-project confirmation and command outcomes in the plan summary; remove only the disposable dump after inspection.
  </action>
  <verify>
    <automated>supabase db push --yes && supabase db dump --linked --schema public --file /tmp/nexuswiki-phase05-0013-linked.sql && rg -n -F -e 'sum_usage_events_since' -e 'GRANT EXECUTE' -e 'NEW.verified_by := OLD.verified_by' -e 'NEW.verified_at := OLD.verified_at' /tmp/nexuswiki-phase05-0013-linked.sql</automated>
  </verify>
  <done>Migration 0013 is deployed to the intended linked Cloud project and its live schema proves both the new service-role aggregate RPC and the audit-preserving replacement trigger function are present.</done>
  <acceptance_criteria>
    - `supabase db push --yes` completes successfully only after the local-stack verification has passed; any prompt/error or unexpected target halts execution and is reported.
    - The linked Cloud schema dump proves the exact aggregate RPC signature and service-role-only execute grant are present.
    - The linked Cloud schema dump proves the deployed trigger function has the OLD-audit-field preservation branch, rather than the former unconditional stamp behavior.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker service_role -> `public.sum_usage_events_since` | Ask's private preflight reads a workspace's complete spend aggregate; browser/API requester JWTs must not receive this privileged aggregate RPC. |
| authenticated editor -> verification trigger | A human transition is the only path allowed to establish a verifier identity and timestamp. |
| worker service_role conflict job -> verification trigger | The automation can mark a page disputed but must not impersonate or erase a preceding human verifier. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-18 | Denial of Service / cost exhaustion | Ask budget preflight | high | mitigate | Database `sum()` covers all rows behind the UTC boundary; no client-side first-page cap remains, and `_check_ask_budget()` keeps the inclusive `spent < cap` allow condition. |
| T-05-19 | Information Disclosure | cost aggregate RPC | high | mitigate | Revoke all roles then grant execute only to `service_role`; local PostgREST regression proves requester JWT (and anon when fixture credentials exist) is denied 401/403 while the service-role RPC path succeeds; no authenticated request route receives the aggregate. |
| T-05-20 | Repudiation | `stamp_wiki_verification` | high | mitigate | Trigger stamps only a non-null requester identity and otherwise carries OLD audit fields through automated service-role transitions; real DB integration pins the behavior. |
| T-05-21 | Tampering | test fixtures | medium | mitigate | All integration setup is loopback-guarded, generates disposable users/workspaces, and preserves existing teardown ordering. |
| T-05-SC | Tampering | npm/pip/cargo installs | high | accept | No new package or dependency. |
</threat_model>

<verification>
Apply migration 0013 to a fresh local stack (`supabase db reset`), then run `uv run pytest apps/worker/tests/test_service_client.py apps/worker/tests/test_queue.py apps/worker/tests/test_handlers.py apps/worker/tests/test_worker_main.py -x -rs` and `uv run pytest -q`. Confirm the local PostgREST least-privilege regression denies requester JWT (and anon if its fixture credential exists) while the service-role aggregate succeeds. After local validation, complete the blocking linked-Cloud deployment/definition check in Task 4 with `supabase db push --yes`; halt and report any unexpected prompt/error. Finally run `git diff --check HEAD -- apps/worker supabase .planning/phases/05-citation-integrity-and-answer-apis`.
</verification>

<success_criteria>
Ask cannot bypass its monthly cap by accumulating more than 1,000 usage events, and an automated confirmed conflict can visibly dispute a page without destroying the prior human verification audit pair. QC-02 and the Phase 05 Ask budget guarantee are therefore safe to re-verify.
</success_criteria>

<output>
Create `.planning/phases/05-citation-integrity-and-answer-apis/05-07-SUMMARY.md` when done.
</output>
