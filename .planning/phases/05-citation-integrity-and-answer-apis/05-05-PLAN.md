---
phase: 05-citation-integrity-and-answer-apis
plan: 05
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - apps/api/src/api/routers/wiki.py
  - apps/api/src/api/routers/graph.py
  - apps/api/src/api/main.py
  - apps/api/tests/conftest.py
  - apps/api/tests/test_workspaces_isolation.py
  - apps/api/tests/test_graph_router.py
autonomous: true
requirements: [QC-02, API-04]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An editor-or-above member can transition a wiki page's verification_status and the database records who and when automatically; a viewer attempting the same call gets 403, not a silent no-op (QC-02)."
    - "A member can read a bounded neighborhood of the wiki-link graph around a seed page without being able to request an unbounded traversal (API-04/D-07.1)."
    - "No RSC-replaceable read endpoint was built — API-04's own carve-out is honored, not reflexively expanded into full CRUD (D-07.3)."
  artifacts:
    - apps/api/src/api/routers/wiki.py
    - apps/api/src/api/routers/graph.py
  key_links:
    - "apps/api/src/api/routers/wiki.py -> apps/api/src/api/db/user.py::UserDb.update_one() -> wiki_pages_update_editor RLS policy -> public.stamp_wiki_verification() trigger"
    - "apps/api/src/api/routers/graph.py -> apps/api/src/api/db/user.py::UserDb.rpc() -> public.wiki_graph_neighborhood()"
---

<objective>
Build the two read/write API surfaces API-04 and QC-02 actually require, per D-07's explicit narrow-scope carve-out ("API-04 excludes anything RSC-direct-readable"): a `PATCH /workspaces/{id}/wiki/{wiki_id}/verify` endpoint that is a thin `UserDb.update_one()` wrapper (zero new authorization code — `wiki_pages_update_editor` RLS and 05-02's `stamp_wiki_verification()` trigger do all the real work), and a `GET /workspaces/{id}/graph` endpoint that is a thin `UserDb.rpc("wiki_graph_neighborhood", ...)` wrapper. Audit the existing `apps/api/src/api/routers/jobs.py` against API-04's job-status wording and document why no new job endpoint is needed (D-07.2) rather than silently skipping that half of the requirement.

Purpose: this phase's CONTEXT.md explicitly warns against "building unnecessary duplicate read paths" — this plan is deliberately the smallest surface that satisfies QC-02 and API-04, reusing 05-02's already-applied schema (trigger + RPC) instead of inventing new authorization or traversal logic.
Output: two new routers registered in `create_app()`, and a documented, no-new-code audit conclusion for the job-status half of API-04.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-citation-integrity-and-answer-apis/05-CONTEXT.md
@.planning/phases/05-citation-integrity-and-answer-apis/05-RESEARCH.md
@apps/api/src/api/routers/jobs.py
@apps/api/src/api/routers/retrieval.py
@apps/api/src/api/db/user.py
@apps/api/src/api/main.py
@apps/api/tests/conftest.py
@apps/api/tests/test_workspaces_isolation.py
@packages/core/src/nexuswiki_core/domain.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QC-02 — PATCH /workspaces/{id}/wiki/{wiki_id}/verify</name>
  <files>apps/api/src/api/routers/wiki.py, apps/api/src/api/main.py, apps/api/tests/conftest.py, apps/api/tests/test_workspaces_isolation.py</files>
  <read_first>
    apps/api/src/api/routers/jobs.py lines 133-146 (retry_job — the exact `db.rpc(...)`-then-403-on-empty shape; QC-02 uses `UserDb.update_one()` instead of an RPC since 05-02's trigger does the stamping, but the overall router shape — `_user_db(request, credentials)`, no inline status codes — is identical),
    apps/api/src/api/db/user.py lines 115-129 (update_one() — raises WorkspaceForbidden on anything but exactly 1 affected row; this is your ENTIRE authorization mechanism, do not add a role check in the router),
    packages/core/src/nexuswiki_core/domain.py (VerificationStatus StrEnum — verified/partial/unverified/disputed — use this exact type for request validation, do not redefine the literal set),
    apps/api/tests/conftest.py (TenantActor dataclass, _create_actor(), two_workspaces_two_users fixture, authed_client fixture — you are ADDING a new fixture here for a second, non-owner member, not modifying the existing ones),
    apps/api/tests/test_workspaces_isolation.py lines 1-50 (CROSS_TENANT_CASES table + CASE decorator — "새 라우터가 늘면 이 표에 행만 추가한다" per 02-CONTEXT.md > D-14; add one row for the new PATCH endpoint, do not build a parallel test file for the cross-tenant half of this endpoint's coverage),
    supabase/migrations/0004_rls_policies.sql lines 192-194 (workspace_members_insert_owner — only the OWNER's JWT can insert a workspace_members row; your new fixture must use the owning actor's own access_token to add the second member, not an admin/service key)
  </read_first>
  <behavior>
    - Test: an owner (implicitly editor+) PATCHes their own workspace's wiki page to verification_status="verified" and the response shows verified_by/verified_at populated by the server, matching neither of which the request body sent (request body only sends verification_status and optional expires_at).
    - Test: a viewer-role member of the SAME workspace attempting the same PATCH gets 403 with the standard FORBIDDEN_BODY, and the page's verification_status is unchanged.
    - Test: PATCH against another workspace's wiki_id (cross-tenant) returns 403, added as a new row in test_workspaces_isolation.py's CROSS_TENANT_CASES table, not a bespoke test.
  </behavior>
  <action>
    apps/api/src/api/routers/wiki.py (new): copy the router-shape boilerplate from jobs.py/retrieval.py — APIRouter(prefix="/workspaces", tags=["wiki"]), _bearer = HTTPBearer(), a local _user_db(request, credentials) helper (small intentional per-router duplication, established convention). class VerifyRequest(BaseModel, model_config=ConfigDict(extra="forbid")): verification_status: VerificationStatus (imported from nexuswiki_core.domain — Pydantic validates against the enum automatically, 422 on an invalid value); expires_at: datetime | None = None. @router.patch("/{workspace_id}/wiki/{wiki_id}/verify") async def verify_wiki_page(workspace_id: UUID, wiki_id: UUID, body: VerifyRequest, request: Request, credentials: Annotated[HTTPAuthorizationCredentials, Depends(_bearer)]) -> dict[str, Any]: db = _user_db(...); values: dict[str, Any] = {"verification_status": body.verification_status.value}; if body.expires_at is not None: values["expires_at"] = body.expires_at.isoformat(); row = await db.update_one("wiki_pages", match={"id": str(wiki_id), "workspace_id": str(workspace_id)}, values=values); return a narrowed dict exposing id/slug/verification_status/verified_by/verified_at/expires_at/disputed (do NOT expose the full row — mirrors jobs.py's _job_response narrowing pattern, keeps internal columns like `content`/`aliases` out of a verification-transition response). Do NOT set verified_by or verified_at in `values` — 05-02's `stamp_wiki_verification()` trigger sets both from `auth.uid()`/`now()` regardless of what the client sends; setting them here would be redundant at best and a spoofable no-op at worst since the trigger overwrites them anyway.

    apps/api/src/api/main.py: import wiki_router from api.routers.wiki, app.include_router(wiki_router).

    apps/api/tests/conftest.py: add a new fixture `workspace_member_with_role` (function-scoped, depends on local_stack) that returns a factory `Callable[[TenantActor, str], TenantActor]` — given an owning TenantActor and a role string ("editor"/"viewer"), creates a second bare auth user via the SAME admin-user-creation half of `_create_actor` (email/password/session, no new workspace), then INSERTs a `workspace_members` row `{workspace_id: owner.workspace_id, user_id: <new user id>, role: <role>}` using the OWNER's access_token (not admin key — `workspace_members_insert_owner` RLS requires the inserting JWT to already hold owner role on that workspace), registers the new user_id/email with `_register()` for the collision guard, and returns a `TenantActor` for the new member (workspace_id/workspace_name copied from the owner's, since the member belongs to the owner's workspace, not a workspace of their own) — teardown deletes the new auth user via admin key (the workspace itself is torn down by the existing owner-actor teardown, cascade removes the membership row).

    apps/api/tests/test_workspaces_isolation.py: add one row to CROSS_TENANT_CASES: `("PATCH", "/workspaces/{workspace_id}/wiki/{wiki_id}", {"verification_status": "verified"}, "남의 워크스페이스 위키 페이지 검증 상태 변경")` — note this table's existing cases use `{workspace_id}` only in the path template; since this new case also needs a `wiki_id`, check whether the existing test functions format only `workspace_id` or need the path template pattern extended to accept a fixture-created wiki_id too (if the existing CASE machinery cannot express a two-id path, add the verify-endpoint cross-tenant coverage as a small dedicated test instead of forcing it into the shared table — do not stretch the table's mechanics past what it can cleanly express).
  </action>
  <verify>
    <automated>uv run pytest apps/api/tests/test_workspaces_isolation.py -x</automated>
  </verify>
  <done>PATCH .../wiki/{wiki_id}/verify exists, is editor+-gated purely by RLS, and verified_by/verified_at always come from the database trigger, never the request body.</done>
  <acceptance_criteria>
    - `verified_by`/`verified_at` in the response always come from the trigger, never from request body echo (test asserts the response values differ from anything the request body could have supplied, since the body never includes those two fields at all).
    - A viewer's PATCH attempt returns 403 and leaves the row's `verification_status` unchanged.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: API-04 — GET /workspaces/{id}/graph (bounded neighborhood read)</name>
  <files>apps/api/src/api/routers/graph.py, apps/api/src/api/main.py, apps/api/tests/test_graph_router.py</files>
  <read_first>
    apps/api/src/api/routers/retrieval.py (full file — the RPC-calling route shape you are mirroring: query params -> Pydantic-validated request -> db.rpc(...) -> response model),
    apps/api/src/api/services/retrieval.py lines 230-262 (RetrievalService._graph() — the malformed-row defense pattern: `if not isinstance(rows, list): raise ValueError("malformed")` before trusting the RPC's shape),
    apps/api/tests/test_hybrid_search_integration.py (full file, 39 lines — the exact cross-tenant real-DB RPC test pattern to mirror: two_workspaces_two_users + user_db fixtures, assert a foreign workspace_id returns empty rows, not an error)
  </read_first>
  <behavior>
    - Test (real DB, mirrors test_hybrid_search_integration.py): calling wiki_graph_neighborhood via UserDb.rpc() with a foreign workspace_id returns an empty list, not an error, from both directions of a two-actor pair.
    - Test: GET /workspaces/{id}/graph?seed_wiki_id=<uuid> against a seed with two real, resolved outgoing wiki_links (created directly via UserDb.insert_one, owner has editor+ implicitly) returns both edges with depth 1.
    - Test: fanout/total_limit query params outside the migration's 1..20 / 1..200 bounds return 422 before the RPC is even called (Pydantic Field(ge=..., le=...) validation, not a round trip to see the SQL exception).
  </behavior>
  <action>
    apps/api/src/api/routers/graph.py (new): APIRouter(prefix="/workspaces", tags=["graph"]), _bearer = HTTPBearer(), local _user_db() helper. class GraphEdge(BaseModel): from_wiki_id: str; to_wiki_id: str; depth: int. class GraphResponse(BaseModel): edges: list[GraphEdge]. @router.get("/{workspace_id}/graph") async def graph_neighborhood(workspace_id: UUID, seed_wiki_id: UUID, request: Request, credentials: Annotated[HTTPAuthorizationCredentials, Depends(_bearer)], fanout: Annotated[int, Query(ge=1, le=20)] = 10, total_limit: Annotated[int, Query(ge=1, le=200)] = 100) -> GraphResponse: db = _user_db(...); rows = await db.rpc("wiki_graph_neighborhood", params={"p_workspace_id": str(workspace_id), "p_seed_wiki_id": str(seed_wiki_id), "p_fanout": fanout, "p_total_limit": total_limit}); if not isinstance(rows, list): raise HTTPException(status_code=502, detail="graph_unavailable") (mirror RetrievalService._graph()'s malformed-row defense — this is a real, not decorative, guard since PostgREST error shapes vary); return GraphResponse(edges=[GraphEdge(**row) for row in rows]).

    apps/api/src/api/main.py: import graph_router from api.routers.graph, app.include_router(graph_router).

    In this same file's module docstring or a comment near the router, cite D-07.2's audit conclusion for API-04's job-status half: `apps/api/src/api/routers/jobs.py::list_source_jobs` already returns per-job `chain_position`/`chain_total` for every job tied to a `raw_source_id` (Phase 2/3, ING-06) — this already IS the "aggregate progress across parse→compile→link_sync→embed" API-04 asks for. No new job endpoint is added by this plan; this comment is the documented decision the decision-coverage gate should find, not a TODO.
  </action>
  <verify>
    <automated>uv run pytest apps/api/tests/test_graph_router.py -x</automated>
  </verify>
  <done>GET .../graph exists, is bounded and RLS-scoped, and the job-status half of API-04 is a documented audit conclusion rather than a new endpoint.</done>
  <acceptance_criteria>
    - A cross-tenant `seed_wiki_id`/`workspace_id` combination returns an empty edge list, not an error or another tenant's edges.
    - `fanout`/`total_limit` outside their bounds return 422 without a database round trip.
    - No new endpoint was added to `apps/api/src/api/routers/jobs.py` — the audit conclusion (existing endpoint suffices) is documented, not silently assumed.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated (any role) -> `GET /workspaces/{id}/graph` | Read-only, RLS-scoped, bounded traversal |
| authenticated (editor+ only) -> `PATCH /workspaces/{id}/wiki/{wiki_id}/verify` | Write, RLS-gated by `wiki_pages_update_editor`, audit fields trigger-stamped |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-13 | Information Disclosure | `apps/api/src/api/routers/wiki.py` (verify endpoint) | medium | mitigate | Reuses the existing single-registration "0 rows affected -> 403, no distinguishing 404" convention (`api/errors.py::WorkspaceForbidden`) — a non-existent `wiki_id` and a cross-tenant `wiki_id` render identically, per 02-CONTEXT.md > D-12. |
| T-05-14 | Elevation of Privilege | `apps/api/src/api/routers/wiki.py` (role enforcement) | high | mitigate | Zero new authorization code in the router — `wiki_pages_update_editor` RLS policy is the only gate, verified by the viewer-403 test in Task 1. |
| T-05-15 | Denial of Service | `apps/api/src/api/routers/graph.py` | medium | mitigate | `fanout`/`total_limit` are Pydantic-bounded (1..20 / 1..200) before the RPC call, and the RPC itself re-asserts the same bounds server-side (defense in depth against a client that bypasses the API's own validation). |
| T-05-SC | Tampering | npm/pip/cargo installs | high | accept | No new package this plan. |
</threat_model>

<verification>
`uv run pytest apps/api/tests/test_workspaces_isolation.py apps/api/tests/test_graph_router.py -x` green against the local stack (requires `05-02`'s migration already applied), then full suite `uv run pytest`.
</verification>

<success_criteria>
An editor can verify a page and the audit trail is database-guaranteed. Any member can browse a bounded graph neighborhood without being able to request an unbounded one. The job-status half of API-04 is a documented "no gap found" conclusion, not silent scope creep into a new endpoint nobody asked for.
</success_criteria>

<output>
Create `.planning/phases/05-citation-integrity-and-answer-apis/05-05-SUMMARY.md` when done
</output>
