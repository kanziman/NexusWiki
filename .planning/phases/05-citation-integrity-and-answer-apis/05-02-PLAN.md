---
phase: 05-citation-integrity-and-answer-apis
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0012_ask_citation_and_graph.sql
autonomous: true
requirements: [API-04, QC-01, QC-02]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The four seeded ask prompt templates instruct the model to copy the alias shown at the head of each context block, not a real slug or chunk UUID, so D-02's anchor scheme is not silently defeated by conflicting prompt text."
    - "wiki_pages.verified_by/verified_at are always set by the database from auth.uid()/now() when verification_status changes, never trusted from client-supplied request values."
    - "A bounded, RLS-enforced graph-read RPC and a bounded conflict-candidate RPC exist for later plans to call — neither is security definer, so RLS remains the tenant boundary."
  artifacts:
    - supabase/migrations/0012_ask_citation_and_graph.sql
    - public.wiki_graph_neighborhood(uuid, uuid, int, int)
    - public.find_similar_wiki_pages(uuid, uuid, double precision, int)
    - public.stamp_wiki_verification()
  key_links:
    - "public.wiki_graph_neighborhood -> apps/api/src/api/routers/graph.py (05-05-PLAN.md)"
    - "public.find_similar_wiki_pages -> apps/worker/src/worker/handlers/conflict.py (05-06-PLAN.md)"
    - "public.stamp_wiki_verification trigger -> apps/api/src/api/routers/wiki.py (05-05-PLAN.md)"
---

<objective>
Land the one migration this phase needs — `0012_ask_citation_and_graph.sql` — before any plan that depends on its RPCs or its trigger can run: (1) fix the four seeded `target_type='ask'` prompt templates' citation instruction text so it matches D-02's short-alias scheme instead of the pre-D-02 real-slug/chunk-UUID wording (D-10), and add a language-follows-question instruction to the same templates (API-03, satisfies the "system-prompt instruction" half of D-08 as data rather than app code), (2) add `wiki_graph_neighborhood` — a new, independently-versioned, bounded graph-read RPC for API-04/D-07.1/D-11, (3) add `find_similar_wiki_pages` — a bounded pgvector cosine-candidate RPC for QC-01/D-05's conflict-detection trigger, and (4) add a `BEFORE UPDATE` trigger on `wiki_pages` that stamps `verified_by`/`verified_at` from `auth.uid()`/`now()` whenever `verification_status` transitions, so QC-02's "who/when" is a database guarantee, not something the API layer has to assert by decoding a JWT.

Purpose: schema changes are append-only and this project's convention requires the push to land before anything downstream depends on it (see the schema-push requirement below); doing it as its own Wave-1 plan lets every other Wave-2+ plan that needs these three objects simply depend on this plan number instead of re-deriving SQL.
Output: `supabase/migrations/0012_ask_citation_and_graph.sql`, applied to both the local stack and Supabase Cloud.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-citation-integrity-and-answer-apis/05-CONTEXT.md
@.planning/phases/05-citation-integrity-and-answer-apis/05-RESEARCH.md
@supabase/migrations/0006_seed_prompts.sql
@supabase/migrations/0007_search_and_queue_extensions.sql
@supabase/migrations/0009_pipeline_ops.sql
@supabase/migrations/0011_retrieval.sql
@supabase/migrations/0004_rls_policies.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 0012 — ask template fix, graph RPC, conflict RPC, verification trigger</name>
  <files>supabase/migrations/0012_ask_citation_and_graph.sql</files>
  <read_first>
    supabase/migrations/0006_seed_prompts.sql (full file — the four ask templates' exact system_prompt text; line 152's "위키 근거는 `[[wiki:slug]]`, 원문 근거는 `[[src:청크id]]` 형식 그대로 쓰세요" and the parallel phrasing at lines 185/221/255 — do not attempt a literal find/replace across four differently-worded passages, append instead),
    supabase/migrations/0011_retrieval.sql (full file — expand_wiki_graph's exact recursive-CTE shape at lines 163-215 to mirror for wiki_graph_neighborhood, and search_wiki_embeddings' HNSW GUC triad at lines 64-87 to mirror for find_similar_wiki_pages),
    supabase/migrations/0007_search_and_queue_extensions.sql lines 236-260 (verified_by/verified_at/expires_at column definitions and their on-delete-set-null rationale — the trigger you add must not conflict with account-deletion set-null behavior),
    supabase/migrations/0009_pipeline_ops.sql lines 96-145 (usage_events table + RLS — grep pattern for "01" is not needed here; used only as a style reference for section banner comments and ACL revoke/grant pairing),
    supabase/migrations/0004_rls_policies.sql lines 108-148 (protect_owner_membership — the existing BEFORE trigger pattern on a related table; stamp_wiki_verification follows the same security invoker + set search_path = public shape)
  </read_first>
  <action>
    Write a single transaction (begin/commit) migration file numbered 0012, following 0011's section-banner comment style, in four numbered sections.

    Section 1 — ask template citation/language fix (D-10, API-03): a single idempotent-in-intent UPDATE public.prompt_templates SET system_prompt = system_prompt || E'\n\n## 인용 형식 (중요)\n\n각 컨텍스트 항목 머리에 표시된 [[wiki:wN]] 또는 [[src:sN]] 형태의 짧은 별칭을 그대로 복사해 인용하세요. 슬러그나 청크 id를 직접 쓰지 마세요 — 표시된 별칭 외의 형식은 인용으로 인정되지 않습니다.\n\n## 답변 언어\n\n질문과 같은 언어로 답변하세요.' WHERE target_type = 'ask' AND workspace_id IS NULL — append rather than attempting a literal-substring replace() across the four differently-worded system_prompt bodies (0006's four templates each phrase the old instruction differently at lines 152/185/221/255; an appended, unambiguous closing instruction is the deterministic and low-risk fix, and it is placed last so it is the model's most recent instruction). Add a comment on the statement citing 05-CONTEXT.md > D-10.

    Section 2 — public.wiki_graph_neighborhood(p_workspace_id uuid, p_seed_wiki_id uuid, p_fanout int default 10, p_total_limit int default 100) returns table (from_wiki_id uuid, to_wiki_id uuid, depth int), language plpgsql, security invoker, stable, set search_path = public. Copy expand_wiki_graph's exact recursive-CTE mechanics (same depth < 2 cap, same cross join lateral fan-out limit, same `not edge.to_wiki_id = any(walk.path)` cycle guard) but select edge triples (from_wiki_id, to_wiki_id, depth), not node-only rows, so a future graph UI has edges to draw (D-07.1). Bound checks: raise exception with errcode 22023 if p_fanout not in 1..20 or p_total_limit not in 1..200 (wider than expand_wiki_graph's retrieval-tuned 1..5/1..50 bounds, per D-11's "dashboard browse is a single explicit user action, not a per-request retrieval cost multiplier" reasoning — 05-RESEARCH.md's sketch at lines 288-338 is ready to copy near-verbatim, verify column names against wiki_links' actual schema: from_wiki_id, target_slug, to_wiki_id, resolved). Version this independently of hybrid-rrf-v1/POLICY_VERSION — it has nothing to do with retrieval fusion. Grant: revoke all on function public.wiki_graph_neighborhood(uuid, uuid, int, int) from public, anon, service_role; grant execute ... to authenticated (matches search_chunks/expand_wiki_graph's exact revoke/grant pairing — service_role explicitly excluded, this is a user-facing read).

    Section 3 — public.find_similar_wiki_pages(p_workspace_id uuid, p_wiki_id uuid, p_similarity_threshold double precision default 0.88, p_limit int default 5) returns table (candidate_wiki_id uuid, similarity double precision), language sql, security invoker, stable, set search_path = public, set hnsw.iterative_scan = 'strict_order', set hnsw.ef_search = '200', set hnsw.max_scan_tuples = '40000' (copy this exact GUC triad from search_wiki_embeddings — 05-RESEARCH.md Pitfall 4 warns a new ad hoc cosine query silently falls back to worse-than-benchmarked pgvector defaults without it). Body: select other.wiki_id, max(1 - (mine.embedding <=> other.embedding)) as similarity from public.wiki_embeddings mine join public.wiki_embeddings other on other.workspace_id = mine.workspace_id and other.wiki_id <> mine.wiki_id where mine.workspace_id = p_workspace_id and mine.wiki_id = p_wiki_id and mine.embedding is not null and other.embedding is not null group by other.wiki_id having max(1 - (mine.embedding <=> other.embedding)) >= p_similarity_threshold order by similarity desc limit least(greatest(p_limit, 1), 20) — use the extensions.vector cosine operator exactly as search_wiki_embeddings does (`operator(extensions.<=>)`), not a bare `<=>`, to stay schema-qualified regardless of search_path. Document in a comment that this compares one page's chunk embeddings against every other page's chunk embeddings in the workspace (bounded to the workspace, not the whole database) and that the 0.88 default is a documented starting assumption pending empirical validation against a real corpus (05-RESEARCH.md Assumption A3), not a literature-backed constant — mirrors how 03-03-PLAN.md's chunking constants were exposed for the same reason. Grant: revoke all from public, anon, authenticated; grant execute to service_role only (this RPC is called exclusively from the worker's conflict-detection job, never from a user request path).

    Section 4 — verification-transition audit trigger (QC-02/D-06): create or replace function public.stamp_wiki_verification() returns trigger language plpgsql security invoker set search_path = public as a function that, only when new.verification_status is distinct from old.verification_status, sets new.verified_by := auth.uid() and new.verified_at := now(), then returns new — this runs regardless of whatever verified_by/verified_at values a client might have sent, so those two columns are always database-asserted, never client-trusted. Verify this does NOT fire on the worker's recompile upsert path: `_upsert_page()` (worker/handlers/compile.py) deliberately omits verification_status from its upsert payload, so on a conflict-triggered UPDATE the column is left unchanged and `new.verification_status is distinct from old.verification_status` evaluates false — add a one-line comment noting this invariant explicitly so a future migration does not accidentally make compile's upsert touch verification_status. create trigger wiki_pages_stamp_verification before update on public.wiki_pages for each row execute function public.stamp_wiki_verification(). No ACL section needed (triggers fire under the invoking role's existing UPDATE permission, already gated by wiki_pages_update_editor).

    End with `notify pgrst, 'reload schema';` then `commit;` exactly as 0011 does.
  </action>
  <verify>
    <automated>cat supabase/migrations/0012_ask_citation_and_graph.sql | /usr/bin/grep -c "create or replace function\|create trigger" </automated>
  </verify>
  <done>Migration file 0012 exists with all four sections (ask-template append, wiki_graph_neighborhood, find_similar_wiki_pages, stamp_wiki_verification trigger), each security invoker/stable where applicable, correct HNSW GUCs, and correct ACL grants.</done>
  <acceptance_criteria>
    - File exists at `supabase/migrations/0012_ask_citation_and_graph.sql`, wrapped in a single `begin`/`commit` transaction.
    - `wiki_graph_neighborhood` and `find_similar_wiki_pages` are both `security invoker` (grep confirms no `security definer` anywhere in the file — a `definer` here would silently reopen the exact cross-tenant hole `search_chunks`' in-file warning exists to prevent).
    - `find_similar_wiki_pages` sets all three HNSW GUCs (`hnsw.iterative_scan`, `hnsw.ef_search`, `hnsw.max_scan_tuples`) inside the function definition.
    - `stamp_wiki_verification()` only reads/writes `new`/`old.verification_status`, `verified_by`, `verified_at` — it does not touch `disputed` or `explored`.
  </acceptance_criteria>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] Apply migration 0012 to local stack and Cloud</name>
  <files>supabase/migrations/0012_ask_citation_and_graph.sql</files>
  <read_first>CLAUDE.md (Dependencies section — local ports 544xx, no local psql, use docker exec -it supabase_db_NexusWiki psql -U postgres -d postgres)</read_first>
  <action>
    Run `supabase db reset` from the repo root to rebuild the local stack from 0001 through 0012 and catch any SQL error before it reaches Cloud. Then run `supabase db push --yes` to apply 0012 to the linked Supabase Cloud project (non-interactive; the project has already had migrations 0008-0011 pushed the same way in prior phases). If `supabase db push` still prompts interactively despite `--yes` (CLI version drift), stop and surface the exact prompt text rather than guessing an answer — this is the one path in this task that may need a human in the loop, per the phase's schema-push requirement.
  </action>
  <verify>
    <automated>docker exec -i supabase_db_NexusWiki psql -U postgres -d postgres -c "select proname from pg_proc where proname in ('wiki_graph_neighborhood','find_similar_wiki_pages','stamp_wiki_verification') order by proname;" | /usr/bin/grep -c "wiki_graph_neighborhood\|find_similar_wiki_pages\|stamp_wiki_verification"</automated>
  </verify>
  <done>Local stack rebuilds cleanly through 0012 and Cloud is pushed to the same state; all three new functions and the corrected ask-template text are observable via psql.</done>
  <acceptance_criteria>
    - `supabase db reset` completes with no error, applying migrations 0001-0012 in order on the local stack.
    - `supabase db push --yes` completes with no unresolved interactive prompt, or the task explicitly surfaces the prompt text and halts rather than guessing.
    - All three new functions (`wiki_graph_neighborhood`, `find_similar_wiki_pages`, `stamp_wiki_verification`) exist in `pg_proc` on the local stack after reset.
    - `select system_prompt from prompt_templates where target_type='ask' and workspace_id is null limit 1` contains the string `[[wiki:wN]]` (the corrected instruction), confirming D-10 landed.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated (RLS) -> `wiki_graph_neighborhood` | Requester-scoped read of the wiki-link graph, bounded fan-out/depth/cycle-guard |
| service_role -> `find_similar_wiki_pages` | Worker-only candidate-pair read across a workspace's own `wiki_embeddings`, never cross-workspace |
| any writer -> `wiki_pages.verified_by`/`verified_at` | Client-supplied values for these two columns are structurally overridden by the trigger, never trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Elevation of Privilege | `public.wiki_graph_neighborhood` | high | mitigate | `security invoker` (not `definer`) so RLS remains the tenant boundary — matches `search_chunks`'/`expand_wiki_graph`'s existing in-file warning against the reverse mistake; verified by grep in Task 1's acceptance criteria. |
| T-05-07 | Elevation of Privilege | `public.find_similar_wiki_pages` | high | mitigate | `security invoker` + explicit `p_workspace_id` predicate on both sides of the self-join, and `EXECUTE` is granted to `service_role` only (never `authenticated`), so no user request path can invoke a cross-workspace comparison. |
| T-05-08 | Tampering | `public.wiki_pages.verified_by`/`verified_at` | medium | mitigate | `stamp_wiki_verification()` trigger unconditionally overwrites these two columns from `auth.uid()`/`now()` on any `verification_status` transition, regardless of client-supplied values — closes the "who/when" spoofing vector QC-02 exists to prevent. |
| T-05-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install in this plan — pure SQL migration. |
</threat_model>

<verification>
`supabase db reset` exits 0 and the three new functions plus the updated ask-template text are observable via `psql` against the local stack; `supabase db push --yes` leaves Cloud in the same state.
</verification>

<success_criteria>
Every downstream plan in this phase that calls `wiki_graph_neighborhood`, `find_similar_wiki_pages`, or relies on `stamp_wiki_verification` can do so against a real, already-applied schema — no plan after this one needs to touch `supabase/migrations/`.
</success_criteria>

<output>
Create `.planning/phases/05-citation-integrity-and-answer-apis/05-02-SUMMARY.md` when done
</output>