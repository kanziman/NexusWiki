---
phase: 05-citation-integrity-and-answer-apis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/nexuswiki_core/citations.py
  - packages/core/tests/test_citations.py
  - packages/core/tests/test_settings.py
  - apps/worker/src/worker/query_embedding.py
  - apps/worker/src/worker/llm.py
  - apps/worker/src/worker/llm_stream.py
  - apps/worker/src/worker/__main__.py
  - apps/worker/src/worker/settings.py
  - apps/worker/tests/test_llm_stream.py
  - apps/worker/tests/test_settings.py
  - apps/worker/tests/test_worker_main.py
  - apps/api/src/api/settings.py
  - apps/api/src/api/services/ask.py
  - apps/api/src/api/routers/ask.py
  - apps/api/src/api/main.py
  - apps/api/tests/test_ask_router.py
autonomous: true
requirements: [CITE-01, CITE-02, CITE-03, CITE-04, API-01, API-03]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "A user who calls POST /workspaces/{id}/ask with a grounded question receives a real streamed answer whose citations are the intersection of what the model actually wrote and what the server actually issued, not the raw retrieval hit list (CITE-02)."
    - "A user whose question retrieves zero evidence gets an explicit 근거를 찾지 못했습니다 response instead of a hallucinated answer or a hung connection (CITE-04)."
    - "The browser never sees a real wiki/source UUID inside a citation anchor before resolution — only short server-issued aliases (CITE-01)."
    - "apps/api never holds OPENROUTER_API_KEY; only apps/worker's private listener calls OpenRouter (D-01, mirrors 02-CONTEXT.md > D-06)."
  artifacts:
    - packages/core/src/nexuswiki_core/citations.py
    - apps/worker/src/worker/llm_stream.py
    - apps/api/src/api/services/ask.py
    - apps/api/src/api/routers/ask.py
  key_links:
    - "apps/api/src/api/routers/ask.py -> apps/api/src/api/services/ask.py -> apps/worker/src/worker/llm_stream.py (private HTTP, bearer token) -> apps/worker/src/worker/llm.py::stream_chat() -> OpenRouter"
    - "apps/api/src/api/services/ask.py::build_issuance_map() -> packages/core/src/nexuswiki_core/citations.py::ISSUED_ANCHOR_PATTERN -> resolve_citations()"
---

<objective>
Build the single, real, end-to-end Ask path: a workspace member calls `POST /workspaces/{id}/ask`, the server issues short citation aliases over the Phase-4 evidence list, streams a real answer from the worker's private OpenRouter listener, and resolves citations by intersecting the model's parsed anchors against the aliases the server actually issued (D-02/D-03, CITE-01/02/03) — with the CITE-04 empty-evidence short-circuit and an SSE contract that carries `meta` → `delta*` → `citations` → `done` in that fixed order (API-01), using the workspace's default `ask` prompt template rendered with `render_template()` as-is so the answer language follows the question (API-03).

Purpose: this is the phase's tracer bullet — it proves the worker-owned-secret / API-relay boundary (D-01) and the anchor-alias citation design (D-02/D-03) actually work wired end-to-end, on real streaming HTTP, before any of the remaining CITE-05/CITE-06/API-02/API-04/QC-01/QC-02 work builds on top of it.
Output: A working `POST /workspaces/{id}/ask` endpoint an authenticated workspace member can call today and get a real, dual-citation-resolved streamed answer for the happy path plus the CITE-04 no-evidence path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-citation-integrity-and-answer-apis/05-CONTEXT.md
@.planning/phases/05-citation-integrity-and-answer-apis/05-RESEARCH.md
@.planning/phases/05-citation-integrity-and-answer-apis/05-PATTERNS.md
@docs/architecture/query-embedding-boundary.md
@apps/worker/src/worker/query_embedding.py
@apps/worker/src/worker/llm.py
@apps/worker/src/worker/__main__.py
@apps/worker/src/worker/settings.py
@apps/api/src/api/settings.py
@apps/api/src/api/services/retrieval.py
@apps/api/src/api/routers/retrieval.py
@apps/api/src/api/main.py
@apps/api/src/api/db/user.py
@apps/api/src/api/errors.py
@packages/core/src/nexuswiki_core/rrf.py
@apps/worker/tests/test_query_embedding.py
@apps/worker/tests/test_worker_main.py
@packages/core/tests/test_settings.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "Ask a grounded question, get a dual-cited streamed answer" — one path only</name>
  <files>
    packages/core/src/nexuswiki_core/citations.py,
    packages/core/tests/test_citations.py,
    packages/core/tests/test_settings.py,
    apps/worker/src/worker/query_embedding.py,
    apps/worker/src/worker/llm.py,
    apps/worker/src/worker/llm_stream.py,
    apps/worker/src/worker/__main__.py,
    apps/worker/src/worker/settings.py,
    apps/worker/tests/test_llm_stream.py,
    apps/worker/tests/test_settings.py,
    apps/worker/tests/test_worker_main.py,
    apps/api/src/api/settings.py,
    apps/api/src/api/services/ask.py,
    apps/api/src/api/routers/ask.py,
    apps/api/src/api/main.py,
    apps/api/tests/test_ask_router.py
  </files>
  <read_first>
    docs/architecture/query-embedding-boundary.md (Required invariants + Rate-limit decision sections — the token-bucket bug fixed in commit 6a14144, do not reintroduce),
    apps/worker/src/worker/query_embedding.py (full file — QueryEmbeddingService, create_query_embedding_app, _reserve_token()),
    apps/worker/src/worker/llm.py (openrouter_client() lines 81-106, render_template() lines 109-135, _chat_body() lines 214-242, _send() lines 289-296 — reuse pattern, do not rewrite complete_structured()),
    apps/worker/src/worker/__main__.py (full file — _serve_query_embeddings lines 37-59, its TaskGroup wiring at line 172),
    apps/worker/tests/test_worker_main.py (the one existing test — monkeypatches create_query_embedding_app and calls _serve_query_embeddings directly; must be updated, not duplicated),
    apps/api/src/api/services/retrieval.py (full file — HttpQueryEmbeddingClient lines 29-62 DI shape, RetrievalResult dataclass lines 65-68, per-call timeout override pattern),
    apps/api/src/api/routers/retrieval.py (full file — _user_db() helper, lazy service-init pattern, HTTPBearer wiring — Ask's router copies this shape),
    apps/api/src/api/db/user.py (select()/rpc() — note select()'s _as_filters is eq-only; for multi-id content fetch you will need a narrow documented seam like apps/api/src/api/routers/jobs.py:100-104's _usage_rows_since, using db._client/_base_url/_headers/_payload directly with an "in.(...)" filter, not a new UserDb method),
    apps/api/src/api/main.py (full file — create_app(), the 2.0s app.state.http_client timeout and its "LLM 호출은 워커의 일이다" comment at line ~53-59 — do NOT reuse that client for the worker call),
    apps/api/src/api/errors.py (register_error_handlers — no new exception type needed for this task; CITE-04 and upstream-failure paths are handled inline in the SSE generator, not via the exception-handler registry, because a StreamingResponse has already started once the generator begins),
    packages/core/src/nexuswiki_core/rrf.py (EvidenceHit — kind/evidence_id/document_id/metadata; source hits carry raw_source_id+chunk_index in metadata, wiki hits carry chunk_index/slug/title but NEITHER carries body text — you must fetch content separately),
    05-RESEARCH.md Architecture Patterns > Pattern 1 (worker listener topology), Pattern 2 (two-regex grammar — copy the BROAD_ANCHOR_PATTERN/ISSUED_ANCHOR_PATTERN/strip_forged_anchors() code verbatim from lines 253-272), Pattern 5 (SSE relay code sketch, lines 346-370 — copy the aiter_lines()/"data: " parsing verbatim, do not re-derive OpenRouter's SSE shape), Code Examples (build_issuance_map lines 429-448, resolve_citations lines 452-479 — use these as the CITE-01/02/03 starting point, drop the CITE-05 sentence-metric lines for this task, they land in 05-03-PLAN.md), Empty-evidence short circuit (lines 503-512),
    05-PATTERNS.md sections for llm_stream.py, __main__.py, llm.py, services/ask.py, routers/ask.py (exact line-anchored excerpts to copy)
  </read_first>
  <behavior>
    - Test 1 (worker, test_llm_stream.py): an unauthenticated POST to /internal/llm-chat is rejected 401 before any OpenRouter call is attempted (mirrors test_query_embedding.py::test_private_endpoint_rejects_missing_token_before_embedding).
    - Test 2 (worker, test_llm_stream.py): an authenticated POST relays the injected chat_stream function's bytes through StreamingResponse unchanged, bounded by asyncio.wait_for per Pitfall 5 (do not let ASGITransport streaming hang the test).
    - Test 3 (worker, test_llm_stream.py): exhausting the token bucket returns 429 without calling the injected chat_stream function (mirrors the query-embedding rate-limit test shape, smaller parameter set is fine — full edge-case parity is not required here).
    - Test 4 (packages/core, test_citations.py): ISSUED_ANCHOR_PATTERN matches "[[wiki:w1]]"/"[[src:s12]]" and rejects "[[wiki:homepage]]"/"[[src:not-a-real-id]]"; BROAD_ANCHOR_PATTERN matches both; strip_forged_anchors() removes anything BROAD-shaped.
    - Test 5 (api, test_ask_router.py): given evidence is empty (RetrievalService stub returns no evidence), POST /workspaces/{id}/ask streams exactly meta -> citations{text=NO_EVIDENCE_MESSAGE, cited_anchor_count=0} -> done, with zero calls made to the injected worker-stream client (CITE-04, no provider spend).
    - Test 6 (api, test_ask_router.py): given evidence is non-empty and a stubbed worker stream yields deltas containing one real ISSUED_ANCHOR_PATTERN token and one fabricated one, the response streams meta -> delta* -> citations{resolved has the real one, fabricated_anchor_count==1, the fabricated token is stripped from any rendered text} -> done, in that exact event-name order.
  </behavior>
  <action>
    Build the vertical slice in dependency order, worker first, API second, then wire them together.

    packages/core/src/nexuswiki_core/citations.py (new): define BROAD_ANCHOR_PATTERN = re.compile(r"\[\[(?:wiki|src):[^\[\]]*\]\]"), ISSUED_ANCHOR_PATTERN = re.compile(r"\[\[(wiki:w\d+|src:s\d+)\]\]"), and strip_forged_anchors(text: str) -> str = BROAD_ANCHOR_PATTERN.sub("", text) — copy from 05-RESEARCH.md Pattern 2 verbatim, matching the module's existing stdlib-only pure-function house style (no classes, matches tokenizer.py's shape). strip_forged_anchors() is not called from anywhere yet in this task (CITE-06's parse.py wiring is 05-03-PLAN.md) — it only needs to exist and be tested here so 05-03 has no new regex design work, only a call site.

    apps/worker/src/worker/query_embedding.py: extract the route body currently inline in create_query_embedding_app() (lines 121-126) into a new exported function add_query_embedding_route(app: FastAPI, service: QueryEmbeddingService) -> None that registers the same POST /internal/query-embedding route on a passed-in app. Keep create_query_embedding_app(service) as a thin wrapper (build FastAPI(openapi_url=None, docs_url=None, redoc_url=None), call add_query_embedding_route(app, service), return app) so every existing call site (including test_query_embedding.py) keeps working unchanged. Add add_query_embedding_route to __all__.

    apps/worker/src/worker/llm.py: add stream_chat(client: httpx.AsyncClient, *, settings: WorkerSettings, messages: list[dict[str, str]]) -> AsyncIterator[bytes], a sibling of complete_structured() (do not modify complete_structured or _chat_body). Build the request body via a new _chat_stream_body(*, settings, messages) -> dict[str, Any] returning {"model": settings.LLM_MODEL, "messages": messages, "stream": True, "usage": {"include": True}} (no response_format — streaming and structured-JSON-with-retry are different call shapes per 05-RESEARCH.md's State of the Art table). Open client.stream("POST", "/chat/completions", json=body) against the OpenRouter base URL already baked into the injected client (reuse openrouter_client() unchanged for construction); if response.status_code >= 400 raise ProviderError(provider=PROVIDER_NAME, status_code=response.status_code, kind="chat_completion") before iterating the body. Otherwise async for line in response.aiter_lines(): if line: yield (line + "\n").encode("utf-8") — this is a transparent byte relay of OpenRouter's own SSE frames (do NOT parse choices[0].delta.content here; that parsing belongs to the API side per 05-RESEARCH.md Pattern 5's code example, which already shows the API doing exactly that against these same "data: {...}" lines). Add stream_chat to __all__.

    apps/worker/src/worker/llm_stream.py (new): mirror query_embedding.py's shape exactly. LlmChatRequest(BaseModel, model_config=ConfigDict(extra="forbid", str_strip_whitespace=True)) with workspace_id: str and messages: list[dict[str, str]]. ChatStreamFunction = Callable[[LlmChatRequest], AsyncIterator[bytes]]. LlmStreamService.__init__ takes chat_stream: ChatStreamFunction, *, internal_token: str, max_concurrency: int = 2, rate_capacity: int = 20, refill_tokens_per_second: float = 0.2, monotonic: MonotonicClock = time.monotonic — construct its own asyncio.Semaphore(max_concurrency), its own _reserve_token() token bucket copied verbatim from query_embedding.py lines 70-84 (NOT a lifetime counter — this is the exact bug commit 6a14144 fixed once already). async def stream(self, request: LlmChatRequest, authorization: str | None) -> AsyncIterator[bytes]: authenticate (authorization != f"Bearer {self._internal_token}" -> HTTPException(401, "internal_unauthorized")) BEFORE reserving a token, then await self._reserve_token(), then async with self._semaphore: async for chunk in self._chat_stream(request): yield chunk. Budget-cap and usage-event recording are explicitly OUT of this task's scope (they land in 05-04-PLAN.md as a wrapping concern around this same stream() method) — do not add a ServiceDb/service_client dependency here yet. def add_llm_stream_route(app: FastAPI, service: LlmStreamService) -> None: @app.post("/internal/llm-chat") async def llm_chat(request: LlmChatRequest, authorization: Annotated[str | None, Header()] = None) -> StreamingResponse: return StreamingResponse(service.stream(request, authorization), media_type="text/event-stream").

    apps/worker/src/worker/settings.py: add to WorkerSettings — LLM_STREAM_INTERNAL_TOKEN: str | None = None (distinct value from QUERY_EMBEDDING_INTERNAL_TOKEN per D-01, same "None is safer than unauthenticated" contract), LLM_STREAM_MAX_CONCURRENCY: int = 2, LLM_STREAM_RATE_CAPACITY: int = Field(default=20, gt=0), LLM_STREAM_RATE_REFILL_TOKENS_PER_SECOND: float = Field(default=0.2, gt=0) — placed next to the existing QUERY_EMBEDDING_* block with a one-line comment noting the lower concurrency/rate reflects 120s streaming calls vs 5s embedding calls (05-RESEARCH.md Pattern 1).

    apps/worker/src/worker/__main__.py: rename _serve_query_embeddings to _serve_internal_listeners(settings: WorkerSettings, stop: asyncio.Event) -> None. If neither settings.QUERY_EMBEDDING_INTERNAL_TOKEN nor settings.LLM_STREAM_INTERNAL_TOKEN is set, return (no listener at all — same fail-safe as before). Otherwise build ONE app = FastAPI(openapi_url=None, docs_url=None, redoc_url=None); if settings.QUERY_EMBEDDING_INTERNAL_TOKEN: add_query_embedding_route(app, QueryEmbeddingService(...)) (same construction as today); if settings.LLM_STREAM_INTERNAL_TOKEN: add_llm_stream_route(app, LlmStreamService(lambda request: stream_chat(<opened per-call openrouter_client(settings)>, settings=settings, messages=request.messages), internal_token=settings.LLM_STREAM_INTERNAL_TOKEN, max_concurrency=settings.LLM_STREAM_MAX_CONCURRENCY, rate_capacity=settings.LLM_STREAM_RATE_CAPACITY, refill_tokens_per_second=settings.LLM_STREAM_RATE_REFILL_TOKENS_PER_SECOND)) — build ONE uvicorn.Server(uvicorn.Config(app, host="0.0.0.0", port=8081, log_level="warning")) exactly as today (do NOT add a second server_task; 05-RESEARCH.md Pattern 1 explicitly flags a second uvicorn.Server as doubling SIGTERM-shutdown surface for no isolation benefit — railway.json's privateNetworking is per-service, not per-port). Because the injected chat_stream closure must open its own httpx.AsyncClient per call (stream_chat needs a live client to call .stream() on, and that client must stay open for the duration of the generator), write a small local async generator wrapper _stream_llm_chat(settings, request) that does `async with openrouter_client(settings) as client:\n    async for chunk in stream_chat(client, settings=settings, messages=request.messages):\n        yield chunk` and pass that as the chat_stream callable — mirrors _embed_query(settings, text)'s existing shape immediately above in this file. Update the TaskGroup wiring at the call site (line ~172) from _serve_query_embeddings to _serve_internal_listeners.

    apps/worker/tests/test_worker_main.py: update the one existing test to match the rename — monkeypatch worker_main.add_query_embedding_route and worker_main.add_llm_stream_route as no-op lambdas instead of worker_main.create_query_embedding_app, call worker_main._serve_internal_listeners(...), keep the FakeService/FakeServer shape and the rate-setting assertions unchanged.

    apps/api/src/api/settings.py: add to ApiSettings — LLM_STREAM_INTERNAL_URL: str | None = None, LLM_STREAM_INTERNAL_TOKEN: str | None = None, LLM_STREAM_TIMEOUT_SECONDS: float = 130.0 (worker's own OpenRouter timeout is 120s; this must be strictly larger so the API does not abort before the worker's own timeout would).

    packages/core/tests/test_settings.py: add "LLM_STREAM_INTERNAL_URL", "LLM_STREAM_INTERNAL_TOKEN", "LLM_STREAM_TIMEOUT_SECONDS" to NON_SECRET_API_FIELDS with a one-line dated comment (matches the file's existing review-ledger convention) — this test is a hard gate (test_api_settings_adds_only_reviewed_non_secret_fields) that will go red on the new ApiSettings fields otherwise.

    apps/api/src/api/services/ask.py (new): NO_EVIDENCE_MESSAGE: Final[str] = "근거를 찾지 못했습니다." — the exact phrase CITE-04 requires. HttpLlmStreamClient(client: httpx.AsyncClient, *, url: str | None, token: str | None, timeout_seconds: float) mirroring HttpQueryEmbeddingClient's DI shape (retrieval.py lines 29-62) with an async def stream(self, *, workspace_id: str, messages: list[dict[str, str]]) -> AsyncIterator[bytes] that opens self._client.stream("POST", f"{self._url.rstrip('/')}/internal/llm-chat", json={"workspace_id": workspace_id, "messages": messages}, headers={"Authorization": f"Bearer {self._token}"}, timeout=httpx.Timeout(connect=5.0, read=self._timeout_seconds, write=5.0, pool=5.0)) — do NOT reuse app.state.http_client's global 2.0s timeout (main.py line 33's comment explicitly says LLM calls are the worker's job and that timeout is calibrated for PostgREST). build_issuance_map(evidence: list[EvidenceHit]) -> dict[str, tuple[str, str]] copied from 05-RESEARCH.md lines 429-448 verbatim (enumerate wiki hits as w1,w2,... by document_id, source hits as s1,s2,... by evidence_id, in the evidence list's return order — not hashed, not truncated UUIDs). @dataclass(frozen=True, slots=True) class CitationResolution: resolved: list[dict[str, str]]; fabricated_anchor_count: int; cited_anchor_count: int; rendered_text: str — a real dataclass, not a bare dict, mirroring RetrievalResult's shape. resolve_citations(full_text: str, issuance: Mapping[str, tuple[str, str]]) -> CitationResolution: parse ISSUED_ANCHOR_PATTERN matches from full_text, normalize "wiki:w1"->"w1"/"src:s1"->"s1", intersect against issuance.keys() (CITE-02: parsed ∩ issued, never parsed ∩ raw search results), compute fabricated = parsed - issuance.keys() (CITE-03), strip every BROAD_ANCHOR_PATTERN-shaped span whose normalized alias is in fabricated from full_text to build rendered_text (leave genuinely resolved anchors in place — they are real citations, not fabrications), return CitationResolution(resolved=[{"alias": a, "kind": issuance[a][0], "id": issuance[a][1]} for a in sorted(resolved)], fabricated_anchor_count=len(fabricated), cited_anchor_count=len(resolved), rendered_text=rendered_text). This is CITE-01/02/03's complete implementation for this task; CITE-05's two ratio metrics are added to this SAME function in 05-03-PLAN.md, not re-derived. async def _fetch_evidence_content(user_db: UserDb, *, workspace_id: str, evidence: list[EvidenceHit]) -> dict[str, str] (keyed by evidence_id/document_id -> content text): batch the wiki document_ids and source evidence_ids separately, and for each non-empty batch call user_db._client.get(f"{user_db._base_url}/wiki_pages", params={"id": f"in.({','.join(ids)})", "workspace_id": f"eq.{workspace_id}", "select": "id,content"}, headers=user_db._headers) (respectively source_chunks with select "id,content") followed by user_db._payload(response) — mirror jobs.py:86-104's _usage_rows_since exact pattern and its `# noqa: SLF001` comments (UserDb's public select() is eq-only and cannot express an "in" filter; this is a documented, narrow seam, not a new UserDb method). class AskService: __init__(self, retrieval_service: RetrievalService, llm_client: HttpLlmStreamClient) -> None. async def ask(self, *, workspace_id: UUID, query: str, requested_k: int, template_id: str | None, user_db: UserDb) -> AsyncIterator[str]: call retrieval_service.retrieve(...) first; if not result.evidence: yield meta event {"no_evidence": True}, yield citations event {"text": NO_EVIDENCE_MESSAGE, "cited_anchor_count": 0, "fabricated_anchor_count": 0}, yield done event {}, return (CITE-04 — zero provider calls). Otherwise: build issuance map; fetch evidence content; select the workspace's default ask template via a two-step lookup mirroring worker/db/service.py's get_default_prompt_template (workspace-scoped is_default first, else global workspace_id-is-null default) using user_db.select("prompt_templates", match={...}) twice — template_id override is explicitly OUT of this task's scope (API-02, lands in 05-03-PLAN.md; for now always use the default). Build wiki_context/source_context blocks: for each issuance entry in alias order emit a header line "[[wiki:w1]] <title>" or "[[src:s1]] (raw_source_id chunk_index)" followed by the fetched content, joined with blank lines. Render user_prompt via nexuswiki_core is not imported here — reuse worker's render_template signature is worker-only; instead build the user message directly by substituting the SAME {{question}}/{{wiki_context}}/{{source_context}} placeholders using worker.llm.render_template is not importable from api (different workspace member) — implement a tiny local substitute using the identical regex-based single-scan approach (copy the _PLACEHOLDER regex and substitution function, do not use str.format, matching the CLAUDE.md anti-pattern warning) OR call the shared substitution logic if packages/core exposes one; if none exists, add a minimal render_template(template: str, values: Mapping[str, str]) -> str to packages/core/src/nexuswiki_core/citations.py's neighboring module is out of scope — place it as a small private helper _render(template, values) inside ask.py using the exact same regex/behavior as worker.llm.render_template (single re.sub scan, raises ValueError on an unmapped placeholder) so both processes enforce the identical "no str.format, no reprocessing" contract independently, since api and worker are separate deployable processes and must not import each other's modules. yield meta event {"template_id": template["id"], "template_name": template["name"], "evidence_count": len(result.evidence), "policy_version": result.meta.get("policy_version")}. Open llm_client.stream(workspace_id=str(workspace_id), messages=[{"role": "system", "content": template["system_prompt"]}, {"role": "user", "content": rendered_user_prompt}]) and inspect the first response status before iterating: if the underlying httpx response is an error (upstream.status_code >= 400), map 401->"internal_unauthorized", 402->"budget_exceeded", 429->"rate_limited", {502,504}->"llm_unavailable", anything else->"llm_unavailable", yield citations event {"error": <token>, "cited_anchor_count": 0, "fabricated_anchor_count": 0}, yield done event {}, return — this generic non-2xx handling is what 05-04-PLAN.md's budget gate (402) plugs into with zero further API-side changes. Otherwise async for line in upstream.aiter_lines(): if not line.startswith("data: ") or line == "data: [DONE]": continue; delta = json.loads(line[len("data: "):])["choices"][0]["delta"].get("content", ""); if delta: accumulate it, yield delta event {"text": delta} (copy this exact parsing from 05-RESEARCH.md Pattern 5, lines 346-370 — this is OpenRouter's own OpenAI-compatible SSE shape, do not re-derive it). After the stream ends, call resolve_citations(full_text, issuance), yield citations event carrying resolution.resolved/fabricated_anchor_count/cited_anchor_count, then done event.

    apps/api/src/api/routers/ask.py (new): copy retrieval.py's router shape exactly (APIRouter(prefix="/workspaces", tags=["ask"]), _bearer = HTTPBearer(), a local _user_db(request, credentials) helper — small intentional duplication per the project's established per-router convention, not extracted to a shared module). class AskRequest(BaseModel, model_config=ConfigDict(extra="forbid", str_strip_whitespace=True)): query: str = Field(min_length=1); requested_k: int = Field(default=8, ge=1, le=8) — reuse RETRIEVAL_MAX_QUERY_CHARS from settings for the same length validation retrieval.py already does (422 on invalid_query), do not add a new max-chars setting. @router.post("/{workspace_id}/ask") async def ask(...) -> StreamingResponse: lazily construct AskService the same way retrieval.py lazily constructs RetrievalService (via request.app.state, falling back to a fresh construction with HttpQueryEmbeddingClient + HttpLlmStreamClient built from request.app.state.http_client and settings.LLM_STREAM_INTERNAL_URL/_TOKEN/_TIMEOUT_SECONDS); return StreamingResponse(_format_sse(service.ask(...)), media_type="text/event-stream") where _format_sse(events: AsyncIterator[tuple[str, dict]]) -> AsyncIterator[str] renders each (name, payload) pair as f"event: {name}\ndata: {json.dumps(payload)}\n\n" — keep this framing function here, not in ask.py's service, so the service module stays framing-agnostic and independently testable (mirrors the plan's Test 5/6 asserting event NAMES and order, not raw SSE text).

    apps/api/src/api/main.py: import ask_router from api.routers.ask, app.include_router(ask_router) alongside the existing four routers.
  </action>
  <verify>
    <automated>uv run pytest packages/core/tests/test_citations.py packages/core/tests/test_settings.py apps/worker/tests/test_llm_stream.py apps/worker/tests/test_worker_main.py apps/worker/tests/test_settings.py apps/api/tests/test_ask_router.py -x</automated>
  </verify>
  <done>POST /workspaces/{id}/ask streams meta -> delta* -> citations -> done for grounded queries, and meta -> citations -> done for evidence-free queries, with citations computed as parsed ∩ issued (never raw evidence), fabricated anchors counted and stripped, and no OpenRouter secret reachable from apps/api.</done>
  <acceptance_criteria>
    - `POST /workspaces/{id}/ask` with a query that retrieves non-empty evidence streams SSE events in the exact order meta, delta (one or more), citations, done.
    - The `citations` event's resolved list is the intersection of parsed anchors and the request-scoped issuance map (CITE-02), never the raw retrieval evidence list.
    - A parsed anchor absent from the issuance map is counted in `fabricated_anchor_count` and does not appear as a working citation in `rendered_text` (CITE-03).
    - `POST /workspaces/{id}/ask` with empty evidence streams meta, citations{text=NO_EVIDENCE_MESSAGE}, done with zero calls to the worker stream client (CITE-04).
    - `apps/api/src/api/settings.py::ApiSettings` still has zero fields named `OPENROUTER_API_KEY`/`SUPABASE_SECRET_KEY`/`DATABASE_URL`/`OPENAI_API_KEY` (SEC-01 unbroken by this task).
    - `python -c "import worker.__main__"` and `python -c "import api.main"` both succeed with no import errors after the rename/refactor.
  </acceptance_criteria>
  <precondition>Phase 4's `RetrievalService`/`RpcUserDb` and `prompt_templates` seed rows (0006) already exist in the schema this task's tests stub against — no live DB call is required for this task's tests (all worker/API tests here use ASGITransport + injected fakes, matching test_query_embedding.py's fully-offline pattern).</precondition>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> apps/api `POST /workspaces/{id}/ask` | Untrusted request carrying a requester JWT; query text and requested_k are untrusted input |
| apps/api -> apps/worker `/internal/llm-chat` | Private-network-only hop, authenticated by a dedicated bearer token distinct from the query-embedding token (D-01) |
| apps/worker -> OpenRouter | Worker-held provider secret; the model's own output (the streamed answer text) is untrusted content re-entering the system as citation anchors |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | `apps/worker/src/worker/llm_stream.py`, `apps/worker/src/worker/settings.py` (`LLM_STREAM_INTERNAL_TOKEN`) | high | mitigate | Token lives only inside `Authorization` header dicts, never logged by attribute name — `nexuswiki_core.logging.REDACTED_KEYS` is an exact-match set on the literal key `token`, not a substring match, so a settings field logged as `LLM_STREAM_INTERNAL_TOKEN=...` would NOT be redacted; the code never does this (matches `QUERY_EMBEDDING_INTERNAL_TOKEN` handling today). |
| T-05-02 | Elevation of Privilege | `apps/api/src/api/settings.py` (`ApiSettings`) | critical | mitigate | `ApiSettings` gains no secret-shaped field this task; `packages/core/tests/test_settings.py::test_api_settings_has_no_field_that_could_hold_a_secret` and the updated `NON_SECRET_API_FIELDS` allowlist enforce this at CI/test time (SEC-01, D-01/02-CONTEXT.md D-06). |
| T-05-03 | Tampering | `apps/api/src/api/services/ask.py::resolve_citations()` | high | mitigate | Citations are computed as parsed-anchor ∩ server-issued-alias-map, never trusting the model's raw text as a citation on its own (CITE-02); this is the primary defense against the model fabricating or copying an attacker-controlled anchor shape into a rendered citation. |
| T-05-04 | Denial of Service | `apps/worker/src/worker/llm_stream.py::LlmStreamService` | medium | mitigate | Auth-before-work token-bucket rate limiter (copied verbatim from the proven, bug-fixed `_reserve_token()` shape) bounds concurrent/total provider-attempt rate per listener instance; full budget-cap enforcement (D-09) is a separate, later plan (05-04) layered on top of this same `stream()` method. |
| T-05-05 | Tampering | `apps/api/src/api/services/ask.py::_fetch_evidence_content()` | medium | mitigate | The `in.(...)` id filter is built only from `document_id`/`evidence_id` values already returned by `RetrievalService.retrieve()` (server-controlled, not user-supplied strings), and the query still carries `workspace_id=eq.<value>` plus the requester's own JWT, so RLS remains the tenant boundary exactly as `UserDb`'s other methods rely on. |
| T-05-SC | Tampering | npm/pip/cargo installs | high | accept | No new third-party package is installed by this task (packages/core, worker, api all use only stdlib + already-pinned `httpx`/`fastapi`/`pydantic`) — Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
Full worker+api+core suite green after this task: `uv run pytest`. Manual smoke deferred to Phase 6 (no browser client exists yet, per 05-VALIDATION.md's Manual-Only Verifications).
</verification>

<success_criteria>
A workspace member with a real JWT can call `POST /workspaces/{id}/ask` and receive a correctly-ordered SSE stream whose citations are computed from the server's own issuance map, not from the model's unverified text or the raw retrieval list — and a member whose query has no evidence gets an explicit, zero-cost refusal instead of a fabricated answer.
</success_criteria>

<output>
Create `.planning/phases/05-citation-integrity-and-answer-apis/05-01-SUMMARY.md` when done
</output>
