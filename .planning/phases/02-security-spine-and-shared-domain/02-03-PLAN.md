---
phase: 02-security-spine-and-shared-domain
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - apps/worker/src/worker/db/__init__.py
  - apps/worker/src/worker/db/service.py
  - apps/worker/tests/test_service_client.py
  - apps/api/src/api/db/__init__.py
  - apps/api/src/api/db/user.py
  - apps/api/src/api/errors.py
  - apps/api/src/api/main.py
  - apps/api/tests/test_user_db.py
  - apps/api/tests/fixtures/banned_import_violation.py.txt
  - pyproject.toml
autonomous: true
requirements: [SEC-02, SEC-04]
user_setup: []

estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "service_client()가 WorkerSettings 인스턴스를 인자로 요구하며 모듈 전역 싱글턴이 없다"
    - "apps/api/** 에 worker.db.service import를 넣으면 ruff check가 non-zero로 종료한다"
    - "service_client()를 ApiSettings 인스턴스로 호출하면 거부된다"
    - "update_one()·delete_one()이 affected rows 0에서 WorkspaceForbidden을 던진다"
    - "update_one()·delete_one()이 affected rows 2 이상에서도 예외를 던진다"
    - "SQLSTATE 42501과 WorkspaceForbidden을 단일 예외 핸들러가 403으로 렌더한다"
    - "읽기 메서드에는 0행 규칙이 적용되지 않아 정상적으로 비어 있는 조회가 200을 반환한다"
  artifacts:
    - apps/worker/src/worker/db/service.py
    - apps/api/src/api/db/user.py
    - apps/api/src/api/errors.py
    - apps/api/tests/test_user_db.py
  key_links:
    - "service_client(settings: WorkerSettings) 시그니처 ↔ 02-02-PLAN의 WorkerSettings 타입"
    - "UserDb 구현 방식 ↔ 02-01-PLAN이 잠근 checklists.json > decisions.db_transport (D-04)"
    - "errors.py의 단일 핸들러 등록 ↔ apps/api/src/api/main.py의 create_app (D-13)"
    - "ruff banned-api 대상 문자열 worker.db.service ↔ 실제 모듈 경로"
  prohibitions:
    - statement: "service_client 경로가 workspace_id 필터 없는 전역 쿼리를 돌려서는 안 된다"
      status: resolved
      verification: test
      check_kind: test
      check_target: apps/worker/tests/test_service_client.py
      check_rule: "worker/db/service.py가 노출하는 모든 테이블 접근 헬퍼가 workspace_id를 필수 인자로 요구한다 — 생략 호출이 TypeError를 낸다"
      check_violation_fixture: "test_service_client.py::test_scoped_helpers_reject_missing_workspace_id 가 workspace_id 없이 각 헬퍼를 호출하는 케이스"
      check_clean_fixture: "test_service_client.py::test_scoped_helpers_accept_workspace_id 가 workspace_id를 주고 호출하는 케이스"
---

<objective>
service key 클라이언트를 import만으로 만들 수 없게 만들고, RLS가 되돌려준 0행이 조용히 성공으로 보이지 않도록 403 매핑을 **한 곳**에 세운다.

Purpose: `service_role`은 BYPASSRLS라 38개 격리 정책이 전부 장식이 되는 지점이 여기다. 그리고 RLS의 `USING` 실패는 예외가 아니라 0행으로 돌아오므로(ARCHITECTURE.md), 매핑이 라우터마다 흩어지면 SEC-04의 "한 곳" 조건이 곧바로 깨진다.
Output: `worker/db/service.py` 팩토리 · ruff banned-api 규칙 · `api/db/user.py`의 쓰기 전용 메서드 2종 · `api/errors.py`의 단일 예외 핸들러
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-security-spine-and-shared-domain/02-SPEC.md
@.planning/phases/02-security-spine-and-shared-domain/02-CONTEXT.md
@.planning/phases/02-security-spine-and-shared-domain/02-PATTERNS.md
@.planning/phases/02-security-spine-and-shared-domain/02-01-SUMMARY.md
@.planning/phases/02-security-spine-and-shared-domain/02-02-SUMMARY.md
@docs/ops/db-transport-spike.md
@apps/api/src/api/health_check.py
@.planning/codebase/ARCHITECTURE.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Notes |
|---|---|---|
| `apps/worker/src/worker/db/__init__.py` | file | 새 패키지 |
| `apps/worker/src/worker/db/service.py` | file | service key 경로 |
| `service_client(settings: WorkerSettings)` | Python function | 인자 필수 팩토리, 모듈 전역 싱글턴 없음 |
| `ServiceDb` | Python class | workspace 스코프를 필수 인자로 받는 헬퍼 묶음 |
| `apps/api/src/api/db/__init__.py` | file | 새 패키지 |
| `apps/api/src/api/db/user.py` | file | 요청자 JWT 경로 |
| `UserDb` | Python class | `update_one()`, `delete_one()` + 읽기 메서드 |
| `apps/api/src/api/errors.py` | file | 예외와 핸들러 |
| `WorkspaceForbidden` | Python exception | 0행 및 다중행에서 발생 |
| `register_error_handlers(app)` | Python function | 단일 등록 지점 |
| `apps/api/tests/fixtures/banned_import_violation.py.txt` | file | ruff banned-api 위반 픽스처 (확장자를 바꿔 평소 수집에서 제외) |
| root `pyproject.toml` `[tool.ruff.lint.flake8-tidy-imports.banned-api]` | config change | SEC-02 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: service_client 팩토리 격리와 ruff banned-api 집행</name>
  <files>apps/worker/src/worker/db/__init__.py, apps/worker/src/worker/db/service.py, apps/worker/tests/test_service_client.py, apps/api/tests/fixtures/banned_import_violation.py.txt, pyproject.toml</files>
  <read_first>
    - apps/worker/src/worker/settings.py — 02-02-PLAN이 만든 `WorkerSettings` (필드 이름과 타입)
    - apps/worker/src/worker/rtt.py — 명시적 keyword-only 인자 팩토리 스타일, 모듈 최상단 상수 배치
    - apps/api/src/api/health_check.py — 16-29행 frozen dataclass 결과 + 주입된 클라이언트 패턴
    - pyproject.toml — 30-35행 `[tool.ruff.lint]`. 02-02-PLAN이 `select`에 `TID`를 이미 추가했다
    - .planning/phases/02-security-spine-and-shared-domain/02-CONTEXT.md — D-06, D-08, D-09
    - .planning/phases/02-security-spine-and-shared-domain/02-PATTERNS.md — `### Ruff / tooling` 절
    - docs/ops/db-transport-spike.md — 02-01-PLAN이 확정한 트랜스포트
  </read_first>
  <behavior>
    - Test 1: `service_client()`를 인자 없이 호출하면 `TypeError`가 난다
    - Test 2: `service_client(ApiSettings(...))`가 거부된다 — `ApiSettings`에는 `SUPABASE_SECRET_KEY` 속성 자체가 없다
    - Test 3: `import worker.db.service` 만으로는 어떤 네트워크 연결이나 자격증명 읽기도 일어나지 않는다 (모듈 전역 클라이언트 부재)
    - Test 4: `ServiceDb`의 모든 테이블 접근 헬퍼가 `workspace_id` 없이 호출되면 `TypeError`를 낸다
    - Test 5: `workspace_id`를 준 호출은 성공하고, 생성된 쿼리 문자열에 `workspace_id` 조건이 들어 있다
  </behavior>
  <action>
`apps/worker/src/worker/db/service.py`를 만든다. 헤더는 한국어 한 줄 요약 + `관련 태스크: P2-JOB-01` + `설계 근거: 02-CONTEXT.md > D-06, D-08` + `설계 근거: checklists.json > decisions.db_access`.

`service_client(settings: WorkerSettings)`를 **인자 필수 팩토리**로 만든다. 모듈 전역 싱글턴을 두지 않는다 — 전역 싱글턴을 두면 import 부작용으로 키를 읽으려 시도하게 되어 D-06의 인과("막히는 것은 키다")가 흐려진다 (02-CONTEXT.md > D-08). `ApiSettings`로는 타입도 속성도 맞지 않아 호출할 수 없다는 것이 이 시그니처의 요점이다. 클라이언트 구성 방식은 02-01-PLAN이 `checklists.json > decisions.db_transport`에 잠근 트랜스포트를 따른다.

같은 파일에 `ServiceDb`를 둔다. `service_role`이 BYPASSRLS이므로 이 클래스가 노출하는 **모든** 테이블 접근 헬퍼는 `workspace_id`를 키워드 인자로 **필수** 요구하고, 만들어내는 모든 쿼리에 그 조건을 반드시 건다. 기본값을 주지 않는 것이 핵심이다 — 기본값이 있으면 잊고 호출해도 통과한다. `⚠️` 주석을 클래스 바로 위에 남긴다: `service_role`은 BYPASSRLS라 조건이 빠지면 38개 격리 정책이 전부 무효가 되고 오류 없이 교차 테넌트를 읽는다. 근거는 `checklists.json > decisions.db_access`.

Phase 2가 실제로 필요로 하는 헬퍼만 만든다 — 02-07-PLAN의 큐 루프가 쓸 `jobs` 조작 경로(`claim_job` / `complete_job` / `fail_job` / `release_job` RPC 호출)와 그 조회. 도메인 테이블 헬퍼는 Phase 3의 일이다.

루트 `pyproject.toml`에 `[tool.ruff.lint.flake8-tidy-imports.banned-api]` 블록을 추가해 `worker.db.service`를 차단하고, 메시지에 왜 차단되는지와 어디를 보라는지를 적는다. `[tool.ruff.lint.per-file-ignores]`에 `"apps/worker/**" = ["TID251"]`을 추가해 worker 자신만 예외로 둔다 (02-CONTEXT.md > D-09). ⚠️ 이 규칙은 1차 방어선이 아니라 **조기 경보**다. 단일 이미지(01-CONTEXT.md > D-01) 때문에 동적 import로는 우회되며, 실제로 막는 것은 `ApiSettings`에 필드가 없다는 사실과 Railway가 api 서비스에 값을 주입하지 않는다는 사실이다 (02-CONTEXT.md > D-06). 이 인과를 뒤집어 쓰지 않는다.

위반 픽스처를 `apps/api/tests/fixtures/banned_import_violation.py.txt`로 커밋한다 — 확장자를 `.py.txt`로 두어 평소 lint/수집 대상에서 빠지고, 검증 시에만 `apps/api/` 아래로 임시 복사해 `ruff check`가 non-zero가 되는지 확인한다. 파일 내용은 금지된 import 한 줄과 그 심볼을 쓰는 한 줄.

`apps/worker/tests/test_service_client.py`를 `apps/worker/tests/test_rtt.py` 형식으로 쓴다 — 한국어 한 줄 모듈 docstring, 서술형 `test_<behavior>` 이름, `-> None`, 클래스 없음. 위 `<behavior>` 5개 케이스를 덮는다. 네트워크가 필요한 부분은 `monkeypatch`로 대체한다.
  </action>
  <verify>
    <automated>uv run pytest apps/worker/tests/test_service_client.py -x -q && cp apps/api/tests/fixtures/banned_import_violation.py.txt apps/api/src/api/_banned_probe.py && ! uv run ruff check apps/api; rc=$?; rm -f apps/api/src/api/_banned_probe.py; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import worker.db.service as m; import inspect; p=inspect.signature(m.service_client).parameters; assert len(p)>=1 and list(p)[0]=='settings'; print('ok')"` 가 `ok`를 출력한다
    - `uv run python -c "import worker.db.service as m; m.service_client()"` 가 `TypeError`로 실패한다
    - 모듈 전역 싱글턴이 없다: `uv run python -c "import worker.db.service as m; assert not [k for k,v in vars(m).items() if not k.startswith('_') and type(v).__name__.endswith('Client')]; print('ok')"` 가 `ok`를 출력한다
    - 위반 픽스처를 `apps/api/src/api/` 아래로 복사한 상태에서 `uv run ruff check apps/api` 가 non-zero로 종료하고 출력에 `TID251` 이 포함된다
    - 픽스처를 제거한 상태에서 `uv run ruff check apps packages` 가 exit 0
    - `uv run ruff check apps/worker` 가 exit 0 — worker 자신은 `per-file-ignores`로 예외 처리되어 자기 모듈 import가 통과한다
    - `uv run python -c "import tomllib,pathlib; c=tomllib.loads(pathlib.Path('pyproject.toml').read_text()); b=c['tool']['ruff']['lint']['flake8-tidy-imports']['banned-api']; assert 'worker.db.service' in b; assert 'TID251' in c['tool']['ruff']['lint']['per-file-ignores']['apps/worker/**']; print('ok')"` 가 `ok`를 출력한다
    - `apps/worker/src/worker/db/service.py`의 모든 `ServiceDb` 메서드 시그니처가 `workspace_id`를 기본값 없이 요구한다 — `uv run pytest apps/worker/tests/test_service_client.py -q` 가 exit 0
  </acceptance_criteria>
  <done>`service_client`가 인자 없이는 만들어지지 않고, `apps/api/**`의 금지 import가 `ruff check`를 non-zero로 만들며, `ServiceDb` 헬퍼가 workspace 스코프를 강제한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UserDb 쓰기 메서드와 WorkspaceForbidden</name>
  <files>apps/api/src/api/db/__init__.py, apps/api/src/api/db/user.py, apps/api/src/api/errors.py, apps/api/src/api/main.py, apps/api/tests/test_user_db.py</files>
  <read_first>
    - apps/api/src/api/health_check.py — 전체. 어댑터 헤더(1-13행), frozen dataclass 결과 + 주입된 클라이언트(16-29행), classify-then-return 오류 패턴(32-47행), `⚠️` 주석 위치(33-34행)
    - docs/ops/db-transport-spike.md — 02-01-PLAN의 결정. asyncpg가 채택된 경우 문서 마지막 문단의 `UserDb` 설계 요건을 반드시 반영할 것
    - apps/api/src/api/settings.py — `ApiSettings`
    - supabase/migrations/0004_rls_policies.sql — 정책이 UPDATE/DELETE에서 어떻게 0행을 만드는지
    - .planning/codebase/ARCHITECTURE.md — "RLS 위반이 항상 에러는 아니다" 절
    - .planning/phases/02-security-spine-and-shared-domain/02-CONTEXT.md — D-04, D-11, D-12, D-13
  </read_first>
  <behavior>
    - Test 1: `update_one()`이 affected rows 0을 받으면 `WorkspaceForbidden`을 던진다
    - Test 2: `delete_one()`이 affected rows 0을 받으면 `WorkspaceForbidden`을 던진다
    - Test 3: `update_one()`이 affected rows 2를 받아도 예외를 던진다 (SPEC Edge Coverage R4)
    - Test 4: `delete_one()`이 affected rows 2를 받아도 예외를 던진다
    - Test 5: affected rows 1이면 정상 반환한다
    - Test 6: 읽기 메서드가 빈 결과를 받으면 예외 없이 빈 컬렉션을 반환한다 (D-11)
    - Test 7: 드라이버가 SQLSTATE `42501`을 올리면 `UserDb`가 그것을 삼키지 않고 그대로 전파한다 — 렌더링은 핸들러의 몫이다
  </behavior>
  <action>
`apps/api/src/api/db/user.py`를 만든다. 헤더는 한국어 한 줄 요약 + `관련 태스크: P2-BE-01` + `설계 근거: 02-CONTEXT.md > D-11, D-12` + `설계 근거: checklists.json > decisions.db_transport`. 이 파일은 `health_check.py`가 맡던 "트랜스포트 교체 지점" 역할의 Phase 2 연장이다 (01-CONTEXT.md > D-11).

`UserDb`는 클라이언트/커넥션을 **인자로 주입받는다**. 모듈 전역을 두지 않는다 — `health_check.py:23-28`의 형태를 그대로 따른다. 02-01-PLAN이 asyncpg를 잠갔다면 D-04가 요구하는 대로 트랜잭션 진입점에서 `set local role authenticated`와 `set local request.jwt.claims`를 강제하고, **그 세팅 없이 쿼리를 실행할 수 있는 공개 API를 두지 않는다**. RPC가 잠겼다면 요청자 JWT를 매 호출에 실어 보낸다.

쓰기 전용 메서드 두 개를 만든다. `update_one(...)`과 `delete_one(...)`은 영향 행 수를 확인해 정확히 1이 아니면 `WorkspaceForbidden`을 **던진다**. 0은 RLS의 `USING`이 막았거나 리소스가 없다는 뜻이고, 2 이상은 스코프가 의도보다 넓게 잡혔다는 뜻이라 둘 다 계속 진행하면 안 된다. 반환값 계약이 아니라 예외인 이유는 호출자가 무시할 수 없어야 하기 때문이다 — `health_check.py`의 classify-then-return을 여기서는 의도적으로 뒤집는다.

읽기 메서드에는 이 규칙을 적용하지 않는다. 범용 쿼리 래퍼로 만들면 "정상적으로 비어 있는 조회"와 "RLS가 막은 0행"을 원리적으로 구분할 수 없으므로, 쓰기 전용 메서드를 따로 두는 것이 SEC-04의 유일한 온전한 해법이다 (02-CONTEXT.md > D-11). `⚠️` 주석으로 이 구분을 읽기 메서드 위에 남긴다.

`apps/api/src/api/errors.py`를 만든다. `WorkspaceForbidden` 예외와, 그것과 SQLSTATE `42501`(WITH CHECK 위반)을 **단일 핸들러**가 Forbidden 응답으로 렌더하는 `register_error_handlers(app)`를 둔다 <!-- planner-discipline-allow: 403 --> <!-- planner-discipline-allow: 42501 --> <!-- planner-discipline-allow: WorkspaceForbidden -->. 두 경로가 다른 곳에서 처리되면 SEC-04의 "한 곳" 조건이 다시 깨진다 (02-CONTEXT.md > D-13). 존재하지 않는 리소스와 격리 위반을 구분하지 않는다 — 둘 다 같은 응답이다. Not Found를 주면 다른 테넌트의 리소스 존재 여부가 응답 코드로 새어나가 열거 공격이 성립한다 (02-CONTEXT.md > D-12). 이 결정을 `⚠️` 주석으로 핸들러 위에 남기고, 응답 본문에 리소스 존재 여부를 암시하는 어떤 정보도 싣지 않는다.

`apps/api/src/api/main.py`의 `create_app`에서 `register_error_handlers(app)`를 호출한다. 등록 지점은 여기 **한 곳**이다.

`apps/api/tests/test_user_db.py`를 `packages/core/tests/test_logging_redaction.py` 형식으로 쓰고 위 `<behavior>` 7개 케이스를 전부 덮는다. 드라이버는 monkeypatch로 대체해 영향 행 수와 SQLSTATE를 직접 주입한다.
  </action>
  <verify>
    <automated>uv run pytest apps/api/tests/test_user_db.py -x -q && uv run ruff check apps packages</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest apps/api/tests/test_user_db.py -q` 가 exit 0이고 7개 이상의 테스트가 수집된다
    - `uv run python -c "from api.errors import WorkspaceForbidden, register_error_handlers; print('ok')"` 가 `ok`를 출력한다
    - `grep -c 'register_error_handlers' apps/api/src/api/main.py` 가 1 — 등록 지점이 정확히 하나다
    - `grep -rn 'WorkspaceForbidden' apps/api/src/api/ | grep -v 'errors.py' | grep -v 'db/user.py'` 가 아무것도 출력하지 않는다 — 예외는 `user.py`에서만 발생하고 `errors.py`에서만 렌더된다
    - `grep -rc '42501' apps/api/src/api/errors.py` 가 1 이상이고 `grep -rn '42501' apps/api/src/api/db/user.py` 가 아무것도 출력하지 않는다 — SQLSTATE 매핑이 핸들러 한 곳에만 있다
    - `apps/api/src/api/db/user.py`의 읽기 메서드가 빈 결과에 예외를 던지지 않음이 테스트로 확인된다
    - asyncpg가 채택된 경우에만: `grep -c 'set local role authenticated' apps/api/src/api/db/user.py` 가 1 이상이고 `grep -c 'request.jwt.claims' apps/api/src/api/db/user.py` 가 1 이상
    - `uv run ruff check apps packages` 가 exit 0
  </acceptance_criteria>
  <done>0행과 다중행이 모두 `WorkspaceForbidden`이 되고, 그것과 `42501`이 단 하나의 핸들러에서 Forbidden으로 렌더되며, 읽기 경로는 그 규칙 밖에 있다.</done>
</task>

<task type="auto">
  <name>Task 3: 전체 회귀 확인과 격리 계약 문서화</name>
  <files>apps/worker/src/worker/db/__init__.py, apps/api/src/api/db/__init__.py</files>
  <read_first>
    - apps/api/src/api/db/user.py, apps/api/src/api/errors.py, apps/worker/src/worker/db/service.py (Task 1·2 결과)
    - pyproject.toml (Task 1이 추가한 banned-api 블록)
    - apps/api/tests/test_health.py — 02-02-PLAN이 갱신한 형태. 예외 핸들러 등록이 기존 경로를 깨지 않았는지 확인
    - .planning/phases/02-security-spine-and-shared-domain/02-SPEC.md — Acceptance Criteria 중 SEC-02·SEC-04 항목
  </read_first>
  <action>
전체 테스트와 lint를 돌려 Task 1·2가 기존 경로를 깨지 않았음을 확인한다. 특히 `create_app`에 핸들러가 붙은 뒤에도 `/health`와 `/health/ready`가 이전과 같은 상태 코드를 낸다는 것 — 예외 핸들러가 광범위한 `Exception` 캐치로 흘러 정상 오류를 삼키면 여기서 드러난다.

`apps/worker/src/worker/db/__init__.py`와 `apps/api/src/api/db/__init__.py`에 각각 한 줄짜리 한국어 docstring을 넣어 두 패키지의 역할 경계를 명시한다: worker 쪽은 `service_role` 전용(BYPASSRLS, `workspace_id` 필터 필수), api 쪽은 요청자 JWT 전용(RLS가 격리를 강제). 근거는 `checklists.json > decisions.db_access`를 인용하고 재서술하지 않는다.

`checklists.json`은 이 플랜에서 건드리지 않는다 — 같은 웨이브의 02-06-PLAN이 그 파일을 쓴다. 이탈이나 특기 사항은 SUMMARY에 기록한다.

02-04-PLAN(라우터와 격리 테스트)이 곧바로 소비할 수 있도록 SUMMARY에 다음을 명시한다: `UserDb`의 쓰기 메서드 정확한 시그니처, `WorkspaceForbidden`의 import 경로, `register_error_handlers`가 이미 `create_app`에 등록되어 있다는 사실(라우터가 상태 코드를 직접 다루면 안 되는 이유).
  </action>
  <verify>
    <automated>uv run pytest -q && uv run ruff check apps packages && uv run pytest --collect-only -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q` 가 exit 0이며 `apps/api/tests`, `apps/worker/tests`, `packages/core/tests` 세 경로가 모두 수집된다
    - `uv run ruff check apps packages` 가 exit 0
    - `apps/worker/src/worker/db/__init__.py`와 `apps/api/src/api/db/__init__.py`가 각각 `decisions.db_access` 문자열을 포함한다
    - `git status --porcelain checklists.json` 가 빈 출력을 낸다 — 이 플랜은 원장을 건드리지 않는다
    - SUMMARY가 `UserDb.update_one` / `UserDb.delete_one`의 정확한 시그니처와 `WorkspaceForbidden`의 import 경로를 문자열로 담는다
  </acceptance_criteria>
  <done>전체 테스트와 lint가 통과하고, 02-04-PLAN이 소비할 인터페이스가 SUMMARY에 정확한 시그니처로 남았다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `apps/api/**` → `worker.db.service` | 코드 경계. ruff banned-api가 조기 경보를 울린다. 실제 방어선은 `ApiSettings`의 필드 부재 |
| `service_role` 커넥션 → 모든 워크스페이스의 행 | BYPASSRLS. `workspace_id` 조건이 유일한 격리 수단 |
| 요청자 JWT → `UserDb` 쓰기 | RLS `USING`이 막으면 예외가 아니라 0행이 돌아온다 |
| `UserDb` 예외 → HTTP 응답 | 존재 여부가 상태 코드로 새면 열거 공격이 성립한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-12 | Elevation of Privilege | `service_client` | critical | mitigate | 인자 필수 팩토리 + 모듈 전역 싱글턴 부재. `ApiSettings`로는 속성이 없어 호출 불가. ruff `TID251`이 `apps/api/**`의 import를 빌드 실패로 만든다 |
| T-02-13 | Information Disclosure | `ServiceDb` 전역 쿼리 | critical | mitigate | 모든 헬퍼가 `workspace_id`를 기본값 없이 요구한다. 생략 시 `TypeError`가 나는 것을 테스트가 고정한다 (prohibitions 항목) |
| T-02-14 | Information Disclosure | 0행 vs 403 혼동 | high | mitigate | `update_one`/`delete_one`이 정확히 1행이 아니면 예외를 던진다. 0행을 성공으로 넘기면 격리 실패가 조용히 지나간다 |
| T-02-15 | Information Disclosure | 리소스 존재 여부 누출 | high | mitigate | 격리 위반과 부재를 구분하지 않는다 (D-12). 응답 본문에 존재 여부를 암시하는 정보를 싣지 않는다 |
| T-02-16 | Tampering | 다중행 UPDATE/DELETE | high | mitigate | affected rows 2 이상도 예외. 스코프가 넓게 잡힌 쿼리가 커밋되는 것을 막는다 (SPEC Edge Coverage R4) |
| T-02-17 | Repudiation | 예외 핸들러가 오류를 삼킴 | medium | mitigate | Task 3이 `/health/ready` 503 경로가 그대로 남았는지 회귀로 확인한다 |
| T-02-18 | Elevation of Privilege | 동적 import로 TID 우회 | medium | accept | SPEC Edge Coverage가 `⛔ dismissed`로 기록. lint는 1차 방어선이 아니며 `ApiSettings` 필드 부재와 Railway env 스코프가 실제 경로를 없앤다 (D-06) |

이 플랜은 새 패키지를 설치하지 않는다 — `pydantic-settings`는 02-02-PLAN에서, `asyncpg`(채택 시)는 02-01-PLAN에서 이미 정당성 확인을 거쳤다. asyncpg가 채택되어 `apps/api`/`apps/worker`의 고정 의존성으로 승격되는 경우에도 재확인은 필요 없으며, 확인 이력을 SUMMARY에 인용한다.
</threat_model>

<verification>
1. `uv run pytest -q` 가 exit 0
2. 위반 픽스처를 `apps/api/src/api/` 아래 둔 상태에서 `uv run ruff check apps/api` 가 non-zero이고 `TID251`을 보고한다
3. 픽스처 제거 후 `uv run ruff check apps packages` 가 exit 0
4. `uv run python -c "import worker.db.service as m; m.service_client()"` 가 `TypeError`로 실패한다
5. `grep -c 'register_error_handlers' apps/api/src/api/main.py` 가 1
</verification>

<success_criteria>
- SEC-02가 충족된다: service key 클라이언트를 import만으로 만들 수 없고 worker 밖 import가 lint 실패가 된다
- SEC-04가 충족된다: 0행과 다중행과 `42501`이 한 곳에서 Forbidden이 된다
- 읽기 경로가 그 규칙 밖에 있어 정상 빈 조회가 성공으로 남는다
- 02-04-PLAN이 라우터를 세워 실제 HTTP 왕복으로 이것을 증명할 수 있다
</success_criteria>

<output>
Create `.planning/phases/02-security-spine-and-shared-domain/02-03-SUMMARY.md` when done
</output>
