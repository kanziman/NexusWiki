---
phase: 02-security-spine-and-shared-domain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/spike/README.md
  - supabase/spike/0001_transport_corpus.sql
  - supabase/spike/0002_search_fn_rpc.sql
  - scripts/spike_db_transport.py
  - docs/ops/db-transport-spike.md
  - checklists.json
autonomous: false
requirements: [DOM-01]
user_setup: []

estimate:
  tokens: 84000
  raw_tokens: 84000
  tasks: 5
  confidence: low

must_haves:
  truths:
    - "요청자 JWT로 호출한 SECURITY INVOKER 검색 함수가 EXPLAIN (ANALYZE) 계획과 실제 반환 행 수를 함께 돌려주고, 판정 조건 3종이 개별 실측값으로 기록된다"
    - "스파이크 코퍼스가 총 50,000행이고 타깃 워크스페이스가 750행(1.5%)이며 고정 시드로 재현 가능하다"
    - "고정 시드 코퍼스에서 3회 반복해 세 번 모두 같은 판정이 나온다"
    - "RPC 경로와 asyncpg session-mode 경로가 각각 실제로 실행되고 두 경로의 실측값이 문서에 나란히 남는다"
    - "GUC 3종이 전부 적용되고 EXPLAIN에 HNSW Index Scan이 있고 k=20에 정확히 20행이 올 때만 RPC가 채택되며, 하나라도 거짓이면 asyncpg가 채택된다"
    - "트랜스포트 결정이 checklists.json > decisions.db_transport에 잠기고 docs/ops/db-transport-spike.md와 재현용 SQL이 커밋된다"
  artifacts:
    - supabase/spike/0001_transport_corpus.sql
    - supabase/spike/0002_search_fn_rpc.sql
    - scripts/spike_db_transport.py
    - docs/ops/db-transport-spike.md
  key_links:
    - "supabase/spike/0002_search_fn_rpc.sql의 함수 시그니처 → 02-06-PLAN의 0007 섹션 1 검색 함수"
    - "checklists.json > decisions.db_transport → 02-03-PLAN의 UserDb 구현 방식(D-04)"
    - "scripts/spike_db_transport.py의 EXPLAIN 파싱 → Phase 4 RTV-08 EXPLAIN 회귀 테스트 원형"
  prohibitions:
    - statement: "스파이크 결과 문서는 실제 관측되지 않은 것을 관측된 것처럼 서술하거나, 판정 조건 3종 중 일부만 충족한 것을 통과로 기록해서는 안 된다"
      status: resolved
      verification: judgment
      check_kind: doc-review
      check_target: docs/ops/db-transport-spike.md
      check_rule: "결과 표가 iterative_scan · ef_search · max_scan_tuples · has_hnsw_index_scan · returned_rows 다섯 열을 개별 실측값으로 담고, 판정 절이 세 조건 각각의 참/거짓을 나열한 뒤 결론을 낸다"
---

<objective>
DB 트랜스포트를 추측이 아니라 실행된 스파이크로 확정한다. 요청자 JWT → RPC/asyncpg → RLS post-filter → HNSW 인덱스 스캔 → 정확히 k행까지 한 경로를 끝에서 끝까지 관통시키고, 그 위에서 D-03의 3조건 판정을 내린다.

Purpose: 트랜스포트 결정은 one-way다 (02-CONTEXT.md > D-03). 뒤집으면 `0007`의 검색 함수 시그니처 · `UserDb` 구현 · Phase 4 융합 계층이 함께 움직인다. 이 페이즈의 나머지 전부가 이 판정 위에 쌓이므로 첫 커밋에서 증명한다.
Output: 재현 가능한 스파이크 하네스(SQL 2종 + 러너) · `docs/ops/db-transport-spike.md` · `checklists.json > decisions.db_transport` 잠금
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-security-spine-and-shared-domain/02-SPEC.md
@.planning/phases/02-security-spine-and-shared-domain/02-CONTEXT.md
@.planning/phases/02-security-spine-and-shared-domain/02-PATTERNS.md
@supabase/migrations/0002_search_schema.sql
@supabase/migrations/0004_rls_policies.sql
@docs/ops/rtt-baseline.md
</context>

## Artifacts this phase produces

이 플랜이 새로 만드는 심볼·파일 (drift 검증 제외 대상):

| Artifact | Kind | Notes |
|---|---|---|
| `supabase/spike/README.md` | file | 스파이크 하네스 실행 순서 |
| `supabase/spike/0001_transport_corpus.sql` | file | 합성 적대적 코퍼스 생성 (고정 시드) |
| `supabase/spike/0002_search_fn_rpc.sql` | file | 스파이크 검색 함수 2종 |
| `scripts/spike_db_transport.py` | file | RPC/asyncpg 양 경로 러너 |
| `public.spike_search_chunks` | SQL function | `(p_workspace_id uuid, p_query extensions.vector(1536), p_k int)` |
| `public.spike_explain_search_chunks` | SQL function | 같은 인자, EXPLAIN 계획과 GUC 관측값을 jsonb로 반환 |
| `SPIKE_CORPUS_TOTAL_ROWS`, `SPIKE_CORPUS_TARGET_ROWS`, `SPIKE_CORPUS_SEED`, `SPIKE_K` | Python constants | `scripts/spike_db_transport.py` |
| `SpikeRunResult` | Python frozen dataclass | `iterative_scan`, `ef_search`, `max_scan_tuples`, `has_hnsw_index_scan`, `returned_rows`, `elapsed_ms` |
| `docs/ops/db-transport-spike.md` | file | 결과 문서 |
| `checklists.json > decisions.db_transport` | ledger key | 트랜스포트 결정 잠금 위치 |

<tasks>

<task type="tracer">
  <name>Task 1: 얇은 한 경로 관통 — 요청자 JWT에서 EXPLAIN 계획까지</name>
  <files>supabase/spike/README.md, supabase/spike/0001_transport_corpus.sql, supabase/spike/0002_search_fn_rpc.sql, scripts/spike_db_transport.py</files>
  <precondition>로컬 Supabase 스택이 떠 있다: `supabase status`가 DB `127.0.0.1:54422`를 보고하고 `docker ps`에 `supabase_db_NexusWiki` 컨테이너가 보인다. 없으면 `supabase start`를 먼저 실행한다.</precondition>
  <read_first>
    - supabase/migrations/0002_search_schema.sql — 30행 pgvector가 `extensions` 스키마에 설치됨, 58-122행 `source_chunks` 정의와 `source_chunks_embedding_idx` HNSW 인덱스
    - supabase/migrations/0004_rls_policies.sql — 44-95행 `is_workspace_member` / `workspace_role` / `has_workspace_role`의 `security definer stable set search_path = public` 형태와 `authenticated` grant
    - supabase/migrations/0003_jobs.sql — 1-26행 파일 헤더 배너·번호 섹션 규칙, 213-229행 revoke/grant 쌍
    - supabase/migrations/0001_core_schema.sql — 90-128행 `raw_sources`, `(id, workspace_id)` 복합 UNIQUE
    - .planning/phases/02-security-spine-and-shared-domain/02-CONTEXT.md — D-01, D-02, D-03, D-04
    - .planning/phases/02-security-spine-and-shared-domain/02-PATTERNS.md — `supabase/migrations/0007_*.sql` 절의 SECURITY INVOKER + GUC 패턴
  </read_first>
  <action>
얇지만 프로덕션 품질인 한 경로를 끝에서 끝까지 뚫는다. 버리는 프로토타입이 아니라 Phase 4 RTV-08 EXPLAIN 회귀 테스트의 원형으로 남는 코드다 (02-CONTEXT.md > D-01과 `<specifics>` 두 번째 항목).

`supabase/spike/0002_search_fn_rpc.sql`에 `public.spike_search_chunks(p_workspace_id uuid, p_query extensions.vector(1536), p_k int)`를 만든다. `security invoker`, `stable`, 그리고 같은 modifier 블록에 `set search_path = public`, `set hnsw.iterative_scan = 'strict_order'`, `set hnsw.ef_search = '200'`, `set hnsw.max_scan_tuples = '40000'` 네 개를 건다. 본문 쿼리는 `public.source_chunks`에서 `workspace_id = p_workspace_id`로 거르고 `embedding <=> p_query` 오름차순 `limit p_k`. pgvector 타입·연산자 클래스는 전부 schema-qualified (`extensions.vector(1536)`) — 근거는 `0002_search_schema.sql:30`. `comment on function`으로 계약과 호출자 제한을 한국어 한 문장으로 남기고 `grant execute ... to authenticated`를 건다.

같은 파일에 `public.spike_explain_search_chunks`를 추가한다. 인자와 GUC modifier 블록이 `spike_search_chunks`와 **완전히 동일**해야 한다 — 다르면 판정이 다른 함수를 보게 된다. `language plpgsql`로 동일 쿼리에 `explain (analyze, buffers, format json)`을 걸어 계획을 얻고, `current_setting('hnsw.iterative_scan', true)` · `current_setting('hnsw.ef_search', true)` · `current_setting('hnsw.max_scan_tuples', true)` 세 값과 `spike_search_chunks` 호출의 실제 반환 행 수를 함께 하나의 jsonb로 묶어 돌려준다. 한 번의 왕복으로 판정 3조건이 전부 관측되어야 한다.

⚠️ 주석을 이 함수 바로 위에 남긴다: `current_setting`만 보면 "설정됐다"만 알고 "플래너가 그것을 따랐다"는 모르며, 전자만 확인하면 Phase 4에서 조용히 seq scan으로 이탈해도 알아채지 못한다. 근거로 `02-CONTEXT.md > D-01`을 인용하고 이유를 재서술하지 않는다.

`supabase/spike/0001_transport_corpus.sql`에는 이 태스크에서 **최소 슬라이스만** 만든다 — 워크스페이스 2개(타깃 1 + 노이즈 1), 각 워크스페이스에 `raw_sources` 부모 1행, 타깃에 `source_chunks` 200행, 노이즈에 2,000행. `setseed`로 시드를 고정하고 임베딩은 1536차 난수 벡터로 채운다(D-02: 임베딩 API 비용 0, 재현 가능, 판정 조건 직접 통제). 행 수는 psql 변수(`:total_rows`, `:target_rows`, `:seed`)로 받아 Task 2가 같은 파일을 그대로 확장할 수 있게 만든다. `(id, workspace_id)` 복합 FK 규약을 지킨다.

`scripts/spike_db_transport.py`에 러너 뼈대를 세운다. 파일 헤더는 한국어 한 줄 요약 + `관련 태스크: P2-BE-01` + `설계 근거: 02-CONTEXT.md > D-01, D-02, D-03`. frozen dataclass `SpikeRunResult`를 두고 이 태스크에서는 **RPC 경로 하나만** 구현한다: 로컬 GoTrue(`http://127.0.0.1:54421/auth/v1`)에서 테스트 사용자를 만들고 password grant로 JWT를 받은 뒤, httpx로 `{SUPABASE_URL}/rest/v1/rpc/spike_explain_search_chunks`에 `Authorization: Bearer <jwt>`와 `apikey` 헤더를 실어 POST한다. 반환 jsonb에서 GUC 3종 값과 계획 트리를 재귀 순회해 얻은 노드 타입 문자열 집합으로 `has_hnsw_index_scan`을 판정하고, 반환 행 수를 `returned_rows`에 담는다. 자격증명은 CLI 인자와 환경변수로만 받고 스크립트에 하드코딩하지 않는다. `--transport`, `--k`, `--repeat` 인자를 지금 정의한다.

`supabase/spike/README.md`에 실행 순서(코퍼스 → 함수 → 러너)와 적용 방법을 한국어로 적는다. 로컬 `psql`이 없으므로 `docker exec -i supabase_db_NexusWiki psql -U postgres -d postgres` 경유임을 명시한다.
  </action>
  <verify>
    <automated>cat supabase/spike/0001_transport_corpus.sql | docker exec -i supabase_db_NexusWiki psql -U postgres -d postgres -v ON_ERROR_STOP=1 -v total_rows=2200 -v target_rows=200 && cat supabase/spike/0002_search_fn_rpc.sql | docker exec -i supabase_db_NexusWiki psql -U postgres -d postgres -v ON_ERROR_STOP=1 && uv run --with httpx python scripts/spike_db_transport.py --transport rpc --k 20 --repeat 1</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/spike/0002_search_fn_rpc.sql`에 `security invoker` 문자열이 존재하고 `security definer`는 존재하지 않는다: `grep -c 'security invoker' supabase/spike/0002_search_fn_rpc.sql` 가 2 이상이고 `grep -c 'security definer' supabase/spike/0002_search_fn_rpc.sql` 가 0
    - GUC 3종이 두 함수 모두에 걸려 있다: `grep -c 'hnsw.iterative_scan' supabase/spike/0002_search_fn_rpc.sql` 가 2 이상, `hnsw.ef_search` · `hnsw.max_scan_tuples` 도 각각 2 이상
    - pgvector 참조가 schema-qualified다: `grep -vE '^\s*--' supabase/spike/0002_search_fn_rpc.sql | grep -c 'vector(1536)'` 가 1 이상이고, 같은 필터에서 `grep -cE '(^|[^.])vector\(1536\)'` 가 0
    - 러너가 exit 0으로 끝나고 stdout에 `iterative_scan`, `ef_search`, `max_scan_tuples`, `has_hnsw_index_scan`, `returned_rows` 다섯 키가 모두 값과 함께 출력된다
    - `scripts/spike_db_transport.py`에 `--transport`, `--k`, `--repeat` 세 인자가 정의되어 있다
    - Task 1은 `asyncpg` 없이 통과한다: `uv run --with httpx python scripts/spike_db_transport.py --transport rpc --k 20 --repeat 1` 가 exit 0 — 공급망 게이트 이전에 설치가 일어나지 않았음을 확인한다
    - 러너 파일에 Supabase 비밀키·비밀번호 리터럴이 없다: `grep -cE 'sb_secret_|postgres:[A-Za-z0-9]' scripts/spike_db_transport.py` 가 0
    - `supabase/spike/README.md`가 `docker exec -i supabase_db_NexusWiki psql` 문자열을 포함한다
  </acceptance_criteria>
  <done>요청자 JWT 한 개로 RPC를 호출하면 GUC 3종 실측값 · EXPLAIN 계획의 노드 타입 · 실제 반환 행 수가 한 번의 왕복으로 돌아오며, 그 코드가 커밋되어 있다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Gate: asyncpg 패키지 정당성 확인</name>
  <what-built>Task 1은 `httpx`만 썼다 — 이미 `apps/api`/`apps/worker`의 고정 의존성이므로 신규 설치가 아니다. Task 2가 **처음으로** `uv run --with asyncpg`를 실행한다.</what-built>
  <how-to-verify>
RESEARCH.md가 없어 `## Package Legitimacy Audit` 표를 참조할 수 없다. 폴백 정책상 `asyncpg`는 `[ASSUMED]`이며 설치 전 사람이 확인해야 한다.

1. `https://pypi.org/project/asyncpg/` 를 연다
2. 확인할 것: 프로젝트 홈페이지가 `github.com/MagicStack/asyncpg`를 가리키는지 · 최근 릴리스 날짜 · 월 다운로드 규모 · 배포자가 MagicStack인지
3. Task 2에서 `--with`에 넘길 정확한 버전을 정한다. 이 시점에는 워크스페이스 고정 의존성이 아니라 일회성 러너 의존성이므로 `uv run --with 'asyncpg==<버전>'` 형태로 버전을 못 박는다 — 버전 없이 실행하면 매 실행이 다른 것을 설치한다
4. 트랜스포트 판정이 asyncpg로 결론나면 이 패키지가 02-03-PLAN에서 `apps/api`/`apps/worker`의 고정 의존성으로 승격된다. 그때 재확인하지 않고 이 확인 이력을 인용한다
  </how-to-verify>
  <resume-signal>확인 결과와 고정할 버전 문자열을 함께 적을 것. 예: "approved — asyncpg==0.30.0". 문제가 있으면 무엇이 이상한지 적을 것.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: 적대적 코퍼스 50,000행 확장 + RPC/asyncpg 양 경로 3회 반복 실측</name>
  <files>supabase/spike/0001_transport_corpus.sql, scripts/spike_db_transport.py, docs/ops/db-transport-spike.md</files>
  <precondition>Task 1의 러너가 RPC 경로에서 exit 0으로 동작하고, 바로 앞 `Gate: asyncpg 패키지 정당성 확인` 체크포인트가 승인되며 고정할 `asyncpg` 버전 문자열을 남겼다. 버전이 없으면 `--with`를 실행하지 않는다.</precondition>
  <read_first>
    - supabase/spike/0001_transport_corpus.sql (Task 1이 만든 파라미터화된 코퍼스 스크립트)
    - scripts/spike_db_transport.py (Task 1이 만든 RPC 러너)
    - .planning/phases/02-security-spine-and-shared-domain/02-CONTEXT.md — D-02, D-03, D-04
    - .planning/phases/02-security-spine-and-shared-domain/02-SPEC.md — R6 전문 (수용기준의 숫자)
    - docs/ops/rtt-baseline.md — 결과 문서 섹션 골격과 표 형식
  </read_first>
  <action>
코퍼스를 SPEC R6이 못 박은 숫자로 확장한다: 총 `50000`행, 타깃 워크스페이스 `750`행(정확히 1.5%), 나머지 `49250`행은 노이즈 워크스페이스들에 분산. 이 비율이 판정의 전부다 — 타깃 비율이 높으면 RPC와 asyncpg가 똑같이 통과해 스파이크가 아무것도 판정하지 못한 채 "검증했다"는 착각만 남는다(02-CONTEXT.md > D-02, `<specifics>` 첫 항목). 시드는 스크립트 안에 고정 상수로 박아 세 번의 반복이 같은 코퍼스를 보게 한다. 코퍼스 적재 후 `source_chunks_embedding_idx`가 실제로 존재하는지 확인하고, 대량 insert 뒤 `analyze public.source_chunks`를 돌려 플래너 통계를 갱신한다 — 통계가 낡으면 HNSW 대신 seq scan이 나와 판정이 오염된다.

`scripts/spike_db_transport.py`에 상수 `SPIKE_CORPUS_TOTAL_ROWS = 50000`, `SPIKE_CORPUS_TARGET_ROWS = 750`, `SPIKE_CORPUS_SEED`, `SPIKE_K = 20`을 모듈 최상단에 대문자 타입 주석과 함께 둔다(`packages/core/src/nexuswiki_core/logging.py`의 `REDACTED_KEYS` 및 `apps/worker/src/worker/rtt.py:15-17`의 상수 배치 관례).

asyncpg 경로를 구현한다. 실행 명령의 `--with`에는 바로 앞 게이트가 승인한 **정확한 버전**을 쓴다(`--with 'asyncpg==<승인된 버전>'`). 버전 없이 실행하면 매 실행이 다른 것을 설치해 3회 반복의 재현성 전제가 깨진다. `--transport asyncpg`일 때 트랜잭션을 열고 그 안에서 `set local role authenticated`와 `set local request.jwt.claims = '<JWT claims JSON>'`을 **먼저** 실행한 뒤 같은 검색 쿼리와 `explain (analyze, buffers, format json)`을 돌린다. GUC 3종도 `set local`로 같은 트랜잭션 안에 세운다. ⚠️ 주석: 이 두 `set local` 중 하나라도 빠지면 `auth.uid()`가 값을 보지 못해 격리가 **에러 없이 조용히 풀린다**. 근거는 `02-CONTEXT.md > D-04`.

⚠️ SPEC 이탈을 문서화할 것: R6은 "asyncpg + Supavisor session mode"를 요구하지만 로컬 스택은 pooler가 비활성(`supabase/config.toml`의 54429)이고, 클라우드 프로젝트에 50,000 × 1536차 벡터를 적재하면 무료 티어 용량을 위협한다. 그러므로 asyncpg 경로는 로컬 DB(`127.0.0.1:54422`)에 **직접 연결**해 측정한다 — 직접 연결은 세션 단위 연결이므로 `set local` GUC 의미론에 관한 한 Supavisor session mode와 동등하다. 이 이탈과 그 근거를 결과 문서의 방법 절에 남긴다(`docs/ops/rtt-baseline.md`가 SPEC 이탈을 결정 인용과 함께 적은 전례를 그대로 따른다).

`--repeat 3`으로 RPC와 asyncpg를 각각 3회 실행하고, 매 실행의 `SpikeRunResult`를 개별 행으로 수집한다. 세 번의 판정이 전부 일치하지 않으면 러너가 non-zero로 종료하며 불일치를 출력한다 — 흔들리는 판정을 통과로 적는 것이 이 태스크의 가장 비싼 실패다.

`docs/ops/db-transport-spike.md`를 `docs/ops/rtt-baseline.md`의 섹션 골격 그대로 쓴다: `## 측정 일시`(타임스탬프 + 커밋 SHA) → `## 방법`(누가 어디서 측정했는지, 코퍼스 구성, 위 SPEC 이탈과 그 근거) → `## 결과`(경로 × 반복 회차를 행으로, `iterative_scan` · `ef_search` · `max_scan_tuples` · `has_hnsw_index_scan` · `returned_rows` · `elapsed_ms`를 열로 하는 우측 정렬 수치 표) → `## 판정`(D-03의 3조건 각각에 대해 참/거짓을 개별로 쓰고 그 다음 결론). 이 태스크에서는 결론까지 쓰되 `checklists.json` 잠금은 하지 않는다 — 그것은 Task 4다. 일부만 충족한 것을 통과로 적지 않는다.
  </action>
  <verify>
    <automated>uv run --with httpx --with 'asyncpg==<승인된 버전>' python scripts/spike_db_transport.py --transport rpc --k 20 --repeat 3 && uv run --with httpx --with 'asyncpg==<승인된 버전>' python scripts/spike_db_transport.py --transport asyncpg --k 20 --repeat 3</automated>
  </verify>
  <acceptance_criteria>
    - asyncpg 설치가 승인된 버전으로 고정되어 실행되었다 — SUMMARY의 실행 명령이 `asyncpg==` 형태를 포함하고 그 버전이 게이트의 resume-signal 값과 일치한다
    - `grep -c 'SPIKE_CORPUS_TOTAL_ROWS: int = 50000' scripts/spike_db_transport.py` 가 1이고 `grep -c 'SPIKE_CORPUS_TARGET_ROWS: int = 750' scripts/spike_db_transport.py` 가 1
    - 실제 적재 결과가 숫자와 일치한다: `docker exec -i supabase_db_NexusWiki psql -U postgres -d postgres -tAc "select count(*) from public.source_chunks"` 가 `50000`을 출력하고, 타깃 워크스페이스 카운트가 `750`을 출력한다
    - 두 러너 명령이 각각 exit 0이고, 각 출력에 3개 회차 행이 있으며 회차별 판정이 전부 동일하다
    - `docs/ops/db-transport-spike.md`가 `## 측정 일시`, `## 방법`, `## 결과`, `## 판정` 네 절을 모두 가진다
    - 결과 표가 다섯 관측 열을 모두 가진다: `grep -c 'has_hnsw_index_scan' docs/ops/db-transport-spike.md` 가 1 이상, `iterative_scan` · `ef_search` · `max_scan_tuples` · `returned_rows` 각각 1 이상
    - `## 판정` 절이 3조건을 개별로 나열한다: 해당 절 본문에 조건별 참/거짓 표기가 3줄 이상 존재한다
    - `## 방법` 절이 asyncpg 로컬 직접 연결 이탈과 그 근거를 명시한다
    - asyncpg 경로 코드에 `set local role authenticated`와 `set local request.jwt.claims` 두 문자열이 모두 존재한다
  </acceptance_criteria>
  <done>50,000행 · 타깃 750행 코퍼스 위에서 RPC와 asyncpg가 각각 3회 실행되어 조건별 실측값이 문서에 개별 기록되고, 세 회차의 판정이 일치한다.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Gate: DB 트랜스포트 결정 잠금 (one-way)</name>
  <decision>DB 트랜스포트를 RPC(SECURITY INVOKER + 요청자 JWT)로 갈 것인가, asyncpg + Supavisor session mode로 갈 것인가</decision>
  <context>
02-CONTEXT.md > D-03이 이 결정을 **one-way**로 못 박았다. 뒤집으면 `0007`의 검색 함수 시그니처(02-06-PLAN) · `UserDb` 구현(02-03-PLAN) · Phase 4 융합 계층이 함께 움직인다. D-03의 판정 규칙은 기계적이다 — GUC 3종 전부 적용 · EXPLAIN에 HNSW Index Scan 존재 · `k=20`에 정확히 20행, **셋 다 참일 때만** RPC. 하나라도 거짓이면 asyncpg.

Task 2가 `docs/ops/db-transport-spike.md`에 조건별 실측값과 기계적 결론을 이미 적었다. 이 체크포인트는 그 근거를 사람이 한 번 눈으로 확인하고 잠그는 자리다. D-03: "애매하면 되돌리기 싼 쪽이 아니라 확실한 쪽으로 기운다."

결정 전에 `docs/ops/db-transport-spike.md`의 `## 결과`와 `## 판정` 절을 읽을 것.
  </context>
  <options>
    <option id="rpc">
      <name>RPC — SECURITY INVOKER 함수 + 요청자 JWT</name>
      <pros>RLS가 자동으로 걸린다. `search_path`가 함수에 고정된다. GUC를 함수 정의에 박아 호출자가 잊을 수 없다. 새 커넥션 계층이 필요 없다</pros>
      <cons>PostgREST 왕복 오버헤드. 검색 함수가 전부 마이그레이션 대상이 되어 쿼리 변경마다 `0008`, `0009`가 필요하다</cons>
    </option>
    <option id="asyncpg">
      <name>asyncpg + Supavisor session mode</name>
      <pros>쿼리를 애플리케이션에서 자유롭게 조립할 수 있다. 세션 GUC를 직접 통제한다. Phase 4 융합 계층이 마이그레이션에 묶이지 않는다</pros>
      <cons>⚠️ 트랜잭션마다 `set local role authenticated` + `set local request.jwt.claims`를 직접 세워야 하고, 하나라도 빠지면 격리가 에러 없이 조용히 풀린다. `UserDb`가 트랜잭션 진입점에서 GUC 세팅을 강제하고 세팅 없이 쿼리를 실행할 수 있는 공개 API를 두지 않아야 한다 (D-04)</cons>
    </option>
  </options>
  <resume-signal>Select: rpc 또는 asyncpg. 기계적 판정과 다른 선택을 하려면 그 이유를 함께 적을 것 — 그 이유가 결과 문서의 판정 절에 기록된다.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 결정을 checklists.json에 잠그고 결과 문서를 확정한다</name>
  <files>checklists.json, docs/ops/db-transport-spike.md</files>
  <reversibility rating="one-way">트랜스포트 결정은 `0007` 검색 함수 시그니처 · `UserDb` 구현 · Phase 4 융합 계층을 동시에 규정한다 (02-CONTEXT.md > D-03).</reversibility>
  <read_first>
    - docs/ops/db-transport-spike.md (Task 2가 쓴 결과·판정)
    - checklists.json — `decisions` 객체의 기존 키 형태(`db_access`, `job_queue`, `search_channels_5way`)를 그대로 따를 것
    - .planning/phases/02-security-spine-and-shared-domain/02-CONTEXT.md — D-05 (잠금 위치가 phase CONTEXT가 아니라 checklists.json인 이유)
  </read_first>
  <action>
체크포인트에서 선택된 트랜스포트를 `checklists.json > decisions.db_transport`에 잠근다. 기존 `decisions.db_access` / `decisions.job_queue` 항목과 **같은 형태**(같은 키 집합, 같은 서술 밀도)로 쓴다. 프로젝트 수명 전체에 걸친 결정이므로 `CLAUDE.md`의 인용 계층 규칙상 phase CONTEXT가 아니라 여기가 맞다 (02-CONTEXT.md > D-05). 결정 근거는 재서술하지 말고 `docs/ops/db-transport-spike.md`를 가리킨다.

`docs/ops/db-transport-spike.md`의 `## 판정` 절을 확정한다. 체크포인트에서 기계적 판정과 다른 선택이 있었다면 그 이유를 판정 절에 명시적으로 남긴다. 문서 마지막에 다운스트림 소비자 문단을 붙인다 — 이 결정이 `0007` 섹션 1 검색 함수(02-06-PLAN), `apps/api/src/api/db/user.py`(02-03-PLAN), `apps/api/src/api/health_check.py`의 트랜스포트 교체 지점(01-CONTEXT.md > D-11)에 각각 어떻게 착지하는지, 그리고 asyncpg가 선택된 경우 D-04가 요구하는 "GUC 세팅 없이 쿼리를 실행할 수 있는 공개 API를 두지 않는다" 제약을 `UserDb` 설계 요건으로 명시한다.

`checklists.json`의 `open_questions` 배열에 스파이크 코퍼스 정리 사실을 기록한다 — 50,000행은 로컬 전용이며 `supabase db reset`(02-06-PLAN Task 2)이 지운다. 클라우드에는 적재하지 않는다.

⚠️ `checklists.json`의 `_notes`가 금지한 파생/요약 필드와 `$schema`를 추가하지 않는다.
  </action>
  <verify>
    <automated>node -e "const j=require('./checklists.json'); const d=j.decisions.db_transport; if(!d) throw new Error('decisions.db_transport 없음'); if(!/rpc|asyncpg/i.test(JSON.stringify(d))) throw new Error('선택된 트랜스포트가 기록되지 않음'); if('$schema' in j) throw new Error('금지된 \$schema 키'); console.log('ok')" && grep -q '다운스트림' docs/ops/db-transport-spike.md</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(!!require('./checklists.json').decisions.db_transport)"` 가 `true`를 출력한다
    - `decisions.db_transport`의 키 집합이 `decisions.db_access`의 키 집합과 동일하다
    - `decisions.db_transport` 값이 `docs/ops/db-transport-spike.md` 경로를 문자열로 포함한다
    - `docs/ops/db-transport-spike.md`의 `## 판정` 절 결론이 `checklists.json > decisions.db_transport`에 잠긴 값과 같은 트랜스포트를 가리킨다
    - 문서 마지막 문단이 `0007` · `user.py` · `health_check.py` 세 다운스트림 소비자를 모두 이름으로 언급한다
    - asyncpg가 선택된 경우에만: 문서가 "GUC 세팅 없이 쿼리를 실행할 수 있는 공개 API를 두지 않는다"는 `UserDb` 설계 요건을 명시한다
    - `git status --porcelain docs/ops/db-transport-spike.md supabase/spike scripts/spike_db_transport.py checklists.json` 가 커밋 후 빈 출력을 낸다
  </acceptance_criteria>
  <done>트랜스포트가 `checklists.json > decisions.db_transport`에 잠기고, 결과 문서가 조건별 실측값·판정·다운스트림 소비자까지 담은 채 커밋되어 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 스파이크 러너 → GoTrue | 테스트 사용자 자격증명이 이 경계를 넘는다 |
| 요청자 JWT → PostgREST `/rest/v1/rpc/` | `authenticated` 롤로 실행되는 신뢰 경계. RLS가 유일한 격리 수단 |
| 러너 → asyncpg 직접 연결 | `DATABASE_URL`(superuser급 로컬 자격증명)이 이 경계를 넘는다. `set local role` 이전에는 RLS가 걸리지 않는다 |
| 커밋된 SQL/스크립트 → 저장소 | 자격증명이 파일에 남으면 영구히 남는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `scripts/spike_db_transport.py` | high | mitigate | 자격증명은 CLI 인자·환경변수로만 받는다. Task 1 수용기준의 `grep -cE 'sb_secret_\|postgres:[A-Za-z0-9]'` 가 0을 요구한다 |
| T-02-02 | Elevation of Privilege | `spike_search_chunks` | high | mitigate | `security invoker`만 사용한다. Task 1 수용기준이 `security definer` 카운트 0을 요구한다 — definer면 RLS가 우회되어 판정 자체가 무의미해진다 |
| T-02-03 | Information Disclosure | asyncpg 경로 | high | mitigate | `set local role authenticated` + `set local request.jwt.claims` 두 문장이 코드에 존재함을 Task 2 수용기준이 grep으로 요구한다. 누락 시 격리가 조용히 풀린다 (D-04) |
| T-02-04 | Tampering | `uv run --with asyncpg` | high | mitigate | T-02-SC와 동일 게이트로 처리한다 — Task 2 **직전**의 `blocking-human` 체크포인트가 설치를 막고, 거기서 정해진 정확한 버전으로만 `--with`를 실행한다 |
| T-02-05 | Repudiation | `docs/ops/db-transport-spike.md` | medium | mitigate | 조건별 실측값 개별 기록 + 커밋 SHA 기재. 판정 재검증이 가능해야 한다 (prohibitions 항목) |
| T-02-06 | Denial of Service | 50,000행 코퍼스 | low | accept | 로컬 Docker 전용. 클라우드에 적재하지 않으며 `db reset`으로 정리된다 |
| T-02-SC | Tampering | `uv run --with` 설치 | high | mitigate | `Gate: asyncpg 패키지 정당성 확인` 태스크가 설치를 수행하는 Task 2 **바로 앞**에 놓이며 `gate="blocking-human"`이라 `workflow.auto_advance`로 자동 승인되지 않는다 |

**패키지 정당성 게이트:** RESEARCH.md가 없으므로 `## Package Legitimacy Audit` 표를 참조할 수 없다. 폴백 정책상 이 플랜이 도입하는 `asyncpg`는 `[ASSUMED]`로 취급한다. ⚠️ 집행 지점은 태스크 목록의 `Gate: asyncpg 패키지 정당성 확인` **체크포인트 태스크 하나**다 — 태스크 본문의 산문 지시는 게이트가 아니라 자기 신고이므로 그것에 의존하지 않는다. `httpx`는 이미 `apps/api`/`apps/worker`의 고정 의존성이므로 신규 설치가 아니며 게이트 대상이 아니다.

⚠️ 이 플랜에는 게이트가 **두 개**이고 서로 다른 것을 막는다. 혼동하지 말 것:
- `Gate: asyncpg 패키지 정당성 확인` (`blocking-human`, Task 2 앞) — 공급망. 검증되지 않은 패키지 설치를 막는다
- `Gate: DB 트랜스포트 결정 잠금` (`blocking`, Task 2 뒤 · Task 3 앞) — one-way 결정. 양쪽 경로를 **측정한 뒤** 결정이 잠기기 **전**에 놓여야 하므로 지금 위치가 맞다
</threat_model>

<verification>
1. `cat supabase/spike/0001_transport_corpus.sql | docker exec -i supabase_db_NexusWiki psql -U postgres -d postgres -v ON_ERROR_STOP=1` 가 오류 없이 끝난다
2. `uv run --with httpx --with 'asyncpg==<승인된 버전>' python scripts/spike_db_transport.py --transport rpc --k 20 --repeat 3` 가 exit 0이고 3회차 판정이 일치한다
3. `uv run --with httpx --with 'asyncpg==<승인된 버전>' python scripts/spike_db_transport.py --transport asyncpg --k 20 --repeat 3` 가 exit 0이고 3회차 판정이 일치한다
4. `node -e "console.log(require('./checklists.json').decisions.db_transport)"` 가 잠긴 결정을 출력한다
5. `docs/ops/db-transport-spike.md`가 4개 절과 5개 관측 열을 모두 가진다
</verification>

<success_criteria>
- DOM-01이 충족된다: 트랜스포트가 스파이크로 결정되고 기록되었다
- 판정 3조건의 실측값이 경로별·회차별로 개별 기록되었고 일부 충족을 통과로 적지 않았다
- 코퍼스가 총 50,000행 · 타깃 750행(1.5%)이며 고정 시드로 재현 가능하다
- 02-03-PLAN(`UserDb`)과 02-06-PLAN(`0007` 섹션 1)이 이 결정을 인용해 진행할 수 있다
</success_criteria>

<output>
Create `.planning/phases/02-security-spine-and-shared-domain/02-01-SUMMARY.md` when done
</output>
