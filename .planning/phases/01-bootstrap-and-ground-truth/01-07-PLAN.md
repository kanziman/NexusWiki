---
phase: 01-bootstrap-and-ground-truth
plan: 07
type: execute
wave: 3
depends_on: ["01-01", "01-05"]
files_modified:
  - .pre-commit-config.yaml
  - .editorconfig
  - README.md
  - pyproject.toml
  - apps/dashboard/.prettierignore
  - .claude/CLAUDE.md
  - checklists.json
  - .gitignore
autonomous: true
requirements: [BOOT-05]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "포맷을 어긴 Python 및 TS 파일의 커밋이 pre-commit에서 거부된다"
    - "`pre-commit run --all-files`가 통과하며 기존 파일을 하나도 변경하지 않는다"
    - "prettier는 `apps/dashboard/**`, ruff는 `apps/**`·`packages/**`로 제한되어 `docs/**`·`checklists.json`·`supabase/**`를 건드리지 않는다"
    - "루트 `README.md`와 `.editorconfig`가 존재한다"
    - "저장소 문서의 Python 앱 경로 표기가 `apps/api` + `apps/worker`로 통일되어 SPEC/ROADMAP/REQUIREMENTS와 일치한다"
    - "`.claude/CLAUDE.md`의 파일 헤더 규약이 결정 인용처를 두 계층으로 명시한다 — 전역·교차 페이즈 결정은 `checklists.json`의 `decisions.<key>`, 페이즈 안에서만 유효한 결정은 그 페이즈의 `NN-CONTEXT.md > D-XX`"
  artifacts:
    - .pre-commit-config.yaml
    - .editorconfig
    - README.md
    - apps/dashboard/.prettierignore
  key_links:
    - "prettier 훅의 `files:` 정규식 범위가 두 포매터가 서로 싸우지 않게 만드는 유일한 장치다 — ruff는 `.py`만, prettier는 `apps/dashboard/` 아래 `.ts/.tsx/.css/.json/.md`만"
    - "prettier 훅은 `pnpm --dir apps/dashboard exec prettier` 를 호출하므로 계획 01-05가 설치한 로컬 prettier 실행 파일에 의존한다"
    - "`docs/**`·`supabase/**`·`checklists.json` 제외가 없으면 첫 실행이 기존 문서를 대규모로 재포맷해 SPEC R5의 '기존 파일 무변경' 수용기준이 즉시 깨진다"
  prohibitions: []
---

<objective>
포맷·린트를 어긴 커밋이 실제로 거부되는 상태를 만든다. 동시에 저장소 문서의 Python 앱 경로 표기를
SPEC/ROADMAP/REQUIREMENTS가 확정한 `apps/api` + `apps/worker`로 통일한다.

Purpose: BOOT-05. 이 페이즈에서 처음으로 두 언어의 코드가 저장소에 들어왔고, 지금 포매터 범위를
잘못 잡으면 첫 `pre-commit run --all-files`가 1,288줄의 SQL 마이그레이션과 57KB 원장을 재포맷해
리뷰 불가능한 커밋을 만든다.
Output: 범위가 좁혀진 `.pre-commit-config.yaml`, 루트 `README.md`, `.editorconfig`, 그리고 경로 표기가
정합된 `CLAUDE.md`·`checklists.json`·`.gitignore`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bootstrap-and-ground-truth/01-SPEC.md
@.planning/phases/01-bootstrap-and-ground-truth/01-CONTEXT.md
@.claude/CLAUDE.md
@.planning/phases/01-bootstrap-and-ground-truth/01-01-SUMMARY.md
@.planning/phases/01-bootstrap-and-ground-truth/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pre-commit(ruff + prettier) · ruff 설정 · `.editorconfig` — 범위를 좁혀서</name>
  <precondition>`apps/api`·`apps/worker`·`packages/core`에 Python 소스가 존재하고(계획 01-01), `apps/dashboard`에 `prettier`가 설치되어 있다(계획 01-05) — 두 포매터 모두 실제 대상 파일이 있어야 SPEC R5의 거부 증명이 가능하다</precondition>
  <read_first>
    - `pyproject.toml` — 계획 01-01이 만든 루트 매니페스트의 현재 내용 (`[tool.uv.workspace]`, `[dependency-groups]`, `[tool.pytest.ini_options]`). `[tool.ruff]`는 아직 없다
    - `apps/dashboard/package.json` — 설치된 `prettier` 버전과 스크립트 (훅이 호출할 실행 경로 확인)
    - `.planning/phases/01-bootstrap-and-ground-truth/01-SPEC.md` §Requirements R5 — 제외 대상(`docs/**`·`checklists.json`·`supabase/**`)이 명시된 이유
    - `.planning/research/STACK.md` §5 Tooling — `.pre-commit-config.yaml` 정확한 형태, `files:` 스코핑이 두 포매터의 충돌을 막는다는 설명, 선택할 ruff 룰셋(`E,F,I,UP,B,ASYNC,S`)
    - `.gitignore` — 이미 제외된 캐시 디렉터리 (`.ruff_cache/`, `.mypy_cache/`)
  </read_first>
  <files>.pre-commit-config.yaml, pyproject.toml, .editorconfig, apps/dashboard/.prettierignore</files>
  <action>
    **`pyproject.toml`에 ruff 설정 추가** — 기존 내용은 건드리지 않고 `[tool.ruff]` 이하만 덧붙인다.
    `target-version = "py312"` (D-04의 Python 3.12 고정과 일치), `line-length = 100`,
    `src = ["apps/api/src", "apps/worker/src", "packages/core/src"]` (`src` 레이아웃이라 이걸 주지 않으면
    `I`(isort) 룰이 자기 패키지를 서드파티로 오분류한다),
    `extend-exclude = ["supabase", "docs", ".planning"]`.
    `[tool.ruff.lint] select = ["E", "F", "I", "UP", "B", "ASYNC", "S"]`.
    `ASYNC`는 async 함수 안의 블로킹 호출을 잡고, `S`(bandit)는 하드코딩된 시크릿을 잡는다 — 둘 다 이
    프로젝트의 실제 실패 모드에 직결된다.
    `[tool.ruff.lint.per-file-ignores]` 에 `"**/tests/**" = ["S101"]` 을 둔다 (테스트의 `assert`가
    bandit 경고를 내지 않게).

    **`.pre-commit-config.yaml`** — repos 두 개.
    (1) `https://github.com/astral-sh/ruff-pre-commit`, `rev: v0.16.1`, 훅 `ruff-check`(`args: [--fix]`)와
    `ruff-format`. 두 훅 모두 `files: ^(apps|packages)/.*\.py$` 로 범위를 고정한다.
    (2) `repo: local` 의 `prettier` 훅 — `entry: pnpm --dir apps/dashboard exec prettier --write`,
    `language: system`, `files: ^apps/dashboard/.*\.(ts|tsx|css|json|md)$`,
    `exclude: ^apps/dashboard/(node_modules|\.next)/`.
    ⚠️ `files:` 스코핑이 이 파일의 전부다. 범위를 비워 두면 첫 `--all-files` 실행이 `supabase/migrations/`의
    1,288줄 SQL과 57KB 원장, `docs/`의 HTML까지 재포맷해 SPEC R5의 "기존 파일 무변경" 수용기준이 즉시
    깨진다. 이 반사실을 파일 상단 한국어 주석에 적는다.
    범용 공백/EOF 정리 훅(`trailing-whitespace`, `end-of-file-fixer` 등)은 **추가하지 않는다** — 저장소
    전역에 적용되어 같은 문제를 일으킨다.

    **`.editorconfig`** — `root = true`. 공통 섹션은 `charset = utf-8`, `end_of_line = lf`,
    `insert_final_newline = true`, `trim_trailing_whitespace = true`, `indent_style = space`.
    언어별로 `[*.py] indent_size = 4`, `[*.{ts,tsx,js,jsx,json,css}] indent_size = 2`,
    `[*.sql] indent_size = 2` (`supabase/migrations/`의 기존 2-space 스타일과 일치),
    `[*.md] trim_trailing_whitespace = false` (마크다운 줄바꿈 보존),
    `[Makefile] indent_style = tab`.

    **`apps/dashboard/.prettierignore`** — `pnpm-lock.yaml`, `.next/`, `node_modules/`, `out/`, `dist/`.
    lockfile을 prettier가 재포맷하면 `--frozen-lockfile` 설치가 깨질 수 있다.

    설정만 만들고 이 태스크에서는 `--all-files`를 실행하지 않는다 — 실행과 판정은 Task 3이다.
  </action>
  <acceptance_criteria>
    - `.pre-commit-config.yaml` · `.editorconfig` · `apps/dashboard/.prettierignore` 세 파일이 모두 존재한다
    - `pre-commit validate-config .pre-commit-config.yaml` 종료 코드 0
    - `.pre-commit-config.yaml` 에 `^(apps|packages)/.*\.py$` 문자열이 2회 등장한다 (ruff 훅 2개 각각)
    - `.pre-commit-config.yaml` 에 `^apps/dashboard/` 로 시작하는 `files:` 정규식이 1회 등장한다
    - `grep -cE '(trailing-whitespace|end-of-file-fixer|check-yaml)' .pre-commit-config.yaml` 이 0 (저장소 전역 훅 미도입)
    - `node -e "const t=require('fs').readFileSync('pyproject.toml','utf8');process.exit(t.includes('target-version = \"py312\"')?0:1)"` 종료 코드 0
    - `pyproject.toml` 에 `ASYNC` 와 `S` 두 룰 코드가 모두 등장한다
    - `pyproject.toml` 에 `extend-exclude` 가 있고 `supabase` · `docs` · `.planning` 셋을 모두 포함한다
    - `uv run ruff check apps packages` 종료 코드 0
    - `uv run ruff format --check apps packages` 종료 코드 0
    - `head -1 .editorconfig` 가 `root = true` 를 포함한다
  </acceptance_criteria>
  <verify>
    <automated>pre-commit validate-config .pre-commit-config.yaml &amp;&amp; uv run ruff check apps packages &amp;&amp; uv run ruff format --check apps packages</automated>
  </verify>
  <done>ruff와 prettier가 각자 정확한 범위로 설정되었고, `docs/**`·`supabase/**`·`checklists.json`은 어느 쪽 대상도 아니다.</done>
</task>

<task type="auto">
  <name>Task 2: 루트 `README.md` 작성 및 저장소 문서의 Python 앱 경로 표기 정합</name>
  <read_first>
    - `.claude/CLAUDE.md` 전체 — Python 앱 경로가 구 표기로 남아 있는 모든 위치(§Languages, §Frameworks, §Configuration, §Naming Patterns, §Component Responsibilities 등)
    - `checklists.json` `phases[0].tasks` — `P0-INIT-01`~`P0-INIT-04` 의 설명 문자열에 남은 구 경로 표기
    - `.gitignore` — Python 섹션 배너 주석의 구 경로 표기
    - `HANDOFF.md` §4 "자주 쓸 명령" 과 §"포트가 기본값이 아닙니다" — README가 옮겨 적을 로컬 실행 정보
    - `docs/ops/railway-deploy-record.md` (계획 01-06) — README에 적을 두 서비스의 start command
    - `.planning/phases/01-bootstrap-and-ground-truth/01-CONTEXT.md` D-09 — "SPEC/ROADMAP/REQUIREMENTS의 `apps/api` + `apps/worker`가 이깁니다"
    - `.planning/research/STACK.md` §"Stack Patterns by Variant" — 서비스별 config-as-code 경로 설정을 못 쓸 경우 두 start command를 README에 기록하라는 지침
  </read_first>
  <files>README.md, .claude/CLAUDE.md, checklists.json, .gitignore</files>
  <action>
    **루트 `README.md`** (신규, 한국어). `HANDOFF.md`의 문체를 따르되 세션 핸드오프가 아니라 프로젝트
    소개다. 담을 절:
    1. 한 문단 소개 — 원시 소스를 LLM이 상호 링크된 위키로 컴파일하고 5채널 하이브리드 검색으로
       원문과 위키 양쪽을 인용한 답변을 돌려주는 팀 단위 SaaS. 테넌트 격리는 애플리케이션이 아니라
       Postgres RLS가 강제한다는 한 줄을 반드시 포함한다.
    2. 저장소 구조 — `apps/api` · `apps/worker` · `packages/core` · `apps/dashboard` ·
       `supabase/migrations` 다섯 항목과 각 한 줄 설명. `apps/dashboard`는 uv 워크스페이스 멤버가
       아니라는 점을 명시한다.
    3. 로컬 개발 — `uv sync --frozen`, `supabase start`, `bash scripts/smoke_tracer.sh`,
       `uv run pytest -q`, `pnpm --dir apps/dashboard exec vitest run`, `pre-commit install`.
       ⚠️ 로컬 포트가 544xx인 이유(같은 머신의 다른 프로젝트가 543xx 점유)와, 로컬 `psql`이 없어
       `docker exec -it supabase_db_NexusWiki psql -U postgres -d postgres`를 쓴다는 점을 적는다.
    4. 배포 — Railway 프로젝트 1개 · 서비스 2개 · 리전 `asia-southeast1` · Root Directory 둘 다 `/`,
       그리고 **두 서비스의 start command를 명시적으로 적는다** (`api`는 이미지 기본 `python -m api`,
       `worker`는 Custom Start Command `python -m worker`). 서비스별 config-as-code 경로 설정이 공식
       레퍼런스에 없어 대시보드에 사는 설정이므로, 이 절이 그 설정의 리뷰 가능한 기록이다.
       Supabase 리전은 `ap-southeast-1`이며 **두 리전 모두 생성 후 변경 불가**임을 적는다.
    5. 규약 — 주석·커밋 메시지·문서는 한국어, 식별자·파일명은 영문. 커밋 형식
       `type(scope): <주제> — <구체적 변경>`, 한 태스크 = 한 커밋.
    시크릿·project ref·접속 문자열은 적지 않는다. env 키는 `.env.sample`을 가리키는 것으로 갈음한다.

    **경로 표기 정합** — SPEC/ROADMAP/REQUIREMENTS가 확정한 이름은 `apps/api`와 `apps/worker`다.
    아래 세 파일에서 구 표기(단일 Python 앱 디렉터리를 가리키던 이름)를 찾아 실제 구조에 맞게 바꾼다.
    <!-- planner-discipline-allow: apps/fastapi-backend -->
    구 표기는 `apps/fastapi-backend` 이며, 문맥에 따라 `apps/api` 또는 `apps/worker` 또는 둘 다로
    치환해야 한다 — 일괄 치환하지 말고 각 문장이 API를 말하는지 워커를 말하는지 읽고 정한다.
    - `.claude/CLAUDE.md` — §Languages · §Frameworks · §Configuration · §Naming Patterns ·
      §Component Responsibilities · §Architecture 등 등장하는 모든 위치. 함께 갱신할 것:
      Python 버전 표기를 3.12로, 패키지 매니저를 uv로 확정, 테스트 러너를 pytest/Vitest 실재로,
      `[PLANNED]`로 표시된 항목 중 이 페이즈에서 실제로 생긴 것(`pyproject.toml`·`uv.lock`·
      `.pre-commit-config.yaml`·`.editorconfig`·루트 `README.md`·`Dockerfile`·`railway.json`)의 표시를 제거.
      **`Write` 도구를 쓰지 말고 `Edit`로 해당 구절만 치환한다** — 전체 재작성은 이 파일의 다른 내용을 날린다.

    **결정 인용처를 두 계층으로 명문화** — `.claude/CLAUDE.md` §File Header Convention에 항목 한 개를
    `Edit`로 추가한다. 현재 규약은 "파일 헤더에 태스크 ID와 `checklists.json`의 결정 키를 인용하고,
    결정의 근거를 인라인으로 되풀이하지 말고 원장을 가리킨다" 한 갈래뿐인데, 이 페이즈에서 들어온 파일
    헤더 다수가 `01-CONTEXT.md > D-XX`를 가리킨다. `D-01`~`D-14`는 이 페이즈 안에서만 유효한 결정이라
    `checklists.json`의 `decisions`에 대응 키가 없기 때문이며, 규약을 어긴 것이 아니라 규약이 한 계층만
    말하고 있었던 것이다. 추가할 문장이 담을 것:
    - 프로젝트 수명 전체에 걸치는 결정(스택 · 배포 · DB 접근 모델 등)은 `checklists.json`의
      `decisions.<key>`를 인용한다.
    - 한 페이즈 안에서만 유효한 결정은 그 페이즈의 컨텍스트 문서를 `NN-CONTEXT.md > D-XX` 형태로
      인용한다 (실제 경로는 `.planning/phases/NN-*/NN-CONTEXT.md`).
    - 어느 계층을 가리킬지는 **결정의 수명**이 정한다. 같은 근거를 두 곳에 적지 않는다는 기존 원칙은
      두 계층 모두에 그대로 적용된다.
    ⚠️ 이것은 주석이 아니라 실제 파일 편집이다 — 규약이 문서에 없으면 다음 페이즈의 작성자가 같은
    갈림길에서 다시 임의로 정한다.
    - `checklists.json` — `phases[0].tasks` 의 `P0-INIT-01`~`P0-INIT-04` 설명 문자열에 남은 구 경로 표기.
      `_notes`가 금지한 파생/요약 필드를 새로 만들지 않는다. `status`·`verification_result`·
      open question은 이 태스크에서 건드리지 않는다 — 계획 01-08이 소유한다.
    - `.gitignore` — Python 섹션 배너 주석의 구 경로 표기를 `apps/api, apps/worker`로 바꾼다.
      제외 패턴 자체는 바꾸지 않는다.
  </action>
  <acceptance_criteria>
    - 루트 `README.md` 가 존재하고 200줄 이하다
    - `README.md` 에 `apps/api` · `apps/worker` · `packages/core` · `apps/dashboard` 네 경로가 모두 등장한다
    - `README.md` 에 `python -m api` 와 `python -m worker` 두 start command가 모두 등장한다
    - `README.md` 에 `asia-southeast1` 과 `ap-southeast-1` 두 리전 문자열이 모두 등장한다
    - `README.md` 에 `54422` 가 등장한다 (로컬 DB 포트 안내)
    - `grep -cE '(sb_publishable_|sb_secret_|postgresql://[^1])' README.md` 이 0
    - `grep -rc 'fastapi-backend' .claude/CLAUDE.md checklists.json .gitignore` 결과가 세 파일 모두 0
    - `grep -c 'NN-CONTEXT.md > D-XX' .claude/CLAUDE.md` 이 1 이상 (페이즈 로컬 결정의 인용 형태가 규약으로 명문화됨)
    - `.claude/CLAUDE.md` §File Header Convention 에 `decisions.<key>` 와 `NN-CONTEXT.md > D-XX` 두 인용처가 **함께** 등장하고, 선택 기준이 결정의 수명이라고 적혀 있다
    - `node -e "JSON.parse(require('fs').readFileSync('checklists.json','utf8'))"` 종료 코드 0 (원장이 여전히 유효한 JSON)
    - `node -e "const j=require('./checklists.json');const t=j.phases[0].tasks;process.exit(t.length===5&&t.every(x=>x.status)?0:1)"` 종료 코드 0 (태스크 5건과 `status` 필드가 보존됨)
    - `git diff --stat .gitignore` 이 보여주는 변경 줄이 2 이하 (배너 주석만 변경)
  </acceptance_criteria>
  <verify>
    <automated>test -f README.md &amp;&amp; ! grep -rq 'fastapi-backend' .claude/CLAUDE.md checklists.json .gitignore &amp;&amp; grep -q 'NN-CONTEXT.md > D-XX' .claude/CLAUDE.md &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('checklists.json','utf8'))"</automated>
  </verify>
  <done>루트 README가 저장소 구조·로컬 실행·두 서비스 start command·리전을 담고, 세 문서의 Python 앱 경로 표기가 실제 구조와 일치하며, 결정 인용처의 두 계층(전역 = `checklists.json > decisions.<key>`, 페이즈 로컬 = `NN-CONTEXT.md > D-XX`)이 `CLAUDE.md`에 명문화되었다.</done>
</task>

<task type="auto">
  <name>Task 3: 거부 증명 — 포맷을 어긴 Python/TS 커밋이 실제로 막히고, `--all-files`가 기존 파일을 바꾸지 않는다</name>
  <precondition>`pre-commit` 실행 파일을 쓸 수 있다 (`uv run pre-commit --version`이 성공) 그리고 `pnpm --dir apps/dashboard exec prettier --version`이 성공한다</precondition>
  <read_first>
    - `.pre-commit-config.yaml` — Task 1이 만든 실제 훅 id와 `files:` 정규식
    - `pyproject.toml` `[tool.ruff]` — 활성화된 룰셋 (어떤 위반을 만들어야 실제로 걸리는지)
    - `.planning/phases/01-bootstrap-and-ground-truth/01-SPEC.md` §Acceptance Criteria — "포맷을 어긴 Python 및 TS 파일의 커밋이 pre-commit에서 거부된다", "`pre-commit run --all-files`가 통과하며 기존 파일을 하나도 변경하지 않는다"
  </read_first>
  <files>.pre-commit-config.yaml, pyproject.toml, apps/dashboard/.prettierignore</files>
  <action>
    **1단계 — 정규화.** 훅을 설치하고(`uv run pre-commit install`) 한 번
    `uv run pre-commit run --all-files`를 돌린다. 이 첫 실행에서 계획 01-01·01-05가 만든 새 파일이
    수정될 수 있다 — 수정 결과를 그대로 커밋해 기준선을 만든다. 이때 `docs/`·`supabase/`·
    `checklists.json`·`HANDOFF.md`·`.planning/`이 **하나도** 수정되지 않아야 한다. 하나라도 수정되면
    Task 1의 `files:` 범위가 잘못된 것이므로 범위를 고치고 되돌린 뒤 다시 시작한다.

    **2단계 — 무변경 판정.** 정규화 커밋 직후 `uv run pre-commit run --all-files`를 다시 돌린다.
    이번에는 종료 코드 0이어야 하고 `git status --porcelain`이 빈 출력이어야 한다. 이것이 SPEC R5의
    "통과하며 기존 파일을 하나도 변경하지 않음" 판정이다.

    **3단계 — Python 거부 증명.** `apps/api/src/api/_lint_probe.py` 를 임시로 만들되 활성 룰을 확실히
    어기게 한다 — 미사용 import 하나(`F401`), 정렬되지 않은 import 순서(`I001`), 그리고 포맷 위반
    (한 줄에 여러 문장, 잘못된 들여쓰기). `git add` 후 `git commit`을 시도하면 pre-commit이 **실패**해야
    한다. 종료 코드가 0이면 이 태스크는 실패다. 확인 후 파일을 삭제하고 인덱스를 정리한다.

    **4단계 — TS 거부 증명.** `apps/dashboard/lib/_lint_probe.ts` 를 임시로 만들되 prettier가 반드시
    다시 쓰게 한다 — 들여쓰기가 어긋난 객체 리터럴, 과도한 줄 길이, 일관되지 않은 따옴표.
    `git add` 후 `git commit`을 시도하면 prettier 훅이 파일을 수정해 커밋이 **실패**해야 한다.
    확인 후 파일을 삭제하고 인덱스를 정리한다.

    **5단계 — 범위 회귀 가드.** `supabase/migrations/0005_storage.sql` 와 `checklists.json` 을
    각각 `touch` 한 뒤 `uv run pre-commit run --files supabase/migrations/0005_storage.sql checklists.json`
    을 돌린다. 두 파일 모두 어떤 훅의 대상도 아니어야 하고 `git status --porcelain`이 여전히 비어 있어야 한다.

    3~5단계의 관측 결과(각 단계의 종료 코드와 어떤 훅이 걸었는지)를 SUMMARY에 기록한다.
    프로브 파일은 저장소에 남기지 않는다 — 다섯 단계가 끝난 뒤 `git status --porcelain`이 비어야 한다.
  </action>
  <acceptance_criteria>
    - `uv run pre-commit run --all-files` 종료 코드 0
    - 그 직후 `git status --porcelain` 이 빈 출력
    - 3단계에서 만든 Python 프로브의 커밋 시도가 종료 코드 0이 **아니다**
    - 4단계에서 만든 TS 프로브의 커밋 시도가 종료 코드 0이 **아니다**
    - 5단계 실행 후 `git status --porcelain` 이 여전히 빈 출력 (`supabase/**`·`checklists.json` 무변경)
    - 모든 단계 종료 후 `apps/api/src/api/_lint_probe.py` 와 `apps/dashboard/lib/_lint_probe.ts` 가 존재하지 않는다
    - `git log --oneline -1 -- docs/` 가 이 계획의 커밋을 포함하지 않는다 (`docs/`가 재포맷되지 않음)
  </acceptance_criteria>
  <verify>
    <automated>uv run pre-commit run --all-files &amp;&amp; test -z "$(git status --porcelain)" &amp;&amp; test ! -f apps/api/src/api/_lint_probe.py &amp;&amp; test ! -f apps/dashboard/lib/_lint_probe.ts</automated>
  </verify>
  <done>포맷을 어긴 Python과 TS 파일의 커밋이 각각 거부되고, `--all-files`가 통과하면서 기존 문서·마이그레이션·원장을 하나도 바꾸지 않는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-commit 훅 정의 → 개발자 머신에서의 코드 실행 | `repo:` URL과 `rev:`가 가리키는 코드가 매 커밋마다 로컬에서 실행된다 |
| `language: system` 로컬 훅 → 개발자 셸 | `entry:`에 적힌 명령이 셸 권한으로 실행된다 |
| 저장소 문서(`README.md`) → 공개 열람 | 저장소가 공개되면 README의 모든 내용이 함께 공개된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | pre-commit `repo:` 원격 훅 | medium | mitigate | `astral-sh/ruff-pre-commit` 하나만 원격으로 쓰고 `rev: v0.16.1`로 태그를 고정한다. 부동 `rev`(브랜치명)를 쓰지 않아 훅 코드가 조용히 바뀌지 않게 한다 |
| T-07-02 | Elevation of Privilege | `language: system` prettier 훅 | low | accept | `entry:`가 저장소 내부 `pnpm --dir apps/dashboard exec prettier` 하나만 호출한다. 임의 셸 스크립트를 두지 않으며, prettier 자체는 계획 01-05의 정당성 게이트를 통과한 패키지다 |
| T-07-03 | Information Disclosure | 루트 `README.md` | medium | mitigate | 시크릿·project ref·접속 문자열을 적지 않는다. env 키는 이름조차 나열하지 않고 `.env.sample`을 가리키는 것으로 갈음. negative grep을 acceptance criteria로 고정 |
| T-07-04 | Tampering | ruff `S`(bandit) 룰 미적용 범위 | medium | mitigate | `select`에 `S`를 포함해 `apps/**`·`packages/**`의 하드코딩 시크릿을 커밋 시점에 잡는다. 테스트 디렉터리만 `S101`을 면제하며 다른 `S` 룰은 테스트에도 유효하게 남긴다 |
| T-07-05 | Denial of Service | 첫 `--all-files` 실행의 대규모 재포맷 | high | mitigate | 두 훅 모두 `files:` 정규식으로 범위 고정, ruff는 `extend-exclude`로 `supabase`·`docs`·`.planning` 추가 차단, 저장소 전역 공백 훅 미도입. Task 3 5단계가 범위 회귀를 기계적으로 가드 |
| T-07-06 | Repudiation | 원장(`checklists.json`) 구조 훼손 | medium | mitigate | 경로 문자열만 치환하고 `status`·`verification_result`·open question은 손대지 않는다. JSON 파싱 성공과 태스크 5건·`status` 필드 보존을 acceptance criteria로 단언 |
| T-07-SC | Tampering | 패키지 매니저 설치 | low | accept | 새 npm/pip 패키지를 추가하지 않는다. `pre-commit`은 계획 01-01의 blocking-human 게이트를 이미 통과했고 `uv.lock`에 고정되어 있다 |
</threat_model>

<verification>
1. `pre-commit validate-config .pre-commit-config.yaml` 종료 코드 0
2. `uv run ruff check apps packages` · `uv run ruff format --check apps packages` 둘 다 종료 코드 0
3. `uv run pre-commit run --all-files` 종료 코드 0이고 직후 `git status --porcelain`이 빈 출력
4. Python 프로브 · TS 프로브의 커밋 시도가 각각 실패
5. `supabase/**`·`checklists.json` 대상 실행 후에도 `git status --porcelain` 빈 출력
6. `grep -rq 'fastapi-backend' .claude/CLAUDE.md checklists.json .gitignore` 가 아무것도 찾지 못함
7. `.claude/CLAUDE.md` §File Header Convention에 결정 인용처 두 계층이 명문화됨 (`grep -q 'NN-CONTEXT.md > D-XX' .claude/CLAUDE.md`)
</verification>

<success_criteria>
- 포맷을 어긴 Python 및 TS 커밋이 실제로 거부된다
- `pre-commit run --all-files`가 통과하면서 기존 파일을 하나도 바꾸지 않는다
- 루트 `README.md`와 `.editorconfig`가 존재하고, README가 두 서비스의 start command와 두 리전을 기록한다
- 저장소 문서의 Python 앱 경로 표기가 `apps/api` + `apps/worker`로 통일되었다
</success_criteria>

## Artifacts this phase produces

**파일:** `.pre-commit-config.yaml`, `.editorconfig`, `README.md`(루트), `apps/dashboard/.prettierignore`

**설정 키(신규):** `pyproject.toml` `[tool.ruff]` (`target-version`, `line-length`, `src`, `extend-exclude`),
`[tool.ruff.lint]` (`select`), `[tool.ruff.lint.per-file-ignores]`

**pre-commit 훅 id:** `ruff-check`, `ruff-format`, `prettier`(local)

**규약(신규 명문화):** `.claude/CLAUDE.md` §File Header Convention의 결정 인용처 2계층 —
전역·교차 페이즈 결정은 `checklists.json > decisions.<key>`, 페이즈 로컬 결정은 `NN-CONTEXT.md > D-XX`.
이 페이즈의 파일 헤더(`packages/core/.../logging.py` → D-13, `apps/api/.../__main__.py` → D-03,
`Dockerfile` → D-01..D-04, `apps/worker/.../rtt.py` → D-14)가 두 번째 계층에 해당한다.

**수정되는 기존 파일:** `.claude/CLAUDE.md`(경로 표기 · `[PLANNED]` 해제 · 결정 인용처 2계층 규약),
`checklists.json`(`phases[0].tasks` 설명 문자열의 경로 표기만),
`.gitignore`(Python 섹션 배너 주석만)

<output>
Create `.planning/phases/01-bootstrap-and-ground-truth/01-07-SUMMARY.md` when done
</output>
