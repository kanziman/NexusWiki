---
phase: 01-bootstrap-and-ground-truth
plan: 05
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/dashboard/package.json
  - apps/dashboard/pnpm-lock.yaml
  - apps/dashboard/tsconfig.json
  - apps/dashboard/next.config.ts
  - apps/dashboard/postcss.config.mjs
  - apps/dashboard/app/layout.tsx
  - apps/dashboard/app/page.tsx
  - apps/dashboard/app/globals.css
  - apps/dashboard/lib/workspace-path.ts
  - apps/dashboard/components/HealthBadge.tsx
  - apps/dashboard/vitest.config.ts
  - apps/dashboard/vitest.setup.ts
  - apps/dashboard/tests/workspace-path.test.ts
  - apps/dashboard/tests/HealthBadge.test.tsx
autonomous: false
requirements: [BOOT-07]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`apps/dashboard/package.json`의 next 버전이 15.5.22 이상이다 (CVE-2025-29927 하한 15.2.3 위)"
    - "`tsc --noEmit`이 통과한다"
    - "`vitest run`이 `'use client'` 컴포넌트 1개 + 순수 함수 1개, 정확히 2개 테스트를 통과한다"
    - "Vitest 스모크 대상이 클라이언트 컴포넌트와 순수 함수로 한정되고 async Server Component 렌더링을 시도하지 않는다 (RSC 테스트 전략은 Phase 6)"
    - "로컬 dev 서버가 기동한다"
    - "Tailwind 4가 CSS-first로 설정된다 — `tailwind.config.js`가 없고 `@tailwindcss/postcss`를 통해 `@import \"tailwindcss\";` 한 줄로 들어온다"
  artifacts:
    - apps/dashboard/package.json
    - apps/dashboard/pnpm-lock.yaml
    - apps/dashboard/tsconfig.json
    - apps/dashboard/postcss.config.mjs
    - apps/dashboard/vitest.config.ts
    - apps/dashboard/lib/workspace-path.ts
    - apps/dashboard/components/HealthBadge.tsx
    - apps/dashboard/tests/workspace-path.test.ts
    - apps/dashboard/tests/HealthBadge.test.tsx
  key_links:
    - "`apps/dashboard`는 uv 워크스페이스 멤버가 **아니다** — 독립 `package.json`과 자체 `pnpm-lock.yaml`을 갖는다 (D-10: JS 패키지가 하나뿐이라 pnpm workspace/Turborepo/Nx를 도입하지 않음)"
    - "`apps/dashboard`에 설치된 `prettier`가 계획 01-07의 pre-commit local 훅이 호출하는 실행 파일이다 — 여기 없으면 그 훅이 동작하지 않는다"
    - "`package.json` scripts의 `NEXT_TELEMETRY_DISABLED=1` 접두가 금지사항 P3의 프런트엔드 쪽 집행 지점이다"
  prohibitions:
    - statement: "빌드·런타임이 사용자 동의 없이 외부 텔레메트리를 전송해서는 안 된다 (Next.js 기본 동작)"
      status: resolved
      verification: test
---

<objective>
Next.js 15.5.22 대시보드를 Tailwind 4 · TypeScript strict · Vitest로 스캐폴딩한다. 이 페이즈에서
배포는 하지 않는다(Vercel은 Phase 6) — 목표는 "로컬에서 기동하고 타입 검사와 테스트가 통과하는 앱이
저장소에 존재한다"까지다.

Purpose: BOOT-07. 하한 15.2.3은 CVE-2025-29927(`x-middleware-subrequest` 헤더 위조로 미들웨어 우회)
때문이며, 이 앱의 테넌트 게이트가 바로 미들웨어라 이 CVE는 격리 전면 우회에 해당한다. 목표는 15.5.22 이상이다.
Output: 독립 `package.json`을 가진 `apps/dashboard`, CSS-first Tailwind 4 설정, 그리고 Phase 6의
테스트 3층 구조 중 아래 두 층(순수 함수 · 클라이언트 컴포넌트)을 여는 Vitest 2건.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bootstrap-and-ground-truth/01-SPEC.md
@.planning/phases/01-bootstrap-and-ground-truth/01-CONTEXT.md
@.planning/phases/01-bootstrap-and-ground-truth/01-PATTERNS.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: npm 패키지 정당성 확인 (공급망 게이트)</name>
  <what-built>
    아직 아무것도 설치하지 않았다. Task 2·3이 `pnpm add`로 끌어올 패키지를 설치 **이전에** 확인하는 자리다.

    `.planning/research/STACK.md` §Sources는 아래를 2026-08-01에 `registry.npmjs.org/<pkg>` 첫 파티
    레지스트리(dist-tags + time map 포함)로 직접 해소했다고 기록한다 — [VERIFIED]로 취급한다:
    `next@15.5.22` · `react@19.2.8` · `typescript@5.9.3` · `tailwindcss@4.3.3` · `vitest@4.1.10` ·
    `@vitejs/plugin-react@6.0.5` · `jsdom@30.0.1` · `@testing-library/react@16.3.2` · `prettier@3.9.6`

    레지스트리 해소 기록이 없어 [ASSUMED]로 남은 7건:
    `react-dom@19.2.8` · `@tailwindcss/postcss@4.3.3` · `postcss` · `vite-tsconfig-paths@6.1.1` ·
    `@testing-library/jest-dom@7.0.0` · `@testing-library/user-event@14.6.1` · `eslint-config-next`
  </what-built>
  <how-to-verify>
    위 [ASSUMED] 7건 각각에 대해 `https://www.npmjs.com/package/<name>` 를 열어 확인한다:
    - 저장소 링크가 기대하는 조직인가 (`facebook/react` · `tailwindlabs/tailwindcss` ·
      `postcss/postcss` · `aleclarson/vite-tsconfig-paths` · `testing-library/*` · `vercel/next.js`)
    - 최근 발행일이 1년 이내인가
    - 주간 다운로드가 오타 스쿼팅 수준(수십~수백)이 아닌가
    - 명시된 버전이 실제로 발행 목록에 존재하는가

    `[SLOP]`(존재하지 않거나 최근 생성된 유령 패키지)로 판정되는 항목이 하나라도 있으면 실행을 중단하고
    그 이름을 보고한다 — 이 게이트는 `auto_advance` 설정과 무관하게 자동 승인되지 않는다.
  </how-to-verify>
  <resume-signal>Type "approved" or name the package(s) you rejected</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Next.js 15.5.22 · Tailwind 4 · TS strict 스캐폴딩</name>
  <reversibility rating="costly">Next 15 라인 고정(15.5.22)은 되돌리려면 `@supabase/ssr` 쿠키 계약과 Phase 6의 미들웨어 테넌트 게이트가 함께 움직인다. Next 16 업그레이드는 v1 이후 별도 작업으로 예산한다.</reversibility>
  <read_first>
    - `.planning/phases/01-bootstrap-and-ground-truth/01-SPEC.md` §Requirements R7, §Constraints (Next.js 하한)
    - `.planning/phases/01-bootstrap-and-ground-truth/01-CONTEXT.md` — D-09(`apps/dashboard` 이름 근거) · D-10(pnpm workspace 미도입)
    - `.planning/research/STACK.md` §3.3(Tailwind 4 CSS-first 정확한 설치 형태) · §"Three findings" 3번(15.5.22 고정과 CVE) · §"What NOT to Use"(Tailwind 3 문법 · Turborepo/Nx/pnpm workspace)
    - `.gitignore` — `node_modules/` · `.next/` · `out/` · `dist/` · `*.tsbuildinfo`가 이미 커버됨
    - `docs/design-systems/design-tokens.css` — Phase 6 UI 작업이 소비할 기존 디자인 토큰 (이 페이즈에서 복제하지 않는다)
  </read_first>
  <files>
    apps/dashboard/package.json, apps/dashboard/pnpm-lock.yaml, apps/dashboard/tsconfig.json,
    apps/dashboard/next.config.ts, apps/dashboard/postcss.config.mjs,
    apps/dashboard/app/layout.tsx, apps/dashboard/app/page.tsx, apps/dashboard/app/globals.css,
    apps/dashboard/lib/workspace-path.ts, apps/dashboard/components/HealthBadge.tsx
  </files>
  <action>
    `apps/dashboard`에 **독립 `package.json`** 을 만든다. 루트에 `package.json`을 만들지 않고,
    pnpm workspace도 Turborepo도 Nx도 도입하지 않는다 (D-10: JS 패키지가 하나뿐이다).
    패키지 매니저는 pnpm이며 `pnpm-lock.yaml`은 `apps/dashboard` 안에 생긴다 — 커밋한다.

    **의존성 (정확한 버전 고정)** — dependencies: `next@15.5.22`, `react@19.2.8`, `react-dom@19.2.8`.
    devDependencies: `typescript@5.9.3`, `@types/react`, `@types/react-dom`, `@types/node`,
    `tailwindcss@4.3.3`, `@tailwindcss/postcss@4.3.3`, `postcss`, `prettier@3.9.6`, `eslint`,
    `eslint-config-next@15.5.22`. 테스트 러너 계열은 Task 3에서 추가한다.
    `tailwindcss`와 `@tailwindcss/postcss`는 **버전이 정확히 일치**해야 한다 — 한 쌍으로 배포된다.

    **`package.json` scripts** — `dev` · `build` · `start` · `lint` · `typecheck` 다섯 개.
    `dev`/`build`/`start` 세 개는 모두 `NEXT_TELEMETRY_DISABLED=1` 접두를 붙여 실행한다
    (금지사항 P3: 사용자 동의 없는 외부 텔레메트리 전송 금지 — 이것이 프런트엔드 쪽 집행 지점이다).
    `typecheck`는 `tsc --noEmit`.

    **`tsconfig.json`** — `"strict": true`. `paths`에 `"@/*": ["./*"]` 하나를 두고
    `include`에 `tests`를 포함시켜 테스트 파일도 타입 검사 대상이 되게 한다.

    **Tailwind 4는 CSS-first** — `tailwind.config.js`를 **만들지 않는다**.
    `postcss.config.mjs`는 `@tailwindcss/postcss` 플러그인 하나만 선언한다.
    `app/globals.css`의 첫 줄은 `@import "tailwindcss";` 이며, v3 문법(`@tailwind base;` 등)은 쓰지 않는다.
    globals.css 하단에 한국어 주석 한 줄로 "디자인 토큰(`docs/design-systems/design-tokens.css`)의
    `@theme` 편입은 Phase 6 UI 작업 범위"라고만 적고, 이 페이즈에서 토큰 파일을 복제하지 않는다.

    **`app/layout.tsx` · `app/page.tsx`** — App Router 최소 셸. `layout.tsx`는 `globals.css`를 import하고
    `lang="ko"`를 둔다. `page.tsx`는 Server Component로 두되 **`await`하는 데이터 페치를 넣지 않는다**
    (Vitest가 async Server Component를 렌더링하지 못하므로 Phase 6까지 이 제약을 유지한다).
    `page.tsx`는 `HealthBadge`를 `status="unknown"`으로 한 번 렌더링한다.

    **`lib/workspace-path.ts`** — 순수 함수 하나. `export function workspacePath(workspaceId: string): string`
    가 `/w/${workspaceId}` 를 반환하고, 빈 문자열이나 공백만 있는 입력에는 `TypeError`를 던진다.
    ROADMAP Phase 6 UI-01의 `/w/[workspaceId]` 테넌시 규약을 코드로 고정하는 자리이며,
    Task 3의 순수 함수 테스트 대상이다.

    **`components/HealthBadge.tsx`** — 첫 줄이 `'use client'`. props는
    `{ status: 'ok' | 'degraded' | 'unknown' }` 하나. 세 상태를 각각 한국어 라벨
    (`정상` · `저하` · `알 수 없음`)로 렌더링하고 `data-status={status}` 속성을 붙인다
    (테스트가 클래스 이름이 아니라 이 속성으로 단언한다 — 스타일 변경이 테스트를 깨지 않게).
    Task 3의 클라이언트 컴포넌트 테스트 대상이다.

    설치가 끝나면 `pnpm --dir apps/dashboard exec tsc --noEmit`과
    `NEXT_TELEMETRY_DISABLED=1 pnpm --dir apps/dashboard build`가 모두 통과하는지 확인한다.
  </action>
  <acceptance_criteria>
    - `node -p "require('./apps/dashboard/package.json').dependencies.next"` 이 `15.5.22` 이상을 출력한다
    - `apps/dashboard/pnpm-lock.yaml` 이 존재하고 `git ls-files apps/dashboard/pnpm-lock.yaml` 이 비어 있지 않다
    - 저장소 루트에 `package.json` 이 **존재하지 않는다** (pnpm workspace 미도입 — D-10)
    - `apps/dashboard/tailwind.config.js` 와 `apps/dashboard/tailwind.config.ts` 둘 다 존재하지 않는다
    - `head -1 apps/dashboard/app/globals.css` 가 `@import "tailwindcss";`
    - `node -p "require('./apps/dashboard/package.json').devDependencies.tailwindcss === require('./apps/dashboard/package.json').devDependencies['@tailwindcss/postcss']"` 이 `true`
    - `grep -c '"strict": true' apps/dashboard/tsconfig.json` 이 1
    - `grep -c 'NEXT_TELEMETRY_DISABLED=1' apps/dashboard/package.json` 이 3 이상
    - `head -1 apps/dashboard/components/HealthBadge.tsx` 가 `'use client'` 를 포함한다
    - `pnpm --dir apps/dashboard exec tsc --noEmit` 종료 코드 0
    - `NEXT_TELEMETRY_DISABLED=1 pnpm --dir apps/dashboard build` 종료 코드 0
    - `apps/dashboard/app/page.tsx` 에 `await` 가 등장하지 않는다 (async RSC 미도입 — Vitest 제약)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir apps/dashboard install --frozen-lockfile &amp;&amp; pnpm --dir apps/dashboard exec tsc --noEmit &amp;&amp; NEXT_TELEMETRY_DISABLED=1 pnpm --dir apps/dashboard build</automated>
  </verify>
  <done>Next 15.5.22 앱이 Tailwind 4 CSS-first · TS strict로 빌드되고, 루트에 `package.json`이 생기지 않았으며, 텔레메트리가 세 스크립트 전부에서 꺼져 있다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Vitest + Testing Library — 클라이언트 컴포넌트 1건 + 순수 함수 1건</name>
  <read_first>
    - `apps/dashboard/lib/workspace-path.ts`, `apps/dashboard/components/HealthBadge.tsx` — Task 2가 만든 실제 시그니처
    - `apps/dashboard/tsconfig.json` — `paths` 별칭 (`vite-tsconfig-paths`가 이걸 읽는다)
    - `.planning/research/STACK.md` §3.6 — Vitest는 async Server Component를 렌더링하지 못한다는 제약과 3층 테스트 분할표
    - `.planning/phases/01-bootstrap-and-ground-truth/01-SPEC.md` §Requirements R7 — 스모크 대상이 정확히 2건으로 한정된 이유
  </read_first>
  <files>
    apps/dashboard/vitest.config.ts, apps/dashboard/vitest.setup.ts,
    apps/dashboard/tests/workspace-path.test.ts, apps/dashboard/tests/HealthBadge.test.tsx,
    apps/dashboard/package.json
  </files>
  <behavior>
    - Test 1 (순수 함수): `workspacePath('abc')` 가 `/w/abc` 를 반환한다
    - Test 1 엣지: `workspacePath('')` 와 `workspacePath('   ')` 가 `TypeError` 를 던진다
    - Test 2 (클라이언트 컴포넌트): `<HealthBadge status="ok" />` 렌더 결과에 `정상` 텍스트가 있고 `data-status` 속성이 `ok` 다
    - Test 2 엣지: `status="degraded"` 와 `status="unknown"` 이 각각 `저하` · `알 수 없음` 을 렌더링한다
  </behavior>
  <action>
    devDependencies에 `vitest@4.1.10`, `@vitejs/plugin-react@6.0.5`, `jsdom@30.0.1`,
    `vite-tsconfig-paths@6.1.1`, `@testing-library/react@16.3.2`, `@testing-library/jest-dom@7.0.0`,
    `@testing-library/user-event@14.6.1` 을 추가한다. `@testing-library/react`는 16 라인이어야
    React 19와 호환된다 — 14/15는 아니다.

    **`vitest.config.ts`** — `plugins: [react(), tsconfigPaths()]`,
    `test: { environment: 'jsdom', setupFiles: ['./vitest.setup.ts'], globals: true,
    include: ['tests/**/*.test.{ts,tsx}'] }`.
    `include`를 `tests/` 로 한정해 `app/` 아래 Server Component가 우연히 테스트 대상이 되지 않게 한다.

    **`vitest.setup.ts`** — `@testing-library/jest-dom/vitest` 를 import하고,
    `afterEach(() => cleanup())` 를 건다.

    **`tests/workspace-path.test.ts`** — 위 Behavior 1과 그 엣지. `describe` 1개, `it` 은 정확히 1개로
    묶어 SPEC R7의 "테스트 2개" 판정과 어긋나지 않게 한다 (엣지 단언은 같은 `it` 안의 추가 `expect`로 둔다).

    **`tests/HealthBadge.test.tsx`** — 위 Behavior 2와 그 엣지. 마찬가지로 `it` 은 정확히 1개.
    클래스 이름이 아니라 렌더된 한국어 텍스트와 `data-status` 속성으로 단언한다.

    `package.json` scripts에 `test` (= `vitest run`)와 `test:watch` (= `vitest`)를 추가한다.

    **async Server Component는 테스트하지 않는다.** React의 RSC 런타임이 테스트 환경에 없어
    `await`하는 컴포넌트를 렌더하는 순간 던진다 — 설정으로 우회할 수 있는 버그가 아니라 문서화된
    제약이다. RSC · 인증 · 미들웨어 테스트 전략은 Phase 6(Playwright)이 소유한다. 이 사실을
    `vitest.config.ts` 상단 한국어 주석에 `⚠️`로 남긴다.
  </action>
  <acceptance_criteria>
    - `pnpm --dir apps/dashboard exec vitest run` 종료 코드 0
    - 그 출력이 `2 passed` 를 포함한다 (테스트 정확히 2건)
    - `pnpm --dir apps/dashboard exec tsc --noEmit` 종료 코드 0 (테스트 파일 포함)
    - `grep -c "tests/\*\*" apps/dashboard/vitest.config.ts` 이 1 이상 (`include`가 `tests/`로 한정)
    - `apps/dashboard/tests/` 아래 파일에 `await` 를 사용하는 컴포넌트 렌더가 없다
    - `node -p "require('./apps/dashboard/package.json').devDependencies['@testing-library/react']"` 이 `16.` 으로 시작한다
    - `apps/dashboard/tests/workspace-path.test.ts` 와 `apps/dashboard/tests/HealthBadge.test.tsx` 각각에서 `it(` 이 정확히 1회 등장한다
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir apps/dashboard exec vitest run &amp;&amp; pnpm --dir apps/dashboard exec tsc --noEmit</automated>
  </verify>
  <done>`vitest run`이 클라이언트 컴포넌트 1건 + 순수 함수 1건, 정확히 2개 테스트를 통과하고 `tsc --noEmit`이 깨끗하다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm 레지스트리 → `pnpm-lock.yaml` → 개발자 머신 / 향후 빌드 | 서드파티 코드가 postinstall 스크립트 권한으로 실행된다 |
| Next.js 빌드/런타임 → Vercel 텔레메트리 엔드포인트 | 프레임워크가 기본적으로 익명 사용 통계를 외부로 보낸다 |
| 브라우저 → 미래의 미들웨어 테넌트 게이트 | 이 페이즈에는 미들웨어가 없지만, 프레임워크 버전 선택이 Phase 6 게이트의 안전성을 미리 결정한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | Next.js 미들웨어 (CVE-2025-29927) | high | mitigate | `next@15.5.22`를 정확히 고정하고 하한 15.2.3 위임을 acceptance criteria의 버전 단언으로 기계 판정. Phase 6의 테넌트 게이트가 미들웨어이므로 이 하한은 격리 요건이다 |
| T-05-02 | Information Disclosure | Next.js 텔레메트리 | medium | mitigate | `package.json`의 `dev`/`build`/`start` 세 스크립트 전부에 `NEXT_TELEMETRY_DISABLED=1` 접두. 계획 01-06의 Dockerfile `ENV`가 컨테이너 쪽 짝 (금지사항 P3) |
| T-05-03 | Tampering | `pnpm add` / npm 설치 경로 | high | mitigate | `.planning/research/STACK.md` §Sources의 첫 파티 레지스트리 해소 기록을 정당성 감사로 채택([VERIFIED] 9건). 미기록 [ASSUMED] 7건은 Task 1의 blocking-human 체크포인트가 설치 이전에 확인. `pnpm-lock.yaml` 커밋 + `--frozen-lockfile` 설치로 이후를 고정 |
| T-05-04 | Information Disclosure | 클라이언트 번들에 시크릿 유입 | high | mitigate | 이 페이즈의 대시보드는 어떤 Supabase 키도 읽지 않는다 — `NEXT_PUBLIC_*` 를 포함해 env 참조를 추가하지 않는다. 번들 grep 검증(SEC-05)은 Phase 2 소관이며, 지금은 표면 자체를 만들지 않는 것이 완화책이다 |
| T-05-05 | Denial of Service | async Server Component 렌더 실패 | low | accept | Vitest의 문서화된 제약. 테스트 `include`를 `tests/`로 한정하고 `page.tsx`에 `await`를 넣지 않아 회피. RSC 전략은 Phase 6 |
| T-05-SC | Tampering | npm 설치 시 `[ASSUMED]`/`[SUS]` 패키지 | high | mitigate | Task 1 blocking-human 체크포인트. `[SLOP]` 판정 시 실행 중단. 이 체크포인트는 자동 승인되지 않는다 |
</threat_model>

<verification>
1. `node -p "require('./apps/dashboard/package.json').dependencies.next"` ≥ 15.5.22
2. `pnpm --dir apps/dashboard exec tsc --noEmit` 종료 코드 0
3. `pnpm --dir apps/dashboard exec vitest run` 이 `2 passed`
4. `NEXT_TELEMETRY_DISABLED=1 pnpm --dir apps/dashboard build` 종료 코드 0
5. `apps/dashboard/tailwind.config.js`/`.ts` 부재, `head -1 app/globals.css` == `@import "tailwindcss";`
6. 저장소 루트에 `package.json` 부재
7. `pnpm --dir apps/dashboard dev` 로 로컬 dev 서버 기동 후 `curl -sf -o /dev/null -w '%{http_code}' http://127.0.0.1:3000/` 이 200
</verification>

<success_criteria>
- next 15.5.22 이상, Tailwind 4 CSS-first, TypeScript strict
- `tsc --noEmit` 통과, `vitest run` 2건 통과, 로컬 dev 서버 기동
- 텔레메트리가 세 스크립트 전부에서 꺼져 있다
- pnpm workspace / Turborepo / Nx 어느 것도 도입되지 않았다
</success_criteria>

## Artifacts this phase produces

**파일:** `apps/dashboard/package.json`, `apps/dashboard/pnpm-lock.yaml`, `apps/dashboard/tsconfig.json`,
`apps/dashboard/next.config.ts`, `apps/dashboard/postcss.config.mjs`, `apps/dashboard/app/layout.tsx`,
`apps/dashboard/app/page.tsx`, `apps/dashboard/app/globals.css`, `apps/dashboard/lib/workspace-path.ts`,
`apps/dashboard/components/HealthBadge.tsx`, `apps/dashboard/vitest.config.ts`,
`apps/dashboard/vitest.setup.ts`, `apps/dashboard/tests/workspace-path.test.ts`,
`apps/dashboard/tests/HealthBadge.test.tsx`

**TypeScript 심볼:** `workspacePath(workspaceId: string): string`,
`HealthBadge` (props: `{ status: 'ok' | 'degraded' | 'unknown' }`)

**npm 스크립트:** `dev`, `build`, `start`, `lint`, `typecheck`, `test`, `test:watch`

**env 키(신규 참조):** `NEXT_TELEMETRY_DISABLED`

**DOM 계약:** `HealthBadge` 가 렌더하는 `data-status` 속성

<output>
Create `.planning/phases/01-bootstrap-and-ground-truth/01-05-SUMMARY.md` when done
</output>
