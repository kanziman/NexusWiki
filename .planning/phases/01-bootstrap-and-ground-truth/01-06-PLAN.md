---
phase: 01-bootstrap-and-ground-truth
plan: 06
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - Dockerfile
  - .dockerignore
  - railway.json
  - scripts/smoke_tracer.sh
  - apps/worker/src/worker/__main__.py
  - docs/ops/railway-deploy-record.md
autonomous: true
requirements: [BOOT-08]

user_setup:
  - service: railway
    why: "계정이 없다. `worker`는 상주 프로세스라 Free 티어로는 상시 구동이 불가능해 Hobby 구독이 필요하다"
    env_vars:
      - name: RAILWAY_TOKEN
        source: "Railway Dashboard -> Account Settings -> Tokens (CLI 비대화형 실행용)"
    dashboard_config:
      - task: "Railway 가입 후 Hobby 플랜($5/mo, 워크스페이스 단위) 구독 — 결제 수단 등록은 사람만 가능"
        location: "Railway Dashboard -> Workspace Settings -> Plans"
      - task: "Usage Limit 설정 (비용 폭주 방지 — checklists.json > decisions.deployment)"
        location: "Railway Dashboard -> Workspace Settings -> Usage Limits"
      - task: "GitHub 저장소 연동 (자동 배포)"
        location: "Railway Dashboard -> Project -> Settings -> Source"

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Railway `asia-southeast1`에 `api`(web)와 `worker`(resident) 두 서비스가 running 상태다"
    - "배포된 `api`의 공개 URL `/health`가 200을 반환한다"
    - "동일 빌드가 증명된다 — 두 서비스의 이미지 다이제스트 일치, 또는 (동일 커밋 SHA + 동일 Dockerfile 경로 + 두 서비스가 동일 `GIT_SHA` 출력)"
    - "두 서비스가 단일 Dockerfile · Root Directory `/`를 쓰고 start command만 다르다"
    - "컨테이너의 PID 1이 파이썬이라 SIGTERM이 직접 도달한다 (shell 래퍼 · `entrypoint.sh` 미사용)"
  artifacts:
    - Dockerfile
    - .dockerignore
    - railway.json
    - docs/ops/railway-deploy-record.md
  key_links:
    - "`railway.json`이 `builder: DOCKERFILE` + `dockerfilePath: Dockerfile` 하나만 코드화하고, 서비스별 start command·리전·헬스체크는 대시보드에 둔다 — 멀티스테이지 타깃 분기를 쓰면 두 서비스의 다이제스트가 구조적으로 달라져 R8 1차 판정이 불가능해진다 (D-01)"
    - "환경변수는 공유 스코프가 아니라 **서비스별 스코프**로 설정한다 — 여기가 금지사항 P1(`api`가 service key에 닿지 않음)의 유일한 집행 지점이다 (D-12)"
    - "런타임 스테이지가 `/app/.venv` 만 COPY하므로 `uv sync --frozen --no-dev --no-editable`가 필수다 — editable 설치는 소스 경로에 의존해 venv만으로는 import가 깨진다 (D-02)"
  prohibitions:
    - statement: "Railway `api` 서비스가 Supabase service key(`SUPABASE_SECRET_KEY`)에 접근할 수 있어서는 안 된다"
      status: resolved
      verification: test
    - statement: "빌드·런타임이 사용자 동의 없이 외부 텔레메트리를 전송해서는 안 된다 (Dockerfile·환경에 `NEXT_TELEMETRY_DISABLED=1` 고정)"
      status: resolved
      verification: test
---

<objective>
단일 Dockerfile 하나로 만든 **문자 그대로 같은 이미지**를 Railway `asia-southeast1`의 두 서비스
(`api` web · `worker` resident)에 올리고, start command만 다르다는 것을 기계적으로 증명한다.

Purpose: BOOT-08. "같은 이미지"는 편의가 아니라 구조적 요건이다 — Phase 2에서 `packages/core`의
한국어 토크나이저가 색인 시점과 질의 시점에 바이트 단위로 같아야 하고, 그 보장을 여기서 이미지 하나로
만들어 둔다. 동시에 여기가 금지사항 P1(`api`는 service key에 닿지 않는다)의 유일한 집행 지점이다.
Output: `Dockerfile` · `.dockerignore` · `railway.json`, 두 개의 running 서비스, 그리고 동일 빌드 판정과
서비스별 환경변수 스코프를 기록한 `docs/ops/railway-deploy-record.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bootstrap-and-ground-truth/01-SPEC.md
@.planning/phases/01-bootstrap-and-ground-truth/01-CONTEXT.md
@.claude/CLAUDE.md
@.planning/phases/01-bootstrap-and-ground-truth/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 단일 Dockerfile (멀티스테이지 · venv만 COPY · exec form CMD) 와 `.dockerignore`</name>
  <precondition>`uv.lock`이 저장소에 커밋되어 있고 `uv sync --frozen`이 클린 클론에서 성공한다 (계획 01-01 산출물). Docker Desktop이 실행 중이다</precondition>
  <reversibility rating="costly">D-01(단일 이미지 · start command만 분기)을 바꾸면 Dockerfile · Railway 두 서비스 설정 · SPEC R8 수용기준이 함께 움직인다. D-03(exec form CMD + 프로그래밍 방식 uvicorn)은 `sh -c` + `exec`로도 대체 가능하지만 `exec` 키워드를 빠뜨리면 조용히 깨지고 증상이 "재배포 시 잡 유실"로만 나타난다.</reversibility>
  <read_first>
    - `pyproject.toml`, `apps/api/pyproject.toml`, `apps/worker/pyproject.toml`, `packages/core/pyproject.toml` — 워크스페이스 멤버 이름과 `[tool.uv.sources]` 관계
    - `apps/api/src/api/__main__.py`, `apps/worker/src/worker/__main__.py` — 실제 엔트리포인트 모듈 이름 (`python -m api` / `python -m worker`)
    - `scripts/smoke_tracer.sh` — 계획 01-01이 만든 `--image` 분기 (여기서 처음 실제로 사용된다)
    - `.gitignore` — `.dockerignore`가 참고할 제외 패턴과 한국어 배너 관례 (`# --- 주제 ---`)
    - `.planning/phases/01-bootstrap-and-ground-truth/01-CONTEXT.md` — D-01 · D-02 · D-03 · D-04, §Deferred Ideas의 `.dockerignore` 범위 위임
    - `.planning/research/STACK.md` §4.1 — Railway Dockerfile shape, Root Directory `/`가 uv 워크스페이스를 깨지 않는 유일한 배치인 이유
  </read_first>
  <files>Dockerfile, .dockerignore, scripts/smoke_tracer.sh</files>
  <action>
    **`Dockerfile`** — 파일 헤더 주석에 `관련 태스크: P0-INIT-04`,
    `설계 근거: 01-CONTEXT.md > D-01 · D-02 · D-03 · D-04`.

    builder 스테이지: `python:3.12-slim` 기반. uv 바이너리는 `COPY --from=ghcr.io/astral-sh/uv:latest
    /uv /usr/local/bin/uv` 로 가져온다. `ENV UV_COMPILE_BYTECODE=1 UV_LINK_MODE=copy
    UV_PYTHON_DOWNLOADS=never`. `WORKDIR /app`. 레이어 캐시를 위해 먼저
    `COPY pyproject.toml uv.lock .python-version ./` 를 하고, 그 다음 `COPY packages/ packages/`,
    `COPY apps/api/ apps/api/`, `COPY apps/worker/ apps/worker/` 를 한다.
    설치는 `RUN uv sync --frozen --no-dev --no-editable`.
    ⚠️ `--no-editable`이 핵심이다. 이게 없으면 워크스페이스 멤버가 소스 경로를 가리키는 editable 설치가 되고,
    런타임 스테이지가 `/app/.venv`만 복사하는 순간 `import api` / `import worker` /
    `import nexuswiki_core` 가 전부 깨진다. 증상은 빌드 성공 후 기동 실패다. 이 반사실을 주석으로 남긴다.

    runtime 스테이지: 다시 `python:3.12-slim`. **런타임 스테이지는 정확히 하나만 둔다** — 멀티스테이지
    *타깃* 분기(`--target api` / `--target worker`)를 만들면 두 서비스의 이미지 다이제스트가 구조적으로
    달라져 SPEC R8의 1차 판정이 불가능해진다 (D-01). 이 인과를 주석으로 남긴다.
    `ENV PYTHONUNBUFFERED=1 NEXT_TELEMETRY_DISABLED=1 PATH="/app/.venv/bin:$PATH"`.
    (`NEXT_TELEMETRY_DISABLED=1`은 이 이미지가 Next를 빌드하지 않더라도 금지사항 P3이 요구하는 고정값이다.)
    비루트 사용자를 만들고(`useradd`로 uid 10001짜리 계정) `COPY --from=builder --chown=` 로
    `/app/.venv` **만** 복사한 뒤 `USER`를 그 계정으로 바꾼다.
    마지막 줄은 exec form `CMD ["python", "-m", "api"]` — D-01에 따라 기본값이 `api`이고, `worker`는
    Railway 서비스별 Custom Start Command로만 분기한다. `ENTRYPOINT`도 `entrypoint.sh`도 두지 않는다.
    ⚠️ shell form이나 shell 래퍼를 쓰면 shell이 PID 1이 되어 SIGTERM이 자식 파이썬에 전달되지 않는다 —
    worker graceful shutdown이 정확히 여기 걸린다. 상세는 `01-CONTEXT.md > D-03`을 가리킨다.
    `EXPOSE`는 두지 않는다 — 포트는 Railway가 주입하는 `$PORT`를 `__main__.py`가 읽어 결정한다.

    **`.dockerignore`** — `.gitignore`의 `# --- 주제 ---` 한국어 배너 관례를 그대로 따른다.
    최소한 다음을 제외한다: `.git/`, `apps/dashboard/`, `node_modules/`, `.next/`, `.planning/`,
    `docs/`, `supabase/`, `scripts/`, `__pycache__/`, `*.py[cod]`, `.venv/`, `.ruff_cache/`,
    `.mypy_cache/`, `.pytest_cache/`, `.env`, `.env.*`, `HANDOFF.md`, `checklists.json`.
    프런트엔드나 문서 커밋이 파이썬 이미지를 재빌드시키지 않게 하는 것이 `apps/dashboard/`·`docs/`·
    `.planning/` 제외의 목적이고, `.env*` 제외는 시크릿이 이미지 레이어에 구워지지 않게 하는 것이다 —
    두 목적을 각각 주석으로 구분해 적는다.

    **`scripts/smoke_tracer.sh`** — 계획 01-01이 만든 `--image <tag>` 분기를 여기서 실제로 동작시킨다.
    이미지 경로에서는 (1) `docker run --rm -e PORT=8011 -p 8011:8011 <tag>` 로 api를 띄워 `/health` 200을
    폴링하고, (2) `docker run --rm <tag> python -m worker` 로 worker를 띄워 `worker.started` 로그를 기다린
    뒤 `docker stop`(SIGTERM)으로 10초 내 종료되는지 본다. 두 실행이 **같은 `<tag>`** 를 쓴다는 사실이
    "이미지 하나"의 로컬 증명이다. 필요한 만큼만 수정하고 `uv run` 분기는 그대로 둔다.
  </action>
  <acceptance_criteria>
    - `docker build -t nexuswiki:local .` 종료 코드 0
    - `bash scripts/smoke_tracer.sh --image nexuswiki:local` 가 `smoke_tracer: ok` 를 출력하고 종료 코드 0
    - `docker run --rm nexuswiki:local python -c "import api, worker, nexuswiki_core"` 종료 코드 0 (`--no-editable` 설치 증명)
    - `docker run --rm nexuswiki:local id -u` 출력이 `0` 이 **아니다** (비루트)
    - `grep -c 'no-editable' Dockerfile` 이 1
    - `grep -c 'NEXT_TELEMETRY_DISABLED=1' Dockerfile` 이 1
    - `grep -cE '^\s*ENTRYPOINT' Dockerfile` 이 0
    - `grep -cE '^\s*CMD \[' Dockerfile` 이 1 (exec form 단일 CMD)
    - `grep -cE '^\s*FROM .* AS ' Dockerfile` 이 2 (builder + runtime, 그 이상의 타깃 없음)
    - `docker image inspect nexuswiki:local --format '{{.Config.Cmd}}'` 출력이 `python -m api` 를 포함한다
    - `.dockerignore` 에 `apps/dashboard/` 와 `.env` 두 항목이 모두 있다
    - `docker run --rm nexuswiki:local sh -c 'ls /app'` 출력에 `apps` 와 `packages` 디렉터리가 없다 (venv만 복사됨)
  </acceptance_criteria>
  <verify>
    <automated>docker build -t nexuswiki:local . &amp;&amp; docker run --rm nexuswiki:local python -c "import api, worker, nexuswiki_core" &amp;&amp; bash scripts/smoke_tracer.sh --image nexuswiki:local</automated>
  </verify>
  <done>단일 이미지가 로컬에서 빌드되고, 같은 태그로 api(`/health` 200)와 worker(SIGTERM 종료 코드 0)가 모두 기동하며, 런타임 이미지에는 venv만 들어 있다.</done>
</task>

<task type="auto">
  <name>Task 2: `railway.json` 코드화 + 두 서비스 생성 (`asia-southeast1`) + 서비스별 환경변수 스코프</name>
  <precondition>Railway 계정이 생성되어 있고 Hobby 플랜이 활성화되어 있으며, `RAILWAY_TOKEN`이 환경에 설정되어 있다 (user_setup 항목 — 결제 수단 등록은 사람만 가능)</precondition>
  <reversibility rating="one-way">Railway 프로젝트 리전은 생성 후 변경 불가이며, Railway에는 서울·도쿄 리전이 없어 APAC 선택지가 `asia-southeast1` 하나뿐이다. 이 선택은 SPEC 제약으로 이미 확정되어 있으므로 되묻지 않고 기록만 남긴다.</reversibility>
  <read_first>
    - `Dockerfile` — Task 1이 만든 실제 CMD와 스테이지 구성
    - `.planning/phases/01-bootstrap-and-ground-truth/01-CONTEXT.md` — D-12(`railway.json` 커밋 · 시크릿 미포함 · 서비스별 스코프 · `RAILWAY_GIT_COMMIT_SHA`)
    - `.planning/phases/01-bootstrap-and-ground-truth/01-SPEC.md` §Prohibitions 1번째 행, §Constraints(예산 · Gunicorn 금지)
    - `.planning/research/STACK.md` §4.1 — `railway.json` 스키마 필드명, 서비스별 config 경로 설정이 커뮤니티 문서에만 있다는 사실과 대시보드 fallback
    - `.env.local` — 클라우드 프로젝트의 `SUPABASE_URL` · `SUPABASE_PUBLISHABLE_KEY` · `SUPABASE_SECRET_KEY` (값을 읽어 Railway에 주입하되 저장소 파일에는 옮겨 적지 않는다)
    - `checklists.json` → `decisions.deployment` — Hobby 구독이 워크스페이스 단위이고 Usage Limit을 건다는 결정
  </read_first>
  <files>railway.json, docs/ops/railway-deploy-record.md</files>
  <action>
    **`railway.json`** (저장소 루트, 커밋) — `$schema`는 `https://railway.com/railway.schema.json`.
    `build` 블록에 `"builder": "DOCKERFILE"` 과 `"dockerfilePath": "Dockerfile"` 만 둔다.
    `deploy` 블록에 `startCommand`를 **넣지 않는다** — 두 서비스가 이 파일 하나를 공유하는데 여기에
    start command를 박으면 분기가 불가능해진다. `healthcheckPath`도 넣지 않는다 — 포트를 노출하지 않는
    `worker` 서비스에까지 적용되면 배포가 실패한다. 두 값은 서비스별 대시보드 설정으로 간다.
    **어떤 시크릿도 이 파일에 넣지 않는다** (D-12). 파일 상단이 아니라 `docs/ops/railway-deploy-record.md`에
    이 설계 이유를 적는다(JSON에는 주석을 쓸 수 없다).

    **Railway 프로젝트 1개 · 서비스 2개** — 둘 다 같은 GitHub 저장소, 둘 다 **Root Directory `/`**,
    둘 다 리전 `asia-southeast1`. Root Directory를 `apps/api`/`apps/worker`로 나누면 빌드 컨텍스트가
    격리되어 `packages/core`와 루트 `uv.lock`이 보이지 않게 되고 워크스페이스가 깨진다.
    - 서비스 `api` — start command는 지정하지 않는다(Dockerfile의 `CMD` 기본값 `python -m api`를 그대로 쓴다).
      Health Check Path `/health`, Health Check Timeout 60초, Restart Policy `ON_FAILURE`(최대 5회).
      공개 도메인을 생성한다.
    - 서비스 `worker` — **Custom Start Command `python -m worker`** (여기가 D-01이 말한 유일한 분기점이다).
      포트를 노출하지 않고 헬스체크도 두지 않는다. Restart Policy `ALWAYS`. 공개 도메인을 만들지 않는다.

    **환경변수는 서비스별 스코프로만 설정한다. 공유 변수 그룹을 만들지 않는다** (D-12).
    - `api` 서비스: `ENVIRONMENT=production`, `LOG_LEVEL=INFO`, `SUPABASE_URL`,
      `SUPABASE_PUBLISHABLE_KEY`. 그리고 이것이 전부다.
    - `worker` 서비스: 위 네 개에 더해 `SUPABASE_SECRET_KEY`.
    ⚠️ 두 서비스가 문자 그대로 같은 이미지를 쓰기 때문에, 시크릿을 공유 스코프에 넣는 순간 `api` 프로세스가
    BYPASSRLS 자격을 얻고 `0004`의 38개 격리 정책이 전부 장식이 된다. 이 사실을
    `docs/ops/railway-deploy-record.md`에 명시한다.
    `PORT`는 Railway가 자동 주입하므로 수동으로 설정하지 않는다. `GIT_SHA`도 수동 설정하지 않는다 —
    Railway 내장 `RAILWAY_GIT_COMMIT_SHA`를 애플리케이션이 읽는다.

    **`docs/ops/railway-deploy-record.md`** 를 만든다. `docs/ops/cloud-bootstrap-record.md`와 같은
    표제 구조(`측정 일시` · `방법` · `결과`)를 쓰고 한국어로 작성한다. 담을 것:
    프로젝트/서비스 이름, 리전, 두 서비스의 Root Directory, 두 서비스의 start command
    (`api`는 이미지 기본값, `worker`는 커스텀), 헬스체크·재시작 정책, 그리고 **서비스별 환경변수 이름
    목록**(값이 아니라 이름만). 연구 문서가 지적한 대로 서비스별 config-as-code 경로 설정이 공식
    레퍼런스에 없으므로, 대시보드에 둔 설정을 이 문서가 대신 리뷰 가능하게 만든다.
    project ref · `DATABASE_URL` · 어떤 키 값도 적지 않는다.
  </action>
  <acceptance_criteria>
    - `railway.json` 이 존재하고 `node -p "require('./railway.json').build.builder"` 가 `DOCKERFILE`
    - `node -p "JSON.stringify(require('./railway.json'))"` 출력에 `startCommand` 와 `healthcheckPath` 가 등장하지 않는다
    - `grep -cE '(sb_publishable_|sb_secret_|postgresql://)' railway.json` 이 0
    - Railway 대시보드/CLI에서 두 서비스 모두 상태가 `running`
    - 두 서비스의 리전이 모두 `asia-southeast1`
    - 두 서비스의 Root Directory가 모두 `/`
    - `worker` 서비스의 Custom Start Command 가 `python -m worker`
    - `api` 서비스의 변수 목록에 `SUPABASE_SECRET_KEY` 가 **없다**
    - `worker` 서비스의 변수 목록에 `SUPABASE_SECRET_KEY` 가 **있다**
    - Railway 워크스페이스에 공유(프로젝트 레벨) 변수 그룹이 없다
    - `docs/ops/railway-deploy-record.md` 에 두 서비스의 환경변수 **이름 목록**이 기록되어 있고 값은 없다
    - `grep -cE 'sb_(publishable|secret)_[A-Za-z0-9]{8,}' docs/ops/railway-deploy-record.md` 이 0
  </acceptance_criteria>
  <verify>
    <automated>node -p "require('./railway.json').build.builder" &amp;&amp; ! node -p "JSON.stringify(require('./railway.json'))" | grep -q startCommand &amp;&amp; ! grep -qE '(sb_publishable_|sb_secret_|postgresql://)' railway.json</automated>
    <human-check>Railway 대시보드에서 (1) 두 서비스가 running, (2) 리전이 둘 다 `asia-southeast1`, (3) `api` 서비스 Variables 탭에 `SUPABASE_SECRET_KEY`가 보이지 않고 `worker` 탭에는 보이는지, (4) 공유 변수 그룹이 없는지 눈으로 확인하고 스크린샷 없이 결과만 SUMMARY에 기록</human-check>
  </verify>
  <done>두 서비스가 `asia-southeast1`에서 running이고, start command만 다르며, service key가 `worker` 스코프에만 존재한다.</done>
</task>

<task type="auto">
  <name>Task 3: 동일 빌드 판정 (1차 다이제스트 → 실패 시 2차 `GIT_SHA`) 과 배포 `/health` 200 확인</name>
  <precondition>두 Railway 서비스가 running 상태이고 `api` 서비스에 공개 도메인이 발급되어 있다</precondition>
  <read_first>
    - `docs/ops/railway-deploy-record.md` — Task 2가 기록한 서비스 구성
    - `apps/api/src/api/routers/health.py` — `/health` 응답이 이미 `git_sha` 필드를 담는다는 사실 (계획 01-01)
    - `apps/worker/src/worker/__main__.py` — 현재 `worker.started` 로그가 무엇을 싣는지
    - `.planning/phases/01-bootstrap-and-ground-truth/01-SPEC.md` §Requirements R8 — 2단 판정 규칙 전문
  </read_first>
  <files>apps/worker/src/worker/__main__.py, docs/ops/railway-deploy-record.md</files>
  <action>
    **worker 기동 로그에 `git_sha`를 싣는다** — `apps/worker/src/worker/__main__.py`의
    `worker.started` 로그 한 줄에 `git_sha` 키를 추가한다. 값은 `RAILWAY_GIT_COMMIT_SHA` →
    `GIT_SHA` → `"unknown"` 순으로 환경에서 읽는다 (api의 `/health`가 쓰는 것과 정확히 같은 우선순위).
    이 한 줄이 SPEC R8 2차 판정의 worker 쪽 관측점이다.

    **1차 판정 — 이미지 다이제스트 일치.** Railway의 두 서비스 최신 배포에서 이미지 다이제스트를
    읽어 비교한다(대시보드 배포 상세 또는 `railway` CLI). 두 값이 같으면 여기서 합격이며 2차는 생략한다.

    **2차 판정 — 비결정적 빌드로 다이제스트가 갈릴 경우.** 세 가지를 모두 확인한다:
    (a) 두 서비스의 배포 커밋 SHA가 같다, (b) 두 서비스의 빌드 설정이 같은 Dockerfile 경로
    (`railway.json`의 `dockerfilePath`)를 가리킨다, (c) 두 서비스가 **런타임에** 같은 `GIT_SHA`를 낸다 —
    `api`는 공개 URL `/health` 응답의 `git_sha` 필드로, `worker`는 배포 로그의 `worker.started` 줄에
    실린 `git_sha` 값으로 읽는다. 세 값이 모두 일치하면 합격이다.

    **배포 `/health` 확인** — `api` 공개 URL에 대해 `curl -fsS -o /dev/null -w '%{http_code}'
    https://<api-domain>/health` 가 `200` 인지 확인한다. `/health/ready`도 함께 호출해 관측된 상태를
    기록하되, 계획 01-03(클라우드 마이그레이션 push)이 아직이면 200이 아닐 수 있으므로
    **이 태스크의 합격 조건에는 넣지 않는다** — 관측값만 문서에 남긴다.

    **`api` 서비스의 service key 부재 재확인** — `api` 서비스 변수 목록을 다시 읽어
    `SUPABASE_SECRET_KEY`(또는 동등한 service key)가 없음을 확인하고 그 결과를 문서에 남긴다.
    이것이 금지사항 P1의 부정 수용기준이며, 배포 후 설정이 드리프트하지 않았음을 보는 자리다.

    `docs/ops/railway-deploy-record.md` 에 판정 섹션을 추가한다 — 사용한 판정 차수(1차/2차), 관측한
    다이제스트 또는 세 일치 값(커밋 SHA · Dockerfile 경로 · 런타임 `GIT_SHA`), 배포 `/health` 상태 코드,
    `/health/ready` 관측값, 그리고 `api` 변수 목록에 service key가 없다는 확인 결과.
    api 공개 도메인은 적어도 되지만 키·ref·접속 문자열은 적지 않는다.
  </action>
  <acceptance_criteria>
    - 배포된 `api` 공개 URL의 `/health` 가 HTTP `200` 을 반환한다
    - `/health` 응답 JSON의 `git_sha` 필드가 `unknown` 이 아니다
    - `worker` 배포 로그의 `worker.started` 줄이 `git_sha` 키를 포함하고 값이 `api` 의 것과 같다
    - 1차 판정(다이제스트 일치) 또는 2차 판정(커밋 SHA + Dockerfile 경로 + 런타임 `GIT_SHA` 3항 일치) 중 하나가 `docs/ops/railway-deploy-record.md` 에 관측값과 함께 기록되어 있다
    - `api` 서비스 변수 목록에 `SUPABASE_SECRET_KEY` 가 없음이 재확인·기록되었다
    - `grep -c 'git_sha' apps/worker/src/worker/__main__.py` 이 1 이상
    - `grep -cE 'sb_(publishable|secret)_[A-Za-z0-9]{8,}' docs/ops/railway-deploy-record.md` 이 0
  </acceptance_criteria>
  <verify>
    <automated>curl -fsS -o /dev/null -w '%{http_code}\n' "$API_PUBLIC_URL/health" &amp;&amp; curl -fsS "$API_PUBLIC_URL/health" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);if(!j.git_sha||j.git_sha==='unknown')process.exit(1);console.log(j.git_sha)})"</automated>
    <human-check>Railway `worker` 서비스의 배포 로그에서 `worker.started` 줄의 `git_sha` 값이 위 명령이 출력한 `api` 의 `git_sha` 와 같은지 확인</human-check>
  </verify>
  <done>배포된 `api` `/health`가 200이고, 두 서비스가 동일 빌드임이 1차 또는 2차 판정으로 기록되었으며, `api`에 service key가 없음이 배포 후에도 확인되었다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 공개 인터넷 → Railway `api` 공개 도메인 | 인증 없는 헬스 엔드포인트 2종이 이 페이즈의 유일한 공개 표면이다 |
| Railway 변수 스토어 → 컨테이너 프로세스 env | 두 서비스가 **문자 그대로 같은 이미지**를 돌리므로, 변수 스코프가 두 프로세스의 권한을 가르는 유일한 장치다 |
| 저장소(공개 가능) → 이미지 레이어 | 빌드 컨텍스트에 들어간 것은 이미지 레이어에 영구히 남는다 |
| 컨테이너 → Docker Hub / uv 배포 채널 | 베이스 이미지와 uv 바이너리가 빌드 시점에 외부에서 들어온다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | Railway 환경변수 스코프 | critical | mitigate | `SUPABASE_SECRET_KEY`를 `worker` 서비스 스코프에만 주입하고 공유 변수 그룹을 만들지 않는다. `api` 변수 목록에 부재함을 Task 2·3에서 두 번 확인 (금지사항 P1). 같은 이미지 배포에서 이 스코프가 BYPASSRLS 접근의 유일한 경계다 |
| T-06-02 | Information Disclosure | 이미지 레이어에 구워진 시크릿 | high | mitigate | `.dockerignore`가 `.env`·`.env.*`를 제외한다. `railway.json`에 시크릿을 넣지 않으며 negative grep으로 고정. 빌드 인자로 시크릿을 넘기지 않는다 |
| T-06-03 | Denial of Service | SIGTERM 미전달로 인한 잡 유실 | high | mitigate | exec form 단일 `CMD`, `ENTRYPOINT`·shell 래퍼 부재를 negative grep으로 고정해 PID 1이 파이썬임을 보장 (D-03). `smoke_tracer.sh --image` 가 컨테이너에서 10초 내 종료 코드 0을 판정 |
| T-06-04 | Elevation of Privilege | 컨테이너 루트 실행 | medium | mitigate | 런타임 스테이지에서 uid 10001 비루트 사용자로 전환. `docker run … id -u` 가 0이 아님을 acceptance criteria로 고정 |
| T-06-05 | Information Disclosure | `/health` 응답의 정보 노출 | medium | mitigate | 응답 필드를 `status`와 `git_sha` 두 개로 제한. 버전·환경명·DB URL·예외 원문을 넣지 않는다 (계획 01-01에서 이미 확정) |
| T-06-06 | Information Disclosure | 빌드/런타임 텔레메트리 | medium | mitigate | Dockerfile `ENV NEXT_TELEMETRY_DISABLED=1` 고정 (금지사항 P3). 계획 01-05의 `package.json` 스크립트가 프런트엔드 쪽 짝 |
| T-06-07 | Tampering | 베이스 이미지 / uv 바이너리 공급망 | medium | accept | `python:3.12-slim`과 `ghcr.io/astral-sh/uv`는 각각 공식 Docker 라이브러리 이미지와 astral-sh 공식 GHCR 배포다. 다이제스트 고정은 v1 이후 과제로 남기고, 지금은 `uv.lock`이 파이썬 의존성을 고정하는 것으로 그친다 |
| T-06-08 | Spoofing | 두 서비스가 실제로는 다른 코드를 돌리는 상태 | high | mitigate | R8 2단 판정. 1차 다이제스트 일치, 실패 시 커밋 SHA + Dockerfile 경로 + 런타임 `GIT_SHA` 3항 일치. 런타임 관측점이 있어야 "설정은 같은데 배포가 다른" 경우를 잡는다 |
| T-06-SC | Tampering | 패키지 매니저 설치 | medium | mitigate | 이 계획은 새 npm/pip 패키지를 추가하지 않는다 — `uv sync --frozen`이 계획 01-01에서 이미 사람이 확인한 목록을 커밋된 `uv.lock`대로 재현할 뿐이다. `--frozen`이 lockfile 밖 해소를 금지한다 |
</threat_model>

<verification>
1. `docker build -t nexuswiki:local .` 성공, `bash scripts/smoke_tracer.sh --image nexuswiki:local` → `smoke_tracer: ok`
2. `docker run --rm nexuswiki:local python -c "import api, worker, nexuswiki_core"` 성공 (venv-only COPY 증명)
3. Railway 두 서비스 running, 리전 `asia-southeast1`, Root Directory `/`
4. 배포된 `api` `/health` → 200, `git_sha` != `unknown`
5. `worker` 로그의 `worker.started`가 같은 `git_sha`
6. `api` 서비스 변수 목록에 `SUPABASE_SECRET_KEY` 부재
7. `railway.json`에 시크릿·`startCommand`·`healthcheckPath` 부재
</verification>

<success_criteria>
- 단일 Dockerfile로 만든 하나의 이미지가 두 서비스에서 돈다 — start command만 다르다
- 배포된 `api` `/health`가 200
- 동일 빌드가 1차 또는 2차 판정으로 증명되고 관측값이 문서에 남는다
- `api`는 service key에 닿지 않는다
</success_criteria>

## Artifacts this phase produces

**파일:** `Dockerfile`, `.dockerignore`, `railway.json`, `docs/ops/railway-deploy-record.md`

**Docker 아티팩트:** 빌드 스테이지 이름 `builder` · `runtime`, 로컬 태그 `nexuswiki:local`,
런타임 비루트 사용자(uid 10001), 이미지 기본 `CMD` = `python -m api`

**Railway 리소스:** 프로젝트 1개, 서비스 `api`(web) · `worker`(resident),
`worker` Custom Start Command `python -m worker`, `api` Health Check Path `/health`

**env 키(신규 참조):** `RAILWAY_TOKEN`, `RAILWAY_GIT_COMMIT_SHA`,
`UV_COMPILE_BYTECODE`, `UV_LINK_MODE`, `UV_PYTHON_DOWNLOADS`, `PYTHONUNBUFFERED`

**로그 필드(신규):** `worker.started` 이벤트의 `git_sha` 키

<output>
Create `.planning/phases/01-bootstrap-and-ground-truth/01-06-SUMMARY.md` when done
</output>
