---
phase: 04-hybrid-retrieval-and-fusion
plan: 09
type: execute
wave: 7
depends_on: [04-08]
files_modified:
  - scripts/benchmark_retrieval.py
  - packages/core/tests/test_retrieval_golden.py
  - docs/ops/benchmark-records/phase-04-rerun-v5-strict-order.json
  - docs/ops/benchmark-records/phase-04-rerun-v5-relaxed-order.json
  - docs/ops/hnsw-order-benchmark.md
autonomous: true
gap_closure: true
requirements: [RTV-04]
must_haves:
  truths:
    - "Running the fixed compare_order_records() in scripts/benchmark_retrieval.py against the existing invalid v4 strict/relaxed pair (phase-04-rerun-v4-strict-order.json git_sha 6adb0453d9cce3267a4d180e1dd95af65733442d vs phase-04-rerun-v4-relaxed-order.json git_sha 466205625053b00d785312db84ae9f1c228ae1b2) now raises VerificationError instead of returning status: ok, proving the fix catches the exact gap 04-VERIFICATION.md found."
    - "_pins() in scripts/benchmark_retrieval.py includes git_sha, and the existing valid v4 graph-off/graph-on pair (both already git_sha 466205625053b00d785312db84ae9f1c228ae1b2) still passes compare_graph_records() unchanged after the fix — no regression to the graph comparator."
    - "A new strict-order and relaxed-order full-path benchmark record pair (phase-04-rerun-v5-strict-order.json / phase-04-rerun-v5-relaxed-order.json) is generated back-to-back from one identical committed git revision against a freshly loaded controlled corpus, and compare-order-records run against that new pair reports status: ok with matching git_sha values on both sides."
    - "docs/ops/hnsw-order-benchmark.md records the new v5 pair's outcome and explicitly marks the old v4 strict/relaxed pair as superseded-invalid due to the runner-revision mismatch, without deleting, overwriting, or renumbering the v4 files or any earlier historical record."
    - "supabase/migrations/0011_retrieval.sql's strict_order default and packages/core/src/nexuswiki_core/retrieval_policy.py:38's graph_enabled=False default remain unchanged; the new v5 evidence alone does not adopt a different default."
  artifacts:
    - path: scripts/benchmark_retrieval.py
      provides: fixed comparator — _pins() carries git_sha; compare_order_records() rejects non-{strict_order,relaxed_order} pairs
    - path: packages/core/tests/test_retrieval_golden.py
      provides: regression tests proving the fixed comparator rejects the historical invalid v4 pair and any same-order_mode pair while still accepting the historical valid graph pair
    - path: docs/ops/benchmark-records/phase-04-rerun-v5-strict-order.json
      provides: valid single-commit strict-order full-path evidence
    - path: docs/ops/benchmark-records/phase-04-rerun-v5-relaxed-order.json
      provides: valid single-commit relaxed-order full-path evidence, same git revision as the strict record
    - path: docs/ops/hnsw-order-benchmark.md
      provides: updated decision log — v5 pair outcome recorded, v4 strict/relaxed pair marked superseded-invalid
  key_links:
    - from: scripts/benchmark_retrieval.py::_pins
      to: scripts/benchmark_retrieval.py::compare_order_records
      via: "compare_order_records calls _pins(left) != _pins(right); git_sha now flows through this existing pin-equality check so a runner-revision mismatch is caught without a separate code path"
      pattern: "_pins\\(left\\) != _pins\\(right\\)"
    - from: scripts/benchmark_retrieval.py::_pins
      to: scripts/benchmark_retrieval.py::compare_graph_records
      via: "compare_graph_records reuses the same shared _pins() helper; the v4 graph-off/graph-on pair already shares one git_sha so this link must keep passing after the fix"
      pattern: "_pins\\(off\\) != _pins\\(on\\)"
    - from: docs/ops/hnsw-order-benchmark.md
      to: docs/ops/benchmark-records/phase-04-rerun-v5-strict-order.json
      via: "the decision log links to and cites the new v5 record pair as the valid comparability evidence, replacing the v4 pair's authority"
      pattern: "rerun-v5"
  prohibitions:
    - requirement_id: RTV-04
      category: correctness
      status: flagged-unverified
      statement: "The new v5 strict/relaxed pair must not be produced by reusing residual rows already present in the local database from a prior benchmark or dev session — each arm's corpus load must be the scoped fresh load the existing loader already performs, not a silently-skipped load against leftover state."
    - requirement_id: RTV-04
      category: governance
      status: flagged-unverified
      statement: "Neither the fixed comparator nor the updated docs/ops/hnsw-order-benchmark.md wording may be read as authorization to change the strict_order or graph_enabled production defaults; any future default change still requires the separate docs/ops/retrieval-policy-change-log.md gate (POLICY_VERSION bump, paired evidence, independent reviewer approval)."
    - requirement_id: RTV-04
      category: evidence-integrity
      status: flagged-unverified
      statement: "The claim that the v5 pair's matching git_sha values constitute a valid runner-identity pin must not be made if the working tree was dirty (uncommitted local changes) at either capture time — both arms must be run back-to-back at one clean, committed revision with nothing committed in between."
---

<objective>
Fix the exact defect 04-VERIFICATION.md found in `compare_order_records()` — its comparability check omits runner identity, so it accepted a strict/relaxed pair captured from two different git revisions as `status: ok` — then use the fixed comparator to produce and validate a genuinely comparable v5 strict/relaxed pair from one identical commit, and record that outcome in the existing decision log. This closes RTV-04, the sole remaining gap in Phase 4. RTV-01, RTV-02, RTV-03, RTV-05, RTV-06, RTV-07, RTV-08, RTV-09 are already SATISFIED per 04-VERIFICATION.md and are out of scope (see below) — this plan does not touch their implementation, does not add a migration, does not change `supabase/migrations/0011_retrieval.sql` or `retrieval_policy.py`'s defaults, and does not regenerate the graph-on/graph-off v4 pair.

Purpose: produce a comparability check that actually proves what it claims, and produce one valid strict-vs-relaxed evidence pair the existing safe-defaults decision can cite.
Output: a corrected `_pins()`/`compare_order_records()` in `scripts/benchmark_retrieval.py`, regression tests proving the fix catches the historical bug, one new append-only valid v5 record pair, and an updated `docs/ops/hnsw-order-benchmark.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-hybrid-retrieval-and-fusion/04-VERIFICATION.md
@.planning/phases/04-hybrid-retrieval-and-fusion/04-08-SUMMARY.md
@scripts/benchmark_retrieval.py
@docs/ops/hnsw-order-benchmark.md
</context>

## Out-of-Scope Requirements (RTV-01, RTV-02, RTV-03, RTV-05, RTV-06, RTV-07, RTV-08, RTV-09)

Per `04-VERIFICATION.md` (verified 2026-08-11T12:12:40Z), these eight requirements are already SATISFIED. This plan's tasks do not add must_haves for them and do not touch their implementation — recorded here per the no-silent-drop rule so they are reviewed, not overlooked:

- **RTV-01** (two-wave concurrent-then-graph retrieval) — SATISFIED, `apps/api/src/api/services/retrieval.py`. Not touched.
- **RTV-02** (rank-only RRF, Python policy layer) — SATISFIED. Not touched.
- **RTV-03** (all three HNSW GUCs set together) — SATISFIED, contract-tested. Not touched.
- **RTV-05** (`channel_hits`/underfill first-class metrics) — SATISFIED. Not touched.
- **RTV-06** (30–50 multilingual golden query set, 36 queries: ko10/en17/mixed9) — SATISFIED. This plan reuses the existing pinned golden set unmodified; it does not add, edit, or re-derive any query or label.
- **RTV-07** (bounded default-off graph channel, real off/on evidence) — SATISFIED, including the v4 graph-off/graph-on pair. Task 1's `_pins()` change is shared code with `compare_graph_records()`, so Task 1 explicitly re-verifies the existing v4 graph pair still passes after the fix — but no graph-channel behavior, RPC, or default changes in this plan, and the graph pair is not regenerated.
- **RTV-08** (EXPLAIN HNSW-index-scan regression test) — SATISFIED. Not touched.
- **RTV-09** (per-channel failure isolation, safe `meta` reporting) — SATISFIED. Not touched.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: make compare_order_records() reject a mismatched-runner or mismatched-mode pair</name>
  <files>scripts/benchmark_retrieval.py, packages/core/tests/test_retrieval_golden.py</files>
  <read_first>
    - `scripts/benchmark_retrieval.py` — `_pins()` (currently returns 8 keys: corpus_sha256, golden_sha256, benchmark_manifest_sha256, generator_seed, raw_workspace_uuid, embedding_model_version, database_identity, repeat_count), `compare_order_records()`, `compare_graph_records()`, and `operational()`'s `git_sha` capture via `subprocess.run(["git", "rev-parse", "HEAD"], ...)`.
    - `.planning/phases/04-hybrid-retrieval-and-fusion/04-VERIFICATION.md` — "Must-have evidence" table row for the FAILED truth and the "Exact remaining gap" section; this is the precise defect this task fixes.
    - `docs/ops/hnsw-order-benchmark.md` — current decision log, so the fix's target (a pair the log currently, wrongly, treats as valid) is understood before Task 2 edits this file.
    - `packages/core/tests/test_retrieval_golden.py` — existing `_benchmark_module()` import-by-path test pattern (see `test_operational_validator_accepts_observed_empty_retrieval_result_only`, `test_policy_content_hash_detects_policy_change_without_version_change`); follow this pattern, do not add a new import mechanism.
  </read_first>
  <behavior>
    - Test 1: `module.compare_order_records()` raises `module.VerificationError` when given the real historical invalid pair — `docs/ops/benchmark-records/phase-04-rerun-v4-strict-order.json` (git_sha `6adb0453d9cce3267a4d180e1dd95af65733442d`) as left and `phase-04-rerun-v4-relaxed-order.json` (git_sha `466205625053b00d785312db84ae9f1c228ae1b2`) as right. This is RED before the `_pins()` change and GREEN after — the concrete proof the fix catches the exact bug 04-VERIFICATION.md reported.
    - Test 2: `module.compare_order_records()` still raises `module.VerificationError` when two records have the same `order_mode` value (both `strict_order`), independent of `git_sha` or any other pin — proves the new order-mode-pair assertion is not redundant with the pin check.
    - Test 3: `module.compare_graph_records()` still returns `{"status": "ok", ...}` for the existing valid pair — `docs/ops/benchmark-records/phase-04-rerun-v4-graph-off.json` and `phase-04-rerun-v4-graph-on.json` (both git_sha `466205625053b00d785312db84ae9f1c228ae1b2`) — proving the shared `_pins()` change causes no regression in the graph comparator.
  </behavior>
  <action>In `scripts/benchmark_retrieval.py`, add `"git_sha"` to the tuple of keys returned by `_pins(record)` so it becomes the ninth pinned field alongside the existing eight. Because both `compare_order_records()` and `compare_graph_records()` call this same shared `_pins()` helper via `_pins(left) != _pins(right)` / `_pins(off) != _pins(on)`, this one change makes both comparators reject a pair whose captured `git_sha` differs, raising the existing `order_pair_pin_or_policy_mismatch` / `graph_pair_pin_mismatch` errors respectively — do not invent a new error string for the `git_sha` case. In `compare_order_records(left, right)`, add an explicit order-mode-pair assertion before the function can return `status: ok`: raise `VerificationError("order_pair_mode_invalid")` unless `{left.get("order_mode"), right.get("order_mode")} == {"strict_order", "relaxed_order"}` — this rejects two records with the same `order_mode`, a missing `order_mode`, or any value outside that exact pair, independent of whether their pins match. Do not modify `compare_graph_records()`'s own diff logic beyond the inherited `_pins()` change; its allowed sole policy diff remains `graph_enabled`. Add the three tests described in `<behavior>` to `packages/core/tests/test_retrieval_golden.py`, loading the JSON record paths under `docs/ops/benchmark-records/` with the file's existing `_load()` helper and the module via `_benchmark_module()`.</action>
  <acceptance_criteria>
    - `_pins()` in `scripts/benchmark_retrieval.py` returns a dict whose keys include `git_sha` alongside the existing eight keys.
    - `compare_order_records()` raises `VerificationError` when called with `docs/ops/benchmark-records/phase-04-rerun-v4-strict-order.json` as left and `phase-04-rerun-v4-relaxed-order.json` as right (their `git_sha` values differ: `6adb0453d9cce3267a4d180e1dd95af65733442d` vs `466205625053b00d785312db84ae9f1c228ae1b2`).
    - `compare_order_records()` raises `VerificationError` when both input records share one `order_mode` value, even with every other pin matching.
    - `compare_graph_records()` still returns `{"status": "ok", ...}` for `docs/ops/benchmark-records/phase-04-rerun-v4-graph-off.json` / `phase-04-rerun-v4-graph-on.json` after the `_pins()` change.
    - `uv run pytest -q packages/core/tests/test_retrieval_golden.py` passes with the three new tests included.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run pytest -q packages/core/tests/test_retrieval_golden.py</automated>
  </verify>
  <done>`_pins()` includes `git_sha`; `compare_order_records()` rejects the historical invalid v4 pair and any same-`order_mode` pair; `compare_graph_records()` keeps accepting the historical valid graph pair; all three new tests plus the existing suite in `test_retrieval_golden.py` pass.</done>
  <reversibility rating="reversible">Pure code + test change to a local dev/ops script; revert with `git revert` if the assertion proves too strict.</reversibility>
</task>

<task type="auto">
  <name>Task 2: regenerate a valid single-commit v5 strict/relaxed pair and record the decision</name>
  <files>docs/ops/benchmark-records/phase-04-rerun-v5-strict-order.json, docs/ops/benchmark-records/phase-04-rerun-v5-relaxed-order.json, docs/ops/hnsw-order-benchmark.md</files>
  <read_first>
    - `scripts/benchmark_retrieval.py` — `operational()` and `main()`'s CLI flags (`--order-mode`, `--graph`, `--repeat-count`, `--db-container`, `--run-id`, `--output`), and the `refusing_to_overwrite_prior_record` guard that forces new filenames.
    - `.planning/phases/04-hybrid-retrieval-and-fusion/04-VERIFICATION.md` — "Exact remaining gap" section: one identical committed revision, freshly loaded corpus, new filenames, retain v4 as superseded.
    - `docs/ops/hnsw-order-benchmark.md` — current decision log structure (the "재생성한 full-path 원시 기록" table and the "Plan-07 기록의 상태" superseded-marking pattern this task must mirror for the v4 pair).
    - Task 1's completed fix in `scripts/benchmark_retrieval.py` — this task's comparator run depends on Task 1's `_pins()`/`compare_order_records()` already being correct.
  </read_first>
  <action>Confirm a clean working tree with `git status --porcelain` (must be empty) and note the current `git rev-parse HEAD` value. Without committing anything in between, run both arms back-to-back at that identical commit: `uv run python scripts/benchmark_retrieval.py --order-mode strict_order --graph off --repeat-count 3 --db-container supabase_db_NexusWiki --run-id phase-04-rerun-v5-strict --output docs/ops/benchmark-records/phase-04-rerun-v5-strict-order.json`, then `uv run python scripts/benchmark_retrieval.py --order-mode relaxed_order --graph off --repeat-count 3 --db-container supabase_db_NexusWiki --run-id phase-04-rerun-v5-relaxed --output docs/ops/benchmark-records/phase-04-rerun-v5-relaxed-order.json`. Each `operational()` invocation freshly loads and then cleans up its own scoped benchmark workspace via the existing `loader_sql`/cleanup contract, so each arm's corpus is freshly loaded even though both arms share one commit. Never pass an `--output` path that already exists — use the `-v5-` filenames only, never edit or overwrite any `-v4-` file. After both records exist, run `uv run python scripts/benchmark_retrieval.py compare-order-records --left docs/ops/benchmark-records/phase-04-rerun-v5-strict-order.json --right docs/ops/benchmark-records/phase-04-rerun-v5-relaxed-order.json` and confirm the printed JSON has `"status": "ok"`. Independently inspect both new records' `git_sha` fields (read the JSON directly) and confirm they are identical strings equal to the commit noted before running either arm. Then edit `docs/ops/hnsw-order-benchmark.md`: add a new row/section documenting the v5 pair's outcome (each arm's `recall_at_k`, `strict_query_pass_rate`, underfill, `latency_ms` p50/p95, and the comparator's `quality_delta`), and explicitly mark the existing v4 strict/relaxed row as superseded-invalid, quoting both differing `git_sha` values (`6adb0453d9cce3267a4d180e1dd95af65733442d` vs `466205625053b00d785312db84ae9f1c228ae1b2`) as the reason, mirroring how the document already marks the Plan-07 records superseded. Do not remove, rewrite, or renumber the existing v4/Plan-07 table rows or JSON files. Do not modify `supabase/migrations/0011_retrieval.sql` or `packages/core/src/nexuswiki_core/retrieval_policy.py` — the v5 evidence documents the comparability fix and a valid measurement; it does not by itself authorize adopting `relaxed_order` or changing any default.</action>
  <precondition>Local Docker Supabase stack is running with a `supabase_db_NexusWiki` container reachable via `docker exec` (verify with `docker ps --format '{{.Names}}'` before running either arm); the git working tree is clean (`git status --porcelain` empty) so both arms capture one identical `git_sha`.</precondition>
  <acceptance_criteria>
    - `docs/ops/benchmark-records/phase-04-rerun-v5-strict-order.json` and `phase-04-rerun-v5-relaxed-order.json` both exist as new files (not edits to any `-v4-` or earlier file) with byte-identical `git_sha` field values.
    - `uv run python scripts/benchmark_retrieval.py compare-order-records --left docs/ops/benchmark-records/phase-04-rerun-v5-strict-order.json --right docs/ops/benchmark-records/phase-04-rerun-v5-relaxed-order.json` prints `"status": "ok"` and exits 0.
    - `docs/ops/hnsw-order-benchmark.md` contains a v5 entry with both arms' `recall_at_k` / `strict_query_pass_rate` / underfill / `latency_ms` figures and the comparator's `quality_delta`, and explicitly labels the v4 strict/relaxed pair superseded-invalid, citing both differing `git_sha` values.
    - `git diff --stat` for this task's commit(s) touches only the three files listed above — `supabase/migrations/0011_retrieval.sql` and `packages/core/src/nexuswiki_core/retrieval_policy.py` do not appear.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run python scripts/benchmark_retrieval.py compare-order-records --left docs/ops/benchmark-records/phase-04-rerun-v5-strict-order.json --right docs/ops/benchmark-records/phase-04-rerun-v5-relaxed-order.json</automated>
  </verify>
  <done>A new v5 strict/relaxed record pair exists from one identical git revision; the fixed `compare_order_records()` reports `status: ok` against it; `docs/ops/hnsw-order-benchmark.md` documents the outcome and marks the v4 pair superseded-invalid; no production defaults changed.</done>
  <reversibility rating="reversible">Append-only new JSON files plus a documentation edit; no migration, no `supabase db push`, no deploy — revert with `git revert`/`git rm` if the recorded evidence needs correction.</reversibility>
</task>

</tasks>

<verification>
Run `UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run pytest -q packages/core/tests/test_retrieval_golden.py apps/api/tests/test_retrieval_service.py` (the full existing focused suite must stay green, not just the three new tests). Run `UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run python scripts/benchmark_retrieval.py compare-order-records --left docs/ops/benchmark-records/phase-04-rerun-v4-strict-order.json --right docs/ops/benchmark-records/phase-04-rerun-v4-relaxed-order.json` and confirm it now exits non-zero with `order_pair_pin_or_policy_mismatch` (proves the fix catches the historical gap that produced this plan). Run the same comparator against the new `-v5-` pair and confirm `"status": "ok"`. Run `UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run python scripts/benchmark_retrieval.py compare-graph-records --off docs/ops/benchmark-records/phase-04-rerun-v4-graph-off.json --on docs/ops/benchmark-records/phase-04-rerun-v4-graph-on.json` and confirm it still prints `"status": "ok"` (no regression). Run `git diff --stat` and confirm `supabase/migrations/0011_retrieval.sql` and `packages/core/src/nexuswiki_core/retrieval_policy.py` do not appear. Run `git diff --check`.
</verification>

<success_criteria>
RTV-04 has a valid comparable strict/relaxed evidence pair — one committed git revision, matching `git_sha`, `compare_order_records()` returns `status: ok` — and the comparator that previously let the invalid v4 pair through now demonstrably rejects it. The graph comparator is unaffected by the shared `_pins()` change. `docs/ops/hnsw-order-benchmark.md` records both outcomes with the v4 pair marked superseded-invalid. Neither the `strict_order` SQL default nor the `graph_enabled=False` Python default changed. RTV-01, RTV-02, RTV-03, RTV-05, RTV-06, RTV-07, RTV-08, RTV-09 remain untouched and SATISFIED per `04-VERIFICATION.md`.
</success_criteria>

<threat_model>
Security enforcement: ASVS L1; block on high severity. This plan's surface is a local
benchmark/comparator script, a local record-regeneration run, and a docs update — no new
network surface, credential, or production default is introduced.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → local Docker Supabase container | `scripts/benchmark_retrieval.py` issues `docker exec -i supabase_db_NexusWiki psql ...`; no network-exposed surface, local-only dev/ops tooling |
| `git rev-parse HEAD` subprocess → benchmark record | the runner-identity pin (`git_sha`) is trusted verbatim from local git state, no external input |
| benchmark record JSON files → comparator CLI | `docs/ops/benchmark-records/*.json` are read as trusted input by `compare_order_records`/`compare_graph_records`; no untrusted/remote source |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-33 | Tampering | `docs/ops/benchmark-records/*.json` | medium | mitigate | `_validate_record()` still recomputes `policy_content_sha256` and requires the `retrieval_service_channel_envelopes_v1` provenance marker plus all five channel envelopes on every query row, so a hand-edited record must reproduce that whole structure faithfully to fake a pass; residual risk of a careful `git_sha`-only edit is accepted at the same trust level as the existing pin model (local dev tooling, not a network attack surface). |
| T-04-34 | Tampering | benchmark corpus load (Task 2) | medium | mitigate | Each `operational()` arm performs the existing scoped fresh-load + cleanup sequence (fixed benchmark workspace UUID, delete-then-insert) rather than reusing ambient DB state; the script already refuses to overwrite an existing `--output` path, guarding against silently reusing a stale record. |
| T-04-35 | Repudiation | `compare_order_records()` / `compare_graph_records()` | low | mitigate | Both comparators run `_validate_record()` on both inputs first and only return `status: ok` after full pin (now including `git_sha`) + policy + order-mode-pair equality checks succeed — no code path returns `ok` without those checks. |
| T-04-36 | Information disclosure | `git_sha` field | low | accept | `git_sha` is a public commit hash, not a credential; no secrets or tokens are read, stored, or logged by this change. |
| T-04-37 | Denial of service | benchmark CLI | low | accept | Local dev-only script invoked manually; no production request path, no exposed endpoint. |
| T-04-38 | Elevation of privilege / unsafe default drift | `supabase/migrations/0011_retrieval.sql`, `packages/core/src/nexuswiki_core/retrieval_policy.py` | high | mitigate | Neither task's action touches these files; Task 2's acceptance criteria and the plan-level `<verification>` explicitly assert via `git diff --stat` that neither file appears in this plan's diff, and the v5 evidence is documented as informational, not as default-change authorization — the separate `docs/ops/retrieval-policy-change-log.md` gate remains mandatory for any future default change. |
</threat_model>

## Artifacts this phase produces

- `scripts/benchmark_retrieval.py`: `_pins()` now includes `git_sha`; `compare_order_records()` additionally asserts the two input records form a distinct `{strict_order, relaxed_order}` pair before returning `status: ok`.
- `packages/core/tests/test_retrieval_golden.py`: regression tests proving the fixed comparator rejects the historical invalid v4 pair and any same-`order_mode` pair, while still accepting the historical valid v4 graph-off/graph-on pair.
- `docs/ops/benchmark-records/phase-04-rerun-v5-strict-order.json`: new append-only valid strict-order full-path evidence, single git revision.
- `docs/ops/benchmark-records/phase-04-rerun-v5-relaxed-order.json`: new append-only valid relaxed-order full-path evidence, same git revision as the strict record.
- `docs/ops/hnsw-order-benchmark.md`: updated decision log — v5 pair outcome recorded; v4 strict/relaxed pair explicitly marked superseded-invalid, citing its mismatched `git_sha` values.

<output>
Create `.planning/phases/04-hybrid-retrieval-and-fusion/04-09-SUMMARY.md` when done
</output>
