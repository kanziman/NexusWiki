---
phase: 04-hybrid-retrieval-and-fusion
plan: 07
type: execute
wave: 2
depends_on: [04-03, 04-04, 04-06]
gap_closure: true
files_modified:
  - scripts/benchmark_retrieval.py
  - packages/core/tests/test_retrieval_golden.py
  - packages/core/tests/fixtures/retrieval/golden_queries.v1.json
  - docs/ops/hnsw-order-benchmark.md
  - docs/ops/benchmark-records/phase-04-strict-order.json
  - docs/ops/benchmark-records/phase-04-relaxed-order.json
  - docs/ops/benchmark-records/phase-04-graph-off.json
  - docs/ops/benchmark-records/phase-04-graph-on.json
autonomous: false
requirements: [RTV-04, RTV-07]
must_haves:
  truths:
    - Strict-order and relaxed-order runs execute against identical pinned corpus, golden set, policy, embedding model, commit, and database context, with raw immutable records retained.
    - Graph-off and graph-on runs execute over the same inputs and expose per-query plus aggregate quality, underfill, channel contribution, and latency deltas.
    - The order-mode selection and graph default are based on retained measured evidence; graph remains default-off unless the comparison and explicit approval justify promotion.
  artifacts:
    - path: scripts/benchmark_retrieval.py
      provides: operational paired-run CLI with order and graph controls
    - path: docs/ops/benchmark-records/phase-04-strict-order.json
      provides: immutable strict-order raw run
    - path: docs/ops/benchmark-records/phase-04-relaxed-order.json
      provides: immutable relaxed-order raw run
    - path: docs/ops/benchmark-records/phase-04-graph-off.json
      provides: immutable graph-off raw run
    - path: docs/ops/benchmark-records/phase-04-graph-on.json
      provides: immutable graph-on raw run
    - path: docs/ops/hnsw-order-benchmark.md
      provides: evidence-backed Phase 4 decision record
  key_links:
    - from: scripts/benchmark_retrieval.py
      to: supabase/migrations/0011_retrieval.sql
      via: benchmark vector SQL mirrors deployed RPC filters, `<=>` operator, candidate limits, and applies only the selected session-local order mode
      pattern: "strict_order|relaxed_order|hnsw\.iterative_scan"
    - from: docs/ops/hnsw-order-benchmark.md
      to: docs/ops/benchmark-records/phase-04-strict-order.json
      via: raw-record path and matching pinned metadata
      pattern: "phase-04-strict-order"
---

<objective>
Turn the pinned golden set into actual paired HNSW order-mode and graph-value evidence, retaining raw runs and a reproducible decision record rather than a fixture-only placeholder.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: extend the benchmark harness for real paired order and graph runs</name>
  <files>scripts/benchmark_retrieval.py, packages/core/tests/test_retrieval_golden.py, packages/core/tests/fixtures/retrieval/golden_queries.v1.json</files>
  <read_first>
    - `scripts/benchmark_retrieval.py` — current fixture-only adapter and record schema.
    - `apps/api/src/api/services/retrieval.py` and `packages/core/src/nexuswiki_core/retrieval_policy.py` — live retrieval orchestration and graph default.
    - `supabase/migrations/0011_retrieval.sql` and `04-06-PLAN.md` — vector order settings and HNSW plan precondition.
    - `docs/ops/hnsw-order-benchmark.md` — the precise unmeasured claims this work replaces.
  </read_first>
  <action>Preserve `--verify` as the offline fixture contract, then add a separately explicit operational mode that loads the pinned corpus/golden data into a controlled local-Supabase benchmark workspace and writes a new immutable JSON record. For vector channels, execute benchmark SQL that exactly mirrors the deployed RPC workspace filter, `<=>` operator, candidate limits, and retrieval shape while setting the requested order mode with `SET LOCAL` in that benchmark transaction; function-level `strict_order` settings cannot be overridden by a caller session, so do not falsely claim the unchanged production RPC directly measured relaxed order. Reuse the deployed lexical and graph RPCs, and the same Python policy/RRF representation, for the remaining channels. Require explicit `--order-mode strict_order|relaxed_order`, `--graph off|on`, repeat count, output path, and environment metadata; reject overwrite, unsupported combinations, hash/version mismatches, an HNSW-plan preflight failure, or missing database/model identity. Extend graph-focused labels if needed so the pinned set contains enough positive and distractor/cycle cases to evaluate per-query graph impact; update fixture hashes and schema tests deliberately.</action>
  <acceptance_criteria>
    - The operational mode emits one record per invocation with input hashes, policy/model/git/database identity, repeat count, order mode, graph state, per-query ranked evidence, channel requested/returned/underfill/contribution, and p50/p95 latency.
    - It refuses to label fixture-adapter output as an order or graph measurement and refuses a run unless named HNSW-index preflight succeeds.
    - Focused fixture tests cover argument validation, no-overwrite behavior, pinned-input mismatch rejection, record schema, and graph-delta computation without requiring a live database.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: execute and retain comparable strict/relaxed and graph off/on raw records</name>
  <files>docs/ops/benchmark-records/phase-04-strict-order.json, docs/ops/benchmark-records/phase-04-relaxed-order.json, docs/ops/benchmark-records/phase-04-graph-off.json, docs/ops/benchmark-records/phase-04-graph-on.json</files>
  <read_first>
    - `docs/ops/retrieval-policy-change-log.md` — comparable-run and approval protocol.
    - The final CLI `--help` and its HNSW preflight output.
  </read_first>
  <action>With a reset controlled local Supabase benchmark workspace that passes Plan 06's named-index contract, execute strict and relaxed with graph off on the identical corpus/golden/policy/model/git/database configuration and repeat count. Then execute graph off and graph on under the selected order mode with those same pinned inputs. Commit four immutable raw JSON records; do not fabricate values, reuse fixture-adapter records, or overwrite prior records. If the controlled environment cannot meet the HNSW preflight, stop this task and report the exact unmet setup condition instead of producing incomparable evidence.</action>
  <acceptance_criteria>
    - Strict and relaxed raw records have byte-for-byte matching corpus/golden/policy/model/git/database identifiers, graph state, and repeat count; only order mode and measured outcomes differ.
    - Graph-off and graph-on raw records have matching identifiers, selected order mode, and repeat count; only graph state and measured outcomes differ.
    - Each record contains per-query results plus aggregate recall/pass/rank, underfill, channel contribution, and latency metrics; no `not_measured_fixture_adapter` status remains in these four records.
  </acceptance_criteria>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: record the evidence-backed order and graph decisions</name>
  <reversibility rating="costly">Changing an HNSW order mode or enabling graph alters retrieval quality, latency, and policy behavior for all callers; rollback is code/configuration work and requires a new paired benchmark record.</reversibility>
  <read_first>
    - The four raw records from Task 2.
    - `docs/ops/retrieval-policy-change-log.md` — evidence gate and reviewer-approval requirements.
  </read_first>
  <action>Replace the current unmeasured-decision text in `docs/ops/hnsw-order-benchmark.md` with commands, environment details, raw-record links, matching hashes, aggregate tables, per-query regressions, and rationale. Select strict or relaxed only after comparing quality (recall, strict pass rate, required-evidence ranks, and underfill) before latency. Record graph-off/on deltas separately and leave `graph_enabled = False` unless the record explicitly approves promotion after quality and safety review; if it remains off, say so based on the measured delta rather than an absence of evidence. Record any approved policy/default change through the existing version-bump/reviewer protocol; otherwise state the verified default remains unchanged.</action>
  <acceptance_criteria>
    - The decision document links all four raw records and demonstrates identical-input comparability for each pair.
    - It names the selected order mode with a quality-first rationale and records the graph default/promotion decision with aggregate and per-query evidence.
    - A reviewer approves any mode/default/policy change; no unapproved graph promotion or undocumented policy version change occurs.
  </acceptance_criteria>
</task>
</tasks>

<verification>
Run `UV_CACHE_DIR=/tmp/nexuswiki-uv-cache uv run pytest -q packages/core/tests/test_retrieval_golden.py`, `scripts/verify_retrieval_contract.sh`, the four recorded operational benchmark commands, and a record-comparison validation that rejects mismatched pins or `not_measured_fixture_adapter`. Confirm every raw-record path cited by `docs/ops/hnsw-order-benchmark.md` exists and is immutable.
</verification>

<success_criteria>Phase 4 retains four actual, comparable benchmark records: strict vs relaxed and graph off vs on. The documented defaults are traceable to those measurements, and graph cannot be promoted without explicit evidence and approval.</success_criteria>

<threat_model>
- T-04-24 incomparable tuning evidence: fail closed on mismatched corpus/golden/policy/model/git/database/repeat metadata and retain immutable raw records for both arms.
- T-04-25 false HNSW measurement: require Plan 06 named-index preflight before operational runs, rejecting tiny-corpus or fixture-adapter results.
- T-04-26 unjustified graph promotion: compute per-query and aggregate graph deltas, maintain default-off absent explicit approved evidence, and preserve graph distractor/cycle cases.
- T-04-27 misleading or destructive benchmark data: use the controlled repository-owned corpus/workspace only, avoid production tenant data, and make output paths append-only/no-overwrite.
</threat_model>
