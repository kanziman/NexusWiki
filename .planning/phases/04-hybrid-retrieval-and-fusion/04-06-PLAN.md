---
phase: 04-hybrid-retrieval-and-fusion
plan: 06
type: execute
wave: 1
depends_on: [04-02]
gap_closure: true
files_modified:
  - supabase/tests/0011_retrieval_contract.sql
  - scripts/verify_retrieval_contract.sh
  - scripts/ci_check_retrieval_contract.sh
  - docs/ops/migration-0011-record.md
autonomous: true
requirements: [RTV-08]
must_haves:
  truths:
    - A local transactional regression contract seeds a selective representative corpus and asserts JSON EXPLAIN uses the named source and wiki HNSW indexes.
    - The assertion exercises both authenticated vector RPC paths under their deployed GUC settings, rather than merely inspecting SQL text or forcing a plan.
    - Contract setup is contained and rolls back cleanly, leaving neither production-like fixtures nor planner-setting side effects behind.
  artifacts:
    - path: supabase/tests/0011_retrieval_contract.sql
      provides: JSON-plan named-index assertions for both vector retrieval paths
    - path: scripts/verify_retrieval_contract.sh
      provides: local-stack executable regression command
  key_links:
    - from: supabase/tests/0011_retrieval_contract.sql
      to: public.search_chunks
      via: EXPLAIN JSON inspection requiring source_chunks_embedding_idx
      pattern: "source_chunks_embedding_idx"
    - from: supabase/tests/0011_retrieval_contract.sql
      to: public.search_wiki_embeddings
      via: EXPLAIN JSON inspection requiring wiki_embeddings_embedding_idx
      pattern: "wiki_embeddings_embedding_idx"
---

<objective>
Close the HNSW planner-regression gap with a real local-Postgres JSON-plan contract that fails when either vector retrieval path silently falls back from its named HNSW index.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: build a rollback-safe, selective HNSW plan fixture</name>
  <files>supabase/tests/0011_retrieval_contract.sql</files>
  <read_first>
    - `supabase/tests/0011_retrieval_contract.sql` — transactional catalog and graph contract conventions.
    - `supabase/migrations/0011_retrieval.sql` — vector RPC signatures, GUCs, workspace/RLS behavior, and HNSW index names.
    - `supabase/tests/0008_search_contract.sql` and `docs/ops/db-transport-spike.md` — prior fixture and plan-observation lessons.
  </read_first>
  <action>Within the existing transaction, seed a dedicated fixed-UUID workspace/source/wiki corpus large and selective enough for the local pgvector planner to choose each deployed HNSW index without disabling sequential scans or globally forcing planner methods. Generate deterministic valid 1024-d vectors and ANALYZE the affected tables. Establish an authenticated requester role/claims that can see only the fixture workspace, then construct representative source and wiki vector searches through the public RPCs. Keep fixture inserts isolated to the test transaction and do not modify migrations, index definitions, or production policy to make the test pass.</action>
  <acceptance_criteria>
    - The fixture has documented cardinality/selectivity rationale and uses valid 1024-dimensional vectors for both source and wiki embeddings.
    - The test neither uses `enable_seqscan = off` nor another forced planner setting, and it rolls back all fixture rows and role/session changes.
    - Both queries run through the same authenticated RPC boundary and HNSW GUC configuration used by retrieval, not a hand-written surrogate query.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: recursively assert named HNSW index scans and make regression failures actionable</name>
  <files>supabase/tests/0011_retrieval_contract.sql, scripts/verify_retrieval_contract.sh, scripts/ci_check_retrieval_contract.sh, docs/ops/migration-0011-record.md</files>
  <read_first>
    - `scripts/verify_retrieval_contract.sh` — local Docker SQL runner and success marker convention.
    - `scripts/ci_check_retrieval_contract.sh` — source-level checks appropriate when Docker is unavailable in CI.
  </read_first>
  <action>Add `EXPLAIN (FORMAT JSON)` assertions that recursively walk each JSON plan and require an `Index Scan`/equivalent vector index node whose `Index Name` is exactly `source_chunks_embedding_idx` for `search_chunks` and `wiki_embeddings_embedding_idx` for `search_wiki_embeddings`. Fail with a message naming the missing index and observed plan shape. Keep the existing catalog GUC assertions as a complementary static contract. Extend the local runner only as needed to surface the plan failure and success marker; extend the CI source guard to require both named-index assertions and reject test-only planner forcing. Record the locally verified corpus cardinality, command, PostgreSQL/pgvector version, and expected named-index proof in the migration operations note.</action>
  <acceptance_criteria>
    - `scripts/verify_retrieval_contract.sh` exits 0 against a reset local Supabase stack and prints `retrieval_contract: ok` only after both named-index assertions pass.
    - Deliberately changing either expected index name causes the SQL contract to fail with that index name in the error output.
    - `bash scripts/ci_check_retrieval_contract.sh` exits 0 and would fail if either named-index assertion or the no-planner-forcing guard disappeared.
  </acceptance_criteria>
</task>
</tasks>

<verification>
Run `supabase db reset`, `scripts/verify_retrieval_contract.sh`, `bash scripts/ci_check_retrieval_contract.sh`, and `git diff --check`. If the local stack is unavailable, record that fact and do not claim the JSON-plan assertion passed; the plan remains incomplete until it runs against the local stack.
</verification>

<success_criteria>A reproducible local contract invokes both deployed vector RPCs and fails whenever either JSON plan no longer contains its exact HNSW index scan.</success_criteria>

<threat_model>
- T-04-20 silent planner regression: a catalog-only GUC check cannot detect seq/btree fallback; real JSON plans for both named indexes make the regression observable.
- T-04-21 false confidence from forced plans: no `enable_seqscan`/planner override is permitted; the corpus is sized and analyzed to produce an honest plan.
- T-04-22 tenant leakage in test scaffolding: fixture searches use the authenticated RPC/RLS path and an isolated workspace, never service-role retrieval.
- T-04-23 persistent test data or configuration: all seed rows and session effects remain inside the existing rollback transaction.
</threat_model>
