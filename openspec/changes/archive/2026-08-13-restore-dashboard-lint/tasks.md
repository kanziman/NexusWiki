## 1. Restore lint configuration

- [x] 1.1 Add the `@eslint/eslintrc` compatibility adapter and dashboard ESLint 9 configuration with appropriate generated-directory ignores.
- [x] 1.2 Run `pnpm lint` and resolve dashboard lint findings without unrelated refactors or behavior changes.

## 2. Verify the dashboard baseline

- [x] 2.1 Run the dashboard test suite and TypeScript check after lint passes.
- [x] 2.2 Re-run the completed Korean wiki slug change's lint verification and update its remaining verification task when successful.
